# installer/preseed/preseed.cfg
#
# Default preseed for the Athena installer.  Empty by design in v1 —
# operator answers every prompt interactively.  Lock specific questions
# here (per docs/plans/comp-01-installer.md, locked answers include:
# locale=en_US.UTF-8, keyboard=us, partman recipe = ESP + ext4 no swap,
# apt-source = file:///cdrom) once the basic flow is working.
#
# Format: debconf-set-selections.  See ./README.md.
