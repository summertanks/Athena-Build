# Athena-Build — Project TODO

> Serialized, trackable backlog produced from a maturity / stability /
> completeness review of the source tree at the date below. Each task has a
> stable ID (`AREA-NN`); cite the ID when committing or referencing work.
> Update the **Status** column inline rather than renumbering.

- **Review date:** 2026-05-07 (initial), maintenance pass 2026-05-14 (`master` @ `37cabc9`), consolidation audit 2026-05-21 (`master` @ `a1e193c`), maintenance pass 2026-05-25 (`master` @ `564cf19`), maintenance pass 2026-06-12 (`master` @ `6598c93`), **full-tree code review 2026-06-12** (8 parallel deep-review agents over every module + cross-cutting seams, all P0/P1 findings hand-verified against source; `master` @ `479d950`) — filed SEC-07, STA-27..53, ARCH-19, UX-08/09, HK-07.  Maintenance pass 2026-06-22 — verified the session-memory backlog against current source and filed the survivors: SELECT-01/02, PERF-01, FED-01/02, SUP-01, DIST-01; dropped four as already-done/fixed (virtual arch-gate → arch_filter.py 14edc2e, build-dep provider-expansion → package.py a37727f, dep-drift removal audit → dep_drift.py WARN, Case-C +asg re-normalise of the 19 sources → complete + tool removed in f5d3534)
- **Reviewer:** Claude (read-only audit, with subsequent maintenance reconciling actual state vs ticket status)
- **Tree size:** ~79,900 LOC (scripts/ + tests/) — 31 top-level scripts modules + `commands`/`coord`/`tui`/`webapi` packages (69 `.py` files), single-file test suite (~34.2k LOC, 1086 tests)
- **Pipeline state:** end-to-end working — `cache build → dep parse →
  source sync → container init → source build → chroot build
  (+verify) → iso build live → iso build installer`.  Installer ISO
  reaches `finish-install` cleanly on VMware BIOS + EFI as of
  2026-05-14 (commit `68266a2`); real-hardware validation still
  pending under COMP-01h.  Identity/branding pass (base-files
  same-name fork, GRUB + wallpaper + os-release) verified on a
  fresh install 2026-05-21 (working tag `working-branding-pathx-2026-05-21`).
  Since 2026-06-11 the three shipped surfaces are composed as
  independent reachability closures (SURFACES-01 in `docs/done.md`):
  live = GNOME closure (~1105 pkgs), disk = own minimal chroot
  (~245 pkgs via `chroot build disk` + `iso build disk`), installer
  ISO pool = manifest-driven (~1252 vs 1569 legacy; unreachable
  pool.list entries are mirror-only).  All three artifacts
  boot-verified 2026-06-11.  Publishing carries per-source lifecycle
  (LEDGER-01) and a byte-reclaim escape hatch (RECLAIM-01), both in
  `docs/done.md`.
- **Status legend:** `todo | wip | done | blocked | wontfix`
- **Severity legend:** `P0` blocker, `P1` important, `P2` quality-of-life,
  `P3` future / nice-to-have
- **Archived tickets:** completed (`done`) and abandoned (`wontfix`)
  rows are moved to [`docs/done.md`](docs/done.md) once their status
  changes.  When closing a ticket here, copy the row to docs/done.md
  in the matching section and remove it from below.

---

## 0. Analysis summary

The project is more mature than the README suggests. The pipeline already
produces a bootable hybrid BIOS/EFI ISO from-source on Debian bookworm,
with multi-mirror APT cache, snapshot.debian.org pinning, dep-drift
verification against on-disk `.deb` files, a curses TUI with live widgets,
and a chroot-aware install path that handles `usrmerge`, debconf
pre-seeding, the libc bootstrap circular-dep, post-install patch overlays,
and an 8-check chroot verifier that gates ISO build.

**Strengths**

- Clear pipeline-stage architecture (`BuildFlags` gates each step).
- Comments and docstrings explain *why*, not just *what* — load-bearing
  invariants are documented inline (e.g. `_check_dep_drift`,
  `_build_chroot_directories`, snapshot resolution).
- Multi-mirror ingest (main / updates / security) with per-mirror SHA256
  gating and per-source `_mirror` stamping so downloads use the correct pool.
- Sound chroot bring-up: real chroot for `dpkg --configure -a` once dpkg is
  unpacked, chrootless fallback only for the bootstrap rounds.
- Tests + smoke driver exist (`tests/test_module.py`, `tests/smoke_dep_drift.py`).
- Reproducibility hook is in place via Snapshot pinning (off by default).

**Top risks (expanded as P0/P1 tasks below)**

1. **~~No GPG verification of `InRelease`~~** — resolved by STA-01:
   `utils.verify_inrelease()` + `[Security]` config gate every fetched
   `InRelease` against a pinned keyring before `Packages` / `Sources` are
   trusted (in `docs/done.md`).
2. **~~`MaxParallelBuilds` is a lie~~** — resolved by COMP-03: source
   builds now run in `ThreadPoolExecutor` with `[Build] MaxParallelBuilds`
   honoured, per-worker scratch dirs, and `HeavyPackages` serialisation
   for memory hogs (in `docs/done.md`).
3. **~~`--force-depends` in `dpkg --configure -a`~~** — resolved by
   STA-02: removed from `_configure_chroot`; STA-03 snapshot pinning +
   `_check_dep_drift` / `_verify_dep_resolution` catch unresolved deps
   before configure (in `docs/done.md`).
4. **~~No APT-repo metadata~~** — resolved by COMP-02 + MIRROR-01:
   `repo index full|minimal` produces signed `InRelease` over the local
   pool; `mirror publish` does per-file additive push + Ed25519-signed
   claims + tier-1 GPG `coord-head` to every configured peer.  See
   [`docs/mirror-setup.md`](docs/mirror-setup.md).
5. **~~No installer image~~** — installer ISO ships since 2026-05-14
   (COMP-01a..g done; reaches `finish-install.d/20final-message` cleanly
   on VMware BIOS + EFI).  Architecture **pivoted 2026-05-10** to
   "rebuild d-i from source via a parallel udeb dep tree through our
   existing pipeline" (replaces the original "import vanilla d-i" plan;
   full plan at `docs/plans/comp-01-installer.md`).  COMP-01h
   (real-hardware testing) is the only sub-phase still open.
6. **~~Module-level globals in `build.py`~~** — resolved by ARCH-01;
   pipeline state now lives on `BuildSession` (cache / dep_tree /
   container / config / flags are instance attrs).  Console facade
   ownership also normalised — `tui.console` is the single surface;
   `cli.py` registers as `tui.tui_instance` so `--headless` mode works
   through the same facade.  Both risks retired 2026-05-09 ↔ 2026-05-14.

---

## 1. Stability & correctness — P0 / P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| SELECT-01 | P2 | DONE 2026-06-25 | **SELECT-LOCK restore fidelity — store raw list bytes.**  `assemble_state` + `federation_state` now stash `seeds_raw` (verbatim text of the 4 list files via new `seeds_raw_from_config`); `restore_list_files` writes each file BYTE-FOR-BYTE when present, falling back to the name-render for older lockfiles / files absent at capture. Operator comments (pool.list ~156 doc lines) + pkg-group ORDER preserved → removes the TRIGGER for SELECT-02's closure drift. Additive schema (no version bump; HMAC covers seeds_raw). Parsed `seeds` stays the closure authority. ~5-10 KB/lockfile. Belt-and-suspenders sha256-stash NOT needed. Test: `test_selection_lock_restore_seeds_raw_byte_faithful` (byte-identical round-trip incl. non-alphabetical groups; legacy fallback). Original text:**SELECT-LOCK restore fidelity — store raw list bytes.**  `cache restore` regenerates `config/{pkg,live,installer,pool}.list` from the lockfile's parsed seed NAMES only (`selection_lock.restore_list_files` / `render_pkg_list` / `render_flat_list`), so (a) comments are destroyed (pool.list lost ~156 operator-doc lines) and (b) pkg-group order is ALPHABETIZED because `write_selection_state` serialises via `json.dumps(sort_keys=True)`.  The reorder is NOT cosmetic — group order changes the closure (+xterm +libutempter0; see SELECT-02).  Fix: capture `seeds_raw` (verbatim bytes of each list file) in `assemble_state`; `restore_list_files` writes it byte-for-byte (fall back to the name-render for older lockfiles); the closure guard keeps using the parsed seeds.  ~5-10 KB on a ~155 KB lockfile.  Removes the TRIGGER for the closure drift.  (Filed 2026-06-22 from session backlog.) |
| SELECT-02 | P2 | todo (Stage-2 shadow landed 2026-07-01) | **Order-independent OR-dependency resolution.**  STAGE 1 DONE 2026-06-25: standalone reference resolver `scripts/or_resolve.py` (`resolve_closure` = Pass A hard-dep fixpoint + Pass B OR groups resolved against the settled closure with a canonical smallest-group tie-break; `resolve_closure_greedy` models the live order-sensitive behaviour). Proven order-invariant over all seed permutations vs the greedy model's divergence (`test_or_resolve_*`, incl. the xterm\|x-terminal-emulator + gnome-terminal-Provides repro). STAGE 2 PENDING (separately accepted): wire the fixpoint into `dependencytree.parse_dependency` + the ~10 `resolve_packages` passes — entangled with the intermediate tier snapshots (live/installer/pool exclusives + per-group credit), so it needs old-vs-new closure diff on a real cache + the permutation harness as the gate. STAGE 2a SHADOW LANDED 2026-07-01 (chosen over a blind cutover): `DependencyTree.order_independence_report()` recomputes the closure over the SAME accumulated seeds (`_seed_history`, captured in `add_lookahead`) with the order-independent fixpoint (`or_resolve.resolve_closure`, over a deps/provides view built from `canonical_pkgs`), and `cache parse` WARNs on divergence — `greedy_only` = order-pulled extras (e.g. xterm when x-terminal-emulator is satisfied by gnome-terminal), `fixpoint_only` = version/conflict drops the fixpoint doesn't model. NON-INVASIVE (selection is unchanged), so it delivers the "old-vs-new closure diff on a real cache" the cutover is gated on, automatically, on the operator's next real `cache parse`. Widened `or_resolve` param types to `Mapping` (covariant). Test: `test_dependencytree_order_independence_report` (xterm/x-terminal-emulator repro flags xterm+libutempter0; a plain chain reports nothing). STAGE 2b CUTOVER remains: once the real-cache divergence set is reviewed + accepted, restructure `parse_dependency` to settle-then-resolve-ORs (or consult the fixpoint's OR-picks) so the shipped closure uses the fixpoint — gated by the shadow's real-cache diff. Original text:**Order-independent OR-dependency resolution.**  `dependencytree.py` (~582-614) resolves `a \| b` OR-groups in a SINGLE greedy pass against the IN-PROGRESS closure ("use an already-selected alt, else pull the first declared"), so the resolved closure depends on seed ORDER, not just the set — a determinism worry.  Reproduced: `xorg` Depends `xterm \| x-terminal-emulator`; `gnome-terminal` Provides `x-terminal-emulator` — pkg.list group order flips whether xterm (+ its dep libutempter0) is pulled (1580 vs 1582 bins).  Fix: defer ORs, resolve each against the FINAL closure to fixpoint (Pass A = seeds + hard Depends/Pre-Depends; Pass B = ORs satisfied if ANY alt in closure else first; iterate to fixpoint).  Makes the closure a pure function of the SET.  Risk: deferring can legitimately drop OTHER currently-pulled first-alternatives → needs old-vs-new closure diff + many-ordering verification.  Removes the CAUSE (SELECT-01 removes the trigger).  (Filed 2026-06-22 from session backlog.) |
| PERF-01 | P3 | DONE 2026-06-25 | **Skip re-downloading the immutable pinned-snapshot InRelease.**  `Cache.__get_files` now reuses a cached InRelease when snapshot-pinned + remote + non-empty cached copy exists (predicate extracted to `Cache._should_reuse_pinned_release`, unit-tested); GPG `verify_inrelease` stays UNCONDITIONAL on the on-disk file, with a delete+re-fetch+re-verify fallback if the cached copy fails verification. Safe because the on-disk filename is URL-derived and the URL embeds the pin timestamp (re-pin → new filename, never stale reuse). Belt-and-suspenders sha256-in-snapshot.state DEFERRED (redundant given URL-keyed filename + unconditional GPG). Original text:**Skip re-downloading the immutable pinned-snapshot InRelease.**  `cache.py:268` re-fetches `InRelease` per mirror on EVERY cache build; for a PINNED snapshot the timestamp is fixed → the file is immutable, so the re-download is pointless.  Fix: when snapshot-pinned AND a cached InRelease exists, skip the download — but keep `verify_inrelease` (GPG) UNCONDITIONAL on whichever file is on disk; on GPG fail delete + re-download once + re-verify (pinning controls re-fetch, GPG controls trust).  LOW value since the IPv4-pin fix (fce12f3) cut re-fetch to ~0.2s (was a multi-min IPv6 connect stall, not server latency) — keep as a minor optimization, not a fix.  Optional belt-and-suspenders: stash the InRelease sha256 in `snapshot.state` at pin time and require a match alongside GPG.  (Filed 2026-06-22 from session backlog.) |

## 2. Conformity to Debian/Ubuntu process — P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| CONF-03 | P1 | todo | Honour the Debian source format: today `BuildContainer.build` calls `dpkg-source -x` then patches and runs `dpkg-buildpackage -us -uc -nc`. For a real distro derivative, the per-package patches should land in `debian/patches/` (quilt format) with a versioned `debian/changelog` entry, then `dpkg-buildpackage` produces a properly-named binNMU `.deb`. Document the chosen path (we are *not* doing it today) and decide whether to keep the current “patch outside debian/” approach. |
| CONF-06 | P2 | CLOSED 2026-06-26 — CONSOLIDATED into AUDIT-02 | Adopt `reprotest` (or equivalent) to verify built `.debs` are reproducible across two runs of `source_build` from the same snapshot.  _Same capability as AUDIT-02 (reproducibility verification by build-twice + diff).  Folded in: AUDIT-02 is the diffoscope-build-twice harness (subset gate now, full-corpus scale-up later); `diffoscope` is the chosen comparator over `reprotest` (poor fit for the snapshot-pinned containerized build)._ |
| CONF-08 | P2 | todo | **Lift `nodoc` from `[Source] BuildOptions` / `BuildProfiles`** (currently `nodoc, nocheck` — `nodoc` is a temporary workaround for missing doc-build tooling in the build pool, not a permanent policy).  Four `patch/source/` entries exist solely to make doc-related rule fragments nodoc-tolerant and should be deleted when this lifts: `libyaml/0.2.5-1/9001-skip-doxygen-under-nodoc.patch` (wraps `$(MAKE) html` + `dh_doxygen` in `ifeq nodoc`), `protobuf/3.21.12-3/9001-guard-doc-install-on-nodoc.patch` (defensive `[ ! -e ]` / `rm -f` for examples README + .gitignore), `p7zip/16.02+dfsg-8/9001-graceful-skip-when-manual-absent.patch` (exit 0 instead of exit 1 when manual absent), `wpa/2.10-12+deb12u3/9001-skip-examples-sed-under-nodoc.patch` (wraps `sed -i debian/*/usr/share/doc/*/examples/*.c` in `ifeq nodoc`).  All four preserve upstream behaviour when docs are present — verified.  (Maturity review 2026-06-25: 2 MORE of the same family found — `libwww-perl/6.68-1` + `libxml-parser-perl/2.46-4+deb12u1` — 6 patches total, not 4.)  Steps: (1) audit which doc-build tools (`doxygen`, sphinx, `texinfo`, gtk-doc-tools, etc.) need to be in the build pool / container; (2) add them to `config/installer.list` and/or pool.list as appropriate; (3) drop `nodoc` from `BuildOptions` and `BuildProfiles` in `config/build.conf`; (4) rebuild end-to-end; (5) `git rm` the four patch trees once builds pass. |
| CONF-14 | P2 | todo | **Fork-version scheme — bump mechanics for `<upstream>+athenaN` → `<base>+athenaN`.**  Today's same-name forks (FORK-01 Path X — `base-files` 12.4+deb12u14+athena1 → +athena2) preserve the upstream NMU suffix in the fork version so the collision gate (`Version(upstream) >= Version(fork)` fails the build) accepts our value.  Concretely: upstream pool has `base-files_12.4+deb12u14`; we ship `12.4+deb12u14+athena1` (strictly greater via string-compare on `+athena1` vs end-of-string).  This works but bakes the upstream `+deb12u14` into our version forever — every upstream security release would require a rebase to `12.4+deb12u15+athena1` (then athena2…), threading upstream bump granularity into our changelog.  **What we want eventually:** `12.4+athena1` (pristine upstream version + our suffix only) — matches the cleaner half of the Devuan/Parrot/Mint convention.  **The hiccup:** `+athena1` < `+deb12uN` in Debian version order (`a` < `d`), so the collision gate would fail.  **Three candidate fixes**, each with its own cost: (a) **NMU-strip recognises +athenaN as a strippable layer** — post-strip upstream version is `12.4` (we already strip +debNuN), upstream `12.4+deb12u14` → `12.4`, our `12.4+athena1` → `12.4` post-strip, neither dominates the other (equal).  Collision gate would need to gain "equal is OK as long as the fork is the kept record" semantics.  (b) **Use a suffix starting with a letter > 'd'** (e.g. `+thor1`, `+xathena1`) so `+xathena1` > `+deb12uN` purely lexicographically.  Reads weird; future readers wonder why the prefix.  (c) **Use Debian epoch** — `1:12.4+athena1` outranks any non-epoch upstream version.  Kali does this.  Nuclear option but unambiguous.  Lean: (a) — most honest, preserves the pristine-upstream invariant we already enforce post-strip, only requires loosening the collision gate.  Tracks the broader "version-scheme cleanup" theme; tied to CONF-13 (upstream-NMU gate) which already special-cases the strippable-suffix family. |
| CONF-17 | P3 | todo | **Migrate to the Debian `pool/` layout when multi-arch / multi-suite arrives.**  Today the repo uses the unified layout — package files live INSIDE `dists/<codename>/<component>/` next to the indexes that list them, no separate `pool/` dir.  Right trade-off at 1 suite × 1 arch: one self-contained subtree (one rsync, one nginx alias, one `find` for audits), no cross-suite reference-count GC (the claim ledger governs file lifecycle directly), no restructure pass at index time.  The costs arrive exactly when a SECOND suite (keeping release N alongside N+1 on the same mirror) or SECOND arch (COMP-04 arm64) exists: every unchanged package is stored + pushed once PER suite/arch tree, and `arch:all` packages can't be shared as a single copy across per-arch trees the way one `pool/` allows.  TRIGGER: commit to shipping 2 suites or 2 arches side-by-side → migrate to `pool/` FIRST, before the second tree lands.  Migration is mechanical but wide: filenames + frozen bytes are UNCHANGED (claims pin filename+sha, so the ledger is unaffected); what moves is file LOCATION + every `Filename:` path — touches index generation, the publish dist-tree push (whose `--exclude=*.deb/*.udeb` guard exists only BECAUSE debs live inside dists/ — pool/ separation removes that sharp edge by construction), audit pool listings, mirror pull path resolution, and the mirror-host layout doc; needs a coordinated re-index + republish across the federation (stage the relocation as a remote move, or rsync re-pushes every byte as a "new" file).  Side benefit: restores compatibility with standard mirror tooling (debmirror/aptly assume `pool/`).  Until the trigger fires this is deliberately NOT worth doing — the unified layout is simpler, and the one incident it caused (local prune/rebuild propagating into the dist-tree rsync) is closed by the exclude guard.  (Filed 2026-07-03 from the mirror-layout discussion.) |

## 3. Completeness — P1 (toward derivative-distro state)

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| COMP-01 | P1 | DONE 2026-06-26 (hardware testing = ongoing) | **Installer ISO** path.  `iso build live` and `iso build installer` both ship today — installer ramdisk reaches `finish-install.d/20final-message` cleanly on VMware BIOS + EFI as of 2026-05-14 (sub-phases COMP-01a..g closed in `docs/done.md`).  Umbrella remains open pending **COMP-01h — real-hardware testing** (sub-row below).  *(architecture **pivoted 2026-05-10** from "import vanilla d-i" to "rebuild d-i runtime from source via parallel udeb dep tree through our existing pipeline" — full plan: `docs/plans/comp-01-installer.md`, project memory `project_installer_from_source.md`.  Two distinct dep graphs (deb world + udeb world) over a single source corpus; same `source build` produces both `.deb` and `.udeb` outputs.  Installer ramdisk is pure udeb closure (no debs, no systemd) — `rootskel`+busybox init runs cdebconf+main-menu+d-i step udebs.  UI is `cdebconf-text-udeb`.  Branding via debconf-overrides + companion udeb (no upstream source patches — see `docs/branding-methodology.md`).  `installer.list` is mixed-universe — udeb names AND deb names; resolver dispatches per-entry.  Total ~5-7 weeks elapsed across COMP-01a..g.)* |
| COMP-01h | P1 | CLOSED 2026-06-26 — ONGOING hardware effort (no code pending; QEMU smoke harness landed) | **Installer Phase 8 — hardware testing** (1-2w). QEMU UEFI + BIOS smoke tests. Real hardware: at least one BIOS box + one UEFI box. Edge cases: small disk, no network, USB-only install media. Validate target boots cleanly after install completes. Treat as the gate before declaring COMP-01 done. *(VMware BIOS + EFI verified end-to-end through `finish-install.d/20final-message` 2026-05-14.)*  **QEMU SMOKE HARNESS (`tests/installer_smoke/run.py`, COMP-12 Phase F) FIXED + made functional 2026-06-26**: was never working end-to-end — `--full`'s `-append` was silently ignored (no `-kernel`/`-initrd`), EFI hardcoded a non-existent `/usr/share/OVMF/OVMF_CODE.fd` with no writable VARS, KVM keyed off `kvm-ok`. Now: `--mode direct` (default) extracts `/boot/{vmlinuz,initrd.gz}` via xorriso and direct-boots with `console=ttyS0` (reliable headless serial + the preseed cmdline actually applies); `--mode efi` discovers OVMF (incl. `_4M`) + copies a per-run writable VARS; `--mode bios` firmware/GRUB path; KVM via `/dev/kvm`; `--self-test` validates argv shapes with no QEMU (passes). PLUMBING PROVEN on the real installer ISO (extract → QEMU spawn → serial capture → known-bad scan). STILL NEEDS: a host with /dev/kvm to run a FULL preseeded install to completion (TCG too slow for the dev sandbox) + the real-hardware BIOS/UEFI/USB/edge-case boxes + INST-05 tasksel live-verify under the smoke. Tracked in `docs/plans/comp-02-robust-build.md` § Phase F.* |
| COMP-04 | P1 | todo | **Architecture support** beyond `amd64`. The code reads `arch` from config but `Dockerfile`, `pkg.list` (`linux-image-amd64`, `grub-efi-amd64`), and `build_iso` ISO name are amd64-hardcoded. Decide on second-arch target (likely `arm64`), parameterise.  NOTE: a second arch on the same mirror is the TRIGGER for CONF-17 (migrate the repo to the Debian `pool/` layout) — do that migration BEFORE the second arch tree lands. |
| COMP-07 | P2 | done | **Cross-build container per release** — DONE 2026-06-25 (maturity review: `Dockerfile` is `ARG RELEASE=bookworm` / `FROM debian:${RELEASE}-slim` — bookworm is only the ARG DEFAULT, not a pin; the image tag bakes in release+snapshot (`athenalinux:build-{release}-{snapshot}`) and rebuilds on change. Functionally present; only stale "bookworm" wording in comments remains.)  Original ask: `Dockerfile` was thought hard-pinned to `bookworm`. Auto-rebuild a per-release image (`bookworm`, `trixie`, `noble`, …) when `CONTAINER_RELEASE` changes; keep them in parallel so the user can cross-target. |
| COMP-11 | P2 | CLOSED 2026-06-26 — PARKED (unactionable now) | **Distro abstraction**: _Parked: the build system depends heavily on the snapshot-pinned reproducibility framework, which has no non-Debian (snapshot.debian.org) equivalent — targeting Ubuntu would give that core capability up. Not actionable at present; revisit in the future if Ubuntu support becomes a goal (would want decomposing into sub-tickets: config switch + shared-base/overlay pkg.list → Dockerfile/base → casper boot stack → ISO/installer → dual validation)._  `[Build] Distro = debian \| ubuntu` selects parallel artifact sets — `pkg.list.<distro>` (Ubuntu uses `casper` instead of `live-boot`, plus its own initramfs hooks and grub package names), `Dockerfile.<distro>` (`FROM ubuntu:${RELEASE}`), and the `os-release` ID/ID_LIKE/VENDOR_NAME fields (subsumes COMP-10).  `[Snapshot] Enabled = false` becomes the implicit default for non-Debian distros (no snapshot.d.o equivalent).  Carries the rest of the "distro-portability" cluster: ARCH-13 (URL externalisation) and HK-05 (dead-config-line cleanup) closed in `docs/done.md`; COMP-11 is the surviving member.  Hardest piece: maintaining two parallel pkg.lists in sync; consider a shared base + per-distro overlay file. |
| INST-01 | P2 | todo | **Installer i18n preseeding + non-en_US locale set.**  Today the language step is interactive at boot (`installer/preseed.cfg` ships no `debian-installer/locale`); installer strings are English-only (`S40-athena-branding` applies `@DISTRIBUTION@` substitutions only, no locale-driven template variants).  Preseed default locale, ship a chosen locale set (en_US + 2-3 candidates — pick from operator preference), pre-bake keymap defaults.  Reuses the existing `fork/source/athena-installer-data/data/S40-athena-branding` mechanism — adds locale overrides alongside the existing string overrides, no new infrastructure.  Touches `config/pool.list` (locale udebs).  Stays inside newt — no front-end change.  Filed 2026-05-29 from the comparative-analysis capability-gap audit (vs elementary / Pop / Mint / similar). |
| INST-02 | P2 | todo | **Installer accessibility — speakup + brltty.**  PARTIALLY DONE (maturity review 2026-06-25): the `brltty-udeb` + `speakup-modules-*-di` udebs ARE already in the closure/manifest; what REMAINS is the GRUB accessibility boot entry (`s`-for-speakup shortcut) + a screen-reader smoke pass.  (Originally filed:) No `speakup` (screen reader) or `brltty` (braille) udebs in the installer closure today (`config/pool.list` / `config/pkg.list` mark a11y as deferred).  Adds the udebs, documents boot-menu shortcut (`s` for speakup at GRUB), validates with a screen-reader smoke pass under QEMU.  Real accessibility gap vs stock Debian d-i.  Touches `config/pool.list` + `installer/grub.cfg` boot menu.  Depends structurally on INST-01 (accessible prompts work better in user's preseeded locale).  Note ISO size cost — verify against current ISO budget before committing.  Filed 2026-05-29 from the comparative-analysis capability-gap audit. |
| INST-03 | P2 | todo | **Guided partition recipe — "Erase disk and install Asgard".**  Vanilla partman today: `installer/preseed.cfg` has zero partman entries; operator gets the full manual / guided picker.  Adds a custom partman recipe ("erase whole disk, single root, swap file, EFI on UEFI / BIOS-boot on legacy") + preseed defaults for the common path; keeps the manual mode reachable.  Removes the steepest cliff in the install flow today.  Stays inside d-i's partman — no front-end change.  New `fork/source/athena-installer-data/data/partman-recipe-asgard`.  Filed 2026-05-29 from the comparative-analysis capability-gap audit. |
| INST-04 | P3 | todo | **Front-end uplift investigation — graphical d-i vs Calamares.**  Newt is text-only; vs elementary / Pop / Mint / similar's polished GUI installers this is the most visible UX delta.  Investigation only at this stage — scope the gtk-frontend rebuild cost (additional udeb closure, Asgard branding through gtk theming, accessibility implications) vs replacing d-i with a Calamares fork.  No code change; writes a separate plan file under `docs/plans/`.  Picked up only after INST-01..03 land — those define the "do we still need this?" bar.  Most likely the way to spend XL effort for diminishing UX returns if started prematurely.  Filed 2026-05-29 from the comparative-analysis capability-gap audit. |
| INST-05 | P2 | CLOSED 2026-06-26 — build-time guards tested; live-verify is ongoing (folds into COMP-01h); ssh-server 0-unique-pkg ACCEPTED | **Tasksel sanity check — verify menu tasks actually select + install their package sets on the target.**  No coding pending: the cdebconf-safety of the generated `.desc` (`Section: user` + ASCII/no-comma/paren sanitisation) is tested (`test_tasksel_desc_generator_shape_and_sanitization`), the Key↔group match is tested (`test_athena_tasksel_task_keys_mirror_pkg_list_groups`), and the `.desc`/`.disk/groups/` are both derived from the signed lockfile so they're consistent by construction.  REMAINS as ongoing VALIDATION (run under the COMP-01h QEMU `--full` smoke / real hardware): tasks render in the menu + selecting each ACTUALLY installs its set (esp ssh-server) + Keys resolve via apt-cache dumpavail at install time.  The ssh-server 0-unique-pkg task is ACCEPTED (its Keys resolve from elsewhere; harmless).  `iso build installer` generates the tasksel menu from the lockfile (this build: 6 task(s) → `/.disk/athena-tasks.desc`) but flagged `INFO: pkg.list group [ssh-server] adds 0 unique packages — all seed(s) already pulled in by an earlier group or required/important.  Tasksel task remains valid (Key entries resolve from elsewhere)`.  That "valid but adds nothing unique" case is exactly where tasksel silently misbehaves — a task that renders in the menu but whose selection installs nothing, or whose `Key` entries don't resolve via `apt-cache dumpavail` (→ cdebconf hides it).  **Live-verify in the d-i installer** (under the COMP-01h QEMU smoke): (1) all 6 tasks render in the menu; (2) selecting each task ACTUALLY installs its intended package set on the target — especially `ssh-server` (does selecting it do anything, and should a 0-unique-pkg task even be offered?); (3) no task silently dropped by cdebconf for a non-`user` `Section` or a `Description` with commas/parens/em-dash (must mirror `debian-tasks.desc` shape — see `athena-tasks.desc` `Section: user` + ASCII tests); (4) every group's `Key` list resolves (the `pkg.list group ↔ fork/source/athena-tasksel/tasks/<group>` Key-match test + the `DEBIAN_TASKS_ONLY=1` env-filter interaction).  Cross-check the 6 generated tasks against `.disk/groups/` (this build: base, desktop, development-tools, gnome-desktop, laptop, ssh-server, standard).  (Filed 2026-06-24 from session — installer ISO build flagged ssh-server 0-unique-pkg.) |

## 4. Architecture & coding practices — P1 / P2

_All architecture & coding-practice tickets are closed — see [`docs/done.md`](docs/done.md) § 4._

## 5. Tests & CI — P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| AUDIT-02 | P2 | todo | **Reproducibility gate — `cmd source reproduce <pkg>` + nightly subset.**  Picks 3-5 representative forked sources (one heavy with `dh_strip` invariants e.g. `linux`; one fork with patch overlay e.g. `base-files`; one pure-data fork e.g. `athena-tasksel`), builds each twice in clean COMP-03 scratch dirs, runs `diffoscope` on the resulting `.deb` pair, fails on non-identical output.  Reuses `BuildContainer.build()` end-to-end + the per-worker scratch dir from COMP-03 (no race between concurrent reproduce runs).  Schedule via nightly CI job (separate from the per-commit gate — diffoscope is slow).  SUBSUMES CONF-06 (reprotest adoption, consolidated 2026-06-26): the 3-5-source subset here is the gate; the eventual full-corpus check is just a SCALE-UP of this same diffoscope-build-twice harness, not a separate capability.  `diffoscope` (not `reprotest`) is the comparator — reprotest orchestrates env variation (path/time/host), a poor fit for the FIXED snapshot-pinned container; build-twice-in-clean-scratch + diffoscope is the right "equivalent".  Note: builds are likely already TIME-reproducible (dpkg-buildpackage defaults SOURCE_DATE_EPOCH from the top changelog date — relied on in bump.py), so the first run mostly surfaces OTHER non-determinism (parallelism ordering, locale).  `diffoscope` is not installed in dev/CI — adoption pulls it in as a dependency.  Filed 2026-05-29 from the comparative-analysis capability-gap audit. |

## 6. Documentation — P1 / P2

_All documentation tickets are closed — see [`docs/done.md`](docs/done.md) § 6.  Keeping docs in sync is now a standing WORKING PRACTICE, not a ticket: inline comments + type hints ride with every code change; prose docs (`docs/*.md`, README) are updated only on LARGE changes, on a SEPARATE branch, with heavy user validation before merge._

## 7. Security & supply-chain — P0 / P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| FED-01 | P2 | WARN-ONLY DONE 2026-06-26 (auto-adopt-union deferred) | **Pull-side stale / closure-limited-ledger detector (design first).**  WARN-ONLY half SHIPPED: `mirror pull` now computes `_ledger_stranded_claims` (live PEER claims whose filename the fetched closure ledger OMITS — our own claims + ledger-covered files excluded) and WARNS LOUDLY (COLOR_ERROR, lists examples) that the ledger is STALE / closure-limited and those files won't be adopted → phantom virtual_hash_conflict; tells the operator to ask the owner to republish a complete ledger. Pure helper + `test_fed01_ledger_stranded_claims_detector`. DEFERRED (the design-decision half): AUTO-ADOPT-UNION (fall back to adopting the live-claim union when the ledger is a strict subset) — open Q whether folded-live-claims is the authoritative published set (any subset = bug) or can legitimately diverge (snapshot timing / pruning); also its redundancy vs the owner-side `closure_ledger_entry_missing` CRITICAL. Warn-only is safe + non-committal; revisit auto-adopt when that's decided. Original text:**Pull-side stale / closure-limited-ledger detector (design first).**  A peer's `mirror pull` TRUSTS the fetched signed closure ledger as its adoption set; when the ledger is stale or closure-limited (`ledger ⊊ folded live claims`) the peer adopts only the subset and STRANDS the rest → phantom `virtual_hash_conflict` at virtual-build (3522 this session: ledger 1731 = install closure vs 4699 actually claimed; heal required the OWNER to republish, and nothing told the peer its mirror was stale).  Today `cmd_mirror.py:1639` only COUNTS ledger-names-no-claim (`_no_claim`) — not a WARNING — and the inverse SUBSET case (live claims the ledger omits) is unguarded.  Fix idea: at pull, fold live claims, compare filename sets vs the fetched ledger; if ledger is a strict subset WARN loudly and/or fall back to adopting the live-claim union.  Open Qs: is folded-live-claims the authoritative published set (any subset = bug) or can they legitimately diverge (snapshot timing / pruning)?  warn-only vs auto-adopt-union?  redundancy vs the owner-side `closure_ledger_entry_missing` CRITICAL.  (Filed 2026-06-22 from session backlog.) |
| FED-02 | P2 | todo | **Co-publisher snapshot-advance coherence (NOT a block).**  A `role=federation` peer running `snapshot select latest` AHEAD of the mirror is the INTENDED multi-publisher test — it BUILDS + OWNS the forward delta while ADOPTING the unchanged base.  Do NOT block/warn the advance.  The guard work is CONSISTENCY of the resulting MIXED-snapshot mirror (base / current / two owners on adjacent versions): drift packages attributed as the peer's-delta-to-own, virtual build / audit must not false-conflict on them, the ledger's latest-per-pkg must prefer the peer's newer-version delta once published, supersession across two builders at adjacent snapshots, and `reconcile_snapshot_pin` forward-adopt when the origin later catches up.  Design + invariants first.  (Filed 2026-06-22 from session backlog.) |
| FED-03 | P2 | DONE 2026-06-26 (B+D; insider case out of scope) | **Builder-id registration is a blind overwrite — add TOFU + tier-1-signed keyring entries (B+D).**  STEP D SHIPPED (D2 anchor + strict-with-migration, both user-chosen): the coord-head now carries a tier-1-signed `builders` map `{id: sha256(pubkey)}` (`schema.new_coord_head(builders=)`, populated at publish from the fetched keyring + own pub; preserved across revoke + reconcile-neighbours). Claim-ADOPTION paths (pull, repo/foreign/cross-mirror audits, virtual-build adoption) now filter the keyring through `identity.verified_keyring_from_head` (strict `enforce_bindings`): a pubkey injected by anyone with SSH-write-but-no-tier-1 has no signed binding → its claims rejected; a swapped pubkey (sha mismatch) → rejected. STRICT: a pre-D head with no `builders` map rejects ALL claims with a "republish to migrate" message — MIGRATION = the owner runs `mirror publish` once (rebuilds the signed map). SCOPE NOTE: display/derived paths (`cmd_mirror_query`, `_mirror_recompute_base`) are NOT strict-enforced (informational / secondarily-guarded floor — not the threat-model's adoption paths). CAVEAT (accepted): tier-1 GPG key is shared → stops OUTSIDERS, not a malicious insider peer (closing that = owner-only tier-1 / rotation-by-old-key, a separate higher-bar decision). Tests: `test_fed03d_*`. Original text:**Builder-id registration is a blind overwrite — add TOFU + tier-1-signed keyring entries (B+D).**  STEP B SHIPPED: `mirror builders register` now probes the existing remote `<id>.pub` BEFORE upload (`transport.remote_pubkey_state` — tri-state present/absent/error, distinguishing a missing key from an ssh failure so a transient hiccup can't be mistaken for "absent"); pure `_register_tofu_decision` policy — absent→upload, same key→idempotent skip, DIFFERENT key→REFUSE (identity-takeover guard), unreadable→REFUSE; `--force` overrides for a deliberate rotation. Handles ssh + local-fs mirrors. Tests: `test_fed03_*` + updated register test (refuse-on-different + force). STEP D (tier-1-signed keyring bindings verified in verify_claim_against_keyring) STILL PENDING — bigger, with the accepted shared-tier-1-key insider caveat (closing the insider case = owner-only tier-1 / rotation-authorized-by-old-key, a separate higher-bar decision). Original text:**Builder-id registration is a blind overwrite — add TOFU + tier-1-signed keyring entries (B+D).**  `mirror builders register` (`cmd_mirror.py:3014`) blindly pushes `<id>.pub` to the mirror's `keyring/builders/<id>.pub`, OVERWRITING any existing key with no same-key/different-key check — so re-registering the same builder-id silently succeeds (observed re-registering `BS3` repeatedly).  Two cases hide behind this: same machine re-running configure (same key → harmless) vs another box claiming the id / a regenerated key (different key → SILENT IDENTITY HIJACK: the id's already-published claims now fail verification, and whoever holds the new key can publish AS that id).  Trap is sharp because the builder-id default is the HOSTNAME (`socket.gethostname()`), so two `debian` boxes collide.  Trust anchor is the id→pubkey binding in `keyring/builders/<id>.pub` (`coord/identity.py:verify_claim_against_keyring`); the mirror is a dumb SSH/HTTP target so it can't enforce — enforcement must be client-side + at verify time.  **Agreed approach = B + D.**  **B (client-side TOFU, small):** before upload, fetch existing `<id>.pub`; absent → register; identical → idempotent no-op (friendly "already registered, key unchanged"); different → REFUSE (with `--force` override).  **D (authenticated bindings, bigger):** tier-1-sign keyring entries on register, verify that signature in `verify_claim_against_keyring`, migrate/sign existing entries — so a pubkey dropped by anyone with SSH write but NO tier-1 key is rejected.  **CAVEAT (decided to accept for now):** the tier-1 GPG PRIVATE key is SHARED across all peers (imported at onboarding, `onboarding.py:322`), so D authenticates against OUTSIDERS (SSH-write-but-no-tier-1) but NOT a malicious INSIDER peer (any peer holds tier-1 and could sign a hijacking binding).  Closing the insider case needs owner-only tier-1 (peers verify with pubkey, don't self-sign coord-head/Release — a publish-model change) OR rotation-authorized-by-old-key — a SEPARATE, higher-bar decision, out of scope for B+D.  Sequence: B first, then D.  (Filed 2026-06-23 from session discussion.)  Maturity review 2026-06-25: the recently-merged pull supersession-reconcile WIDENS this — a hijacked builder key can publish a higher-version claim that drives a peer's `_reconcile_pulled_supersession` to retire+prune its OWN genuine file, no publish round-trip to catch it. Ship step B (client-side TOFU refuse-on-different-key) first. |
| FED-04 | P2 | DONE 2026-06-25 | **Stale-lock breaker for `mirror publish` — a crashed/power-failed publish strands the remote flock.**  Implemented: (1) the flock'd hold-shell now runs a background HEARTBEAT that touches `<lock>.holder` every 15s + records the holder PID in `<lock>.pid` (`coord/transport.py:_flock_hold_script`), so a live-but-slow multi-GB push keeps the mtime fresh; (2) `remote_flock_lock_status`/`lock_is_stale` (stale ⇔ held + heartbeat > 75s) + `remote_flock_break` (kill holder PID, fuser fallback, clear sidecars); (3) new `mirror unlock [<name>] [--force]` command (`_unlock_decision` pure policy — probe-fail/free/refuse-live/break; refuses a fresh heartbeat unless --force, confirms before breaking otherwise); (4) both publish acquire-failure messages now report staleness + point at `mirror unlock`. Tests: `test_fed04_*` (script shapes, status parse + staleness, mocked status/break IO, unlock policy). psmisc no longer required (PID-kill primary). Original text:**Stale-lock breaker for `mirror publish` — a crashed/power-failed publish strands the remote flock.**  The publish holds `/var/lock/repo-coord.lock` via `flock -w` over a long-lived SSH session (`coord/transport.py:remote_flock_acquire`), writing a `<lock>.holder` sidecar with the builder-id and `rm`-ing it on clean release.  If the publisher DIES mid-publish (power failure on the builder; mirror host itself stays up), the orphaned `flock`+`cat` chain keeps the lock open on the mirror until sshd finally reaps the dead TCP connection — which can be HOURS (no keepalive tuning).  Meanwhile every retry fails `could not acquire remote flock — builder 'X' is publishing` (`publish.py:~819`) with the ONLY recourse a manual `fuser -k /var/lock/repo-coord.lock; rm -f …holder` on the mirror.  Observed 2026-06-24: BS2 power-failed mid-publish → PIDs held the lock 41 min, `.holder=BS2`, `flock -n` HELD; remediation was manual `fuser -k` (which needs `psmisc`).  **Fix:** detect a GENUINELY-stale lock and offer to break it (kill holder + rm sidecar) with operator confirmation — but the staleness signal must NOT false-positive on a slow-but-LIVE publish over a poor link (the exact conditions here), or one peer breaks another's in-flight publish.  Sidecar mtime alone is insufficient (a legit multi-GB push over 3 MB/s legitimately holds for a long time).  Better: a HEARTBEAT — the lock holder touches `<lock>.holder` every N seconds while alive (background `while sleep N; do touch …holder; done` inside the flock'd shell), so stale ⇔ heartbeat older than a few intervals; the breaker checks `flock -n` (still held) AND heartbeat age > threshold, then SIGKILLs the holder + clears the sidecar.  Optionally add `mirror unlock [--force]` as an explicit operator command (there is none today) and tune sshd `ClientAliveInterval` on the mirror as a belt-and-suspenders so dead sessions reap faster.  Touches `coord/transport.py` (remote_flock_acquire heartbeat + a break helper) and `publish.py` (the acquire-failure path → offer break when stale).  (Filed 2026-06-24 from session — BS2 power-failure stranded the publish lock.) |
| SUP-01 | P3 | todo | **Live-dpkg grype matcher (deferred until a consumer asks).**  SBOM-driven CVE scanning already ships (`cmd_supply_chain.py` `cmd_cve` shells grype over the SBOM — the source of truth, since NMU-stripped binaries false-positive against live grype), and `strip_nmu` stamps `X-Athena-Upstream-Version` for provenance.  A grype matcher that scans the LIVE dpkg status of an installed system (mapping our stripped versions back to upstream via the X-field) is intentionally deferred — row kept open so the capability isn't forgotten if a consumer needs installed-system scanning.  (Filed 2026-06-22 from session backlog.) |

## 8. Operator UX — P2 / P3

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| UX-06  | P3 | todo | Localised messages — today everything is English-only. |
| UX-07  | P2 | todo | **Run fully offline — a `--offline` command option.**  Once the closure is populated locally (the snapshot-pinned local build mirror via `container local mirror`, plus `cache/`, `repo/`, fetched coord trees), a build / cache-parse / audit should run on an air-gapped host with NO network access.  Today several paths still reach out: the snapshot.d.o InRelease/Packages fetch (`cache.__get_files`), per-source `download_file`, `mirror pull`/`publish` transport.  Audit every network call site, add a global `--offline` flag that REFUSES network (clear error) and serves every input from local state — making the localmirror the authoritative offline source — or fails loudly when a required artifact isn't already local.  (Filed 2026-06-26.) |
| UX-08  | P2 | todo | **Enable / disable individual fork packages.**  Today every `fork/source/<pkg>` is built + prepended to the mirror UNCONDITIONALLY (`fork_mirror.generate_fork_mirror` loops the whole tree) — the only way to drop a fork is to delete its tree.  Add a per-fork enable/disable toggle (a config list and/or a `fork enable\|disable <pkg>` command) honoured by `generate_fork_mirror` + the collision gate, so an operator can ship a SUBSET of forks (e.g. build without `athena-branding`) or A/B a single fork's effect without removing its source.  Disabled forks: skip build, don't prepend, don't claim — and the upstream package they'd shadow ships instead.  (Filed 2026-06-26.) |
| RMIRROR-01 | P2 | done | **Per-remote local-build-mirror toggle.** Today the localmirror is the machine-wide `create_local_mirror` (local.conf), which governs BOTH BS1's local build mirror AND the on-remote mirror staged at `container remote init` — so enabling it for a remote builder also makes BS1's `cache parse` offer to build its own (thin-link download). Make it a per-remote-container setting instead: prompt during `container remote add` ("Enable a local build mirror?") and persist it on the `[Remote.<name>]` entry in remote.conf (a `LocalMirror=true` key); `container remote init` / `source remotebuild` read it per-remote; BS1's own localmirror stays a separate decision. (Filed 2026-06-24 during remote-localmirror testing.) |
| API-01 | P2 | done | **HTTP API for the platform** — DONE 2026-06-25 (maturity review: all 4 planned chunks shipped + tested (7 `test_api01_*`) + documented; only the file layout diverged from the plan — backend/routes collapsed into `webapi/jobs.py` + `__init__.py`). key-protected (X-Api-Key, `config/api.key` 0600 autogen), localhost-default FastAPI server exposing pipeline state, build records + all sidecars (`.build.json`/`.buildlog`/`.vbuildlog`/container logs with tail-windowing), redacted config, repo/mirror state, and a thin **command dispatcher** (`POST /api/v1/command` feeds the existing noun-verb dispatcher; jobs queue on the single session = single-writer preserved; prompts fail fast, sudo via ATHENA_SUDO_PASSWORD).  OpenAPI `/docs` auto-generated.  Web UI lives in a SEPARATE repo consuming `/openapi.json`.  Build-host-only deps: `python3-fastapi python3-uvicorn python3-httpx` (apt).  Full design: `docs/plans/api-01-web-api.md`.  Filed 2026-06-07 (decisions taken with operator during the thor1 rebuild). |

## 9. House-cleaning — P3

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| DIST-01 | P3 | todo | **Closed-source distribution strategy (discuss later, low priority).**  Explore compiling/packaging the toolchain without shipping the Python source (Nuitka / Cython / PyArmor), with the GPL caveat that derivative-distro tooling interacting with GPL components may compel source availability regardless of the obfuscation method.  Strategic/legal discussion item — no code yet.  (Filed 2026-06-22 from session backlog.) |

## 10. Consolidation audit — 2026-06-25

> Full-tree consolidation pass (10 parallel deep-review agents over every
> module, all findings hand-verified against source).  Sanity baseline at the
> time was green (ruff + mypy + 1086-test suite all pass).  CONS-01..09 were
> FIXED in the same pass (triad-gated); CONS-10..16 were deferred.  CONS-10
> (P1), CONS-11/12/13 (P2) + CONS-14/15/16 (P3) all landed 2026-06-25 — the
> consolidation backlog (CONS-01..16) is fully CLOSED.

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| CONS-01 | P1 | done | **Decommission accepted "n" as yes.** `cmd_mirror_builders_decommission` used `if not _resp:` to cancel — but a YESNO `get_response()` returns truthy `"n"`, so answering *no* fell through and revoked the builder federation-wide (the exact trap documented at cmd_mirror.py:416). Fixed to `if _resp.lower() not in ('y','yes')`. |
| CONS-02 | P2 | done | **Missing `f` prefix** in the pre-chroot signing-key prompt (build.py:1265) — operator saw the literal `{self.config.signing_key_uid}`. F541-class. Fixed. |
| CONS-03 | P2 | done | **`container remote purge` ran `ssh` with no `-i <key>`** (build.py:1808) — failed auth under `BatchMode=yes` for any remote registered via guided `add` (key in config/<name>.key, not ~/.ssh). Routed through `_ro._ssh_base`. |
| CONS-04 | P2 | done | **`ensure_remote_image` ignored `docker save`'s exit code** (remote_orchestrate.py:135) — a failed local save could still report `'transferred'` for an image not on the remote. Now requires `_save.returncode == 0` too. |
| CONS-05 | P2 | done | **`copy_ssh_key` could leave a world-readable private key** (utils.py:2150) — if copyfile succeeded but chmod 0600 failed, the 0644 key was left on disk while returning False. Now `os.remove`s it on chmod failure. |
| CONS-06 | P1 | done | **One malformed `remote.conf` numeric bricked the whole CLI** (utils.py list_remotes) — `getint`/`getfloat` raise bare `ValueError`, not caught by `BuildConfig.__init__`'s `except (configparser.Error, OSError)`. Wrapped in a `_typed` fallback helper. |
| CONS-07 | P2 | done | **Merged the two obsolescence emitters** in coord/publish.py — `emit_obsolescence_claims` was a strict subset of `emit_supersession_obsolescence` (and compared by `built_version`, which is pristine-equal across asg levels — a latent supersession bug). Publish Step 6c now uses the superset; old function + its test repointed/removed (~75 lines). Single emitter for both publish + pull-reconcile. |
| CONS-08 | P3 | done | **Dead-code removal (grep-verified zero consumers):** utils `search()`, `_BUILD_RECORD_LEGACY_VERSIONS`; virtual_build `_PIN_RE`, `_binary_active_by_convention` (no-op); repo_audit `AuditResult`, `_BUMP_REWRITE_FIELDS`; onboarding `_add_mirror_interactive(register=)` dead param; dead local re-imports (buildcontainer urlparse, bump PkgRelation). `write_snapshot_state` legacy params KEPT (deliberately-tested back-compat). |
| CONS-09 | P3 | done | **Comment/docstring hygiene:** operator-facing command-name drift fixed (`build_cache`→`cache build`, `parse_dependency`→`cache parse`, `generate_signing_key`→`key generate`, `verify_signing_key`→`key verify`, `build_chroot`/`build_bootable`→`chroot build`, `repo strip`→`repo repair strip`); stale docstrings (signing "does NOT import keys" vs shipped `import_key`; apt_repo Stage-A header; disk_image "update-grub"); truncated fragments (`#.`, `# /`, `# D`). Bulk inline ticket-tag stripping deferred → CONS-15. |
| CONS-10 | P1 | done | **`source remotebuild` under a pending update silently skipped the re-spin.** A same-base update re-spin's colliding prior-generation file satisfied `_remotebuild_one_source`'s skip gate, which lacked the `not _bump_target` exception the local builder has — so an unforced `remotebuild` SKIPPED the delta and the update never built/shipped. (On verifying the code the dominant failure was the silent skip, not unstamped output — the `+asg` stamp self-derives in `_normalize_built_artifacts`, which runs locally on recovery.) Fixed by threading the locally-computed bump decision through: update-mode detection now applies to remote too, `_do_update_build(remote=True)` fans the delta out to the remotes, and `_remotebuild_one_source` carries `bump_active`/`bump_release` into the skip-gate exception + a post-build predicate/stamper guard. No bump logic runs on the remote. Test: `test_remotebuild_handles_update_mode_cons10`. |
| CONS-11 | P2 | done | **scp recovery not reconciled with the build marker.** `run_remote` scp'd `out/*.deb` back with no integrity check, then returned the marker's output list — a partial scp (dropped mid-transfer) yielded a `phase=done` record claiming more .debs than landed. Fixed: `run_remote` now reconciles the recovered files against the marker and signals a transport failure (exit 12) on any missing artifact, so the fan-out re-queues the package on another remote (remote_dir is wiped in `finally`). `_remotebuild_one_source` maps 12 → 'transport'. Test: `test_run_remote_reconciles_partial_recovery_cons11`. |
| CONS-12 | P2 | done | **`.deb` push trusted same-name remote files (`--ignore-existing`).** A truncated/partial prior push left a wrong-size remote `.deb` that `--ignore-existing` skipped forever → claim pins a sha the pool can't serve → `claim_apt_sha_mismatch` at peers. Fixed: `push_single_deb` now uses **`--size-only`** — a correct immutable `.deb` still skips by the cheap size stat, but a wrong-size file is healed by re-sending. (`_RSYNC_BASE` has no `--partial`/`--inplace` → atomic rename, and the frozen-bytes publish gate + RECLAIM-overwrite already guard same-name-different-bytes, so the only realistic gap was truncation = wrong size.) Same-size byte corruption is the residual (peer pull-verify / `mirror audit`; the qcow2 keeps its `remote_sha256` verify+repush). |
| CONS-13 | P2 | done | **Local build-mirror "valid-on-partial" + remote hardening.** (1) `local_mirror.download` stamped the `.snapshot` marker unconditionally, so a partially-failed mirror was marked valid-for-snapshot — and since `_ensure_local_mirror` early-returns when valid, the missing files were skipped FOREVER. Fixed: the marker is now withheld unless every file succeeded (matches the remote runner's `not failed` gate); a partial mirror is retried next `cache parse` (resumable). (2) `remote_localmirror.populate` gained a disk-space preflight (mirrors `local_mirror.disk_check`, 5% headroom) returning a clean `__disk__` failure. (3) `_download` now catches `http.client.HTTPException`, so a dropped chunked transfer (`IncompleteRead` — not an `OSError`) records one failure instead of aborting the whole population. Tests: `test_local_mirror_download_withholds_marker_on_failure_cons13`, `test_remote_localmirror_hardening_cons13`. |
| CONS-14 | P3 | done | **`repo repair cleanup force` under `--yes` could prune live-claimed files.** The publish-before-prune gate was `informational=True`, which the facade auto-answers "yes" under `--yes` — so an unattended session could delete files a mirror still serves (the ordering hazard). Fixed: the publish-before-prune gate is now **non-informational** (a headless `--yes` run defaults to "n" and aborts there), while the final IRREVERSIBLE-delete prompt STAYS informational — so automated cleanup still works in the CORRECT order (publish first → no live claims → gate skipped → obsolete set pruned). Test: `test_cleanup_publish_before_prune_gate_not_informational_cons14`. |
| CONS-15 | P3 | done | **Bulk inline ticket-tag comment cleanup.** Stripped the bare Athena ticket IDs (CONF-01 ×20, UPD-01 ×15, MIRROR-01 ×13, STA-*, SEC-05, …) from code comments/docstrings across 47 files — 149 lines — preserving all rationale; Debian/standards tokens (DEP-3, SHA-256, ISO-8601, UTF-8, RSA-*, RFC-822) and live `CONS-N` refs kept. Done via a reviewable strip script + hand-fixed the ~11 grammatical holes (ticket-as-noun, `Pre-<ticket>`, line-wrapped colons). Triad green (1279/1279). Dated-incident narration kept (it's rationale, not a bare tag); a handful of stale line-number refs remain (low value). |
| CONS-16 | P3 | done | **`compute_build_closure` tier machinery duplicated `classify_tiers`.** Its `toolchain`/`language`/`leaf`/`added` tiers + `runtime_closure` param were computed but consumed only by a test, while the only production consumer (`local_mirror.py`) reads just `all`, and the real build-path tiering is the adjacency-based `classify_tiers` (cmd_cache.py). Fixed: `compute_build_closure` now returns only `{all, unsatisfiable}`; `classify_tiers` is the single tiering path. Module docstring + the test repointed (`test_build_closure_compute_returns_all_and_unsatisfiable`); classify_tiers' own test unchanged. |

## 11. Maturity review — 2026-06-25

> Full maturity/stability/completeness review (9 parallel deep-review agents:
> backlog reconciliation ×2 + subsystem/lens reviews ×7), all findings verified
> against source.  Per-subsystem verdict: Athena (toolchain) is a
> **correctness-hardened, mature** build/federation system; Asgard (the distro)
> is **alpha** — bootable end-to-end on VMs, not yet turnkey on metal.  No P0s.
> MAT-01 (P1) was fixed in the same pass; the rest are filed below.  This review
> ALSO reconciled the existing backlog (closed API-01 + COMP-07, corrected
> INST-02/CONF-08).  Source-availability + FED-03 are the two headline gaps.

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| MAT-01 | P1 | done | **Fork-wipe failed OPEN → could wipe a pull-adopted fork.** `read_build_record` returns None for BOTH "absent" and "exists-but-unreadable" (corrupt/HMAC-fail), so a transient read failure made `_fork_is_pull_adopted` return False → the adopted fork was WIPED → the peer wrongly took ownership (the exact federation violation the guard prevents). Fixed: refactored to a tri-state `_fork_adoption_status` (`adopted`/`not-adopted`/`unknown`); an UNREADABLE record (file exists) → `unknown` → SKIP the wipe without seeding the hash (a readable re-run resolves it); the `except` fallback also now fails safe. Test: `test_fork_invalidation_skips_wipe_on_unreadable_record`. (fork_mirror.py.) |
| MAT-02 | P1 | todo | **No source is published (0 `.dsc` vs 4589 `.deb`).** The build is binary-only (`dpkg-buildpackage -b`); the input `.dsc` is consumed at extraction and never re-emitted, so the `dpkg-scansources` Sources index scans an empty set. Asgard ships binaries with no in-archive source → not DFSG/GPL source-available, not third-party rebuildable, no provenance from `.deb` back to the patched source. Distinct from CONF-03's quilt-vs-patch *process* framing — this is the *availability* consequence. Fix: re-emit `.dsc`+tarballs (`dpkg-source -b` the patched tree, or at minimum republish the upstream `.dsc` set) into pool/. Extends CONF-03. |
| MAT-03 | P2 | todo | **Live-ISO squashfs root is never identity-scanned.** SEC-07 ("CLEAN") covered text/config surfaces only; `iso.py` builds `filesystem.squashfs` with no `audit_identity` call and `_audit_staged_iso` can't see inside the blob. Debian identity/telemetry residue in the *booted* root (os-release, debconf db, docs) ships undetected — a gap against the "no leakage in shipped output" invariant. Fix: run `audit_identity` over the chroot tree before `mksquashfs`. (iso.py, identity_scan.py.) |
| MAT-04 | P2 | done | **Build-container hardening — RW input mounts.**  DONE 2026-06-25: `/source` and `/patch` are now bind-mounted **read-only** (the build copies the source out + reads patches, never writing back), so a passwordless-root `debian/rules` can no longer corrupt the host input trees through the mount; `/repo` (output) stays rw. Test: `test_mat04_build_container_mounts_source_and_patch_readonly`.  DEFERRED (separate scope): `network_mode='none'` is NOT a drop-in — the build needs outbound network to apt-fetch build-deps from the snapshot mirror (even a localmirror can fall back), so true network isolation requires a guaranteed-complete offline closure first; and unquoted pkg-names in the root `bash -c` (latent — Debian pkg-name charset has no shell metachars today) is a defense-in-depth `shlex.quote` follow-up.  SHLEX.QUOTE FOLLOW-UP DONE 2026-07-01: every source-derived name interpolated into the root container shell is now `shlex.quote`'d — build-dep names + OR-group alts (`_render_install_cmd`), the source package name + `.dsc` filename (the `cp /source/…* .` glob keeps its `*` outside the quotes), and the patch filenames.  No-op for legit Debian names (charset has no shell metachars); defense-in-depth against a malicious/untrusted source's `debian/control`.  Test: `test_render_install_cmd_shlex_quotes_pkg_names`.  STILL DEFERRED: `network_mode='none'` (blocked on a guaranteed-complete offline closure — see UX-07; forcing it today breaks the apt-fetch of build-deps from the snapshot mirror).  ORIGINAL: full outbound net + RW input mounts. `/source` and `/patch` are bind-mounted **rw** into a passwordless-root container, so a malicious/buggy `debian/rules` can corrupt host input trees for other packages in the run (cheap fix: `mode:'ro'`). The container also has unrestricted outbound internet (apt is only *pinned*, not isolated) — `network_mode='none'` is the real fix. Also: pkg names interpolated unquoted into a root `bash -c` (latent injection, goes live with untrusted sources). Accepted under the trusted-source model today, but the cheap RO + quoting wins should land. (buildcontainer.py:1213-1222, 1060-1106.) |
| MAT-05 | P2 | DONE 2026-06-25 | **CI strictness — flipped `check_untyped_defs` + enforced F401.**  F401: removed 79 genuinely-dead imports (45 scripts + 34 tests; verified NONE were real re-exports — the pyproject justification was stale), modernized 2 `# type:` comments in package.py to real annotations, dropped `F401` from the ruff ignore. `check_untyped_defs = true`: drove 122→0 errors — 28 base.py cross-mixin stubs (typed surface for cross-cluster calls), ~30 Optional-guard asserts, and genuine type bugs incl. the removed-param `password=` call (HK-07 residue), a `_show` variable-type collision masking a 4-vs-2-tuple unpack in `cmd_package_cleanup`, and a **latent bug**: `cmd_strip_repo`'s tunnel-skip pre-pass called `.get('Source')` on a `dict[Version,list[Package]]` (always None → silently fell back to binary name; now reaches an actual Package stanza). F841 stays ignored (defensive try/except locals). Unannotated function bodies are now type-checked. |
| MAT-06 | P2 | done | **Dead persistence layer removed.**  DONE 2026-06-25: the session-persistence / `resume` layer (UX-04) was abandoned after repeated consistency bugs (operator decision) and is now PURGED — deleted `persistence.py` (353 LOC), the dead `import persistence` + dormant `BuildFlags.restored_summary`, 7 `test_ux04_*`/`test_persistence_*` tests + the orphaned helper/registry/logger-map entries, and refreshed README + architecture docs. The pickle `__getstate__`/`__setstate__` hooks on Cache/DependencyTree/Package/Source are KEPT as dormant, correct serialization support (comments de-referenced). The `IncludeBuildClosure` `build_closure_pkg_names`/`build_closure_tiers` attrs are INTENTIONALLY KEPT — the self-hosting build (ship a distro able to build all its own source packages, all build-deps over install deps) will consume them.  ORIGINAL: dead persistence + inert closure attrs. `persistence.py` (353 LOC) + `import persistence` (build.py, never called) are orphaned since the `resume` command was removed; only tests keep them warm. `IncludeBuildClosure`'s flag-on path computes `dep_tree.build_closure_pkg_names`/`build_closure_tiers` with ZERO downstream readers — even enabled, the feature is inert (`compute_build_closure` itself stays live via local_mirror). Remove or re-wire both. |
| MAT-07 | P2 | DONE 2026-06-25 (REMOTE-API) | **Remote-build operational gaps → API control plane.**  Resolved by replacing the SSH-tail transport with an API-based agent (user-chosen architecture, the wall-clock-timer concern made the timer approach unfit for multi-hour builds): `scripts/remote_agent.py` runs DETACHED on the remote, drives one build, and serves a localhost HTTP API (GET /status, /logs?from=N, POST /abort, /shutdown) gated by a per-remote `X-Athena-Token`; BS1 reaches it over an SSH local-forward and POLLS. WATCHDOG is PROGRESS-based (build-log growth OR container CPU, kills on no-progress for --hang-secs, default 30 min) — NOT wall-clock, so a healthy multi-hour build is never killed. Orphan reaping FIXED: container is named `athena-build-<bundle>`, agent `docker kill`s a hung one, orchestrator `docker rm -f`s by name in finally. Builds now survive orchestrator disconnects (detached agent + resumable log offsets; tunnel auto-reopens; agent-unreachable >5min → re-queue). Token at onboarding (`container remote add` → config/<name>.remote-token, removed on delete). 4 chunks on feat/remote-agent-api. CLEANUP DONE (7de918e): dead `run_remote` SSH-tail transport removed. E2E: `tests/e2e_remote_agent.py` (manual, needs Docker) — LOCAL-DOCKER mode PASSED real (image build→container→.deb recovered→reaped; hung container actually killed by the progress watchdog via `docker kill`, no leftover). FULL-TRANSPORT mode (`--remote user@host`) drives run_remote_agent over real SSH — VALIDATED via a localhost loopback (ssh harkirat@localhost + real Docker): real ship → detached agent → ssh -L tunnel → poll → docker build/run → recover .deb → reap, exit 0. The loopback CAUGHT + FIXED a real bug: the detached-start used `cd dir && nohup … &`, where `&` backgrounds the whole `cd && nohup` compound as a subshell that runs the long-lived agent in its foreground and so holds the SSH channel open → the start ssh never returns; fixed to `cd dir || exit 1; nohup … &` so only the fd-redirected agent is backgrounded. The two-REAL-remotes fan-out (distinct hosts) still wants a run on live hardware, but the transport itself is now real-validated. Original text:**Remote-build operational gaps.** No remote-side build WATCHDOG — a hung remote build (stuck container/apt) pins a fan-out slot forever; only operator SIGINT escapes. Orphan-container reaping is MANUAL-only (`container remote purge`): SIGINT/transport-fail/kill all leave the remote `docker run` running. Fix: `timeout` the remote build → transport-12 (auto-requeue); tag containers + `docker rm -f` by label on cleanup. Also: the two-real-remotes fan-out e2e is still unvalidated. (remote_orchestrate.py:300-318, cmd_source.py:1993-2003.) |
| MAT-08 | P2 | done | **No on-media integrity manifest.**  DONE 2026-06-25: both ISO builders now write a Debian-style `md5sum.txt` (sorted `<md5>  ./path`, excluding itself) at the staging root as the LAST step before grub-mkrescue, via a shared `utils.write_iso_md5sum_manifest` (sudo askpass — staging has root-owned files). This ACTIVATES the installer's already-present `cdrom-checker` ("Check disc integrity"); the live ISO also gained a `live-media-check` GRUB boot entry. So a corrupt USB/DVD write is now caught at boot instead of failing mid-install. Best-effort (a manifest failure WARNs, never blocks the ISO). SHA256SUMS deferred (no on-media reader). Tests: `test_write_iso_md5sum_manifest_mat08`, `test_iso_builders_write_md5sum_manifest_mat08`.  ORIGINAL: d-i integrity-check / live-boot media verification had nothing to verify against. d-i's integrity-check and live-boot media verification have nothing to verify against; a corrupt USB write fails silently mid-install on real hardware — squarely in COMP-01h's blast radius. Per-file hashing infra already exists. Fix: emit a checksum manifest into the ISO tree before mkrescue. (iso_installer.py, iso.py.) |
| MAT-09 | P2 | done | **Live/disk images shipped a fixed root password (`root`).**  DONE 2026-06-25: the base chroot now sets a per-build RANDOM root password (was `--root-password=root`), printed + written to a 0600 root-credentials.txt, and bakes a `%sudo NOPASSWD` sudoers.d so the live user (added to `sudo` by live-config) can still escalate. The DISK image goes cloud-style: LOCK root + add an `asgard` sudo user with a random console password (passwordless sudo via the baked rule). Installer target unaffected (d-i sets it fresh). Tests: `test_mat09_live_root_password_randomized_and_sudo_guaranteed`, `test_mat09_disk_image_locks_root_and_adds_sudo_user`.  ORIGINAL: only the live username was randomized; the root password was the literal `root`. SEC-04 randomized the live *username*; the *root* password is still the literal `root` on every image — a shipped default credential for a "real distro". Fix: randomize or lock the live root password. (chroot.py:1540.) |
| MAT-10 | P2 | DONE 2026-06-26 (a–e) | **Build/chroot crash-consistency + robustness cluster.**  (a) DONE: `_umount_chroot_fs` now post-asserts `ismount` per target, reports a WEDGED survivor + returns bool; the chroot-wipe path REFUSES (raises) on a wedged mount instead of `find -delete`ing through a live host bind-mount (squashfs/ISO path already guarded by mksquashfs `-e proc/sys/dev`). (b) DONE: `selected_srcs`/`download_size` initialised in `BuildSession.__init__` (were lazy-only → latent AttributeError). (c) DONE (parity test, consolidation deferred): structural canary `test_mat10c_bump_decision_parity_triggers` asserts both the real-build + virtual-build +asg decisions reference the same DELTA + LINEAGE triggers. (d) DONE: selection-accept reordered so `write_selection_state` (the signed authority) is the LAST/commit write, after the deprecation marks — a crash leaves the authority unchanged + re-derivable marks. (e) DONE: `_verify_index_against_release` fails CLOSED when no InRelease SHA covers the index (was fail-open). Original text:**Build/chroot crash-consistency + robustness cluster.** (a) Wedged-mount → contaminated image: `umount -lf` rc unchecked + no stale-mount sweep on resume/`for_iso` (chroot.py:446, buildsystem.py:118) — add an `ismount` post-assert + unconditional early sweep. (b) `selected_srcs`/`download_size` lazy-init'd outside `__init__` → latent AttributeError (base.py:47-48). (c) Triple-duplicated bump decision (bump.py / buildcontainer.py / virtual_build.py) with no parity test. (d) Selection-accept path (cmd_cache.py:946-963) writes lockfile-shrink + mark-deprecated non-transactionally (crash between → drift only `mirror audit` catches). (e) `_verify_index_against_release` fails OPEN when Release lists no SHA for a path (cache.py:456). |
| MAT-11 | P3 | DONE 2026-06-26 (1,4,6 done; 5,7 moot; 2,3 accepted as-is) | **Minor hardening cluster.**  (1) DONE: `apt_pkg.init_system()` added before `version_compare` in the 2 coord sites that lacked it (filter_pending_by_ownership, project_post_publish_state) — were at risk of the lexical `6.10<6.9` compare. (4) DONE: remote-build `_ssh_base`/`_scp_base` now use `StrictHostKeyChecking=accept-new` (parity with every other ssh site). (6) DONE: AST drift test `test_mat11_mirror_help_subcommands_all_dispatch` (advertised ⊆ real for the mirror umbrella; reusable helper). (5) MOOT (verified): `revoke_builder` writes `revoked_builders` into the TIER-1-SIGNED coord-head (re-sign needs the signing key) + freshness-gated — a plain SSH writer can't un-revoke; already covered by FED-03/head signing. (7) MOOT: `base-files/etc/os-release` already carries HOME_URL/SUPPORT_URL/BUG_REPORT_URL. ACCEPTED AS-IS (operator decision 2026-06-26 — both are fine; revisit only if they surface in an architecture issue): (2) `_audit_staged_iso` deliberately fails OPEN on a missing allowlist (test convenience); (3) single `[Audit] IdentityScan` switch gates all three identity gates (by-design). Original text:**Minor hardening cluster.** apt_pkg-init inconsistency → lexical version-compare fallback (kernel `6.10<6.9` class) in 3 coord sites; identity gate `_audit_staged_iso` FAILS OPEN when the allowlist file is absent (iso_installer.py:1093); single `[Audit] IdentityScan` switch disables all three identity gates; remote-build ssh omits `StrictHostKeyChecking=accept-new` (inconsistent with every other ssh site); `revoke_builder` is reversible by any SSH writer (fold into FED-03 keyring work); tooltip subcommand lists have no drift test (sharpens DOC-06 into an `advertised ⊆ real` assertion). os-release lacks HOME_URL/SUPPORT_URL/BUG_REPORT_URL. |

---

## How to update this file

- When you start a task, change its status to `wip` and (optionally) add a
  parenthetical with the commit / PR.
- When you finish, change to `done` and append the closing commit hash in
  italics on the same row.  **Then move the row to `docs/done.md`** under
  the matching section header — keeps this file focused on open work.
  Same drill for `wontfix`.  Do **not** delete the row outright; the
  embedded history (commit hashes, run notes, decision rationale) is the
  audit trail.
- New tasks: append to the relevant section here with the next free ID
  (`STA-18`, `COMP-11`, etc.). Never re-use an ID, including IDs that
  already live in `docs/done.md`.
- If priorities change, edit the `Sev` column; do not move rows between
  sections.

