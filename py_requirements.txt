# Python package requirements
# Format: import_name   install_name
# Lines starting with # are ignored

# install_name is the Debian/apt package (build-system.sh suggests
# `sudo apt install <install_name>`); the import_name is what Python sees.
psutil        python3-psutil
requests      python3-requests
apt_pkg       python3-apt
debian        python3-debian
gnupg         python3-gnupg
docker        python3-docker
apt           python3-apt
aptsources    python3-apt
