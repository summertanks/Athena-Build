# Pre-build-cycle codebase audit — 2026-06-29

> One auditor per code file (80 files) + adversarial verification of every high/critical finding + five real-data boundary tests against the cached pinned snapshots + a coverage-gap pass over the version-scheme tests. Generated by a multi-agent audit workflow (95 agents, ~4.5M tokens).

## Summary

- **Files audited:** 80
- **Fully clean:** 6
- **Verified high/critical:** 8 (each independently re-checked by an adversarial verifier)
- **Medium/low:** 199 (39 medium / 160 low) — single-pass, NOT adversarially verified; confirm before acting
- **Medium/low by category:** correctness 68, missing-test 35, dead-code 35, redundancy 24, boundary 19, optimization 18

## Real-data boundary tests

Each test imported `scripts/bump.py` and ran against the real cached snapshots, using `dpkg --compare-versions` as the ordering oracle.

| Test | Ran | Passed | Cases | Violations |
|---|---|---|---|---|
| transpose-ordering | True | True | 66,739 | 0 |
| strip-pristine-idempotence | True | True | 83,726 | 0 |
| dep-constraint-rewrite | True | True | 66,960 | 0 |
| filename-prediction-parity | True | False | 63,762 | 1 |
| tilde-floor-safety | True | True | 5,629 | 0 |

### ⚠️ filename-prediction-parity

Pure (no-build) parity test of the two consumers in scripts/bump.py: predictor _needs_bump_build (commands/cmd_source.py:688, transposed_version(strip_nmu_suffix(bin_ver)+src_suffix)) vs normalizer _normalize_built_artifacts->transpose_deb (buildcontainer.py:1905, transpose of binary's own version, +bN stripped). Script: (scratchpad helper filename_parity.py). Parsed REAL cache: bookworm main+security+contrib+non-free-firmware Packages & Sources (20260624 snapshot). 63762 real (source,binary) tuples across pristine(59062), +debNuK(3823), ~debNuK(877), epoch(4072), embedded-+deb(7), +nmu(113). dpkg used as ordering oracle.

ONLY divergence = shim-signed family (the task's explicit high-scrutiny 'embedded +deb (shim)' case). All other categories pass byte-identical, including lvm2/e2fsprogs/samba multi-binary-base, epoch (amanda-common 1:3.5.1-11+deb12u2 -> +asg1u2), ~deb floors (adequate ~asg), +nmu (kept), and the two-level libhttp-daemon-perl 6.16-1+deb13u1~deb12u1 (A==B because there binary_version==source_version so the suffix matches; verified with real data).

NOT counted as violations (explained): (1) gcc-11/gcc-12 cross-source Binary: overlap — my first pass naively joined gcc-11's Binary: list to the global-highest binary (libgomp-plugin-* at 12.2.0-14+deb12u1 actually belongs to gcc-12 per its Packages Source: field). Fixed by attributing each binary to its canonical Source: (mirrors virtual_build's _upstream_canonical_source skip); the 2 gcc 'violations' vanished. (2) One secondary ordering_fail (libhttp-daemon-perl 6.16-1+deb13u1~asg1u1 vs fully-stripped 6.16-1) was a false positive in my own heuristic: a ~asg token sorts below its IMMEDIATE +deb base (design intent, dpkg-verified) and is legitimately above the fully-stripped pristine because the embedded +deb13u1 elevates it; not a parity violation (A==B).

passed=false because the shim-signed divergence is a genuine function-level defect in _needs_bump_build, surfaced by the very sample flagged for highest scrutiny; it is dormant only by virtue of the Tunneled= config, not by correctness.

- **shim-signed** — _needs_bump_build (predictor) and _normalize_built_artifacts (normalizer) produce DIFFERENT filenames for shim-signed and shim-signed-common. Source ver=1.44~1+deb12u1, binary ver=1.44~1+deb12u1+15.8-1~deb12u1. Predictor A: shim-signed_1.44~1+deb12u1+15.8-1+asg1u1_amd64.deb. Normalizer B: shim-signed_1.44~1+deb12u1+15.8-1~asg1u1_amd64.deb. They differ in SIGN (+asg vs ~asg) and dpkg confirms A>B (not equal) -> skip-gate would never match -> infinite-rebuild risk. Same divergence on shim-signed-common (2 binaries, 1 source). Root cause: _needs_bump_build reconstructs the binary's upstream version as pristine_base(binary) + SOURCE_suffix, where source suffix='+deb12u1'; but the binary's OWN trailing redistribution token is '~deb12u1' (the build appends a +15.8-1~deb12u1 tail, so binary_version != source_version). transpose then rewrites the wrong-signed token. The normalizer and the virtual_build predictor both correctly use the binary's OWN version via strip_binNMU; only _needs_bump_build's src_suffix reconstruction is the odd path. CURRENTLY MASKED: shim-signed is listed in config/build.conf:119 Tunneled=, so it never reaches _needs_bump_build (tunnels use upstream filenames). Latent/dormant, not active, but would fire if shim-signed (or any source whose build-generated binary version has a trailing token differing from the source's) were ever moved to built mode. Fix: have _needs_bump_build derive the expected version from the binary's own upstream version (strip_binNMU) like the normalizer/virtual_build, not from src_suffix.

## Confirmed high/critical findings (adversarially verified)

### [HIGH] `scripts/buildcontainer.py:2015-2045` — Segregate rollback moves an already-published .deb out of the repo (append-only violation, data loss)

- **Category:** correctness
- **Evidence:** In the exact-name collision branch (lines 2015-2016) the freshly-built scratch dup is os.remove'd and the EXISTING PUBLISHED path `_dst` is appended to `_moved_paths` (this entry was never moved out of source_dir). If a LATER file in the same source then fails its move, the OSError rollback loop (2035-2039) treats every `_moved_paths` entry as a relocation to reverse and does `os.rename(_done_dst, _back)` into source_dir — so it yanks the published artifact out of dists/.../binary-<arch>/ into the per-worker scratch dir, which build()'s finally `shutil.rmtree(_scratch_dir)` (line 1524) then deletes. Net result: a byte-identical rebuild of a multi-binary source can DESTROY a previously-published .deb and return [] (recording nothing). Reachable on any pristine (no +deb token, so segregate's pre-normalize name == published name) multi-binary source rebuilt when a sibling move hits ENOSPC/EPERM/dest-is-a-file. The append-only invariant the branch comment (1989-2009) is explicitly protecting is broken by its own rollback.
- **Suggested fix:** Track kept-existing duplicates separately from genuinely-moved files (e.g. append `_dst` to a `_kept_existing` list, not `_moved_paths`, in the collision branch), and have the rollback loop reverse ONLY genuinely-moved entries. The kept-existing published file must never be renamed during rollback; still return it (or merge the two lists) for the success-path caller so output tracking stays complete.
- **Verification:** Confirmed real by reading scripts/buildcontainer.py:1922-2051 plus caller at 1371-1380 and build() finally at 1524. In the exact-name collision branch (2015-2016) the freshly-built scratch dup is os.remove'd and the EXISTING PUBLISHED path _dst is appended to _moved_paths even though it was never moved out of source_dir. source_dir is the per-worker _scratch_dir (call site line 1371-1372: _segregate_built_artifacts(src_pkg, _scratch_dir,...)). The OSError rollback loop (2035-2039) treats every _moved_paths entry as a relocation to reverse: _back = os.path.join(source_dir, basename(_done_dst)); os.rename(_done_dst, _back). For the kept-existing entry this renames the published .deb (e.g. dists/.../binary-amd64/foo.deb) INTO the scratch dir; the original scratch copy was removed at 2015 so the target name is free and the rename succeeds. The function returns [] (caller records outputs=[] at 1376-1380), then build()'s finally shutil.rmtree(_scratch_dir, ignore_errors=True) at 1524 deletes the relocated published artifact. Net: a byte-identical multi-binary rebuild destroys a previously-published .deb and records nothing. Reachability is real: segregate runs BEFORE _normalize_built_artifacts (1397), so filenames are pre-normalize; for a pristine source (no [+~]debNuK token) the raw built name equals the published name, producing an exact collision on rebuild. os.listdir order is arbitrary so a collision file can precede a sibling whose os.rename fails with ENOSPC/EPERM/EXDEV/dest-is-a-file, firing the rollback. This breaks precisely the append-only invariant the branch comment (1989-2009) claims to protect. Severity high: silent destruction of published bytes with an empty build record, and ENOSPC mid-source is plausible during a multiday build cycle; only mitigation is that it needs a concurrent filesystem error during a sibling move. The suggested fix (separate kept-existing list excluded from rollback, merged only into the success-path return) is correct.

### [HIGH] `scripts/signing.py:327-345` — verify_key filters by config.signing_key_uid, not the key actually present — breaks federation peer onboarding when origin UID != peer default

- **Category:** correctness
- **Evidence:** verify_key() first calls get_key_info(config) (line 337), which runs `gpg --list-secret-keys --with-colons config.signing_key_uid` (signing.py:178-181). I confirmed empirically that gpg returns rc=2 when that UID does not match any on-disk key, so get_key_info returns None and verify_key returns (False, "no signing key for '<uid>'"). In the federation join, onboarding._validate_and_install_gpg imports the ORIGIN's tier-1 key into signing_home and immediately calls signing.verify_key(config) (onboarding.py:414) — BEFORE _prompt_machine_settings adopts the real UID via signing.actual_signing_uid (onboarding.py:198, which runs later at run_onboarding:127, after _onboard_federation returns at :113). signing_key_uid is a machine-local local.conf key not carried in canonical.json, so a peer's value stays the default 'Athena Build <athena@local>'. The origin operator is explicitly prompted to customize the identity (onboarding.py:238-239). When they do, the peer's verify_key falsely reports the freshly-imported key as unusable and aborts the join (onboarding.py:415-417). The module already exposes actual_signing_uid() precisely because config UID is unreliable for peers, yet verify_key ignores it. The sign step's `--local-user uid` (line 357) shares the same defect but is masked by the earlier get_key_info short-circuit.
- **Suggested fix:** In verify_key, resolve the on-disk UID first (uid = actual_signing_uid(config) or config.signing_key_uid) and use that for both the existence check and `--local-user`; or gate on get_key_info via the unfiltered key. Alternatively fix the caller to set config.signing_key_uid = actual_signing_uid before onboarding.py:414. Add a federation-onboarding test that does NOT stub verify_key (test_module.py:652 currently stubs it, hiding this).
- **Verification:** REAL bug, confirmed by code trace + empirical gpg test. verify_key (signing.py:337) gates on get_key_info(config), which runs `gpg --list-secret-keys --with-colons config.signing_key_uid` (signing.py:178-181) — filtered by the CONFIGURED uid; the sign step also uses --local-user config.signing_key_uid (signing.py:345,357). In federation onboarding, _validate_and_install_gpg imports the ORIGIN's tier-1 key into signing_home (onboarding.py:410) then immediately calls verify_key(config) at onboarding.py:414, aborting on failure (415-417). At that point config.signing_key_uid is still the peer's local.conf default 'Athena Build <athena@local>' (utils.py:2828 fallback; machine-local, absent from canonical.json). The real UID is adopted only later by _prompt_machine_settings via signing.actual_signing_uid (onboarding.py:198), which run_onboarding invokes at :127 AFTER _onboard_federation returns at :113. I confirmed _onboard_federation (255-326) never sets signing_key_uid before the verify call. I empirically reproduced the gpg semantics in a fresh homedir: list with matching UID rc=0, list with non-matching peer default 'Athena Build <athena@local>' rc=2 (→ get_key_info None → verify_key returns (False,'no signing key for ...')), list with no filter (actual_signing_uid) rc=0. So when the origin customizes its signing identity (explicitly prompted at onboarding.py:238-239), a peer's verify_key falsely reports the freshly-imported key unusable and aborts the join. actual_signing_uid exists precisely for this (its docstring says so) but verify_key ignores it. The onboarding tests stub verify_key (test_module.py:652: `lambda cfg: (verify_ok,'stub')`), hiding the defect. Severity high (not critical): conditional on the origin customizing the UID — if origin keeps the default, origin key UID == peer default and the join succeeds; and it fails loud (aborts) rather than silently corrupting. But customization is a supported, encouraged path (the whole project identity is 'Asgard', strongly implying a non-default signing UID), so it will realistically break peer onboarding.

### [MEDIUM] `scripts/bump_version.py:181-184` — --freeze-stamp commit fails: _buildstamp.py is gitignored, git add aborts

- **Category:** correctness
- **Evidence:** _files includes _BUILDSTAMP_PY when _a.freeze_stamp, then `_git('add', *_files)` runs. scripts/_buildstamp.py is gitignored (.gitignore:113, confirmed via `git check-ignore`). `git add` on an ignored path exits 1 (reproduced), so _git() raises SystemExit. By this point _rewrite() has already mutated pyproject.toml and _version.py (lines 163-168) and _write_buildstamp ran, leaving a dirty tree with NO commit and NO tag — a half-applied release. The buildstamp is by design gitignored ('never commit a stale stamp', line 117), so it should not be in the commit at all.
- **Suggested fix:** Drop _BUILDSTAMP_PY from the commit set: `_files = [_PYPROJECT, _VERSION_PY]` unconditionally. The freeze-stamp file is meant to stay untracked; rewriting it on disk (done at line 170) is sufficient.
- **Verification:** REAL bug, reproduced. scripts/_buildstamp.py is gitignored (.gitignore:113) and untracked (`git ls-files --error-unmatch` → exit 1; the file does not currently exist). `git add` on an explicitly-named ignored path exits 1 even when batched with valid paths — reproduced in a scratch repo: `git add good.py ig.py` → `multi-add-exit=1` with "paths are ignored... Use -f". In bump_version.py the `--freeze-stamp` commit path is unguarded: `--no-commit` returns at line 172 and `--dry-run` at line 175, both BEFORE line 181. So with `--freeze-stamp` (no --no-commit/--dry-run), line 163-168 already rewrote pyproject.toml + _version.py, line 169-170 wrote _buildstamp.py, then line 181-183 puts _BUILDSTAMP_PY into _files and `_git('add', *_files)` returns 1 → _git() raises SystemExit (line 49-50). Commit (184) and tag (188-192) never execute → half-applied release: version files mutated, stamp written, no commit, no tag. The docstring (lines 24, 116-117) explicitly says the stamp is gitignored and must never be committed, so its presence in the commit set is wrong by the script's own design; the suggested fix (drop _BUILDSTAMP_PY from _files unconditionally) is correct since line 170 already writes it to disk. Severity corrected high→medium: the flag combo fails deterministically, but the failure is loud (SystemExit + git error), the dirty tree is trivially recoverable and re-runnable, and this is a manually-invoked toolchain release tool that does not gate the imminent build cycle.

### [MEDIUM] `scripts/dependencytree.py:583-622, 835` — OR-grouped Pre-Depends (alt_pre_depends) are never resolved or validated

- **Category:** correctness
- **Evidence:** package.py:207-208 splits Pre-Depends into pre_depends (single-name groups) and alt_pre_depends (OR groups, e.g. 'Pre-Depends: A | B'). In parse_dependency, line 583 adds only _selected_pkg.pre_depends and line 588 sets _alt_depends = _selected_pkg.alt_depends — alt_pre_depends is never read, so an alternative pre-dependency whose providers are not otherwise pulled in never enters selected_pkgs. validate_selection (line 835) also iterates only .alt_depends, so the unsatisfied OR pre-dep is never even flagged. Verified: `grep alt_pre_depends dependencytree.py` returns no hits; dep_drift.py/chroot.py/surfaces.py/print_commands.py all consume it, confirming it is a populated, real field that only the resolver ignores. No test exercises this (tests/test_module.py stubs set alt_pre_depends=[]).
- **Suggested fix:** Treat alt_pre_depends like alt_depends: in parse_dependency feed `_selected_pkg.alt_depends + _selected_pkg.alt_pre_depends` through the alternative-selection loop (line 588/590), and in validate_selection iterate `alt_depends + alt_pre_depends` (line 835). Add a resolver test with a package declaring 'Pre-Depends: A | B'.
- **Verification:** CODE CLAIM CONFIRMED. package.py:206-208 splits Pre-Depends into `pre_depends` (single-name groups) and `alt_pre_depends` (OR groups). In the resolver, dependencytree.py:583 adds only `list(_selected_pkg.pre_depends)` and line 588 sets `_alt_depends = _selected_pkg.alt_depends`; `grep -n alt_pre_depends scripts/dependencytree.py` returns NONE. So OR-grouped Pre-Depends never reach the alt-selection loop (590-622), meaning — unlike alt_depends, which defaults to its first alternative at line 621-622 — no provider is pulled for an OR pre-dep. validate_selection at line 835 iterates only `.alt_depends`, so an unsatisfied OR pre-dep is also never flagged (silent). The field is genuinely live: it is consumed by chroot.py:652, surfaces.py:54, print_commands.py:1071-1083, dep_drift.py and cmd_cache.py — only the resolver/validator ignore it. The asymmetry contradicts the resolver's own comment at line 582 ("Pre-Depends must be satisfied ... treat same as Depends"). So this is a real latent defect with a silent-failure mode.\n\nSEVERITY DOWNGRADED high→medium because it does NOT manifest as a broken closure in the current live configuration. The actual distro data has only three distinct OR Pre-Depends patterns (repo Packages grep): `debconf(>=0.5)|debconf-2.0`, `debconf|cdebconf`, and `init`'s `systemd-sysv|sysvinit-core`. All their providers are pulled into selected_pkgs by other routes: `debconf` is an explicit [base] seed (config/pkg.list:64) and a transitive Depends of nearly everything; `systemd-sysv` is an explicit [base] seed (pkg.list:82). Critically, NOTHING Depends on systemd-sysv via a regular Depends (`awk` over all Packages = empty) — the init metapackage's OR Pre-Depends is the ONLY dep-graph link to an init system, so if systemd-sysv were ever dropped from pkg.list (relying on `init` to pull it the Debian-standard way), the resolver would silently omit the init system AND validate_selection would not warn. So today the bug is masked by explicit seeding; it is a fragile, silent correctness gap rather than an active build-breaker. Reachable in principle (build-mode/surfaces subsets, or any pkg.list trim), not in the current full-distro path. The suggested fix (feed `alt_depends + alt_pre_depends` through both the selection loop and validate_selection) is correct and low-risk.

### [MEDIUM] `scripts/fork_mirror.py:615-618, 722-724` — Empty/malformed changelog raises IndexError that escapes the narrow except and aborts the entire cache build

- **Category:** correctness
- **Evidence:** _read_pkg_version (line 667) calls str(Changelog(fh).version). I verified in this env that Changelog('').version raises IndexError ('list index out of range'), and ChangelogParseError's MRO is (Exception, BaseException) — NOT a subclass of OSError or ValueError. Both call sites guard only `except (OSError, ValueError)` (line 616 in _generate_source_packages, line 723 in _build_packages_stanzas). _discover_fork_source_trees (line 262-267) admits a fork dir based on debian/control alone, so a tree with a present-but-empty/truncated changelog passes discovery, then IndexError propagates out of _generate_source_packages -> generate_fork_mirror -> cache.py:137, crashing the cache build with an opaque traceback. This contradicts the module's stated contract ('Failures are logged + skipped — don't fail the whole helper', line 604-605) and the sibling _compute_dep_hash which deliberately uses the broad `except (OSError, Exception)` (line 314) for exactly this changelog read.
- **Suggested fix:** Broaden the except at both call sites (and/or inside _read_pkg_version) to also catch IndexError and debian.changelog.ChangelogParseError — or mirror _compute_dep_hash's `except (OSError, Exception)` — and `continue`, so a broken fork is logged-and-skipped rather than aborting the run.
- **Verification:** The finding is technically accurate on every checked point.

VERIFIED FACTS:
- scripts/fork_mirror.py:667-671 `_read_pkg_version` does `str(Changelog(fh).version)`. Confirmed in this env that `Changelog('').version` (and whitespace-only) raises IndexError ('list index out of range'). A present-but-empty/truncated changelog therefore raises IndexError. (Note: a missing changelog raises FileNotFoundError/OSError which IS caught; malformed-but-nonempty content like 'not a changelog' returns version 'None' without raising — so the live trigger is specifically an empty/truncated-to-no-entry changelog.)
- IndexError MRO is (IndexError, LookupError, Exception, BaseException). `issubclass(IndexError, ValueError)` and `issubclass(IndexError, OSError)` are both False. Both guard sites — line 616 in `_generate_source_packages` and line 723 in `_build_packages_stanzas` — catch only `(OSError, ValueError)`, so IndexError escapes. ChangelogParseError MRO is (ChangelogParseError, Exception, ...) — also not OSError/ValueError, confirming the secondary claim (though by default strict=False it usually does not raise).
- `_discover_fork_source_trees` (lines 250-269) admits a fork dir based solely on `debian/control` existing (line 262-267); it never checks the changelog, so a tree with empty changelog passes discovery.
- No wrapping try/except: cache.py:137 calls `fork_mirror.generate_fork_mirror(buildconfig)` with no guard, and generate_fork_mirror calls `_generate_source_packages` (line 159) directly. An IndexError thus propagates out and aborts the cache build with an opaque traceback.
- Contract contradiction confirmed: the docstring (lines 602-603) says 'Failures are logged + skipped (don't fail the whole helper)', and the sibling `_compute_dep_hash` (line 314) deliberately uses `except (OSError, Exception)` for the exact same changelog read, gracefully yielding an empty version. The two source-pkg sites are inconsistent with that intent.

So this is a real, reachable robustness bug; the suggested fix (broaden to catch IndexError/ChangelogParseError, or mirror `_compute_dep_hash`) is correct.

SEVERITY: downgraded high->medium. The trigger is an uncommon malformed-input state (operator-maintained fork trees normally carry a valid changelog; a missing file is already handled). The failure mode is a loud, immediately-visible crash with an obvious fix, not silent data corruption or a wrong-but-accepted build artifact. It degrades a should-skip path into an abort, which is worth fixing before a long build cycle, but it is not high-impact in the silent-corruption sense.

### [MEDIUM] `scripts/identity_scan.py:232-239` — IndexError when 'apt-install' appears only in an inline comment

- **Category:** correctness
- **Evidence:** Line 232 gates on `'apt-install' in stripped` (the full line), but line 236 strips a ' #' inline comment into `head`, and line 239 does `head.split('apt-install',1)[1]`. If the token lives only after a ' #' comment, `head` no longer contains it, so the split returns a 1-element list and `[1]` raises IndexError. Confirmed live: `_parse_apt_install_line('echo done # call apt-install foo later\n')` raises IndexError. audit_chroot_hooks (line 368) calls this with no try/except, so the exception propagates and aborts the whole hook audit, which runs in the installer build flow (installer_chroot.py:492). Hook scripts commonly carry trailing comments mentioning apt-install.
- **Suggested fix:** After computing `head`, guard: `if 'apt-install' not in head: return None` before the split (the comment-stripped command is what matters), or wrap the split and return None on absence.
- **Verification:** Confirmed real logic flaw at scripts/identity_scan.py:232-239. The presence-gate at line 232 tests the full lstripped line ('apt-install' in stripped), but line 236 strips a ' #' inline comment into `head` and line 239 splits that comment-stripped `head` on 'apt-install' and indexes [1]. When 'apt-install' appears ONLY after a ' #' inline comment on a line whose command portion does not start with '#', the gate passes yet `head` lacks the token, so split returns a 1-element list and [1] raises IndexError. The full-comment case is caught by stripped.startswith('#') at line 230, but inline comments are not. Live repro: _parse_apt_install_line('echo done # call apt-install foo later\\n') raises IndexError at line 239. Consumer chain is unguarded: called at line 368 inside audit_chroot_hooks's per-line loop (no try/except), which is invoked from installer_chroot.py:492 within _audit_and_strip_chroot_hooks, itself called at installer_chroot.py:121 in the live installer-chroot build flow; I verified there is no surrounding try/except (lines 437-520), so the exception aborts the whole hook audit and the installer build. Severity downgraded from high to medium: the trigger is contingent on a specific input shape (apt-install in a trailing inline comment on a command line), and a grep of the current fork hook scripts under fork/source/athena-* found NO such pattern, so it does not fire on the present tree — it would require an upstream/future hook with that comment shape in the scanned subtrees (pre-pkgsel.d / finish-install.d). It also fails loudly with a precise traceback rather than silently corrupting build output. The suggested fix (guard `if 'apt-install' not in head: return None` before the split) is correct.

### [MEDIUM] `scripts/webapi/readers.py:231-254` — mirror_state() reads legacy mirror.<name>.state files, ignoring the authoritative signed mirror.conf

- **Category:** correctness
- **Evidence:** scripts/mirror.py (lines 630-858) establishes that mirror state now lives in ONE HMAC-signed config/mirror.conf; load_mirror_conf migrates legacy mirror.<name>.state files into it and leaves the old files 'in place (non-destructive)'. read_mirror_state()/registered map all read from mirror.conf. But readers.mirror_state() globs only `mirror.*.state` (line 239) and never opens mirror.conf. Consequence: (a) it reports FROZEN/stale data from post-migration leftover .state files; (b) any mirror registered only into mirror.conf (no legacy .state file ever existed) is INVISIBLE to the API; (c) it serves data without the HMAC signature verification mirror.py enforces, contradicting this function's own docstring claim of serving authoritative federation metadata.
- **Suggested fix:** Replace the .state globbing with mirror.py's authoritative loader (load_mirror_conf / the registered-map accessor), returning the verified mirrors dict from mirror.conf and surfacing its verify status; drop the direct .state reads.
- **Verification:** REAL bug, confirmed against code. readers.mirror_state() (scripts/webapi/readers.py:236-247) only globs files matching `mirror.*.state` and never opens mirror.conf. scripts/mirror.py makes mirror.conf the sole authoritative store: module comment lines 630-639 ("Mirror state lives in ONE HMAC-SIGNED document, config/mirror.conf"); write_mirror_state (842-855) writes ONLY mirror.conf via save_mirror_conf; read_mirror_state (832-839) reads ONLY mirror.conf and refuses on badsig/malformed; load_mirror_conf (715-754) enforces HMAC via _verify_record (line 746). _migrate_legacy_mirror_state docstring (line 678) confirms legacy .state files are left in place non-destructively after migration. All three sub-claims verified: (a) leftover .state files go stale because no code writes them post-migration; (b) fresh/newly-registered mirrors exist only in mirror.conf, so the .state glob yields an empty `_mirrors` dict and they are invisible to GET /api/v1/mirror; (c) the reader skips the HMAC verification mirror.py enforces, contradicting the docstring's "authoritative federation metadata" claim. Live path confirmed: scripts/webapi/__init__.py:163-167 wires readers.mirror_state into the /api/v1/mirror route. Severity corrected high->medium: this is a read-only observability/diagnostics web endpoint that serves already-public federation metadata; it cannot corrupt builds, mis-version artifacts, or affect the imminent build cycle. Suggested fix (route through load_mirror_conf / read_mirror_state and surface verify status) is correct.

### [LOW] `scripts/commands/cohorts.py:126-129` — _tunnel_filenames_for_source picks source_hashtable[0] (parse order), not the resolved/highest-version source

- **Category:** correctness
- **Evidence:** Line 126-128 does `_cands = _cache.source_hashtable.get(src_name, [])` then `_src = _cands[0]`. source_hashtable is a `defaultdict(list)` appended in cache-parse order (cache.py:775), so `_cands[0]` is arbitrary w.r.t. version when a source exists in multiple snapshots (main + security + updates — a documented mixed-snapshot reality). Every other consumer resolves the source deliberately: cmd_source.py:1457 uses `max(_sources, key=lambda s: s.version)` ('Highest version wins'); cmd_virtual.py:108-114 `_lookup` prefers `selected_srcs.get(name)` and only falls back to `_candidates[0]`. The caller _do_tunnel already HOLDS the resolved source (cmd_tunnel.py:503 `self.dep_tree.selected_srcs.get(name)` -> _src_pkg) but passes only `src_pkg.package`, forcing this re-lookup to the wrong object. Consequence: `_src.binary` (the binary set, line 138) and base/version are taken from a possibly-older version, while the download URL is built from the SELECTED source's dir (`src_pkg.directory`, cmd_tunnel.py:273) — a version mismatch that yields a 404 or wrong-binary download, the exact failure the docstring (L105-108) says this path exists to prevent. It also breaks the docstring's 'SAME gates virtual_build uses' claim, since virtual predicts from the selected source.
- **Suggested fix:** Prefer the already-resolved source: look up `self.dep_tree.selected_srcs` / `self.udeb_dep_tree.selected_srcs` for src_name first; failing that use `max(_cands, key=lambda s: s.version)` (mirroring cmd_source.py:1457 and cmd_virtual's _lookup), not `_cands[0]`. Better still, have _do_tunnel pass the src_pkg object it already holds.
- **Verification:** The factual assertion is true: cohorts.py:126-127 does `_cands = _cache.source_hashtable.get(src_name, [])` then `_src = _cands[0]`, and source_hashtable is a defaultdict(list) appended in cache-parse order (cache.py:775), so _cands[0] is arbitrary w.r.t. version when a source coexists in multiple snapshots. It is also true that other consumers resolve deliberately (cmd_source.py:1457 max-by-version; cmd_virtual _lookup prefers selected_srcs). However, the finding's high-severity CONSEQUENCE (version mismatch -> 404 / wrong-binary download) is refuted by the data flow. The download basenames returned by _tunnel_filenames_for_source come exclusively from _resolve_tunnel_filename (cohorts.py:183-202), which resolves each binary via selected_pkgs first, else the cache's HIGHEST-version record (apt_pkg.version_compare) — it never reads _src's version or base. The URL is then assembled in _do_tunnel as `{_base}/{src_pkg.directory}/{_filename}` (cmd_tunnel.py:273) using the caller's ALREADY-RESOLVED selected source (selected_srcs, cmd_tunnel.py:503), not _cands[0]. So both the basename and the directory track the resolved/highest source; the finding's claim that 'base/version are taken from a possibly-older version' is a misread — this function reads neither base nor version off _src. What _src=_cands[0] actually governs is only (a) which binary NAMES are enumerated (_src.binary, line 138) and (b) their Package-List arch/profile gating metadata (_package_list_index). For typical Debian point releases the binary set is invariant across security/updates, so _cands[0] and the selected source enumerate the same set. The genuine residual is a low-severity correctness/consistency nit: in the narrow mixed-snapshot case where a source coexists at multiple versions WITH a differing binary set and _cands[0] is the non-selected version, enumeration could drift (under/over-list the tunnel set); an unresolvable binary is logged and skipped (lines 145-149), not a 404, and no silent corruption occurs. The suggested fix (prefer selected_srcs / max-by-version) is a reasonable defensive improvement and the inconsistency with sibling consumers is real, but the claimed high-severity 404/wrong-binary failure mode cannot manifest from this line because filename resolution is decoupled from _src.

## Test-coverage gaps (version-transpose scheme)

Audited test coverage of the version "transpose" scheme (scripts/bump.py + consumers) against the behavior checklist. Coverage of the PURE primitives and most consumers is strong and rigorous: trailing ~deb->~asg with dpkg-verified ordering, embedded +deb preservation, epoch preservation, +nmu/dotted/+really pass-through, +pP/+bN u0-anchoring (unit + dpkg ordering), per-binary base divergence (samba/e2fsprogs), _needs_bump_build filename parity, and cross-source floor transpose are all well asserted (tests around lines 9215-9420, 9949, 22598, 26770-26950, 27237, 29843).

THREE gaps for a high-stakes build cycle: (1) MISSING tunnel keep-+bN behavioral coverage — the sole tunnel test (line 1122) only greps _do_tunnel source for the string 'transpose_deb' and asserts nothing about bytes; no test exercises transpose_deb in the tunnel call shape (no patch/force/siblings) to prove a tunnelled X+debNuK+bN stays unchanged (frozen +bN + embedded +deb retained) and a trailing +debNuK rewrites to +asg. This is the top risk: a trailing-token-regex regression would silently corrupt frozen inter-binary +bN pins on the real firmware/microcode path. (2) PARTIAL fork changelog-verbatim no-op — the +athenaN fork shape (e.g. base-files 12.4+deb12u14+athena1) appears in no transpose test, and no test runs a fork .deb through _normalize_built_artifacts to confirm it stays '+athena1' (no +asg/+p). (3) MISSING sibling '=' pin restamp carrying +bN — only the +p1 tail is tested; the force_bn branch of _restamp_sibling_pins is uncovered. Mitigating context for (3) and consumer +bN anchoring: bn_bump_count is only ever set to 0 and never incremented anywhere in scripts/, so force_bn is always None at runtime today — the +bN force path is shipped but inert (no producer), lowering immediate risk but leaving the consumer wiring untested. Minor doc bug: transposed_version docstring lines 784-785 omit the u0 anchor that the tests correctly assert.</summary>
</invoke>


| Behavior | Status | Missing assertion |
|---|---|---|
| Tunnel keep-+bN: a tunnelled binary whose trailing token is a binNMU (X+debNuK+bN, e.g. firmware/microcode) must transpose to a NO-OP (embedded +deb not trailing) and KEEP the +bN frozen inter-binary pins; conversely a tunnel with a TRAILING +debNuK must rewrite to +asgRuK. | missing | No behavioral test exists. The only tunnel test (test_tunnel_transposes_and_needs_no_ledger, line 1122) is a pure source-string grep of BuildSession._do_tunnel asserting the literal 'transpose_deb' appears and 'asg_next_n'/'published_ledger' do not — it asserts NOTHING about output bytes/versions. cmd_tunnel.py:314 calls utils.transpose_deb(path,'asg',R) with NO patch_level/force_bn/sibling_names (the tunnel call shape), and that shape is never exercised behaviorally. Add a dpkg-deb test: build foo_1.0-2+deb12u3+b1_amd64.deb, call transpose_deb(path,'asg',1) (tunnel shape), assert status=='unchanged' and dpkg-deb Version stays '1.0-2+deb12u3+b1' (both +b1 AND embedded +deb12u3 retained); plus a companion building foo_1.0-2+deb12u3 (no +b) asserting it becomes foo_1.0-2+asg1u3 with Version 1.0-2+asg1u3. This is the highest-value gap: a regression in _TRANSPOSE_RE trailing-token detection would silently rewrite or strip a frozen +bN inter-binary pin on a real production path with zero test coverage. |
| Fork changelog-verbatim no-op (Model A): a fork version such as base-files 12.4+deb12u14+athena1 must pass through transpose()/transpose_deb()/_normalize_built_artifacts unchanged — the embedded +deb followed by +athenaN is never rewritten, and (was_patched=False) no +asg and no +asg1u0+p1 are added. | partial | The +athenaN fork shape is absent from every transpose test. test_transpose_no_token_unchanged (line 9290) covers +nmu1/+nmu1+b1/dotted/+really but not +athena; test_transpose_embedded_deb_is_left_alone (line 9278) covers shim 1.44~1+deb12u1+15.8-1~deb12u1 and 12.2.0-14+deb12u1+25.2 but not the X+debNuK+athenaN fork shape; and NO test runs a +athena artifact through _normalize_built_artifacts. Add (a) a unit row: transpose('12.4+deb12u14+athena1','asg',1)=='12.4+deb12u14+athena1', and (b) an end-to-end _normalize_built_artifacts test on a fork .deb (e.g. athena-base-files_12.4+deb12u14+athena1) with was_patched=False asserting the on-disk filename and DEBIAN/control Version stay '...+athena1' (no +asg, no +asg1u0+p1). Forks reach _normalize_built_artifacts via the same path as everything else (buildcontainer.py:1905), so the no-op is behavior, not bypass — yet it is unasserted. |
| Sibling '=' pin rewrite carrying +bN: a force-binNMU build (force_bn set) must restamp same-source/cross-base sibling '=' pins to carry the +bN tail (anchored to +asgRu0 on a pristine base). | missing | Sibling '=' pin restamp is tested only for the +p1 patch tail (test_transpose_deb_round_trip line 9999 'git-man (= 1:2.39.5-0+asg1u3+p1)'; test_normalize_built_artifacts_cross_base_sibling_pin_gets_patch_level line 29884 'libcom-err2 (= 1.47.0-2+asg1u2+p1)'). The force_bn branch of _restamp_sibling_pins (bump.py:915) — which appends +bN to a sibling pin — has NO test; only the pure transposed_version unit (line 9326/9328) exercises +bN at all. Add a transpose_deb test with force_bn=1 and a cross-base sibling, asserting the sibling pin becomes 'libX (= <base>+asg1uK+b1)' (and +asg1u0+b1 on a pristine base). SEVERITY NOTE: bn_bump_count is only ever initialised to 0 (buildcontainer.py:628, utils.py:1145) and never incremented anywhere in scripts/, so force_bn is always None at runtime today — the entire +bN force path (including this sibling restamp and the consumer-side +bN anchoring) is shipped but inert. The path is still live-reachable and the unit-level anchoring is correct, but the lack of a producer means the consumer wiring (_normalize_built_artifacts reading bn_bump_count → +bN) is fully untested. |
| trailing ~deb -> ~asg ordering (sign preserved, sorts BELOW pristine) | covered | test_transpose_trailing_tilde_deb_keeps_sign (9271) + test_transpose_scheme_boundary_table_and_ordering (9374) which asserts via dpkg --compare-versions that 2.4.67-1~asg1u2 lt 2.4.67-1. |
| embedded +deb preservation (real identity, never rewritten) | covered | test_transpose_embedded_deb_is_left_alone (9278) covers shim and cross-gcc shapes; strip_binNMU keeps +deb (9313). |
| epoch preservation through transpose | covered | test_transpose_trailing_plus_deb (7:5.1.9), test_virtual_build_transpose_preserves_epoch (26532), test_transpose_deb_round_trip (1:2.39.5 incl on-disk dpkg-deb readback), pristine_base epoch examples. |
| +nmuN / dotted -N.M / +really kept (no trailing deb token) | covered | test_transpose_no_token_unchanged (9290: +nmu1, +nmu1+b1, 1.0-3.3, +really) and strip_binNMU (9314-9315). |
| +pP / +bN anchoring to u0 on a pristine base (so p,b do not outrank upstream updates) | covered | Unit + ordering covered: test_transposed_version_appends_p_and_b (9318: 5.2.15-2->+asg1u0+p1, 1.0-2 force_bn=1 ->+asg1u0+b1, +deb12u3 ->+asg1u3+p2+b1) and boundary table with dpkg _lt('1.0-2+asg1u0+b1','1.0-2+asg1u1'). Consumer +p anchoring also covered by test_normalize_built_artifacts_patch_on_pristine_gets_p1_no_ledger. (Consumer +bN anchoring untested — see force_bn gap above. Minor: transposed_version docstring lines 784-785 are STALE, showing '5.2.15-2+p1'/'1.0-2+b1' WITHOUT the u0 anchor the tests correctly assert.) |
| per-binary base divergence (source base != binary base; uniform P) | covered | test_compute_transposed_versions_per_binary_base_uniform_p (9331, samba ldb) and test_normalize_built_artifacts_cross_base_sibling_pin_gets_patch_level (29843, e2fsprogs comerr-dev vs libcom-err2). |
| _needs_bump_build filename-prediction parity (intrinsic K, ledger-independent, patch-level from record) | covered | test_needs_bump_build_predicts_transpose_filename (27237) covers clean-base/respin u1/stale-u1-vs-u2/uniform-K/patched-+p1 cases; test_validate_transpose_prediction_matches_built_asg (22598) covers synth-vs-built parity. |
| cross-source dep floor transposed (not stripped) so it stays satisfiable | covered | test_normalize_built_artifacts_cross_source_dep_is_transposed (26912, perl floor), test_virtual_build_transposes_inherited_depends_constraint (25670), test_transpose_control_text_version_deps_and_provenance (9347). |

## Medium / low findings (by file)

Single-pass auditor output — re-verify before acting. Severity/category/line are the auditor's own.

### `scripts/apt_repo.py` (2)

- **[medium/correctness]** `:307-311` (conf high) — **generate_repo_indexes udeb scan omits allow_empty=True (sibling generate_apt_repo sets it) — empty debian-installer/ dir aborts the whole publish**
  - _scan_packages_to defaults allow_empty=False (line 545). The single-suite sibling generate_apt_repo passes allow_empty=True for its udeb scan (line 133) precisely because an empty debian-installer component is valid. generate_repo_indexes only guards on os.path.isdir(_udeb_abs) (line 306) then calls _scan_packages_to(repo_root,_udeb_rel,...,udeb=True) with allow_empty defaulting False. If that dir exists but contains no .udeb (Stage C created it but no udebs landed, or they were pruned), dpkg-scanpackages -t udeb emits empty stdout; _run_dpkg_scan's allow_empty=False branch (lines 501-517) treats zero-byte output as failure -> returns False -> generate_repo_indexes returns False -> cmd_index_repo (cmd_repo.py:65) fails the entire repo index / mirror publish. The binary path immediately above (277-286) defends the empty case with a _deb_count skip; the udeb path has neither that count-guard nor allow_empty=True.
  - *Fix:* Pass allow_empty=True to the udeb _scan_packages_to call (matching generate_apt_repo line 133), or pre-count .udeb files and skip the subdir when zero, mirroring the _deb_count guard used for the binary component.
- **[low/missing-test]** `:306` (conf medium) — **No test exercises an empty-but-present debian-installer/ udeb dir in generate_repo_indexes**
  - test_apt_repo_generate_repo_indexes_skips_empty_component_but_indexes_others (tests/test_module.py:4857) mocks dpkg-scan* to always emit a non-empty 'Package: stub' stanza (the _fake_subprocess_run stdout writer), so the udeb scan never returns empty output. No test creates a present debian-installer/binary-amd64 dir with no .udeb files, which is exactly the path that the allow_empty drift above would fail. The empty-component test only covers the binary component skip, not the udeb sub-scan.
  - *Fix:* Add a case: create dists/<suite>/main/debian-installer/binary-amd64/ with no .udeb, have the scan mock emit empty stdout for udeb argv, and assert generate_repo_indexes still returns True (or skips cleanly) rather than failing.

### `scripts/arch_filter.py` (3)

- **[low/optimization]** `:110-119` (conf high) — **Map build spawns 206 dpkg-architecture subprocesses (~4.4s) on first use, per process**
  - _build_maps() runs `dpkg-architecture -a <a> -qDEB_HOST_MULTIARCH` once for every arch returned by `dpkg-architecture -L`. Measured on this host: 206 arches, 4.41s wall, 207 process spawns. It is lazy and memoized in _MAPS so it's one-time per process, but every command that touches the filter (mirror/repo audit/publish, cmd_repo, coord/publish) pays the full cost. The same triplet/cputable data is available in /usr/share/dpkg/tupletable + cputable (cputable is already parsed by _read_cputable_aliases), so the per-arch fork loop is avoidable.
  - *Fix:* Derive the triplet map by parsing /usr/share/dpkg/tupletable (joined with the already-read cputable) instead of forking dpkg-architecture once per arch; fall back to the subprocess path only if the table file is absent.
- **[low/correctness]** `:110-134` (conf medium) — **check=True makes one bad arch abort the whole map, silently disabling the filter for the process**
  - All three subprocess.run calls in _build_maps use check=True inside a single try; the per-arch `-a <a>` query is inside the loop. If any one arch raised CalledProcessError, the except at 132 discards the entire partial map and returns ({}, set()). Per _maps()/is_foreign_target_binary (185-187) an empty triplet map degrades to KEEP-everything for the rest of the process, so a single misbehaving arch would let ALL cross-toolchain binaries through to the pool, not just the one. Not currently live (verified 0/206 arches fail on this host), but the per-arch loop should tolerate individual failures rather than nuke all.
  - *Fix:* Wrap only the inner per-arch `dpkg-architecture -a` call in its own try/except and `continue` on failure, so a single arch error degrades that arch, not the entire filter.
- **[low/missing-test]** `:152-162` (conf medium) — **No test for the populated-map-but-no-arch-table degrade branch**
  - _is_foreign_arch returns False whenever _arch_table() is None (DpkgArchTable unavailable) even though _maps() may be fully populated (dpkg CLI present). This is a distinct documented degrade path from the empty-map path that test_arch_filter_failsafe_keeps_on_uncertainty (tests/test_module.py:39166) covers — that test mocks _maps to empty, never the arch-table-None-with-populated-map case. A regression making matches_architecture mis-ordered or raising would still pass the current suite because no test exercises a real triplet match against a stubbed-None arch table.
  - *Fix:* Add a test that patches _arch_table to return None while leaving _maps populated and asserts a known-foreign name (e.g. binutils-aarch64-linux-gnu) returns False (KEEP), documenting the degrade contract.

### `scripts/build.py` (4)

- **[medium/correctness]** `:713` (conf high) — **patch_list sort key (x[:5]) diverges from buildcontainer's full-name sort feeding an order-sensitive hash**
  - Line 713 sets _src.patch_list = sorted(_patch_files, key=lambda x: x[:5]). That list is then hashed at line 785 (utils.patch_set_hash(_patch_dir, _src.patch_list)) and compared to the stored record hash. But the build-time baseline in buildcontainer.py:1052 builds _live_patch_list = sorted(...) (FULL filename, no key) and stamps patch_set_hash over THAT order (buildcontainer.py:1062/1115). utils.patch_set_hash docstring states it hashes 'tuples in the given ORDER' — it is order-sensitive. For any source with two patches sharing the same first 5 characters (e.g. '9001-alpha.patch' / '9001-beta.patch', a normal Debian numbered-series pattern), the [:5] sort ties and resolves by nondeterministic os.listdir order, so _refresh_patches' recomputed hash differs from the stored full-sort hash. Result: the record is treated as content-changed and dropped (line 809), forcing a needless full rebuild on every `cache parse` / `patch refresh`, and the hash can even flap run-to-run. cmd_source.py:154 reuses the same [:5]-ordered src.patch_list, widening the surface.
  - *Fix:* Sort by the full filename: `_src.patch_list = sorted(_patch_files)` (drop the key=lambda x: x[:5]) so build.py and buildcontainer.py:1052 produce identical ordering and matching hashes. Full-name sort still preserves the numeric-prefix ordering the [:5] key was meant to keep.
- **[low/redundancy]** `:1125` (conf high) — **Redundant re-import of local_mirror inside cmd_init_remote_container**
  - local_mirror is already imported at module top (line 44) and used unqualified earlier (e.g. line 1132 local_mirror.plan). The function-local `import local_mirror` at line 1125 is dead duplication.
  - *Fix:* Delete line 1125; the module-level import already binds the name.
- **[low/redundancy]** `:563` (conf medium) — **cmd_clean_buildroot bypasses the documented sudo funnel and never scrubs the password**
  - _collect_validated_sudo_password's docstring (line 391-398) claims 'Every command handler that gates privileged work funnels through here.' cmd_clean_buildroot (lines 563-564) and cmd_clean_all (614-615) instead inline Prompt(PROMPT_PASSWORD)+_refresh_sudo. cmd_clean_all scrubs the local copy at line 653; cmd_clean_buildroot never scrubs _password, diverging from the codebase's stated sudo-password hygiene.
  - *Fix:* Route cmd_clean_buildroot through _collect_validated_sudo_password (or at minimum scrub _password before return), and update the funnel docstring to reflect the two intentional bypasses.
- **[low/correctness]** `:1600-1678` (conf medium) — **cmd_container_remote_add leaks the copied SSH key when add_remote fails**
  - The SSH key is copied to config/<name>.key at line 1601 BEFORE utils.add_remote is attempted at line 1672. On the add_remote failure path (line 1676 `if not _ok: ... return`) the copied key is left orphaned on disk, whereas cmd_container_remote_delete (lines 1728-1737) is careful to remove config/<name>.key. A retry/abort thus leaves stale credential material in config/.
  - *Fix:* On the add_remote failure path, best-effort os.remove(_keydst) (mirroring delete's cleanup) before returning.

### `scripts/build_closure.py` (5)

- **[medium/correctness]** `:94, 198` (conf medium) — **Closure expansion order is set-iteration dependent → non-deterministic OR-group resolution**
  - _install_expand seeds the BFS with `_frontier = list(_inset)` (line 94) where `_inset` is a set, and compute_build_closure passes `_direct | set(TOOLCHAIN_SEEDS)` (line 198), also a set. Set iteration order over str keys is PYTHONHASHSEED-randomized per process. For a genuine OR-group `A|B` that appears with reversed order elsewhere (e.g. `default-mta|mail-transport-agent`, alternative -dev providers), the satisfied-check at 105-108 means whichever of A/B is visited first short-circuits the other, so the final `all` set can differ run-to-run. This module's docstring explicitly sells reproducible, snapshot-free closures (lines 6-7), and the set feeds local_mirror's download manifest — a non-deterministic mirror contradicts that goal.
  - *Fix:* Process the frontier deterministically: seed with `sorted(_inset)` and either use a deque FIFO or sort newly-added picks before extending; pure-virtual single-name picks are already deterministic via `sorted(_providers)[0]`, so sorting the frontier closes the remaining gap.
- **[medium/redundancy]** `:187-195` (conf medium) — **compute_build_closure ignores arch + build-profile restrictions that the cmd_cache resolver honours**
  - local_mirror.plan builds `_sbd` from the RAW joined Build-Depends/-Indep/-Arch strings (local_mirror.py:114-119) and compute_build_closure parses them with PkgRelation.parse_relations via `_rels` (line 188), which carries but never consults rel['arch']/['restrictions']. The sibling closure in cmd_cache.py:734-737 instead calls `Source.build_depends(self.config.arch, frozenset(self.config.build_profiles), cache=...)`, which filters arch and active build profiles via apt_pkg.parse_src_depends (package.py:657). So the local-mirror closure is a superset that includes arch-foreign (`foo [arm64]`) and inactive-profile (`<!nocheck>`) build-deps the cache-parse closure drops. Harm is over-download/mirror bloat rather than a build break (best.get() skips ones absent for our arch), but it is real drift between two resolvers that are meant to agree.
  - *Fix:* Either have local_mirror feed compute_build_closure the already-arch/profile-filtered groups (reuse Source.build_depends output), or extend _pick/_rels to honour rel['arch'] against the target arch and rel['restrictions'] against active profiles; document explicitly if superset behaviour is intended.
- **[medium/missing-test]** `:77-79, 105-108` (conf high) — **Provides-based resolution path has zero test coverage**
  - Both build_closure tests (test_module.py:39357, 39366) and the local_mirror fixture (39406, Provides:'') pass an EMPTY provides_index. The selection_lock build_closure test exercises a different module. So _pick's provider branch (lines 77-79, `sorted(_providers)[0]`) and _install_expand's provides satisfied-check (lines 105-108, `set(provides_index.get(...)) & _inset`) are never executed by any test, despite virtual-package resolution being core to closure correctness over a real Debian universe (awk/c-compiler/-dev virtuals).
  - *Fix:* Add a compute_build_closure test with a non-empty provides_index where (a) a direct build-dep is a pure virtual satisfied only via a provider, and (b) a transitive Depends is already satisfied by a provider already in the set, asserting both the chosen provider and the skip.
- **[low/missing-test]** `:145-149, 153` (conf high) — **classify_tiers transit-through-non-member and toolchain/language overlap untested**
  - test_build_closure_classify_tiers_uses_adjacency (39372) uses an adjacency whose every node is also a member and whose toolchain/language reach sets are disjoint. The two non-obvious behaviours of _reach are therefore unverified: (1) traversal continues through nodes NOT in `_members` (line 147 appends `_d` regardless of membership) to reach a member behind a non-member transit node, and (2) the `_language = _reach(...) - _toolchain` subtraction (line 153) assigning a doubly-reachable member to toolchain.
  - *Fix:* Add an adjacency where a toolchain seed reaches a member only through a non-member intermediary, and a member reachable from both seed sets, asserting it lands in toolchain not language.
- **[low/optimization]** `:106` (conf medium) — **Provides set rebuilt per-relation inside the install-closure hot loop**
  - Line 106 does `set(provides_index.get(_r.get('name', ''), ()))` for every alternative of every Depends/Pre-Depends group of every package popped from the frontier. Over a full build closure (~11k binaries per MEMORY) this re-materialises provider lists into sets millions of times. provides_index values are static during the call.
  - *Fix:* Convert provides_index values to sets once at the top of compute_build_closure/_install_expand (or accept a pre-set-ified index), then intersect directly without per-iteration set() construction.

### `scripts/buildcontainer.py` (2)

- **[low/missing-test]** `:24422-24474` (conf high) — **Rollback test does not cover the dup-then-failure case that triggers the data-loss bug**
  - test_comp03_segregate_rolls_back_on_move_failure only exercises a genuinely-moved first file being rolled back; test_segregate_never_deletes_existing_published_deb only exercises a successful (no-rollback) collision. Neither combines a kept-existing duplicate with a subsequent move failure, so the published-file-destroying rollback path (lines 2015/2035-2039) is untested and the bug ships green.
  - *Fix:* Add a test: pre-publish file A in dest, place rebuilt-dup A plus a second file B in scratch where B's dest forces an OSError; assert the published A still exists in its dest dir after the call (and is not relocated into scratch).
- **[low/dead-code]** `:142, 174-175` (conf high) — **self.build_path / self.build_profiles / self.build_options stored but never read**
  - grep across scripts/ shows self.build_path (line 142) has zero readers; self.build_profiles (174) and self.build_options (175) appear only in their assignment and a docstring (line 1128) — the live code paths read self.config.build_profiles_for()/build_options_for() (compose_recipe, lines 1015/1018) and self.config.build_profiles/build_options directly, never the instance copies. Vestigial after the compose_recipe refactor.
  - *Fix:* Drop the three unused instance attributes (or, if kept for API symmetry, add a comment noting they are intentionally unused); self.repo_path is also only referenced in comments but is harmless to keep.

### `scripts/buildlog.py` (3)

- **[medium/correctness]** `:138` (conf high) — **write() opens output without encoding='utf-8'; non-ASCII content lost under C/ASCII locale**
  - Line 138 `open(_tmp, 'w')` uses locale.getpreferredencoding(False). The narrative deliberately contains non-ASCII glyphs emitted by this module — '…' in file() (line 102) and '→' in relocation() (line 108) — plus '→' bullets from buildcontainer.py:717/724 and cmd_tunnel.py:416. Under LANG=C / ASCII locale the f-write raises UnicodeEncodeError, which write()'s except (line 142) swallows, so the .buildlog is silently never written — exactly the artifact OBS-04 exists to produce. This is inconsistent with the rest of the codebase: utils.py:1205/1240/1260 all pass encoding='utf-8' explicitly.
  - *Fix:* open(_tmp, 'w', encoding='utf-8') (matching utils.py). Optionally errors='replace' for extra safety.
- **[low/missing-test]** `:128-143` (conf high) — **No test exercises write() under a non-UTF-8 locale despite non-ASCII payload**
  - test_buildlog_writes_header_sections_and_files (tests/test_module.py:23951) writes '→' and asserts it round-trips, but runs under the test runner's UTF-8 locale and reads back with a default-encoding open() (line 23966), so it cannot catch the line-138 encoding gap. test_buildlog_write_never_raises_on_unwritable_dir covers IO failure but uses only ASCII. The C-locale encode failure path is untested.
  - *Fix:* Add a test that monkeypatches locale.getpreferredencoding to return 'ascii' (or opens via a forced ascii encoder) and asserts the file with '→'/'…' content is still written and readable.
- **[low/correctness]** `:89-124` (conf medium) — **Accumulation methods format values outside the try/except, contradicting the 'never propagates' invariant**
  - The module docstring (lines 16-21) states every public method swallows all exceptions and a caller 'may invoke any method ... without a try/except'. But only _append (147-151), write (136-143) and __init__ (75-78) are guarded. kv() (line 90 `f"{str(key):<{_COL}}{value}"`), file() (line 98), footer() (line 122) and header() format the value/__format__ BEFORE the guarded _append; a value whose __str__/__format__ raises would propagate out. test_buildlog_methods_tolerate_bad_input_and_helpers passes object() (whose __str__ does not raise), so the claim is untested for a raising __str__. Severity is low only because all three live callers wrap the whole block in try/except (buildcontainer.py:653, cmd_tunnel.py:380, cmd_virtual.py:191), so the stated 'no try/except needed' contract is not actually relied upon.
  - *Fix:* Either wrap the format calls in the same best-effort try (e.g. format inside _append-protected helpers) or soften the docstring claim to 'best-effort given normally-stringable values'.

### `scripts/buildsystem.py` (3)

- **[low/correctness]** `:104` (conf high) — **Password-failure error reads stdout, but sudo writes diagnostics to stderr**
  - `_proc = subprocess.run(['sudo','-S','-v'], ... capture_output=True, text=True)` then `raise RuntimeError(f"Incorrect password or user not in sudoers file: {_proc.stdout.strip()}")`. sudo emits its prompt and "Sorry, try again"/"is not in the sudoers file" messages to stderr, never stdout, so on a wrong password `_proc.stdout` is empty and the operator gets a message with a blank tail. The sibling wipe-failure handler at line 134 correctly uses `_proc.stderr.strip()`, confirming the convention; this site (and the for_iso copy) drifted.
  - *Fix:* Use `_proc.stderr.strip()` (optionally `or _proc.stdout.strip()`) in both the __init__ (line 104) and for_iso (line 188) validation error messages, matching the wipe handler at line 134.
- **[low/redundancy]** `:83-104` (conf high) — **sudo env-var pickup + prompt + validation duplicated verbatim in __init__ and for_iso**
  - Lines 83-104 (constructor) and 172-189 (for_iso) are a near-identical copy of the ATHENA_SUDO_PASSWORD pop, interactive Prompt fallback, and `sudo -S -v` validation. The copies have already drifted from the command-layer single-source helper `_collect_validated_sudo_password` (base.py:97 / build.py:391) and carry the same stdout-vs-stderr defect in both. Copy-paste here is exactly how the line-104 diagnostic bug propagated to line 188.
  - *Fix:* Extract a private staticmethod/helper on BuildSystem (e.g. `_collect_and_validate_sudo()`) and call it from both __init__ and for_iso so the env pickup, prompt, and validation live in one place.
- **[low/dead-code]** `:83` (conf high) — **Redundant `import os as _os` shadows the module-level `os` import**
  - `os` is already imported at module top (line 11) and used throughout (lines 52, 60, 64, 168). Both `__init__` (line 83) and `for_iso` (line 172) do `import os as _os` solely to call `_os.environ.pop(...)`, which `os.environ.pop(...)` does identically. The alias adds no isolation and is vestigial.
  - *Fix:* Drop the two `import os as _os` lines and call `os.environ.pop('ATHENA_SUDO_PASSWORD', None)` directly.

### `scripts/bump.py` (2)

- **[low/correctness]** `:785` (conf high) — **transposed_version docstring example contradicts actual code + its own test**
  - Line 785 documents `transposed_version('1.0-2', 'asg', 1, force_bn=1) → '1.0-2+b1'`. But _append_patch_force (line 1073) synthesizes the u0 anchor whenever `patch_level > 0 OR force_bn is not None`, so force_bn=1 on a pristine base yields `1.0-2+asg1u0+b1`. This is the intended behavior (anchor +bN inside the update namespace so it sorts below the next upstream update) and is pinned by tests/test_module.py:9326 (`== '1.0-2+asg1u0+b1'`) and :9419. The docstring is the only artifact asserting the un-anchored `1.0-2+b1`, which is the exact wrong sort behavior the anchor exists to prevent — a maintainer reading it could 'fix' the code back to a regression.
  - *Fix:* Change the line-785 example to `transposed_version('1.0-2', 'asg', 1, force_bn=1) → '1.0-2+asg1u0+b1'` to match _append_patch_force and the existing tests.
- **[low/redundancy]** `:381-491, 812-887` (conf medium) — **strip_nmu_from_control_text and transpose_control_text duplicate ~50 lines of field-walk/X-field/sibling-idiom logic**
  - Both functions (strip path still live via cmd_repo.py:398; transpose path live via transpose_deb) carry byte-identical scaffolding: the Version snapshot + X-Athena-Upstream-Version insertion (lines 406-442 vs 828-858), the identical _constraint_re and per-line _in_target field walk over _NMU_STRIP_FIELDS (lines 446-476 vs 860-880), and the trailing _rewrite_sibling_idiom_in_text call (486-489 vs 882-885). Only the inner per-version op differs (strip_nmu_suffix vs transpose). A future change to relation-field handling or the X-field convention applied to one will silently drift from the other.
  - *Fix:* Extract the shared scaffold into one helper parameterized by the per-version rewrite callable (e.g. `_rewrite_control_text(content, version_op)`), and have both public functions pass strip_nmu_suffix / partial(transpose, prefix, release).

### `scripts/bump_version.py` (1)

- **[low/missing-test]** `:169-184` (conf high) — **No test covers the --freeze-stamp commit path**
  - tests/test_module.py only exercises _next_version (test_bump_version_next_computation) and lockstep version drift; there is no test invoking main() with --freeze-stamp, which is exactly why the gitignored-add failure above went unnoticed.
  - *Fix:* Add a test that runs main(['patch','--freeze-stamp']) against a temp git repo with scripts/_buildstamp.py gitignored and asserts the commit succeeds.

### `scripts/cache.py` (5)

- **[medium/correctness]** `:651-658, 985-991` (conf medium) — **UnicodeDecodeError from index parse escapes the OSError-only guard**
  - `utils.readfile` (utils.py:4056) opens with `open(filename,'r')` — no encoding= — so it decodes Packages/Sources using the locale default. Under a C/POSIX locale (common in CI/minimal build containers) that is ASCII, and any non-ASCII byte in a Debian index (accented maintainer names, em-dashes in Description) raises UnicodeDecodeError, a ValueError subclass. `__build_cache` (line 654) and `_ingest_udeb_indices` (line 987) catch only `except OSError`, so the decode error propagates uncaught out of __build_cache -> __init__, crashing the whole cache build instead of setting error_str / returning False gracefully. Other call sites in utils.py explicitly pass encoding='utf-8' (e.g. utils.py:2048), so the omission in readfile is inconsistent. The bytes were already SHA256-verified, so this is purely a decode-robustness gap.
  - *Fix:* Either pin readfile to `encoding='utf-8'` (Debian indices are UTF-8) with `errors='replace'` as a fallback, and/or broaden the two `except OSError` clauses here to `except (OSError, ValueError)` so a decode failure becomes a graceful error_str/skip rather than an uncaught crash.
- **[low/dead-code]** `:158, 161-162` (conf high) — **release_info / pkg_list / src_list initialised but never populated or read**
  - `self.release_info=''` (158), `self.pkg_list=[]` (161), `self.src_list=[]` (162) are assigned in __init__ and never written again or read anywhere. A repo-wide grep for `.release_info`/`.pkg_list`/`.src_list` against a Cache instance finds only these init lines (and a test that merely re-sets them at test_module.py:21473-21475); real consumers use package_hashtable/source_hashtable. Vestigial attributes.
  - *Fix:* Delete the three unused attribute initialisations (and the corresponding test stub lines) to remove confusion about a cache 'package list' that is never built.
- **[low/dead-code]** `:86` (conf high) — **Cache._VALID_CONSTRAINTS class attribute is unused**
  - `_VALID_CONSTRAINTS = utils.VALID_CONSTRAINTS` is defined on Cache (line 86) but never referenced via self/Cache; the only constraint validation in this module is `_lookup_packages` (line 48) which reads `utils.VALID_CONSTRAINTS` directly. dependencytree.py has its own `_VALID_CONSTRAINTS` (line 67) that it actually uses. The Cache copy is dead.
  - *Fix:* Remove the unused class attribute (and its comment), or have _lookup_packages reference it if a single source of truth on Cache is wanted.
- **[low/redundancy]** `:854-893, 919-938` (conf medium) — **Fork-source-drift audit duplicates the collision-gate comparison**
  - `_audit_fork_source_drift` (854) and the inner `_check` of `_verify_no_fork_collisions` (919) implement the same pattern: take a `Dict[name,List[(mirror,upstream_ver)]]`, compute fork max version from the matching hashtable, and flag every entry where `Version(up) >= Version(fork)`. The only differences are the source-vs-binary hashtable, the `.version` vs key extraction, and warn-vs-fail. The two have already drifted (source side uses `max(..., key=Version)` over `.version`; binary side uses `max(keys, key=Version)`), which is exactly the copy-paste drift risk.
  - *Fix:* Extract a single helper `_dropped_entries_at_or_above_fork(drops, fork_versions_for(name))` returning the offending tuples, and have both the advisory source audit and the fatal binary/udeb gate call it.
- **[low/correctness]** `:925-933` (conf low) — **Provides-injected versions can inflate fork version and defeat the collision gate**
  - In __build_cache (lines 714-717) every package's Provides are appended into package_hashtable under the provided name, keyed by the provided version (Package.get_provides substitutes the provider's own version when unversioned). Fork supersede only drops records whose real `Package:` name matches a fork name (line 693), never providers. So for a same-name fork F, `_fork_versions = list(hashtable.get(F,{}).keys())` at line 925 can include the version of an unrelated upstream package Q that `Provides: F`. If Q's version exceeds the fork's, `max(_fork_versions)` (line 933) yields an inflated `_fork_ver`, so `Version(up) >= Version(fork)` can be false and the gate silently passes even though the genuine upstream F dominates the fork. Low likelihood (needs an unrelated provider of the fork name at a higher version) but it weakens a safety gate.
  - *Fix:* When computing the fork's version for the gate, restrict to records whose real `_pkg.package == _name` (e.g. iterate the Package objects in the version lists and filter by `.package`) rather than trusting every key, which may be a Provides alias.

### `scripts/chroot.py` (5)

- **[medium/correctness]** `:1505-1569` (conf medium) — **systemd-firstboot --setup-machine-id bakes a FIXED /etc/machine-id, overriding the documented empty-first-boot design**
  - generate_system_configs writes an empty /etc/machine-id at line 1505 (docstring line 1439/1503: 'empty; systemd generates on first boot'), then runs `systemd-firstboot --root=... --setup-machine-id` at line 1562-1570. systemd-firstboot treats an empty /etc/machine-id as uninitialized and writes a concrete random ID, so the file ships NON-empty. disk_image.py:276 rsyncs the chroot with `-aHAX` into the root partition verbatim, so every disk image / clone built from this base chroot carries the SAME machine-id (DHCP-DUID / systemd uniqueness collisions). The empty-write at 1505 plus the --setup-machine-id flag are mutually contradictory.
  - *Fix:* Drop `--setup-machine-id` from the systemd-firstboot invocation (the empty /etc/machine-id at line 1505 already gives correct first-boot generation), or re-truncate /etc/machine-id to empty immediately after the firstboot call so each install/clone derives a unique ID at boot.
- **[medium/missing-test]** `:763-769` (conf high) — **_compute_install_batches install_set surface-filter branch has no direct test**
  - All test_module.py tests for _compute_install_batches (lines 2206-2366) call it with only libc_seed_set and exercise the legacy `else` branch (lines 770-778). The other references (2469, 2537) mock the whole method with `lambda ...install_set=None`. The `if install_set is not None` branch (763-769) — the live/disk surface mechanism (`gate_complete=not _no_gate`, install_set=_live_set from cmd_build.py:140-142) that the imminent build depends on — is never run against the real implementation, so a regression in the canonical∩install_set−seed filter would not be caught.
  - *Fix:* Add a test passing install_set explicitly (e.g. a graph where install_set excludes some selected packages) asserting only install_set∩canonical−seed members appear in the batches and that legacy _extras/_group_extras/_pool_extras are NOT re-subtracted.
- **[low/dead-code]** `:486,570-576` (conf high) — **_configure_chroot is only ever called with is_final=True; the is_final=False branch and default are dead**
  - Grepped every caller: chroot.py:213 and chroot.py:259 both pass is_final=True; tests/test_module.py:2638 also passes is_final=True (other test references mock the method). The `is_final: bool = False` default (line 486) and the intermediate-round else branch (lines 570-576, 'Round configure ... will retry in next round') are never executed in production — the per-round configuring was refactored into the per-batch _configure_packages path, leaving _configure_chroot as the final sweep only.
  - *Fix:* Either drop the is_final parameter and inline the final-pass behavior, or keep it but delete the unreachable else branch; if retained for symmetry add a test that exercises is_final=False.
- **[low/optimization]** `:1139-1141` (conf medium) — **_get_deb_files triggers a full repo os.listdir per +asg-stamped package on every unpack/retry pass**
  - _get_deb_files calls utils.find_matching_artifact(_main, _filename); per bump.py:367-378 that does an exact-path stat fast-path then, on a MISS, os.listdir(dst_dir) and a full scan. Security-delta packages live on disk as +asg<R>u<N>-stamped names while the index Filename is pristine, so every stamped package always misses the fast path and scans the whole binary-<arch> dir (~11k .debs per the closure notes). _get_deb_files runs inside _unpack_packages, which is invoked per batch plus per-batch retry (line 189) plus up to 5 post-sweep rounds (line 237) — so the same directory is re-listed O(stamped_pkgs × passes) times.
  - *Fix:* Build a one-time {pristine_base: path} index of _main (and _doc) at the start of build_chroot and look up stamped variants from it, instead of re-listdir'ing the repo dir for every stamped package on every pass.
- **[low/redundancy]** `:526-583` (conf high) — **Command construction and 'Setting up' parse duplicated between _configure_chroot and _configure_packages**
  - _configure_chroot (lines 521-583) and _configure_packages (lines 943-992) both build the same chroot-vs-chrootless `sudo -S env ... dpkg ... --force-confdef --force-confnew` command (only `-a` vs named list + optional --force-depends differ) and both parse `re.match(r'^Setting up (\S+?)(?::\S+)? \(', _line)` into a set (lines 579-582 vs 964-968). Copy-paste siblings that can drift — e.g. a future change to the force flags or env would have to be made in both.
  - *Fix:* Extract a shared `_dpkg_configure_argv(named=None, force_deps=False)` builder and a `_parse_setting_up(stdout)` helper used by both methods.

### `scripts/cli.py` (2)

- **[low/correctness]** `:355, 380` (conf high) — **quit/exit detection drifts between _dispatch_one (first token) and the loop break-checks (full line)**
  - _dispatch_one keys the special-token check on the parsed command: `cmd, args = parts[0], parts[1:]` then `if cmd in ('quit','exit'): return False` (line 402-405). But wait() (line 355) and _run_one_shot() (line 380) decide whether that False meant 'quit' by comparing the WHOLE stripped line: `if line.strip() in ('quit','exit')` / `if _cmd.strip() in ('quit','exit')`. So `quit now` is recognized as quit by _dispatch_one (returns False) but the break-check sees 'quit now' not in the set: in the REPL the loop does NOT break (operator's quit silently ignored); in one-shot it is counted as a failure (_failed += 1) instead of a clean queue end. Verified by reading both call sites; trailing-whitespace-only cases ('exit ') still work because wait/run_one_shot also strip.
  - *Fix:* Make _dispatch_one the single source of truth for the quit/exit decision — e.g. return a small sentinel/enum (RAN/EMPTY/QUIT/FAILED) instead of a bool, or have callers test `line.split()[:1] == ['quit']`-style first-token match consistent with _dispatch_one. Then both loops break on the same condition _dispatch_one used.
- **[low/missing-test]** `:417-421` (conf high) — **command_gate refusal path has no test coverage**
  - _dispatch_one enforces the onboarding gate: `gate = getattr(self, 'command_gate', None); if gate is not None and not gate(cmd): self.ERROR(...); return False` (lines 417-421). `grep -c command_gate tests/test_module.py` returns 0 — no test exercises a registered command being refused while ungated, nor that help/quit/exit bypass the gate (help is dispatched at line 406 before the gate; quit/exit return at line 405 before it). This is the pre-configuration safety gate, so the refuse-vs-allow boundary is worth pinning.
  - *Fix:* Add a test: construct a Cli via object.__new__, register a fake command, set command_gate=lambda c: c=='configure', assert dispatching the fake command returns False + emits the 'configure first' ERROR, while 'help' still runs and 'quit' still returns False (bypass).

### `scripts/commands/cmd_audit.py` (4)

- **[medium/correctness]** `:785-800` (conf high) — **Stale-files row colors green (ok=True) when only malformed (corrupt) .debs are present**
  - _report_stale_files_warning computes `_n_stale = len(_orphan)+len(_drift)+len(_foreign)` (line 785), then prints the row with `ok=(_n_stale == 0)` (line 800). Malformed is deliberately excluded from _n_stale yet the early-return clean guard at line 786 DOES include it (`_n_stale == 0 and not _malformed`). So a repo with only corrupt/unparseable .debs falls through the early return, prints '0 stale … N malformed', and the row renders green/COLOR_INFO. The sibling gate _preflight_audit_repo treats malformed as a hard risk (line 242-243 folds len(_stale_malformed) into _n_stale → _bad), so `chroot build` would BLOCK on exactly the artifacts `repo audit` reassuringly green-lights. The ok= flag forgot the malformed term the early-return guard remembered.
  - *Fix:* Make the row consistent with the gate: `ok=(_n_stale == 0 and not _malformed)`, so malformed artifacts color the row amber the way the preflight gate counts them.
- **[low/redundancy]** `:785` (conf high) — **audit stale-count includes foreign but preflight excludes it (sibling drift)**
  - _report_stale_files_warning's `_n_stale` (line 785) adds `len(_foreign)`, driving the row to ok=False whenever foreign cross-toolchain by-products exist. The sibling _preflight_audit_repo explicitly excludes foreign from its `_n_stale`/`_bad` (lines 242-243) with a documented rationale (lines 229-241, 'Foreign cross-toolchains are NON-gating … gating local builds on them would be a workflow trap'). Net effect: `repo audit` turns the stale row amber on every gcc/binutils rebuild (which regenerates foreign by-products) while the actual build gate correctly ignores them. The two siblings disagree on which buckets compose 'stale': preflight = orphan+drift+malformed; audit = orphan+drift+foreign.
  - *Fix:* Align the two: drop `len(_foreign)` from the audit row's gating _n_stale (keep it in the informational text/`_frn` suffix only), and add `len(_malformed)` so both functions use the same orphan+drift+malformed composition.
- **[low/dead-code]** `:943-944` (conf high) — **Unreachable final `else` branch in gap classification**
  - In _report_gap_classification the chain (lines 935-944) is exhaustive before the trailing `else`. For _in_dt=True the first two branches (in_r→other, not in_r→build_failed) cover both cases; for _in_dt=False branch3 `_in_up and not _in_dt` and branch4 `not _in_up` cover in_up True/False. No (in_dt, in_r, in_up) combination reaches the final `else: _other.append(_target)`. Also the `not _in_dt` in branch3 is always True at that point (redundant), since in_dt=True can never fall through branch1+branch2.
  - *Fix:* Drop the unreachable `else` (or fold branch4 into it as `else: _transitional`) and simplify branch3's condition to `elif _in_up:`.
- **[low/dead-code]** `:44-48` (conf high) — **Misleading 'tiebreak prefers consumer sorts first' comment in _dedupe_bidirectional_conflicts**
  - The docstring (line 28) and inline comment (lines 44-45) claim the dedup keeps 'the entry whose consumer name sorts first' for 'stable output'. The code does no sorting — it keeps whichever pair member appears first in the input `conflicts` iteration order (lines 46-49, frozenset key + first-seen). test_dedupe_bidirectional_conflicts_collapses_pairs (test_module.py:19248) only asserts exactly-one survives, never which, so the claimed tiebreak is neither implemented nor tested. Output stability depends entirely on audit_conflict_cohort's emission order, not on name sort.
  - *Fix:* Either implement the documented behavior (on collision, replace with the entry whose consumer sorts lexicographically first) or correct the comment/docstring to state it keeps the first-encountered entry.

### `scripts/commands/cmd_build.py` (2)

- **[low/correctness]** `:1045-1052` (conf high) — **_verify_chroot check 2 ignores subprocess returncode → false PASS on a failed dpkg call**
  - Check 2 builds `_lines = _r.stdout.splitlines()` and `_incomplete = [...]` but never inspects `_r.returncode`. If `dpkg --get-selections` itself fails inside the chroot (broken dpkg db, missing root, sudo rejection mid-run), stdout is empty → `_lines == []` → `_incomplete == []` → `_check(..., not _incomplete, ...)` prints `'0 packages installed' [PASS]`. Contrast check 1 (line 1041) and checks 5/6 which gate on `_r.returncode == 0`. The overall verify is still protected by check 1 (`dpkg --audit`) failing in that scenario, so chroot_verified ends up False; impact is a misleading per-line PASS, not a wrong overall gate decision.
  - *Fix:* Fold the returncode into the ok condition: `_check('All packages fully installed', _r.returncode == 0 and not _incomplete, ...)` and surface a distinct detail (e.g. dpkg stderr) when returncode != 0. Add a test asserting check 2 FAILs when the faked `--get-selections` run returns non-zero/empty.
- **[low/boundary]** `:1048` (conf medium) — **check 2 counts held/deinstall-marked packages as 'incomplete' (endswith '\tinstall')**
  - `_incomplete = [l.split()[0] for l in _lines if l and not l.endswith('\tinstall')]` treats any selection state other than the literal `\tinstall` as incomplete. `dpkg --get-selections` emits `pkg\thold`, `pkg\tdeinstall`, `pkg\tpurge` for non-install states; a *held* package is fully installed but would be flagged, failing check 2 and blocking ISO build. The existing test (tests/test_module.py:1456 `_fake_run` returns `'pkg\tinstall\n'`) only exercises the install case, so this path is untested. In practice the build pipeline does not apt-mark hold, so it is unlikely to fire on a freshly built chroot.
  - *Fix:* Only flag genuinely-incomplete states: e.g. keep packages whose status is not in {install, hold} (held == installed), or parse the second column explicitly via `l.split('\t')` and compare the state token rather than using endswith.

### `scripts/commands/cmd_cache.py` (3)

- **[low/correctness]** `:247` (conf medium) — **Build-mode parse leaks OSError + leaves spinner running (dist path guards it)**
  - _cache_parse_build_mode calls utils.parse_build_pkg_list(self.config.build_pkg_list_path) with no try/except. Per utils.py:3909 the function's docstring states it 'Raises OSError on unreadable file (caller surfaces)' (it only short-circuits on os.path.isfile False; a present-but-unreadable file hits readfile() and raises). The sibling distribution path explicitly catches this for pkg.list at cmd_cache.py:485-492 ('except (OSError, ValueError)'). In build mode there is no guard, and _spiner (created line 362) has no finally, so an OSError propagates out of cmd_parse_dependency with the Spinner never .done()'d.
  - *Fix:* Wrap the parse_build_pkg_list call (line 247) in try/except OSError mirroring lines 485-492: on error print a console error, log it, call _spiner.done() (or move the whole build-mode branch under the spinner's cleanup), set dep_check_ready False and return False.
- **[low/dead-code]** `:878` (conf high) — **`if self.config.build_mode != 'build'` guard is always true (unreachable in build mode)**
  - The build-mode branch at lines 409-424 returns unconditionally in both the success and failure sub-branches, so execution only reaches line 878 in distribution mode. The condition `if self.config.build_mode != 'build':` can therefore never be False here. Compare line 375 where the same check IS load-bearing (it runs before the early return).
  - *Fix:* Drop the redundant condition (de-indent the closure-guard block) or replace it with an `assert self.config.build_mode != 'build'` to document the invariant without implying a live alternative path.
- **[low/missing-test]** `:271` (conf medium) — **No test covers highest-version pick across multiple cached versions in build mode**
  - _cache_parse_build_mode picks max(_versions.keys()) where keys are debian.debian_support.Version objects (cache.py:73, package.py:481/540) — correct Debian ordering. But tests/test_module.py:test_cache_parse_build_mode_resolves_named_pkgs_only only stocks ONE version per name, so it never exercises the max() across versions where lexicographic vs Debian order diverge (e.g. 1.9-1 vs 1.10-1). Given build mode is the imminent cycle, a regression to str-keyed max would go uncaught.
  - *Fix:* Add a build-mode test that puts two versions of one package in the offline cache (e.g. 1.9-1 and 1.10-1) and asserts selected_pkgs holds the Debian-higher 1.10-1, locking in Version-object ordering against an accidental string-max regression.

### `scripts/commands/cmd_mirror.py` (3)

- **[medium/correctness]** `:2202` (conf high) — **mirror reclaim omits --no-iso, so the ISO release-media gate refuses it**
  - cmd_mirror_reclaim calls `self.cmd_mirror_publish(_n, reclaim_intents=_sel)` with no `--no-iso`. In cmd_mirror_publish `_no_iso = ('--no-iso' in args or 'no-iso' in args or _publish_mode=='build')` (line 1309) is therefore False, so the gate at line 1325 `if _missing_isos and not _no_iso:` REFUSES and returns False whenever the current version+snapshot live/installer ISOs are absent from image/. This directly contradicts the publish docstring (lines 1205, 1302) and comments which explicitly name `reclaim` as a `--no-iso` incremental case, and diverges from the sibling cmd_mirror_withdraw_foreign which correctly passes `--no-iso` (line 2363). Reclaim is a per-file byte rewrite that never rebuilds ISOs, so the gate blocks the sanctioned filename-immutability exception with no operator override (reclaim's arg parser at line 2093-2095 does not forward --no-iso either).
  - *Fix:* Call `self.cmd_mirror_publish(_n, '--no-iso', reclaim_intents=_sel)` at line 2202 so a reclaim publishes the repo/claims without requiring current-snapshot install media.
- **[low/missing-test]** `:36436` (conf high) — **Reclaim test asserts the buggy publish args, locking in the ISO-gate regression**
  - tests/test_module.py:36436 `assert _a == ('m1',)` in test_cmd_mirror_reclaim_lists_resolves_and_confirms asserts cmd_mirror_publish was invoked with only the mirror name — i.e. it encodes and protects the missing-`--no-iso` behavior of cmd_mirror_reclaim (line 2202). There is no test exercising the ISO release-media gate interaction for reclaim, so the contradiction with the documented `--no-iso` design went uncaught.
  - *Fix:* Once line 2202 forwards `--no-iso`, update the assertion to `assert _a == ('m1', '--no-iso')` and add a case asserting reclaim still succeeds when _release_iso_descriptors reports missing ISOs.
- **[low/redundancy]** `:1745-1801` (conf medium) — **Four near-identical coord-fetch + head-verify + keyring-bind + read_all_claims blocks**
  - The ~15-line sequence pull_remote_coord -> read_coord_head -> verified_keyring_from_head/binding_drop_summary -> read_all_claims is copy-pasted in cmd_mirror_pull (1745-1801), cmd_mirror_reclaim (2132-2157), cmd_mirror_withdraw_foreign (2280-2305) and cmd_mirror_audit (2628-2659). Drift risk is real: cmd_mirror_query (3074) and _mirror_recompute_base (2426) read claims with an UNbound keyring (revoked={}, no verified_keyring_from_head), so the head-binding trust step is applied inconsistently across siblings.
  - *Fix:* Extract a helper e.g. `_fetch_and_verify(name, st) -> (head, by_builder, fetched) | None` and call it from all four commands so the FED-03-D keyring binding is applied uniformly.

### `scripts/commands/cmd_repo.py` (2)

- **[low/dead-code]** `:435` (conf high) — **Strip completion message points to removed `repo audit_nmu` command**
  - Line 435 tells the operator to `Run `repo audit_nmu` to confirm zero residue.`, but `cmd_repo` (lines 244-248) only dispatches `audit` and `repair`; there is no `audit_nmu` action. `audit_nmu` was absorbed into `cmd_audit` (cmd_audit.py:340,372,526: "the former ... `repo audit_nmu` commands" / "NMU RESIDUE (was `repo audit_nmu`)"). Typing `repo audit_nmu` now falls through to `_group_help`, misdirecting the operator after a strip.
  - *Fix:* Change the hint to `Run `repo audit` to confirm zero residue.` (NMU residue is now part of the unified audit).
- **[low/correctness]** `:866` (conf high) — **Cleanup report understates scanned components (omits main-udeb)**
  - The summary at line 864-867 prints `Scanned N .deb/.udeb file(s) under <repo>/{main,doc,dbgsym,tests}`, and the docstring (lines 786-817) lists only orphan/drift over the same set. But `_scan_stale_files` iterates `utils._STALE_SCAN_SUBDIRS` = ('main','main-udeb','doc','dbgsym','tests') (line 605; confirmed utils.py:105), and the whole point of the comment at lines 600-604 was that superseded udebs in main-udeb were previously missed. The message hides that main-udeb is now scanned (and deletable), which can mislead an operator reviewing which dirs a force prune touched.
  - *Fix:* Include `main-udeb` in the component list string (e.g. `{main,main-udeb,doc,dbgsym,tests}`), ideally derived from `utils._STALE_SCAN_SUBDIRS` so it can't drift again.

### `scripts/commands/cmd_run.py` (3)

- **[medium/boundary]** `:314` (conf high) — **cmd_set truncates multi-token values; `set signing-uid 'Name <email>'` is non-functional**
  - Line 314 does `_param, _value = args[0], args[1]`, discarding args[2:]. The command line is tokenized with a plain `line.split()` (scripts/cli.py:401 `parts = line.split()`; cmd,args = parts[0],parts[1:]) — there is no shlex anywhere in cli.py/tui.py/build.py. So `set signing-uid 'Athena Builder <build@x>'` arrives as args=['signing-uid', "'Athena", 'Builder', "<build@x>'"], and _value becomes "'Athena". `_set_signing_uid` (line 272) then fails its own validation (`'<' in _v and _v.endswith('>')`) and prints "signing-uid must be 'Name <email>'" even though the operator typed exactly that. The signing-uid is the only settable whose canonical form contains spaces, so it is the affected key; the configure wizard prompt is the only working path to set it.
  - *Fix:* In cmd_set, build the value from the remaining tokens: `_value = ' '.join(args[1:])` (and strip surrounding quotes), so space-containing values like signing-uid reach the handler intact.
- **[low/redundancy]** `:116-205` (conf high) — **Boolean-parse block duplicated verbatim across three setters**
  - `_set_include_recommends` (116), `_set_include_build_closure` (142) and `_set_create_local_mirror` (189) each repeat the identical `_v.lower() in ('true','1','yes','on') / ('false','0','no','off') / else error` ladder. Copy-paste drift risk: the first two also clear dep_check_ready, the third does not, so the blocks have already diverged.
  - *Fix:* Extract a `_parse_bool(value) -> Optional[bool]` helper returning None on invalid input and have all three call it, keeping their differing post-set side effects local.
- **[low/correctness]** `:185` (conf medium) — **Machine-local setters mutate self.config before an unguarded write_local_conf, unlike _set_mode**
  - _set_mode (line 94-98) wraps write_local_conf in try/except OSError and carries on with a warning. The machine-local setters (_set_name L184-185, _set_create_local_mirror L201-202, _set_jobs L220-221, _set_cpus L237-238, _set_memory L252-253, _set_docker_server L266-267, _set_signing_uid L280-281) set self.config first, then call write_local_conf with no guard. On OSError the exception is caught up in cli.py:_dispatch_one (line 428) and surfaced as a raw 'command raised OSError' message, leaving the in-memory config mutated but unpersisted with no success line printed — divergent in-memory vs disk state plus a confusing trace-style message.
  - *Fix:* Mirror _set_mode: wrap each write_local_conf in try/except OSError and emit a 'could not persist to local.conf' warning rather than letting it bubble to the generic dispatcher handler.

### `scripts/commands/cmd_snapshot.py` (3)

- **[medium/correctness]** `:269-310` (conf high) — **`_snapshot_select_force` omits the eager build.conf reconcile its forward-only sibling does, leaving the visible config stale**
  - `_set_snapshot_pin` (line 384) calls `utils.reconcile_snapshot_pin(self.config)` right after writing snapshot.state, with an explicit comment (lines 377-383) that the point is to AVOID the surprise "updated build.conf to match" warning on the next process start. `_snapshot_select_force` writes snapshot.state (line 301) + history + invalidates cache flags but never calls reconcile_snapshot_pin, so build.conf [Snapshot] Timestamp is left stale. I verified build.py:2200 runs reconcile_snapshot_pin on startup and prints exactly that COLOR_WARNING 'updated build.conf to match' (build.py:2203-2208) whenever state != build.conf — so a `select force` backtrack triggers the precise surprise-warning the forward path was written to prevent. snapshot.state is authoritative so the build pin is still correct (not data-corrupting), but the documented bad UX reliably occurs.
  - *Fix:* After `append_snapshot_history` in `_snapshot_select_force`, call `_synced = utils.reconcile_snapshot_pin(self.config)` and surface it in the confirmation line, mirroring `_set_snapshot_pin` (so backtracks keep build.conf honest immediately). Add a regression test asserting build.conf is synced after force.
- **[low/optimization]** `:214-223` (conf medium) — **Redundant double GET to snapshot_timestamp_api (latest + candidates) in list/interactive-select**
  - In `_cmd_snapshot_list`, `_snapshot_latest()` (line 215) calls `utils._query_snapshot_latest(self.config.snapshot_timestamp_api, ...)` and then `utils.list_snapshots_between(self.config, _cur, _latest)` (line 223) GETs the SAME `config.snapshot_timestamp_api` endpoint again (verified utils.py:1814 fetches the same URL and computes from the same full `result` dict that already contains the max=latest). Same pattern in `_snapshot_select_interactive` (lines 410+415) and overview. Two network round-trips where one full response suffices; the second fetch also opens a TOCTOU window where `_latest` and the candidate list can come from different API snapshots, so the `← latest` mark or the candidate set can disagree.
  - *Fix:* Fetch the result once (or add a helper returning (latest, between)) and derive both `_latest` and the in-range candidates from a single response.
- **[low/missing-test]** `:466-507` (conf high) — **`_cmd_snapshot_workload` has no test coverage**
  - grep of tests/test_module.py for `snapshot_workload`/`_cmd_snapshot_workload` returns nothing, while sibling paths (`_snapshot_select_interactive`, `_set_snapshot_pin` forward-only refuse, `_snapshot_select_force` backtrack, unpublished-builds warning) all have dedicated tests. The dep_check_ready gate, the `_cur == _target` short-circuit (line 489), the non-timestamp/`latest` rejection (line 483), and the Guard-A preflight truncation `_off[:20]` (line 505) are untested.
  - *Fix:* Add a test stubbing `_workload_current_to_target`/`_preflight_stamp_invariant` to cover: dep_check_ready=False early-out, current==target short-circuit, malformed target rejection, and the names+guard rendering.

### `scripts/commands/cmd_source.py` (3)

- **[low/correctness]** `:1413-1417` (conf high) — **`.disabled` marker text leaves literal `{pkg}` unexpanded (missing f-prefix)**
  - In `_fork_set_enabled` the marker body is built from concatenated string literals: line 1414 `f'{pkg} disabled` ...'` is an f-string (pkg substituted) but line 1416 `'run `source fork {pkg} enabled` to '` is a PLAIN string, so the written file literally contains "run `source fork {pkg} enabled`" with unexpanded braces. The two adjacent references to the same package are inconsistent (first substituted, second literal). No test asserts this content (grep for 're-include in cache builds' finds only the source line), so it is unguarded. Matches the repo's recurring F-string-placeholder class noted in memory.
  - *Fix:* Add the `f` prefix to the line 1416 fragment: `f'run `source fork {pkg} enabled` to '` (or make the whole write a single f-string / .format).
- **[low/correctness]** `:1007` (conf medium) — **Audit suggests a non-runnable `source build (pool)` remediation command**
  - `_subset_for` (line 979) returns 'pool' for rebuild candidates in `dep_tree.pool_extras_src_names`, and both `_cmd_map`s (lines 1007 and 1081) map 'pool' → 'source build (pool)'. 'pool' is not in `_SOURCE_SUBSETS` ('pkg','live','installer','recommended','all','indl'), so typing `source build (pool)` parses '(pool)' as a package name and dead-ends at 'Unknown package'. An operator with stale_pass/needs_build pool-tier sources is given a remediation string they cannot run verbatim.
  - *Fix:* Map 'pool' to the actual command that builds pool extras (e.g. `source build all`) or annotate explicitly that pool sources are only built via `source build all`.
- **[low/optimization]** `:928-931` (conf medium) — **cmd_source_audit recomputes update-pending + workload twice per run**
  - cmd_source_audit calls `self._update_build_pending()` (928) and `self._workload_since_snapshot(self._mirror_floor())` (929); `_print_next_run_build_queue` (invoked at 1147) then calls `_update_build_pending()` again (1165) and `_workload_since_snapshot(_mirror_floor())` again (1179) for the same floor. `_update_build_pending` itself re-derives `_mirror_floor`/`_snapshot_current`/`list_mirrors` each call. fetch_source_versions_at is disk-cached (repo_audit.py:779) so this is not a network re-fetch, but it re-loads/re-parses the floor JSON and re-walks the selected-srcs version compare twice. Minor; only the redundant in-process recompute, not a hot loop.
  - *Fix:* Compute `_update_build_pending()` and the floor workload once in cmd_source_audit and pass the results into `_print_next_run_build_queue` instead of recomputing.

### `scripts/commands/cmd_supply_chain.py` (2)

- **[medium/boundary]** `:242` (conf medium) — **artifact lookup omits the `or {}` fallback used everywhere else → AttributeError on null artifact**
  - Line 242 does `_m.get('artifact', {}).get('name', '')` with no `or {}` guard. Every other accessor in this method is defensive: line 237 `(_m.get('vulnerability', {}) or {})`, line 261 `_m.get('artifact', {}) or {}`, line 260 `_m.get('vulnerability', {}) or {}`. If a grype match emits `"artifact": null` (explicit JSON null vs. missing key — dict.get returns None, not the `{}` default), line 242 raises AttributeError. This fires AFTER the report is written at line 220, so the operator gets a saved report plus an unhandled traceback instead of the severity summary.
  - *Fix:* Change line 242 to `len({(_m.get('artifact', {}) or {}).get('name', '') for _m in _matches})` to match the `or {}` idiom at lines 237/260/261.
- **[low/missing-test]** `:226-270` (conf high) — **grype JSON-render/severity-summary path has no test**
  - Grep of tests/test_module.py shows cmd_cve coverage is source-level only: test_cve_skips_when_grype_absent (grype-absent no-op), test_sta48_cve_report_path_never_overwrites_input_sbom (_cve_report_path), and registration pins. Nothing exercises lines 226-270 (matches parsing, _by_sev Counter, top Critical/High rendering, line-242 component count). The artifact-null AttributeError above would go uncaught.
  - *Fix:* Factor lines 226-270 into a pure helper taking the parsed grype doc and add a unit test feeding a synthetic doc (one match with `"artifact": null`, one with missing `fix`) — testable without a grype binary.

### `scripts/commands/cmd_tunnel.py` (3)

- **[low/correctness]** `:299-308, 354, 367, 449-452` (conf high) — **Non-integer [Build] VERSION fallback ships +deb-named debs but records pristine built_version (inconsistent record + misleading message)**
  - When `int(self.config.build_version)` fails, `_release` is set to None (line 308) and the normalisation loop skips transpose, so `_final_paths` is keyed by the UPSTREAM filename (e.g. foo_1.0-2+deb12u3_amd64.deb) and `_outputs_sorted` (line 354) holds those +deb names. Yet `update_build_record` records `built_version=strip_nmu_suffix(str(src_pkg.version))` (line 367) = pristine '1.0-2', and the console prints the `_upstream_ver → _pristine_ver` arrow (line 452) although nothing on disk was stripped/transposed. The recorded built_version disagrees with the on-disk output filenames, and +deb-named artefacts leak into the pool (against the self-contained-no-Debian-residue rule). Reachable only on misconfigured non-integer VERSION, so low.
  - *Fix:* In the `_release is None` branch, record built_version equal to the actual (un-transposed) upstream version and skip the '→ pristine' arrow, or hard-fail the tunnel when VERSION is not an integer rather than silently shipping upstream-named debs.
- **[low/missing-test]** `:159-473` (conf high) — **No behavioral test exercises _do_tunnel; only a getsource string assertion**
  - tests/test_module.py:1122 (test_tunnel_transposes_and_needs_no_ledger) only does `inspect.getsource(BuildSession._do_tunnel)` and asserts the substrings 'transpose_deb'/absence of 'asg_next_n'/'asg_ledger'. No test drives the actual flow: the stale-file wipe (lines 238-261), the already-present skip + upstream-sha recording (263-271), the build-record entry/terminal lifecycle roll (205-216, 359-375), or the `_release is None` fallback. Regressions in any of these paths pass CI.
  - *Fix:* Add a test that constructs a fake src_pkg + a tmp pool with a real dpkg-deb-built .deb, runs _do_tunnel, and asserts: transposed on-disk filename, republished_from provenance keyed by final name, and build-record outputs == on-disk names.
- **[low/optimization]** `:238-261` (conf medium) — **Stale-file wipe calls os.listdir(_dst_dir) once per binary — quadratic for large firmware sources**
  - The `for _filename in _upstream_files` loop (from line 226) calls `os.listdir(_dst_dir)` inside it (line 239) for every binary. For firmware-nonfree (50+ binaries all routed to the same non-free-firmware component dir, which accumulates hundreds of .debs across firmware sources) this is O(binaries x dir_size) directory scans + string ops per tunnel. The wipe only ever REMOVES different-pristine files (never re-creates the names it deletes), so the listing is safe to take once.
  - *Fix:* Snapshot `os.listdir(_dst_dir)` per component dir once before the download loop (or memoize by dir), then filter from the cached list inside the loop.

### `scripts/commands/cmd_virtual.py` (2)

- **[medium/correctness]** `:90-104` (conf medium) — **validate's _canon_map diverges from synth's filtered universe (Provides-alias misattribution)**
  - cmd_virtual_validate builds _canon_map by walking package_hashtable/udeb_hashtable raw, picking the highest-version record (_ap.version_compare) and taking _rec[0] when the value is a list, then reading .get('Source'). This is exactly the antipattern from_cache() warns against at virtual_build.py:679-684 ('pick the entry whose Package field equals _name rather than _pkgs[0]'): for a Provides-aliased name like telnet, the epoch-bearing alias inetutils-telnet (2:...) outranks the standalone telnet (0.17...) so _canon_map['telnet']='inetutils'. The synth side (synthesize_source_binaries) instead derives canon from package_universe (from_cache, which filters to the standalone Package-field==name producer) via _upstream_canonical_source at virtual_build.py:585. validate_against_build_records:842-846 and the synth filter at virtual_build.py:586-588 must agree on canon(binary) for the two sides to 'stay consistent exactly' (virtual_build.py:832-835); they don't for Provides-aliased binaries that are also declared by an in-scope source, producing spurious virtual_validate_synth_over_predicted/under WARNING drift in the self-test. Also redundant: _universe = from_cache(self.cache) is already computed at line 84 and carries 'Source' per version (virtual_build.py:724-733), so the second raw walk is duplicate work that produces a WRONG result.
  - *Fix:* Build _canon_map from the already-filtered _universe (for each binary pick the apt-highest version key in _universe[bn] and read its 'Source', falling back to bn) instead of re-walking the raw hashtables and taking _rec[0]; this reuses from_cache's standalone-producer filter so validate's canon matches synth's canon.
- **[low/dead-code]** `:369` (conf high) — **Redundant `if self.dep_tree is not None` guard**
  - cmd_virtual_build already returns False at lines 266-270 when self.dep_tree is None, so the guard at line 369 (`if self.dep_tree is not None: _install_corpus |= ...`) is always True on this path. Harmless but misleading — implies dep_tree can be None here.
  - *Fix:* Drop the guard (or merge with the udeb_dep_tree block) since the None case is already short-circuited at the top of the method.

### `scripts/commands/cohorts.py` (1)

- **[low/missing-test]** `:25325-25383` (conf high) — **No test covers multi-version source_hashtable selection in _tunnel_filenames_for_source**
  - test_tunnel_filenames_full_set_arch_profile_filtered (tests/test_module.py:25358-25359) builds `source_hashtable = {'fw': [_Src()]}` with a single version, and test_tunnel_filenames_for_source_uses_upstream_not_stripped (26726) similarly. None exercise the case where the list holds two versions whose `.binary` sets or versions differ — precisely the case where `_cands[0]` selects the wrong source. The wrong-selection bug therefore ships green.
  - *Fix:* Add a test with `source_hashtable = {'x': [older_src, newer_src]}` (parse order older-first) where newer_src declares an extra/renamed binary, asserting the predicted filename set matches the selected/highest-version source, not _cands[0].

### `scripts/coord/config_manifest.py` (1)

- **[medium/correctness]** `:136-142` (conf high) — **Partial list application leaves pkg.list overwritten but reported as NOT applied**
  - In the v2 branch apply_canonical_config writes the four list files sequentially: _write(pkglist_path,_pkg) runs first, then pool/live/installer. If any later _write raises OSError (e.g. disk-full mid-pull) the function returns (False,'write failed',None). pkg.list is already overwritten on disk. The sole caller cmd_mirror.py:_apply_canonical_config (line 3488-3500) only invalidates flags.cache_ready/dep_check_ready when _ok is True, and prints 'NOT applied' on failure. Result: the peer's on-disk pkg.list now holds the federation content while pool/live/installer keep stale content AND no re-resolve is forced and selection.state is not seeded — a silently inconsistent selection going into a multiday build. The v1 branch (line 156-160) has the same pkg-written-then-pool-may-fail shape.
  - *Fix:* Make application all-or-nothing: write each list to a temp file in the same dir, fsync, and os.replace() them only after all four succeed (atomic rename); or stage into a list of (path,text) and write under a try that restores prior contents on any OSError. At minimum, document/return that pkg.list may already be mutated so the caller can force a re-resolve even on partial failure.

### `scripts/coord/head.py` (4)

- **[medium/dead-code]** `:156-206` (conf high) — **is_fresh has zero consumers — staleness/rollback policy is never enforced**
  - grep -rn 'is_fresh' across scripts/ and tests/ returns ONLY the definition in head.py. The module docstring (lines 24-26) claims it is 'called by `coord sync pull` in P2 and the publish state machine in P3', but every consumer of read_coord_head (mirror.py:570/1468/2268, publish.py:859/1659, cmd_mirror.py x5, cmd_virtual.py:415, onboarding.py:405) only verifies the GPG signature and never calls is_fresh. The two checks unique to is_fresh — head_time < InRelease Date (rollback signal) and age > COORD_HEAD_MAX_AGE_SECONDS — are therefore not enforced anywhere. read_coord_head returns a signed-but-stale (rolled-back) head as valid. Inline InRelease-sha comparison exists at cmd_mirror.py:2703, but the time-based staleness/rollback defense does not.
  - *Fix:* Either wire is_fresh into the pull/publish read paths (call it right after read_coord_head with the freshly-fetched InRelease sha + Date), or, if the rollback defense is intentionally deferred, drop is_fresh and its docstring promise so it is not mistaken for active protection during the build cycle.
- **[medium/correctness]** `:70-103` (conf medium) — **write_coord_head is non-atomic: a transient gpg failure destroys the previously-valid signed head**
  - Line 70 truncates and overwrites the live authority file coord-head.json in place. Line 84-89 then removes the prior .sig. If gpg --detach-sign fails (line 95, e.g. transient agent/tty/EPIPE), line 99-103 unlinks the new JSON — but the old .sig was already deleted at line 84, so on-disk state is now NO head at all, and the previously-valid signed head is gone. A crash between line 71 (new JSON) and gpg completion leaves new-JSON + stale-old-sig, which read_coord_head's gpg --verify (line 134) rejects → returns None. Unlike a Packages text manifest, this is the single canonical coord state for a high-stakes multiday cycle.
  - *Fix:* Write JSON to a temp file in coord_dir, sign the temp, then os.replace() both the .json and .sig into place only after the signature succeeds — so a sign failure leaves the prior good head untouched.
- **[low/boundary]** `:188-195` (conf high) — **is_fresh expects ISO-8601 for inrelease_date_iso, but the only InRelease Date producer yields RFC2822 — predates check silently disabled**
  - is_fresh parses inrelease_date_iso with datetime.fromisoformat (line 190). The sole producer of an InRelease Date in the codebase, publish.py:_read_inrelease_sha256_and_date (line 671), returns the raw 'Date:' header value, which in a Debian InRelease is RFC2822 (e.g. 'Thu, 26 Jun 2026 12:00:00 UTC'), not ISO-8601. fromisoformat raises ValueError on that, the except sets _ir_dt=None (line 195), and the rollback/predates check at line 196 is then silently skipped (None guard). So when is_fresh is eventually wired with the real producer's output, its headline rollback defense is a no-op rather than a failure.
  - *Fix:* Parse the InRelease Date with email.utils.parsedate_to_datetime (RFC2822) before passing in, or have is_fresh accept the raw Date header and parse RFC2822 itself; on a parse failure treat it as a hard fail rather than silently skipping the comparison.
- **[low/redundancy]** `:75-83` (conf high) — **homedir-missing branch leaves an orphan stale .sig, diverging from the mirrored repo_audit scrub pattern**
  - The docstring (lines 13-16) claims to mirror repo_audit._write_signed_manifest verbatim. That function's failure path calls _scrub_manifest() which removes BOTH path and path+'.sig' (repo_audit.py:446-453). Here the homedir-missing branch (lines 79-82) reaches BEFORE the prior-sig removal at line 84 and unlinks only _path, leaving the old coord-head.json.sig on disk. read_coord_head checks isfile(_path) first (line 125) so this orphan is functionally harmless, but it is copy-paste drift from the pattern the docstring claims to follow and leaves a misleading signed-sig with no data.
  - *Fix:* In the homedir-missing branch, also unlink the .sig (or factor a single _scrub() helper removing both _path and _sig, matching repo_audit) so failures never leave a dangling signature.

### `scripts/coord/identity.py` (1)

- **[low/dead-code]** `:217-235` (conf high) — **verify_claim_against_keyring is never called; real read-time verification reimplements it inline**
  - grep across the whole repo (scripts/ + tests/) shows the only reference to verify_claim_against_keyring is a docstring mention in scripts/coord/store.py:14 — it is never invoked. The actual claim-verification path, store.read_all_claims (store.py:151-183) -> read_builder_claims (store.py:106-148), performs the keyring-membership check (line 172) and revoked check (line 176) inline and then calls identity.verify_claim directly (line 139). So the keyring lookup + revoked filtering + verify that this function bundles are duplicated live elsewhere, and this copy is orphaned. The store.py module docstring ("applied at read time (identity.verify_claim_against_keyring)") is therefore inaccurate and could mislead a future auditor into thinking revocation is enforced through this function. Not a correctness/security gap (revocation IS enforced inline), purely vestigial code + doc drift.
  - *Fix:* Either delete verify_claim_against_keyring (and fix the store.py:14 docstring to reference the inline read_all_claims path), or refactor read_all_claims/read_builder_claims to call it so the single keyring+revoked+verify policy lives in one place. If kept, add a unit test (none exists — the FED-03 D test at test_module.py:20937 covers enforce_bindings/verified_keyring_from_head, not this function).

### `scripts/coord/policy.py` (3)

- **[medium/correctness]** `:32` (conf high) — **ORPHAN_WARN_AFTER_DAYS documents a 14-day INFO grace period that is never implemented; orphans always WARN immediately**
  - policy.py:32 comment states orphans are 'INFO-only' below the threshold and only 'escalates to WARN' after 14 days, to cover 'short-window crashes and snapshot pivots'. The only consumer is reconcile.py C-1 (line 149) which hardcodes severity='WARN' with no age/mtime/time.time check (verified: grep for ORPHAN_WARN_AFTER_DAYS, days, mtime, age across reconcile.py returns no threshold logic). The constant is imported nowhere (grep across scripts/+tests shows zero references outside policy.py). During a multiday build with snapshot pivots — exactly the case the doc says is INFO-only — every transient missing .deb fires a WARN, contradicting the documented policy.
  - *Fix:* Either (a) implement the threshold in reconcile.py C-1: stat the claim/record mtime, emit INFO when age < ORPHAN_WARN_AFTER_DAYS*86400 and WARN otherwise, or (b) if immediate WARN is intended, delete the constant and rewrite the policy.py comment to match. Do not leave the documented contract unenforced before a high-stakes cycle.
- **[low/dead-code]** `:41` (conf high) — **TUNNEL_REPUBLISH_OK is an unconsumed constant; its documented 'audit should flag' safety belt does not exist**
  - policy.py:41 claims audit should flag when a package newly appears in tunnel records. grep for TUNNEL_REPUBLISH_OK across scripts/ and tests/ returns only the definition site; no reconcile/audit code reads it. The described safety-belt check is unimplemented, so the True value is inert.
  - *Fix:* Remove the constant and its comment, or wire it into the tunnel/republished_from audit path it claims to govern. At minimum stop documenting behavior that no code performs.
- **[low/dead-code]** `:10` (conf high) — **Module docstring references a COORD_HEAD_FRESHNESS knob that is not defined in the file**
  - policy.py:10 lists 'COORD_HEAD_FRESHNESS = require InRelease.Date <= head_time' as one of the v1 hardcoded knobs, but no such symbol is defined (only COORD_HEAD_MAX_AGE_SECONDS exists, line 54). The freshness check actually lives in head.py:202 using COORD_HEAD_MAX_AGE_SECONDS. The docstring name is vestigial and misleads a reader grepping for the knob.
  - *Fix:* Update the docstring to name the real knob (COORD_HEAD_MAX_AGE_SECONDS) or drop the stale COORD_HEAD_FRESHNESS line.

### `scripts/coord/publish.py` (3)

- **[medium/boundary]** `:157-158` (conf medium) — **Drift detection treats whole-binary pool absence as drift, bypassing the pulled_from no-reclaim guard**
  - `_drifted = ... any(_fn != _pool_latest.get(_fn.rsplit('_',2)[0]) for _fn in _outputs)`. `_pool_latest.get(bn)` returns None when the binary NAME is entirely absent from the pool, so `_fn != None` is True and drift fires. The inline comment claims detection is by 'latest-mismatch, not mere pool-absence', but absence of an entire binary DOES flip it. Critically, line 163 (`if _rec.get('pulled_from') and not _drifted: continue`) then no longer skips a pulled/adopted record: any pulled source whose declared `outputs` contains a binary not present in the merged pool (e.g. a -dbgsym/udeb or transitional binary that was pruned or never adopted) is treated as a local rebuild and re-derived from the pool — re-claiming adopted files we do not own. Tests at tests/test_module.py:31863 cover the rebuilt-at-new-version drift but not the fully-absent-binary case. In remote_publish this is contained by filter_pending_by_ownership (line 561-578 BLOCKs owner!=us with non-higher version), so it surfaces as spurious ownership_blocked warnings + extra hashing rather than corruption; local_publish has no such filter but is single-host/no-peer so unreachable there.
  - *Fix:* Require actual presence before declaring drift: `_latest = _pool_latest.get(bn); _latest is not None and _fn != _latest`. A fully-absent declared binary then no longer flips drift (owned sources still drop it via the push-fail path), and a genuinely adopted pulled record stays skipped.
- **[low/correctness]** `:1366-1402` (conf low) — **Step 6c obsolescence can re-assert ownership over a file Step 6b just deprecated**
  - Step 6b (line 1322-1364) appends `deprecated` claims (releases ownership to commons). Step 6c builds `_view` from `_by_builder` + `_pending` only (line 1372-1373) — it does NOT include the deprecation claims just written. If binary X migrated source-old (now deselected→deprecated in 6b) to source-new (builds X at a higher version, in `_pending`), project_owners(_view) still sees the OLD X file as our live PUBLISHED claim and emits a `new_obsolescence` for it at a higher seq than the deprecation. On the next fold the obsolete claim (highest seq) wins, and per project_owners (store.py:386-391) an obsolete winner retains ownership (only deprecated/tunneled are no-owner) — silently undoing the deprecation's release-to-commons for that filename.
  - *Fix:* Exclude filenames deprecated in this same publish from the 6c obsolescence input (or merge the 6b deprecation claims into `_view` so they win the fold), so a released file is not re-owned as an obsolete prune-candidate.
- **[low/redundancy]** `:1223` (conf high) — **Redundant nested `pool_remote_spec is not None` checks**
  - Line 1223 (`if pool_remote_spec is not None:` for the completeness guard) and line 1275 (`if _codename and pool_remote_spec is not None:`) are both already inside the `if pool_remote_spec is not None:` block opened at line 1164, where that condition is invariantly True. The re-checks are dead conditions.
  - *Fix:* Drop the inner `pool_remote_spec is not None` clauses (keep `_codename` on line 1275); they add no guard inside the enclosing block.

### `scripts/coord/reconcile.py` (5)

- **[medium/correctness]** `:130-203` (conf high) — **audit_local hash-verifies only ONE binary per multi-binary source (lossy projection)**
  - audit_local builds its live view from _store.project_live_claims (line 134-135), which keys claims by (package, built_version) (store.py:245: `_key = (str(_c.get('package')), str(_c.get('built_version')))`). Per publish.py:228 the claim `package` field is the SOURCE name and `built_version` is uniform per source (asg-stamp uniform N), so all N binaries of e.g. firmware-nonfree share one key and collapse to a single surviving claim. Consequently C-1/C-2 (lines 139-172) hash-verify only 1 of N binaries per source, and _claimed_filenames (line 143) holds only 1 of N filenames so the other binaries are falsely emitted as 'unclaimed_pool_file' INFO at lines 188-194. This is the exact failure detect_hash_conflicts (line 326-332) and project_owners (filename-keyed) were written to avoid; audit_local picks the wrong projection. Latent today because audit_local has no production caller (only tests).
  - *Fix:* Drive C-1/C-2/C-3 from a filename-keyed live view (e.g. store.iter_live_claims_by_filename or project_owners filtered to builder_id) instead of project_live_claims, so every binary filename is hash-verified and accounted for.
- **[medium/correctness]** `:542-550` (conf high) — **publish_halt_reason fails OPEN on an unreadable HALT sentinel**
  - The function returns None for FileNotFoundError (correct) but ALSO returns None for any other OSError (line 548-550: permission denied, I/O error, IsADirectoryError). Both consumers treat None as 'no halt, publish allowed' (publish.py:622 `if _halt is not None: refuse`; publish.py:811 same). So a PUBLISH_HALT sentinel that exists but cannot be read lets publish proceed despite the safety brake being set — fail-open on a security/correctness gate. (An empty-but-readable sentinel correctly fails closed since ''.strip() is not None.)
  - *Fix:* On a non-FileNotFoundError OSError, return a non-None sentinel string (e.g. 'PUBLISH_HALT present but unreadable: <err>') so the caller refuses; only a genuine FileNotFoundError should map to None.
- **[low/dead-code]** `:104, 211, 386` (conf high) — **audit_local / audit_cross / audit_repo have no production caller**
  - grep across scripts/ shows audit_local is invoked only in tests (test_module.py:33670,33704), audit_cross is invoked nowhere (not even tests), and audit_repo is invoked nowhere. The real repo-side gate in cmd_mirror.py:2663-2987 reimplements the pieces inline (check_federation_consistency + detect_hash_conflicts + audit_cross_mirror_head_drift) rather than calling audit_repo, so R-3/R-5 logic here is never exercised. The module docstring (line 24) also references 'cmd_coord_audit (P1 wired)', a command that does not exist — stale doc.
  - *Fix:* Either wire these three into a `coord audit` command (and fix finding #1 first) or mark them explicitly as staged-for-P3 primitives; correct the stale 'cmd_coord_audit (P1 wired)' docstring claim on line 24.
- **[low/missing-test]** `:139-194` (conf high) — **No test covers audit_local/audit_cross with a multi-binary source**
  - test_coord_reconcile_audit_local_orphan_detection (33648) and _hash_mismatch_critical (33679) both use a single-binary claim (package='ghost'/'x', one filename). There is no test where two claims share the same package + built_version but differ in filename, which is exactly the case the project_live_claims collapse (finding #1) mishandles. detect_hash_conflicts DOES have a multi-binary test (33572) but audit_local does not, so the projection-collapse regression is uncaught.
  - *Fix:* Add an audit_local test with two published claims (same package+built_version, distinct filenames, both present in pool with matching hashes) and assert BOTH are hash-verified and neither is reported as unclaimed_pool_file.
- **[low/optimization]** `:180-194` (conf medium) — **audit_local emits one INFO Finding per unclaimed pool file (unbounded)**
  - The C-3 loop appends a Finding for every pool file not in _claimed_filenames (lines 188-194). On a full self-host pool (~11k binaries per MEMORY) audited by a builder that owns few claims — or, compounded by finding #1, even a fully-claimed multi-binary pool — this produces thousands of INFO Findings held in one list, drowning the CRITICAL/WARN signal the report exists to surface.
  - *Fix:* Aggregate unclaimed pool files into a single count-bearing INFO Finding (or cap the per-file emission), rather than one Finding per file.

### `scripts/coord/schema.py` (1)

- **[low/missing-test]** `:597-617` (conf high) — **canonicalize_neighbour_records dedup is order-dependent first-wins; test claims (untested) dict-overrides-str**
  - Dedup is strict first-wins keyed on lowercased url (line 609 `if not _u or _u in _seen`), so the FIRST occurrence's public_url/public_proto win regardless of shape. If a bare v2 string for a url is encountered before a richer v3 dict for the same url, the dict's public_url/public_proto are silently dropped. tests/test_module.py:34968-34969 docstring asserts 'dict overrides str if the same URL appears in both shapes', but the test data (lines 34971-34980) never places a str and a dict with the SAME url together — the only collision is two dicts ('SSH://B/p' then 'ssh://b/p/'), so the documented str-vs-dict-merge property is neither implemented nor exercised. Verified not currently reachable in production: mirror.py:603/2201 feed homogeneous lists (head.get('neighbours') is canonicalized-all-dicts via new_coord_head; all_mirror_neighbour_records(config) is all dicts), so no live data loss today.
  - *Fix:* Either (a) add a test feeding ['ssh://b/p', {'url':'ssh://b/p','public_url':'https://b'}] and assert the intended winner, then make dedup prefer the record with non-empty public_url/public_proto on collision; or (b) correct the test docstring to state the actual first-wins semantics so a future mixed-shape caller isn't misled.

### `scripts/coord/store.py` (4)

- **[medium/correctness]** `:373-391` (conf medium) — **project_owners ranks cross-builder claims by builder-local seq, so a higher-seq DEPRECATION can outrank a live PUBLISHED takeover**
  - The winner is chosen purely by sorted(key=(-seq, builder)) at lines 373-379; claim_state is never part of the ranking. seq is builder-local (max_seq is per-file, schema.new_* assign builder-local seqs). When builder A deprecates filename F (marker is YIELDED by iter_live_claims_by_filename, lines 297-303, and keeps builder/sha) and builder B legitimately takes it over via republish (filter_pending_by_ownership lines 549-552 KEEP when owner is None), both A's deprecation and B's published claim become candidates for F. If A's deprecation seq > B's publish seq (typical: established builder vs newcomer), the deprecation wins -> _is_deprecated True -> builder=None (line 389-391). B's genuine ownership is never recognized: B's emit_deprecation_claims/emit_supersession_obsolescence (publish.py:270/336) gate on owner=='B' and will skip F forever, and existing_owners (publish.py:1010) reports F as no-owner permanently. The docstring's 'assuming the conflict was already gated' caveat does NOT cover this — a deprecation->takeover is a sanctioned ownership transfer, not a hash conflict detect_hash_conflicts gates.
  - *Fix:* Make the winner key prefer a live PUBLISHED claim over a DEPRECATION/OBSOLETE marker for the same filename before falling back to seq (e.g. key = (state_rank, -seq, builder) where published outranks marker states), or break cross-builder ties by built_at/version rather than builder-local seq.
- **[low/dead-code]** `:265-277` (conf medium) — **Cross-builder merge + conflict-key branch in project_live_claims is never reached (only ever called single-builder) and is untested**
  - grep shows the sole caller is reconcile.py:135, which always passes {builder_id: _my_claims} (a single-builder dict). With one builder, the cross-builder loop at 266-277 only ever does _out[_key]=_claim (line 271) and the conflict-key branch at 275-276 (`_key[1] + '!' + str(builder)`) can never fire. test_module.py exercises project_live_claims only with {'alice': [...]} (lines 31050, 33750). The documented (pkg,ver,builder) conflict-surfacing behavior is thus both dead in production and unverified.
  - *Fix:* Either drop/guard the cross-builder branch with a comment that no caller passes >1 builder, or add a multi-builder test asserting the conflict-key shape so the documented behavior is pinned before any future multi-builder caller relies on it.
- **[low/correctness]** `:96` (conf medium) — **append_claim ignores os.write return value, defeating the 'complete line in one write()' atomicity invariant on a short write**
  - Line 96 `os.write(_fd, _line)` discards the byte count. The module docstring (lines 16-20) and max_seq's comment (lines 48-50) treat a torn line as a hard error signal and rely on each line being written atomically. os.write does NOT retry a partial write; on ENOSPC the syscall can return a short count with no exception (disk fills exactly mid-line), then os.fsync (line 97) succeeds and a truncated JSONL line is committed and later silently dropped by claim_from_jsonl (None) — losing a real claim/seq with no error.
  - *Fix:* Loop os.write until len(_line) bytes are written (write-all helper), or write to a temp file and os.rename for true atomicity; raise on short write so disk-full surfaces instead of producing a torn line.
- **[low/correctness]** `:43-103` (conf low) — **No atomic seq allocation: concurrent same-builder appends can duplicate seq**
  - Callers compute the next seq as max_seq(...)+1 (publish.py:644/914/1293/1743/1792) then append_claim. The flock in append_claim (line 94) only prevents byte interleaving, not seq reuse. The module docstring (lines 7-10) explicitly contemplates 'a concurrent rebuild from the same builder', and two such writers can both read max_seq=N and both append seq N+1, producing duplicate seqs that corrupt the retracts_seq/deprecates_seq/reclaims_seq back-ref fold in project_live_claims/iter_live_claims_by_filename.
  - *Fix:* Allocate seq under the same flock inside append_claim (re-read max_seq while holding LOCK_EX and stamp claim['seq']), or document that concurrent same-builder publish is unsupported and enforce a single-publisher lock at the command layer.

### `scripts/coord/transport.py` (1)

- **[low/correctness]** `:119-134` (conf high) — **_run_rsync_streaming drops the final un-newline-terminated line from the failure tail**
  - The read loop does `*_lines, _buf = _buf.split(b'\n')` and only ever processes complete lines from `_lines`; the residual `_buf` is never flushed after the loop exits (EOF at line 114-115 or OSError at 112-113). When rsync's last stderr/stdout output before EOF lacks a trailing newline, that final fragment — often the most relevant error message, since stderr is merged via stderr=subprocess.STDOUT — is silently discarded and never appended to `_tail`. Verified this path is reachable: it is the failure-detail string returned to push_single_deb's on_bytes branch (line 181-185) for ISO/release-asset pushes. It does NOT affect ok/fail (that comes from `_proc.returncode`), so impact is limited to a possibly-truncated logged detail message.
  - *Fix:* After `_proc.wait()`, if `_buf.strip()` is non-empty and its first token is not a digit, append `_buf.strip().decode('utf-8','replace')` to `_tail` before building the detail string.

### `scripts/dep_drift.py` (3)

- **[low/dead-code]** `:13-14, 74-76` (conf high) — **Vestigial mixin type stubs and stale module docstring**
  - Module docstring (lines 13-14) states the methods access `self._dir_repo` and `self.strip_build_version`, but `grep` over dep_drift.py shows neither is referenced anywhere in the code — the methods actually use `self._dir_repo_main` (line 149) and `self.normalize_repo_filename` (line 143). The mixin type stubs `_dir_repo` (74), `_config` (75) and `strip_build_version` (76) have zero references (`self._dir_repo`, `self._config`, `strip_build_version` each match only the declaration/docstring). They are vestigial and the docstring misleads a maintainer about the real BuildSystem contract.
  - *Fix:* Drop the unused `_dir_repo`, `_config`, and `strip_build_version` stubs (or replace with `_dir_repo_main`/`normalize_repo_filename`, which are the real dependencies) and update the docstring lines 13-14 to name the attributes actually used.
- **[low/missing-test]** `:219-223` (conf medium) — **Version-sync (the 144-spurious-mismatch fix) has no test**
  - The docstring (lines 111-120) calls the Version sync load-bearing: without it `_verify_dep_resolution` reported 144 spurious mismatches (stripped consumer vs unstripped provider). But grepping tests/test_module.py and tests/smoke_dep_drift.py for any assertion on `_pkg_obj.version`/`['Version']` after `_check_dep_drift` finds none — `test_sta24_check_dep_drift_warns_on_lost_selected_dep` stubs out `_verify_dep_resolution` entirely. A refactor that drops the `_pkg_obj.version = _deb_pkg.version` line would not be caught.
  - *Fix:* Add a test that runs `_check_dep_drift` over a canonical pkg whose cache Version is unstripped (e.g. 0.8-10+deb12u1) and disk Version stripped (0.8-10), then asserts `_pkg_obj.version`/`_pkg_obj['Version']` equal the disk value.
- **[low/correctness]** `:326-327` (conf high) — **Hard-dep violation message renders empty constraint as '( )'**
  - For hard pre_depends/depends, the violation string unconditionally formats `({_dep[2]} {_dep[1]})`; for an unversioned dep `_dep[2]` and `_dep[1]` are empty, yielding e.g. `libc6 ( )`. The sibling alt-group formatter at line 333 correctly guards with `if _alt[2] else _alt[0]`. Cosmetic-only (does not affect pass/fail), but is copy-paste drift between the two branches and clutters the abort log operators read.
  - *Fix:* Mirror the line-333 conditional: emit `{_dep[0]}` alone when `_dep[2]` is empty, else `{_dep[0]} ({_dep[2]} {_dep[1]})`.

### `scripts/dependencytree.py` (2)

- **[low/dead-code]** `:1229-1253` (conf medium) — **__getstate__/__setstate__ pickle support is unreachable (resume layer removed)**
  - The docstring at 1229-1235 states no caller pickles a DependencyTree today (the session-persistence/resume layer that did was removed — MAT-06), and MEMORY confirms resume was removed. The 'Plain dicts -> pickle-safe for resume' rationale on _pins/_pinned_chosen (lines 83-84) is likewise vestigial. No live path serializes this object.
  - *Fix:* Either delete __getstate__/__setstate__ and the pickle-safety comments, or add a regression test that round-trips a DependencyTree so the dormant code is exercised and stays correct if revived.
- **[low/correctness]** `:376-380` (conf medium) — **resolve_packages re-resolves raw (unstripped) seed names, unlike add_lookahead**
  - add_lookahead strips/skips empty-or-whitespace names (lines 235-237) before processing, but resolve_packages then calls self.parse_dependency(pkg) on the SAME raw list at line 376 without stripping. An empty/whitespace entry that add_lookahead silently skipped will reach parse_dependency, hit the empty-name branch (line 421-423) returning None, and be reported as an unresolved dependency with a spurious 'parse_dependency() returned None' error at line 379. Inconsistent handling of the same input between the two methods.
  - *Fix:* Filter/strip the seed list once at the top of resolve_packages (mirror add_lookahead's `if not name or name.isspace(): continue` + strip) before both add_lookahead and the parse_dependency comprehension consume it.

### `scripts/diag_installer_status.py` (5)

- **[medium/correctness]** `:142` (conf medium) — **errors='replace' defeats the script's own non-ASCII byte detection**
  - The stated purpose (docstring + lines 108-115) is to find 8-bit/non-ASCII bytes that trip libdebian-installer. But open(path,'r',errors='replace') decodes via locale UTF-8 and turns each invalid byte into U+FFFD (0xfffd). The NON-ASCII check still fires (0xfffd>127), but the reported 'char 0x{_o:x}' is 0xfffd, not the real offending byte, so the diagnostic loses the actual byte value it exists to surface. The sudo fallback (line 146, text=True) has the same decode behavior.
  - *Fix:* Read in binary ('rb') and decode latin-1 (1:1 byte->codepoint) so ord(_c) is the true byte value; or report the original byte offset/value explicitly.
- **[low/dead-code]** `:92-93` (conf high) — **Unused lineno parameter in audit_stanza**
  - audit_stanza(fields, lineno, raw_lines) never references lineno in its body; the caller (line 164) passes _line_start but the function ignores it. The line number is printed by the caller (line 168) instead.
  - *Fix:* Drop the lineno parameter from audit_stanza and the call site, or actually use it in issue messages.
- **[low/correctness]** `:128-132` (conf low) — **EMPTY-VALUE heuristic flags legitimately empty dpkg fields**
  - Multi-line dpkg fields (e.g. Conffiles:) with the synopsis empty are correctly spared because continuation joins make _v non-empty (line 72). But a field that legitimately carries an empty value with no continuation will be reported as an issue, producing false positives in normal status files. This is a heuristic check, not a parser-fatal condition.
  - *Fix:* Document as heuristic in output, or restrict EMPTY-VALUE to fields known to require values (Package, Version, Description synopsis).
- **[low/correctness]** `:108-114` (conf medium) — **Char position reported is into the joined value, not the source line**
  - After continuation folding (line 72) _value contains '\n'+raw segments, so enumerate(_value) pos _i is an offset into the multi-line joined string, not the column on the original line. Reported 'at pos {_i}' can mislead when locating the byte in the file.
  - *Fix:* Track position per raw line, or report the line number alongside the offset.
- **[low/missing-test]** `:45-89` (conf medium) — **Core parser parse_stanzas has no test**
  - grep found no test referencing parse_stanzas/audit_stanza and no tests/test_diag*. The continuation/malformed/empty-stanza branches (lines 61-88) and the __MALFORMED__ accumulation are untested, so regressions in the parser's rfc822 emulation would be silent.
  - *Fix:* Add a small tests fixture exercising: blank-line separation, leading-continuation-with-no-field (line 74), bad non-header line (line 83), and trailing stanza with no terminating newline (line 87).

### `scripts/disk_image.py` (4)

- **[low/dead-code]** `:815-820` (conf high) — **_convert_to_qcow2 takes an unused password param with a stale chown docstring**
  - def _convert_to_qcow2(raw_path, qcow2_path, password) immediately does `del password` and the comment says 'we may need sudo only for chown — see below', but there is no chown below: qemu-img runs as the invoking user, _raw was created by `truncate` as the user (loop/mkfs write block data via root but never change file ownership), so the produced qcow2 and the os.unlink(raw) are all user-owned. The param and the chown comment are vestigial. The build_disk_image call site at line 584 still passes `password`.
  - *Fix:* Drop the `password` parameter (and its argument at the call site) or, if kept for signature parity, replace the misleading 'see below' chown comment with a note that no privileged step is needed here.
- **[low/correctness]** `:586-605` (conf medium) — **Sparse _raw intermediate is never removed on failure paths**
  - _raw is only unlinked inside _convert_to_qcow2 on the success path (line 843). The finally block (586-605) tears down loop/mounts and rmdir's _mnt but never unlinks _raw. Any early `return False` (truncate/losetup/sfdisk/mkfs/rsync/grub failure) leaves a size_gb-apparent sparse raw file in image/. Not a correctness break (a retry's `truncate -s` overwrites the same deterministic _stem+'.raw'), but during a multiday cycle a failed disk build leaves a stray large file with no operator signal.
  - *Fix:* In the finally block, attempt `os.unlink(_raw)` (guarded by os.path.exists / OSError) when the function did not reach a successful convert, or document that the raw is intentionally retained for post-mortem.
- **[low/boundary]** `:569-578` (conf low) — **e2fsck exit code 3 (errors-corrected + reboot-bit) falls through both branches silently**
  - e2fsck exit status is a bitmask: 1=errors corrected, 2=corrected+reboot-recommended. The code logs `in (1,2)` as 'cleaned' and treats `>= 4` as serious, but rc==3 (bits 1|2 both set) matches neither branch, so a genuinely-cleaned fs returning 3 produces no log line. Behaviorally harmless (image still fine) but loses the orphan-cleanup signal the block exists to record.
  - *Fix:* Treat the low bits explicitly: `if _fsck.returncode & 0b11 and _fsck.returncode < 4` (or `in (1,2,3)`) for the cleaned case before the `>= 4` serious check.
- **[low/optimization]** `:423,453` (conf high) — **_has_bios_modules invoked twice (one os.path.isdir for the spinner label, one for the branch)**
  - Line 423 calls _has_bios_modules(_mnt) only to format the spinner string, then line 453 calls it again to decide whether to run BIOS grub-install. Two filesystem isdir() stats of the same path within a few lines.
  - *Fix:* Compute `_bios = _has_bios_modules(_mnt)` once before the spinner and reuse it at both sites.

### `scripts/fork_mirror.py` (4)

- **[low/missing-test]** `:667` (conf high) — **No test covers a fork with an empty/malformed changelog**
  - grep of tests/test_module.py for empty/malformed-changelog and IndexError returns nothing; existing fork tests (test at ~18014 for empty fork/source/ dir, ~18029 happy path) never exercise a present-but-unparseable changelog, so the crash above is untested.
  - *Fix:* Add a test that drops a fork tree with debian/control present and an empty (0-byte) debian/changelog, then asserts generate_fork_mirror logs+skips that pkg and does not raise.
- **[low/optimization]** `:804-805` (conf high) — **_SUBSTVAR_RE is recompiled on every binary stanza**
  - `import re as _re` and `_SUBSTVAR_RE = _re.compile(...)` live inside _format_packages_stanza, which is invoked once per binary stanza across all forks (loop at line 742). The pattern is constant; recompiling per call is wasted work and is inconsistent with the module otherwise importing at top.
  - *Fix:* Hoist `import re` to the module imports and define _SUBSTVAR_RE = re.compile(r'\$\{[^}]+\}') at module scope.
- **[low/redundancy]** `:289-339, 358-377, 704-750` (conf medium) — **debian/control is independently opened and parsed in three helpers**
  - _compute_dep_hash (line 324), _binary_names_from_control (line 371, hand-rolled startswith('Package:') line parsing rather than Deb822), and _build_packages_stanzas (line 728) each re-open and re-parse the same debian/control. _binary_names_from_control's line-based parse also drifts from the Deb822-based parse used elsewhere (it would miss a line-folded/continued Package field, though that is not valid control syntax).
  - *Fix:* Factor a single `_parse_control(pkg_dir) -> list[Deb822]` helper and derive binary names / dep-hash / stanzas from it, removing the bespoke startswith parser.
- **[low/correctness]** `:942` (conf medium) — **_write_release hardcodes 'Architectures: amd64 all' regardless of buildconfig.arch**
  - The Release line is the literal 'Architectures: amd64 all'; the function receives no arch and buildconfig.arch (used in register_fork_mirror line 219 and _build_packages_stanzas) is ignored. For a non-amd64 build_arch the fork Release would advertise the wrong arch. Impact is bounded today because apt treats this flat (release='./', component='') repo's Packages Architecture field as authoritative and the pipeline is amd64, so it's cosmetic now but a latent trap for an arch port.
  - *Fix:* Pass build_arch into _write_release and emit f'Architectures: {build_arch} all'.

### `scripts/identity_scan.py` (2)

- **[medium/boundary]** `:270-283` (conf medium) — **Redirect/pipe attached to a token without a space is treated as a package name**
  - The redirect detection (literals set + `^\d*[<>]&?\d*$` regex) only matches when the redirect is its own shlex token, i.e. space-separated. Confirmed live: `_parse_apt_install_line('apt-install foo >file\n')` returns `(['foo', '>file'], False)` — '>file' is emitted as a package. A spurious unpooled pkg then yields a 'fail' action in audit_chroot_hooks (line 389) that can hard-abort the build on a hook that is actually fine.
  - *Fix:* Strip leading redirect operators from each token (e.g. break when `_t` starts with any of < > | & after stripping leading digits), or split tokens on embedded redirect chars before the per-token classification.
- **[low/dead-code]** `:182` (conf high) — **UnicodeDecodeError except clause is unreachable**
  - Line 168-169 opens with `errors='replace'`, so iterating the file (line 170) can never raise UnicodeDecodeError; only OSError is reachable. The `UnicodeDecodeError` in the `except (OSError, UnicodeDecodeError)` at line 182 is dead. Same pattern (errors='replace') makes the OSError-only handling in audit_chroot_hooks consistent, confirming intent.
  - *Fix:* Drop UnicodeDecodeError from the except tuple (cosmetic), leaving `except OSError`.

### `scripts/installer_chroot.py` (3)

- **[low/dead-code]** `:477-486` (conf high) — **_allow_path first computation is dead in the only live call path**
  - The sole caller (commands/cmd_build.py:387) always passes installer_dir=os.path.join(self.config.working_dir, 'installer'), so os.path.basename(os.path.normpath(installer_dir)) is always 'installer'. That makes the branch at line 484 always true, unconditionally overwriting the _allow_path computed at lines 477-478 (installer_dir/'..'/'installer'/...). The first assignment never survives in production.
  - *Fix:* Drop the lines 477-478 pre-computation and just compute _allow_path = os.path.normpath(os.path.join(installer_dir, 'strip-hooks-allowlist')); keep the basename guard only if a non-'installer' caller is genuinely expected (none exists today).
- **[low/correctness]** `:259-260` (conf high) — **_resolve_udeb_files docstring cites strip_build_version but code uses normalize_repo_filename + find_matching_artifact**
  - Docstring says 'Maps the cache's Packages-index Filename via strip_build_version (drops Debian buildd's +bN bin-NMU suffix)', but the body (lines 283,291) calls utils.normalize_repo_filename then utils.find_matching_artifact. No symbol strip_build_version exists (grep finds none). Stale doc can mislead a maintainer auditing the +asg-stamp reconciliation path before a build cycle.
  - *Fix:* Update the docstring to reference normalize_repo_filename (+bN/+debNuN strip) and find_matching_artifact (pristine-or-+asg-stamp match).
- **[low/optimization]** `:291` (conf medium) — **find_matching_artifact triggers an os.listdir per stamped/missing udeb**
  - For each selected udeb whose pristine name isn't found exactly on disk, utils.find_matching_artifact (bump.py:356) falls back to os.listdir(dst_dir) and scans for a match_pristine_base candidate. In a closure where many udebs are +asg-stamped (e.g. post-security delta), this is O(n_udebs * n_files) over the binary-<arch>/debian-installer dir. Pristine udebs hit the exact-path fast path, so impact is bounded, but a fully-stamped refresh re-lists the directory once per udeb.
  - *Fix:* If profiling shows it matters, build a single dirlist/base-index map once before the loop and look up stamped variants from it; otherwise leave as-is (correctness is fine).

### `scripts/iso.py` (1)

- **[low/redundancy]** `:151-152` (conf high) — **Quote-stripping is already done at config load — inline strip is dead-defensive**
  - iso.py:151-152 does `cfg.build_distribution.strip('"').strip("'")` and the same for build_version, but utils.py:2681 and 2684 already assign these via `_strip_quotes(config_parser.get('Build', ...))` at config construction, so the values reaching build_iso are already unquoted. The inline strip can never change the value in practice. Worse, `.strip('"').strip("'")` strips ALL leading/trailing quote chars (not a matched pair like _strip_quotes), so the two paths have subtly different semantics for pathological inputs — a maintenance trap if anyone relies on it.
  - *Fix:* Use `_name = cfg.build_distribution` / `_version = cfg.build_version` directly (they are already _strip_quotes'd at load), removing the inline strip; or if defensive stripping is desired, call the shared utils helper rather than re-implementing different semantics.

### `scripts/iso_installer.py` (4)

- **[low/correctness]** `:871-872` (conf medium) — **deb_whitelist=None silently bypasses exclude_names (superseded-fork exclusion)**
  - _select_pool_files returns `_all, 0` immediately when deb_whitelist is None, BEFORE the `_exclude = exclude_names or set()` filtering block (lines 878-899). The docstring at 873-876 and cmd_build.py:846 establish exclude_names as a SECURITY-relevant physical exclusion (drop apt-setup-udeb superseded by athena-setup-udeb so anna doesn't run upstream generators — 'the security.debian.org bug'). A caller passing exclude_names with whitelist=None would ship the superseded binaries anyway. Not reached in production today (cmd_build.py:782/837 always passes a non-None surface_closure set), so latent.
  - *Fix:* Apply the exclude_names filter to the legacy path too: in the `deb_whitelist is None` branch, drop entries whose parsed package name is in exclude_names before returning, rather than short-circuiting past the exclusion logic.
- **[low/correctness]** `:883-889` (conf high) — **udebs are never version-deduplicated, unlike .debs**
  - In the whitelist branch every .udeb is appended to `_udebs` unconditionally (lines 883-889) with no `_by_name`/`_debian_version_cmp` dedup, whereas .debs keep only the highest version per name (lines 903-911). The .deb dedup comment (834-835) notes 'our source-build pipeline can leave older binaries behind' — the same is true for udebs (e.g. kernel-image-*-di after an ABI rebuild). Both versions then get staged and indexed by apt-ftparchive. apt/anna normally pick the highest version so this is mostly dead weight/ISO bloat rather than a wrong-pick, but it contradicts the .deb path and can ship stale-ABI udebs alongside the current one.
  - *Fix:* If pool size or stale-udeb selection matters, run udebs through the same highest-version-per-name dedup as .debs; otherwise document explicitly that multi-version udeb shipping is intentional and relies on apt picking the highest.
- **[low/missing-test]** `:5563-5582 (tests/test_module.py)` (conf high) — **No test covers exclude_names combined with deb_whitelist=None**
  - test_iso_installer_select_pool_files_excludes_superseded (test_module.py:5370) only exercises exclusion with deb_whitelist=set(); test_iso_installer_select_pool_files_legacy_mode_keeps_everything (5563) passes deb_whitelist=None but no exclude_names. The interaction that would catch the line 871-872 bypass (finding 1) is untested, so the asymmetry can regress silently.
  - *Fix:* Add a test calling _select_pool_files([repo], deb_whitelist=None, exclude_names={'apt-setup-udeb'}) and assert the excluded binary is dropped (or, if bypass is intended, assert+document that it is kept).
- **[low/optimization]** `:803-806` (conf high) — **_debian_version_cmp re-imports apt_pkg and calls init_system() on every comparison**
  - Each call does `import apt_pkg; apt_pkg.init_system()` then version_compare. Invoked from the _by_name dedup loop (line 906) — only on duplicate-name collisions so not strictly hot, but init_system() per call is needless repeated work. The import is module-cached but init_system is re-run each time.
  - *Fix:* Import apt_pkg once at module load (or memoize a module-level flag) and call init_system() a single time, then have _debian_version_cmp call version_compare directly.

### `scripts/local_mirror.py` (3)

- **[low/dead-code]** `:97-163` (conf high) — **plan() `config` parameter is never used**
  - plan(cache, dep_tree, config, include_index=False) takes `config` but the body (lines 112-163) only references cache, dep_tree, build_closure and include_index — `config` is never read. Both live callers pass it (cmd_cache.py:1037 `local_mirror.plan(self.cache, self.dep_tree, self.config)`, build.py:1132) and the tests pass a throwaway `object()` (test_module.py:39436/39452/39462). Snapshot rewriting is already baked into pkg._mirror.url at ingest, so plan needs nothing from config.
  - *Fix:* Drop the `config` parameter (and update the three call sites + tests), or use it if an arch/suite was intended to come from config.
- **[low/dead-code]** `:355-361` (conf high) — **Final `return f'{_f:.1f}TB'` in human_size is unreachable**
  - The loop condition is `if _f < 1024 or _u == 'TB'`. On the last iteration `_u == 'TB'` is always true, so the function always returns inside the loop; line 361 can never execute.
  - *Fix:* Remove line 361 (the post-loop return), or restructure the loop so the final fallthrough is the TB case.
- **[low/boundary]** `:280` (conf low) — **Release hardcodes `Architectures: amd64 all`**
  - _write_release always emits `Architectures: amd64 all`. apt filters a repo's Packages by the Architectures listed in its Release; if the build host/closure is ever non-amd64 the local mirror would be silently ignored by apt (falling back to snapshot for everything). No arch is derived from config or the actual .debs present. Consistent with the current amd64-only host assumption, but it is an undocumented hard assumption rather than a derived value.
  - *Fix:* Derive the arch line from the host/dpkg arch (or scan the present .deb Architecture fields) instead of hardcoding amd64.

### `scripts/mirror.py` (4)

- **[low/optimization]** `:1042-1050` (conf high) — **apt_pkg import + init_system() called inside the per-claim collision loop**
  - In project_post_publish_state, the `import apt_pkg as _apt_pkg` and `_apt_pkg.init_system()` are executed inside the inner `for _c in _claims` loop, every time a remote claim's derived binary name collides with an existing package (`if _existing is not None:`). init_system() is idempotent but is a C-level config reload; on a large federated remote ledger (thousands of claims, many name collisions) this is re-run once per collision. The sibling site audit_closure_ledger (line 1762) correctly hoists init_system() to the top of the function. find_publish_closure_breaks -> project_post_publish_state is the live publish-time gate (coord/publish.py:1120), so this runs during every publish.
  - *Fix:* Hoist `import apt_pkg` and a single `apt_pkg.init_system()` to the top of project_post_publish_state (before the builder/claims loops), matching the pattern already used in audit_closure_ledger.
- **[low/optimization]** `:832-839` (conf high) — **read_mirror_state reloads + HMAC-verifies the entire mirror.conf on every call; callers invoke it in O(mirrors) loops**
  - read_mirror_state calls load_mirror_conf, which reads the whole config/mirror.conf and runs _verify_record (HMAC) over the full document on each call. add_mirror (944-948), all_mirror_urls (985-988), all_mirror_neighbour_records (2055-2066), find_mirror_by_url (896-899) and reconcile_neighbours all iterate list_mirrors() and call read_mirror_state per name, so each of those operations re-reads + re-verifies the entire signed doc N times. N (configured mirror count) is small in practice, so impact is minor, but the redundancy is real and avoidable.
  - *Fix:* Add a thin helper that calls load_mirror_conf once and iterates `_doc['mirrors']`, and have the loop-based functions (all_mirror_urls, all_mirror_neighbour_records, find_mirror_by_url, add_mirror's duplicate-URL scan) use it instead of per-name read_mirror_state.
- **[low/redundancy]** `:1049` (conf low) — **Inconsistent exception scope around apt_pkg.version_compare vs the sibling site**
  - project_post_publish_state guards version_compare with `except (SystemError, TypeError)` (line 1049) while audit_closure_ledger guards the same apt_pkg.version_compare call with a broad `except Exception` (line 1801). Probing apt_pkg.version_compare with empty/garbage/epoch versions shows it does NOT raise (returns sentinel ints), so the divergence is currently harmless, but it is copy-paste drift between two siblings doing the identical operation and would diverge in behavior if a future apt_pkg ever raised a different error type during the publish gate.
  - *Fix:* Pick one convention for both sites; given the inputs are controlled Debian versions, a narrow `except (SystemError, TypeError, ValueError)` (or a shared helper) applied to both keeps them aligned.
- **[low/correctness]** `:729-735` (conf medium) — **Read-only/status helpers trigger a file WRITE via the legacy-migration path**
  - load_mirror_conf performs save_mirror_conf when it migrates legacy mirror.<name>.state files (line 731). load_mirror_conf is reached from purely read/status-named helpers: mirror_conf_status (776), read_mirror_state (832), list_mirrors (881), get_registration (804). So a status/read call can mint and write a freshly-signed mirror.conf as a side effect. It is one-time (only on the FileNotFoundError branch) and non-destructive (legacy files left in place, no os.remove/_wipe), so it does not violate the destructive-helper rule, but it is a surprising write-on-read and a minor concurrency hazard if two processes first-touch concurrently on a box whose HMAC key is also being minted.
  - *Fix:* Either document the one-time migration write explicitly at each read-named caller, or split migration into an explicit step invoked only from a write/setup path so status/read helpers never write.

### `scripts/onboarding.py` (4)

- **[low/correctness]** `:184-191` (conf high) — **Jobs input silently clamped/defaulted, diverging from `set jobs` which rejects**
  - _prompt_machine_settings catches ValueError -> silently uses _default_jobs (line 189-190) and `_jobs = max(1, min(_jobs, 8))` silently clamps an over-range value. The sibling `_set_jobs` in commands/cmd_run.py:207-219 instead prints an error and refuses for both non-int and out-of-range (1-8 docker-py pool limit). An operator typing 16 here gets 8 with no feedback; typing 'abc' gets the default with no feedback. Copy-paste drift between the two entry points that set the same MaxParallelBuilds.
  - *Fix:* Mirror _set_jobs: on non-int or out-of-range, print a one-line warning (e.g. '  jobs clamped to 8 (docker-py pool limit)') before persisting, so the wizard and `set jobs` behave consistently.
- **[low/correctness]** `:363` (conf medium) — **set_registration return value ignored in federation flow**
  - mirror.set_registration is declared `-> bool` (mirror.py:815) but its return is discarded here. If it fails, role='federation' was already written to local.conf (line 360) and setup_complete is later set True, leaving a peer whose configured_summary (line 60-62) will warn 'NO mirror is registered' on every startup with no abort. All preceding steps in this branch check their bool returns; this one breaks the pattern.
  - *Fix:* Capture the return and, if False, console.print an error and `return False` so the wizard stays un-configured and re-runnable, consistent with the other guarded steps.
- **[low/redundancy]** `:172, 502` (conf high) — **Redundant local re-imports of already top-level modules**
  - `os` is imported at module top (line 26) yet re-imported as `import os as _os` inside _prompt_machine_settings (line 172) and used only for _os.cpu_count; likewise `utils` is imported at top (line 30) but re-imported inside _copy_key (line 502). The aliasing adds noise with no functional purpose.
  - *Fix:* Drop the local re-imports and use the module-level `os` / `utils`.
- **[low/missing-test]** `:188-191` (conf medium) — **No test covers jobs out-of-range / non-numeric clamp behavior**
  - tests/test_module.py exercises run_onboarding happy paths (test_onboarding_first_system_declines_mirror, test_onboarding_federation_happy_path_*) which pass through _prompt_machine_settings with a valid jobs answer, but no test feeds a non-int or >8 value to assert the clamp/default outcome. Given the silent-clamp drift noted above, the behavior is unverified.
  - *Fix:* Add a wizard test that scripts a jobs answer of e.g. '16' and 'abc' and asserts the persisted MaxParallelBuilds is 8 / default respectively.

### `scripts/or_resolve.py` (5)

- **[medium/correctness]** `:83-104` (conf high) — **seeds iterated twice — a generator yields an empty closure on the default (real_pkgs=None) path**
  - resolve_closure declares seeds: Iterable[str]. When real_pkgs is None (the documented common case, lines 74-76), line 83 calls _infer_real(seeds,...) which does set(seeds) at line 145, fully consuming a one-shot iterator. Line 104 then re-iterates: `_pending = {_pull_name(_s) for _s in seeds}` — for an exhausted generator this is empty, so the function returns set() / a wrong closure with no error. Tests only ever pass lists (test_module.py:37617,37625,37641,37656,37668) so the gap is uncaught. resolve_closure_greedy avoids this because its param is Sequence[str], but the Iterable signature here invites generators.
  - *Fix:* Materialize once at the top of resolve_closure: `_seeds = list(seeds)` and use `_seeds` for both _infer_real and the _pending comprehension (or narrow the annotation to Sequence[str]).
- **[medium/correctness]** `:33, 121-134` (conf high) — **Docstring claims 'minimal closure' but the per-group min() tie-break is not minimal**
  - Docstring line 33 asserts 'Iterating A/B to a fixpoint yields the minimal closure for the set.' The Pass B loop resolves exactly the single canonically-smallest unsatisfied group per outer iteration (min(_unsat), line 133) and pulls its first alternative. Counterexample I traced: deps={'p':[('a','b')],'q':[('b','c')],'a':[],'b':[],'c':[]}, seeds p,q. Iter1: min(('a','b'),('b','c'))=('a','b') -> pull 'a'. Iter2: ('b','c') still unsat -> pull 'b'. Result {p,q,a,b}; the true minimal is {p,q,b} (b satisfies both groups). The closure is order-independent and deterministic (the primary goal), but NOT minimal. If the live multi-pass resolver adopts this expecting minimality it will over-pull packages into the closure.
  - *Fix:* Reword the docstring to claim 'a deterministic, order-independent closure' rather than 'the minimal closure', or change Pass B to prefer an alternative that satisfies the most outstanding groups before falling back to first-declared.
- **[low/boundary]** `:55-61, 133-134` (conf medium) — **Empty OR group is never satisfiable and crashes with IndexError**
  - _or_groups (lines 57-61) appends tuple(_d) for any non-str dep element, including an empty list/tuple -> (). In Pass B, `any(_satisfied(_a, _closure) for _a in ())` is False, so () is always unsatisfied and is added to _unsat. min(_unsat) selects () first (empty tuple sorts below all), then line 134 evaluates _first[0] -> IndexError on the empty tuple. A malformed/empty OR group anywhere in deps therefore hard-crashes resolution instead of being ignored.
  - *Fix:* Skip empty groups in _or_groups: `if not isinstance(_d, str) and len(_d): _out.append(tuple(_d))`.
- **[low/optimization]** `:121-134` (conf medium) — **Pass B re-scans the whole closure and re-parses every package's OR groups on every outer iteration, pulling at most one group per iteration**
  - Each outer A/B cycle resolves a single OR group (line 133-134) but Pass B (lines 122-126) rebuilds _unsat by iterating the entire _closure and calling _or_groups(deps.get(_pkg,[])) for every package every time. With G unsatisfied groups this is O(G) full closure scans plus repeated re-parsing of dep lists — roughly quadratic in closure size on OR-heavy graphs. Acceptable for the synthetic test graphs but a concern if this algorithm is lifted into the ~10-pass live resolver over the full 11k-binary closure.
  - *Fix:* Cache per-package or_groups once, and either resolve all currently-unsatisfied groups whose chosen alt doesn't change others in a single pass, or track only groups touched by newly-added packages instead of rescanning the full closure each iteration.
- **[low/missing-test]** `:37649-37673` (conf high) — **No test exercises multi-group / non-minimal OR interaction or generator seeds**
  - The four or_resolve tests cover only single-OR-group graphs (xterm graph resolves in one Pass B pull; the a|b vs b|a tie-break graph likewise). None exercises (a) two distinct OR groups where one alternative could satisfy both — which would expose the false 'minimal closure' claim from finding above, (b) a pulled alternative that itself introduces a new OR group across multiple outer iterations, or (c) passing seeds as a generator (would expose the double-consumption bug). The order-independence property is asserted, but the minimality and iterator-safety properties are untested.
  - *Fix:* Add a test with deps {'p':[('a','b')],'q':[('b','c')],...} asserting the documented invariant, and a test calling resolve_closure((s for s in ['xorg']), ...) to pin generator behavior.

### `scripts/package.py` (2)

- **[medium/missing-test]** `:385-446` (conf high) — **add_constraint conflict-resolution matrix and VersionConstraint have no real unit test**
  - The 7x7 `constraint_action` matrix (line 396) drives version-pin conflict resolution during dep-tree build (called live at dependencytree.py:656) and feeds `constraints_satisfied` (consumed at dependencytree.py:829). grep of tests/test_module.py shows every `add_constraint` reference is a mock stub (`def add_constraint(self, v, o): pass` at lines 5946/6040/11596/13785/21615) and there is no test that constructs a real Package, adds conflicting constraints (e.g. `>> X` then `<< X`, or `>= X` then `<= X` -> eq), and asserts the resolved constraint or `constraints_satisfied`. A single wrong cell in this matrix silently mis-selects a package version with no test to catch it before a multiday build.
  - *Fix:* Add a parametrized test over representative (new,old) operator pairs asserting the resulting stored constraint (nc/xg/eq/err semantics), plus a `constraints_satisfied` test covering the Provides-version satisfaction path (lines 364-382).
- **[low/boundary]** `:184` (conf medium) — **Priority field not guarded against present-but-None, unlike every other field**
  - Line 184 does `self['Priority'].strip()` directly. The required-field loop (line 170) explicitly defends against present-but-None values (`self[_field] is None or self[_field].strip() == ''`) and `_parse` (line 198) uses `self.get(field,'') or ''`, proving the authors consider a present-but-None Deb822 value possible. A stanza with an empty `Priority:` line (value None) raises AttributeError here, which is caught at the call site (cache.py:676 catches AttributeError) and the whole package is silently SKIPPED — instead of defaulting to 'optional' as intended by the else-branch at line 187.
  - *Fix:* Mirror the defensive pattern: `if (self.get('Priority') or '').strip(): self.priority = self['Priority'].strip() else: self.priority = 'optional'`.

### `scripts/print_commands.py` (2)

- **[medium/dead-code]** `:1159-1167` (conf high) — **`print provides` can never show multiple providers — always reports 'no contention'**
  - `session.dep_tree.selected_pkgs` is `Dict[str, package.Package]` (dependencytree.py:99 `self.selected_pkgs: Dict[str, package.Package] = {}`). The loop `for name, pkg in selected_pkgs.items()` iterates UNIQUE dict keys, and appends once per name into `virtuals[name]`. Therefore every `virtuals[name]` list has exactly one element, `multi = {v: ps for ... if len(ps) > 1}` is ALWAYS empty, and the view unconditionally prints the '(every virtual name has a single provider — no contention)' branch. The advertised purpose ('Virtual packages with multiple providers') is unreachable because selected_pkgs collapses each virtual name to a single chosen provider; contention data would have to come from the cache's provider index, not selected_pkgs.
  - *Fix:* Source the providers from the APT cache's virtual/provides index (e.g. cache.package_hashtable / a provides map listing all candidates for a virtual name) rather than from selected_pkgs, which only retains the single resolved winner. Alternatively retire the view if contention is intentionally not tracked.
- **[low/redundancy]** `:695-722` (conf medium) — **`_print_build_times` and `_summary_build_times_section` re-load and re-parse all build records independently**
  - Both `_print_build_times` (line 695) and `_summary_build_times_section` (line 729) call `_iter_build_records(session)`, which does `os.listdir` + `utils.read_build_record` (HMAC-verified read) for every `<pkg>.build.json` in log/build/. In `summary()` only one is called, but the two siblings also drift in how they coerce `elapsed_seconds` (one clamps negatives to 0.0 and falls back across status/phase, the other only accepts int/float and silently drops the rest), so the same records can yield different totals between the full view and the summary section.
  - *Fix:* Extract a single helper that returns normalized (elapsed, pkg, status, version) rows and have both call sites consume it, so coercion rules and the verified-record read are shared and consistent.

### `scripts/release_index.py` (2)

- **[low/dead-code]** `:96` (conf high) — **Unreachable fallback in _human_size**
  - The loop iterates the 5-element tuple ('B','KB','MB','GB','TB'); the guard `if _f < 1024.0 or _u == 'TB'` always returns on the final 'TB' iteration, so the trailing `return f"{n} B"` on line 96 can never execute.
  - *Fix:* Drop the dead `return f"{n} B"` line (or keep only if you intend to defend against an empty units tuple, which is not the case here).
- **[low/missing-test]** `:127-130` (conf medium) — **Empty-ISO HTML branch untested**
  - test_release_index_manifest_and_html (tests/test_module.py:31054) only exercises the non-empty `_iso_rows` path. The `if _iso_rows else` branch rendering the '<p class="muted">No ISO images...' placeholder (lines 127-130), reachable when build_release_manifest filters out all file-less ISOs, has no coverage.
  - *Fix:* Add an assertion that render_index_html(manifest with isos=[]) contains the 'No ISO images published' placeholder and no <table>.

### `scripts/remote_agent.py` (4)

- **[medium/correctness]** `:106-124` (conf high) — **read_log next-offset uses pre-read getsize, not frm+len(data) → duplicate log bytes on concurrent growth**
  - Line 111 takes `_size=os.path.getsize(path)`; line 121 then does `_fh.read()` which reads to the CURRENT EOF. If the build process appends between the getsize and the read (the common case during an active multi-hour build streaming to build.log), `len(_data)` exceeds `_size-frm`, but line 124 returns `_size` (the stale size) as the next-offset. The orchestrator (remote_orchestrate.py:440 `_log_off = int(_lhdr.get('X-Log-Next', _log_off))`) logs ALL returned bytes (line 437 splitlines) then resumes from `_size`, re-fetching the bytes in `[_size, frm+len(_data))` it already printed → duplicated log lines. The next-offset must describe the bytes actually delivered.
  - *Fix:* Return `(_data, frm + len(_data), _size)` (or set `_size = frm + len(_data)` after the read). This is lossless and overlap-free regardless of concurrent growth, and still satisfies test_remote_agent_helpers_name_and_log_offsets (frm+len==12 there).
- **[medium/boundary]** `:289-290 / 368-374` (conf high) — **Empty token fails OPEN — token-file read error yields an unauthenticated agent**
  - main() sets `_token=''` when `--token-file` is absent OR unreadable (line 373-374 `except OSError: _token=''`). `_auth_ok` (line 290) does `self.headers.get('X-Athena-Token','') == self._srv().token`; with token '' a request carrying NO header (also '') compares ''=='' → True, so every localhost/tunnel request is accepted. The auth tests only cover wrong-token and missing-header-against-a-nonempty-token; the empty-token fail-open path is untested. A partial scp / perm error on the remote agent.token silently drops the API's only auth layer.
  - *Fix:* Fail closed: if the resolved token is empty, either refuse to start the server (sys.exit non-zero) or make `_auth_ok` return False whenever `self._srv().token == ''`.
- **[low/dead-code]** `:50` (conf high) — **_TERMINAL set is defined but never referenced**
  - `_TERMINAL = {PHASE_DONE, PHASE_FAILED, PHASE_TIMEOUT, PHASE_ABORTED}` is declared but grep across scripts/remote_agent.py and tests/ finds only this definition line — no consumer. The orchestrator and tests hard-code the same tuple instead (remote_orchestrate.py:443, test_module.py:23154).
  - *Fix:* Either delete `_TERMINAL`, or use it in run_build/elsewhere as the single source of terminal-phase truth (and reuse it for the duplicated literal tuples in callers).
- **[low/missing-test]** `:106-124` (conf high) — **No test covers read_log under concurrent file growth (the offset bug above)**
  - test_remote_agent_helpers_name_and_log_offsets only exercises static files where `_size == frm + len(_data)` coincide, so it cannot catch the stale-next-offset bug and would still pass the corrected implementation. There is no test that appends to the log between getsize and read (or simply asserts next == frm + len(data) when more bytes exist than the initially-sized window).
  - *Fix:* Add a case that writes N bytes, monkeypatches/forces a larger read window, and asserts the returned next-offset equals frm + len(returned_bytes) (no overlap on the subsequent poll).

### `scripts/remote_build.py` (1)

- **[low/redundancy]** `:172-175` (conf high) — **Artifact-collection glob duplicated verbatim in remote_agent.py**
  - main() lines 172-175 build _outputs via `sorted(os.path.basename(_f) for _pat in ('*.deb','*.udeb') for _f in glob.glob(os.path.join(_out, _pat)))`. The byte-identical block exists in scripts/remote_agent.py lines 251-254 (verified), and remote_agent already `import remote_build` (line 39). Two siblings keep their own copy of the same out/ scan, so a future change (e.g. adding *.ddeb or *.changes) must be made in both or they silently drift on what counts as a produced artifact.
  - *Fix:* Expose a shared helper in remote_build.py, e.g. `def collect_outputs(out_dir) -> list[str]` returning the sorted basenames, and call it from both main() and remote_agent.py's post-build block.

### `scripts/remote_localmirror.py` (3)

- **[low/missing-test]** `:225-257` (conf high) — **Range-resume / 200-restart byte accounting is untested**
  - test_remote_localmirror_populate_downloads_skips_indexes (tests/test_module.py:39465) only exercises full fresh download, size-match skip, and sha mismatch. The subtle accounting — partial file on disk seeds _file_done (225), pre-charged into _cum_done (242), then the success normalisation `_cum_done += _size - _file_done` (254) that compensates a server-ignored-Range 200 restart, and the failure rollback `_cum_done -= _file_done` (247) — is never driven by a test (no partial-file-on-disk case). This is the trickiest logic in the file and a regression here would silently corrupt the two-bar progress totals the BS1 orchestrator drives.
  - *Fix:* Add a test that pre-writes a truncated partial of one entry (via a file:// or mocked urlopen returning 206), asserts the download completes and result counts are correct; and a second mocked case where the server returns 200 to a Range request, asserting cum_done lands exactly on total_size.
- **[low/boundary]** `:202` (conf high) — **total_size:null crashes here but is guarded one line above**
  - Line 187 defensively coerces `int(plan.get('total_size', 0) or 0)` (handles a JSON null), but line 202 does `int(plan.get('total_size', 0)) or 1` with no `or 0` guard — `int(None)` raises TypeError. In the live BS1 path local_mirror.build_plan always emits an int (local_mirror.py:126/157), so it doesn't currently reach a live crash, but the module is documented as GENERIC (reads plan.json directly), and the two lines parsing the same key disagree on null-handling.
  - *Fix:* Make line 202 match line 187: `_cum_total = int(plan.get('total_size', 0) or 0) or 1`.
- **[low/dead-code]** `:98-99` (conf medium) — **416 'already complete' branch is unreachable given upstream guards**
  - `if _e.code == 416 and expected_size and _existing >= expected_size:` can never be True on the live path: reaching _download with a truthy expected_size means line 217 already skipped any file whose on-disk size == _size, and line 85-91 truncates any file larger than expected_size to _existing=0, so _existing is always strictly < expected_size when expected_size is truthy. A real 416 therefore falls through to the `return (False, 'HTTP {code}')` branch. The 'already complete' return is dead defensive code.
  - *Fix:* Either drop the dead sub-condition or document it as purely defensive; no functional change needed since the fall-through still returns a sane failure.

### `scripts/remote_orchestrate.py` (2)

- **[medium/missing-test]** `:463-472` (conf high) — **Partial scp-down recovery (exit 12) and SIGINT abort (130) paths are untested**
  - tests/test_module.py only exercises phase=done (test_run_remote_agent_polls_to_done...) and phase=timeout (test_run_remote_agent_watchdog_timeout_is_transport). The _missing-artifact branch at 465-472 — which returns exit 12 when scp recovers fewer .deb/.udeb than _status['outputs'] — has no test, yet exit 12 is the load-bearing signal cmd_source.py:1838 uses to re-queue a package on another remote during the multiday fan-out. The abort path (130) at 449-450 and the AGENT_DEAD_GRACE_SEC unreachable->12 path at 428-431 are likewise uncovered. The existing fake (_run_remote_agent_fakes) always writes adduser_1_amd64.deb on the out/*.deb scp, so it cannot reach the missing-artifact branch.
  - *Fix:* Add a test where /status reports outputs=['a.deb','b.deb'] but the out/*.deb scp materializes only one file, asserting (12, [...]) ; and a test driving _AgentHandle.terminate()/abort to assert (130, []).
- **[low/correctness]** `:354-360` (conf medium) — **_open_tunnel omits StrictHostKeyChecking=accept-new, diverging from _ssh_base contract**
  - _open_tunnel builds its own ssh argv (['ssh','-o','BatchMode=yes','-o','ExitOnForwardFailure=yes', ...]) and does NOT add `-o StrictHostKeyChecking=accept-new`, unlike _ssh_base (line 53-54) whose docstring explicitly lists 'the agent tunnel' as an accept-new site. Not a live failure today because mkdir (line 364), scp (377) and the agent-port cat (399) all run through _ssh_base first and record the host key under accept-new before the tunnel opens — so on a fresh remote the key is already known. But the divergence is silent drift: if call ordering ever changes so the tunnel is the first contact, it would fail under BatchMode with an unrecorded host key.
  - *Fix:* Add '-o','StrictHostKeyChecking=accept-new' to the _open_tunnel argv (or build it from a shared helper) so all ssh sites are consistent with the _ssh_base docstring.

### `scripts/repo_audit.py` (4)

- **[medium/missing-test]** `:900-959` (conf high) — **No test covers audit_nmu_residue on an anchored +asg<R>uK+bN built package**
  - tests/test_module.py:10266 (test_audit_nmu_residue_skips_tunneled_sources) only exercises tunneled-skip and a genuine upstream '+deb12u1' residue. There is no case asserting that a NON-tunneled package shipped at e.g. '1.2.3-4+asg1u3+b1' (the force-binNMU anchored form emitted by buildcontainer.py:1907) is NOT flagged. That gap is exactly why the false-positive in the finding above is latent; the transpose scheme was just rewritten, so this path is unguarded.
  - *Fix:* Add a RepoState case with a built pkg at '1.2.3-4+asg1u0+b1' and '1.2.3-4+asg1u3+p1+b1' (no tunnel set) and assert audit_nmu_residue returns no finding for it, plus a sibling Depends '(= 1.2.3-4+asg1u3+b1)' likewise clean, while a real '...+deb12u1' is still flagged.
- **[low/redundancy]** `:881, 1043, 1173` (conf high) — **PkgRelation re-imported locally three times despite module-level import**
  - PkgRelation is imported at module scope (line 38) and used directly in the type annotation at line 970. _build_provides_index (881), audit_dep_closure (1043) and audit_conflict_cohort (1173) each redundantly re-run 'from debian.deb822 import PkgRelation'. The local imports shadow nothing and are dead duplication.
  - *Fix:* Delete the three function-local 'from debian.deb822 import PkgRelation' lines; the module-level import already covers them.
- **[low/correctness]** `:168-210` (conf high) — **scan_repo_state docstring + ValueError message omit the valid 'main-udeb' label it explicitly supports**
  - The docstring (lines 171-176) and the error message (lines 207-209) say the subdir must be 'one of main/doc/dbgsym/tests', yet the function itself special-cases subdir=='main-udeb' to add '-t udeb' (lines 273-274), and utils.deb_dir_for (utils.py:3318) accepts 'main-udeb' (plus contrib/non-free/non-free-firmware). cmd_audit/source-verify pass 'main-udeb'. The mismatch is misleading for an operator who hits the error path with a typo and is told the udeb label is invalid.
  - *Fix:* Update the docstring label list and the ValueError text to include 'main-udeb' (ideally derive the message from utils.deb_dir_for's accepted keys to avoid future drift).
- **[low/correctness]** `:1040-1042` (conf high) — **audit_dep_closure docstring claims weak list holds 4-tuples; it emits 3-tuples**
  - Docstring line 1040-1042: 'Returns (unresolved, weak): each is a list of (pkg, field, relation_str, why) tuples.' But the weak branch appends a 3-tuple '(_pkg, _field, _rel_str)' (line 1082) with no 'why', and every consumer unpacks it as 3 (cmd_audit.py:862 'for _pkg, _field, _rel in weak'; virtual_build.py:1387). Only the unresolved list is a 4-tuple. Doc-only, but it misdescribes the contract of a freshly-consumed return shape.
  - *Fix:* Reword the docstring to state unresolved is (pkg, field, relation_str, why) 4-tuples and weak is (pkg, field, relation_str) 3-tuples.

### `scripts/sbom.py` (3)

- **[low/correctness]** `:16,98` (conf high) — **"empty hash for pristine sources" is false — pristine emits sha256-of-empty constant**
  - Docstring line 16 ("empty hash for pristine sources") and inline comment line 98 ("empty hash + 0 count for pristine sources") claim a pristine source gets an empty patch-set hash. But line 103 calls utils.patch_set_hash(dir, []) and that function (utils.py:707) returns hashlib.sha256().hexdigest() for an empty list — i.e. the fixed digest e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855, never ''. So the emitted property `athena:patch-set-hash` is a non-empty constant for every pristine source, and any consumer trying to detect 'pristine' via empty hash will be wrong. Count (0) is correct; hash claim is not.
  - *Fix:* Either set athena:patch-set-hash to '' when not _patch_files, or fix the docstring/comment to say pristine sources carry the canonical empty-digest sha256 rather than an empty string.
- **[low/missing-test]** `:135-221` (conf high) — **generate_cdx has no functional test — only a structural source-grep assertion**
  - tests/test_module.py:20579 asserts only that the string '_atomic_write_bytes' appears in inspect.getsource(sbom.generate_cdx). No test constructs a fake dep_tree/udeb_dep_tree and invokes generate_cdx to validate: the deb-wins dedup (lines 156-160), PURL shape (_purl_for_source), per-source patch-count/patch-set-hash/patch-files properties (lines 104-113), the empty-out_path early return (line 150-152), or DSC-sha256 inclusion. Given a multiday build cycle, output-shape regressions here would go unnoticed.
  - *Fix:* Add a unit test that builds two stub Source objects (one in both trees to exercise setdefault dedup, one udeb-only) plus a stub BuildConfig, calls generate_cdx to a temp path, and asserts bomFormat/specVersion/component count/PURL/patch properties and the out_path='' no-op.
- **[low/redundancy]** `:167-172` (conf medium) — **_strip_quotes re-applied to values BuildConfig already quote-strips at load**
  - build_distribution (utils.py:2681), build_version (2684) and build_codename (2683) are all assigned through _strip_quotes at config load, so calling _strip_quotes again at lines 167/168/171 is a no-op duplication of intent (and inconsistent with arch/base-id which use plain str()). Harmless but invites copy-paste drift if one site's stripping changes.
  - *Fix:* Drop the redundant _strip_quotes wrapping here (use str()), or add a comment that it is defensive belt-and-suspenders; keep one canonical stripping site.

### `scripts/select_packages.py` (3)

- **[low/boundary]** `:51-52, 130-131` (conf high) — **POOL_GROUP '(pool)' CAN collide with a real [(pool)] section, contrary to the inline claim**
  - The comment at lines 51-52 asserts the parenthesised name 'can't collide with a real pkg.list [group] header', but utils.parse_pkg_list_groups uses section_re = r'^\s*\[([^\]]*)\]\s*$' (utils.py:3970) which accepts any non-']' content, so a header literally written `[(pool)]` parses to group name '(pool)'. _load_model then unconditionally overwrites self._groups[POOL_GROUP] (line 130) and self._meta[POOL_GROUP] (line 131) with the pool.list contents, silently discarding that real group; on save _save() excludes POOL_GROUP from the pkg.list writer (line 497), so the real group's packages are never written back. Pathological name, but the no-collision guarantee in the comment is false.
  - *Fix:* Either validate/reject a real group literally named '(pool)' in _load_model (raise or rename), or detect the clash before the line-130 overwrite and warn; at minimum correct the comment to state the collision is possible and unhandled.
- **[low/correctness]** `:501-504` (conf medium) — **_save writes pool.list unconditionally, creating a spurious empty file that discard cannot roll back**
  - POOL_GROUP is always inserted by _load_model (line 130), so `if POOL_GROUP in self._groups` (line 501) is always true and write_flat_list(self._poolpath, _pool_sel) (line 503) always runs. When pool.list never existed and the pool tier is empty (_read_flat returns [] for a missing file), write_flat_list still emits a single-newline file. The consumer cmd_cache._backup_select_lists (cmd_cache.py:1138) only backs up files that already exist, and _restore_select_lists only copies backups back, so a pool.list CREATED during the session is not deleted on a 'discard' intent — leaving a stray file after a transaction the operator cancelled. Not live in this repo (config/pool.list exists), but reachable on a fresh checkout lacking pool.list.
  - *Fix:* Skip the pool.list write when the pool tier is empty AND the file does not already exist, or have the discard path in cmd_cache remove files it created; track created-vs-modified in the backup map.
- **[low/optimization]** `:123-125, 350` (conf medium) — **Redundant re-parsing/re-flattening on the render hot path**
  - _load_model (lines 124-125) parses the same pkg.list file twice — parse_pkg_list_groups then parse_pkg_list_group_meta each do an independent readfile + full regex pass (utils.py:3967,4034). Per keystroke, handle_key rebuilds the full row list via self._rows() (line 350) and _render rebuilds it again (line 249); _format_pkg_row additionally does a linear self._entry() scan per visible row (line 302). Marginal at current pkg.list sizes (~hundreds), but it is needless re-computation that scales with total package count on every key event.
  - *Fix:* Cache the flattened rows and invalidate on model mutation (toggle/add/drop); pass the entry's selected flag into _Row so _format_pkg_row need not re-scan; share one file read between the two parse helpers.

### `scripts/selection_lock.py` (2)

- **[low/boundary]** `:91-93` (conf high) — **Transient OS read errors are reported as STATUS_MALFORMED (treated as tamper)**
  - read_selection_state catches every non-FileNotFoundError OSError (EIO/EACCES/ENFILE/transient NFS) and returns STATUS_MALFORMED. classify() (line 355) maps MALFORMED -> ACTION_HARDSTOP, and closure_matches_lock() (line 438) maps it -> COHERENCE_BADLOCK. Consumers then refuse: cmd_cache hard-stops and cmd_build.py:565-569 prints 'iso build: REFUSED — selection.state is untrusted (badsig/malformed). `cache purge-state` to re-baseline.' So a transient read glitch during the multiday cycle surfaces as a tamper/corruption verdict whose suggested remedy (purge-state) is documented as 'heavy mirror impact'. STATUS_MALFORMED's own docstring (lines 79-80) defines it as 'present but not valid JSON', which an I/O error is not.
  - *Fix:* Distinguish transient I/O from corruption: either re-raise/propagate the OSError (fail loudly without re-baseline guidance) or add a STATUS_IOERROR that callers treat as retryable rather than as untrusted/tamper. At minimum, do not steer the operator toward purge-state on an OSError.
- **[low/correctness]** `:352-356` (conf medium) — **classify() returns two dicts that alias the SAME empty set objects for added/removed**
  - _empty = {'bins': set(), 'srcs': set()} is built once, then returned as `dict(_empty), dict(_empty)` for BOOTSTRAP (line 354) and HARDSTOP (line 356). dict() is a shallow copy, so both returned dicts share the identical set() objects, and added['bins'] IS removed['bins']. The same pattern exists in closure_matches_lock (line 436/439). Verified current consumers only READ these (cmd_cache.py:993 `if _added['bins'] or _added['srcs']`; cmd_build.py:560-561 len()), so no live bug today — but any future consumer that does added['bins'].add(...) would silently mutate removed['bins'] too.
  - *Fix:* Return freshly constructed dicts with independent sets, e.g. `return ACTION_BOOTSTRAP, {'bins': set(), 'srcs': set()}, {'bins': set(), 'srcs': set()}` (or a `_fresh_empty()` helper), instead of two shallow copies of one shared template.

### `scripts/signing.py` (2)

- **[low/boundary]** `:158-163` (conf medium) — **format_gpg_time leaks OverflowError on out-of-range epoch (only ValueError/OSError caught)**
  - The except clause catches (ValueError, OSError) only. I verified datetime.fromtimestamp(int(s), tz=utc) raises OverflowError ('timestamp out of range for platform time_t') for values like '99999999999999999999', which is NOT a subclass of ValueError/OSError. The docstring promises it 'degrades to the raw upstream value rather than swallowing the field', but an OverflowError would instead propagate and crash the display path (build.py:941-942 prints created/expires). gpg is unlikely to emit such a value, but a corrupted/hostile colons line would trip it.
  - *Fix:* Add OverflowError to the except tuple: `except (ValueError, OSError, OverflowError): return s`.
- **[low/dead-code]** `:114-116` (conf high) — **Unreachable `if not parts: continue` guard in parse_secret_keys_colons**
  - str.split(':') always returns a non-empty list (''.split(':') == [''], so `not parts` is always False — verified). The guard never fires; parts[0] on a blank line just yields rec='' which falls through all the elif checks harmlessly.
  - *Fix:* Remove the dead `if not parts: continue` lines (cosmetic; no behavior change).

### `scripts/surfaces.py` (1)

- **[low/optimization]** `:98-120` (conf high) — **Extras fixpoint rescans the entire closure every round instead of only newly added nodes**
  - In surface_closure's include_recommends_extras block, the outer `while _changed` loop re-iterates the full `_closure` (line 102 `for _n in _closure`) on every round to collect recommended extras. Because every extra in `_extras_canon` is available from the first round, all extras recommended by the initial hard closure are pulled in round 1; subsequent rounds only need to inspect the recommends of nodes newly added by the previous round's `_wanted`/hard-drain. Rescanning every closure member (up to ~1105 for the live surface, 11252 for full self-host) each round is wasted work proportional to rounds*|closure|. Correctness is fine (convergence holds, `_changed` set whenever `_wanted` non-empty); only efficiency suffers.
  - *Fix:* Track a `_frontier` of newly added nodes (the union of `_wanted` and the hard-deps added in the inner `while _stack` drain) and scan only `_frontier`'s recommends in the next round, instead of `for _n in _closure`. This is build-time, low-frequency code, so the win is modest.

### `scripts/tasksel_desc.py` (2)

- **[low/boundary]** `:28` (conf medium) — **_sanitize keeps non-whitespace ASCII control chars (ord 0-8, 11, 14-31)**
  - The guard is `ord(_ch) > 126 or _ch in _BANNED`. Control chars 0x00-0x08/0x0E-0x1F are <=126 and not in _BANNED, so they pass through. The trailing `' '.join(...split())` only collapses Python whitespace (tab/nl/cr/ff/vt), so e.g. 0x01 (SOH) survives into the emitted Description/Key. cdebconf is documented (module docstring + tasksel-desc-keep-minimal memory) to silently drop a task on unexpected characters, so a stray control char in a pkg.list group name or `## Description:` would drop the whole task. In practice pkg.list content is hand-authored ASCII, so this is near-unreachable, hence low.
  - *Fix:* Tighten the keep-condition to printable ASCII, e.g. drop any `_ch` where `ord(_ch) < 32 or ord(_ch) > 126`, before the whitespace-collapse.
- **[low/missing-test]** `:55` (conf high) — **No test covers meta value being None or a group present in groups but absent from meta with empty seeds**
  - test_tasksel_desc_generator_shape_and_sanitization (tests/test_module.py:39021) exercises happy path, [base] skip, empty-group skip, sanitization, and fallback title. It never passes `_meta = {'g': None}` (the `or {}` branch at line 55 guarding a None per-group meta) nor a control char. The `(_meta.get(_group, {}) or {})` None-coalesce is real defensive code with no asserting test, so a regression removing it would not be caught.
  - *Fix:* Add a case with `_meta={'gnome-desktop': None}` asserting it falls back to the title without raising, locking in the `or {}` guard.

### `scripts/tui/dispatcher.py` (2)

- **[low/correctness]** `:238-243` (conf high) — **console_mark() can raise CancelledError on shutdown instead of returning 0**
  - console_mark()'s poll loop only catches _FutureTimeout and otherwise returns 0 when state.quit. But _cancel_pending_futures() (line 306-307) calls _e.future.cancel() on any queued ConsoleMark during shutdown; a cancelled Future makes fut.result(timeout=0.2) raise concurrent.futures.CancelledError, which escapes the loop. Consumers (facade.py:82 `b.console_mark()`, cli.py) expect an int, contradicting the docstring 'shutdown raced in — don't hang the caller / return 0'.
  - *Fix:* Catch CancelledError alongside _FutureTimeout in the console_mark() loop and return 0 (and similarly decide/ document the request_prompt() cancel path).
- **[low/dead-code]** `:150-155` (conf medium) — **IDLE_TIMEOUT / WIDGET_IDLE_TIMEOUT values never affect the actual loop wait**
  - _tick (line 271-272) computes `_ms = max(1, min(int(_compute_timeout()*1000), INPUT_POLL_MS))` with INPUT_POLL_MS=50. _compute_timeout() returns at most IDLE_TIMEOUT(1.0s)=1000ms or WIDGET_IDLE_TIMEOUT(0.1s)=100ms, both >50ms, so they are always clamped to INPUT_POLL_MS; only a widget next_frame_at deadline <50ms ever wins. The comment at line 154-155 claims '10 Hz' for legacy widgets but the effective cap is 20 Hz, and the two IDLE constants' values are moot.
  - *Fix:* Either drop/relabel IDLE_TIMEOUT & WIDGET_IDLE_TIMEOUT (they no longer set the wait) or remove the INPUT_POLL_MS clamp if longer idle sleeps were intended; fix the '10 Hz' comment to 20 Hz.

### `scripts/tui/events.py` (1)

- **[low/dead-code]** `:173-178` (conf high) — **ALL_EVENT_TYPES is unused (no test or discovery consumer)**
  - grep -rn ALL_EVENT_TYPES across scripts/ and tests/ returns only its definition site in events.py:173/177; the comment claims it is 'handy for tests / discovery' but no test references it and no dispatcher completeness check imports it. It can silently drift out of sync with the class list / dispatcher._handle isinstance chain (lines 389-403) without any test failing.
  - *Fix:* Either add a test asserting ALL_EVENT_TYPES matches the dispatcher._handle isinstance chain (catching a forgotten handler when a new event is added), or drop the list if discovery is not actually needed.

### `scripts/tui/facade.py` (1)

- **[medium/boundary]** `:164-168` (conf medium) — **PROMPT_OPTIONS under stdin EOF returns an unvalidated (empty) answer, crashing consumers that index by it**
  - On EOF, cli.py:265 prompt() returns '' and sets stdin_eof=True. In get_response the EOF guard (line 164) only special-cases PROMPT_YESNO (returns 'n'); for every other type it `break`s with whatever was read BEFORE the OPTIONS membership check at line 169. So an OPTIONS prompt returns '' (or any non-option string). A live consumer, dependencytree.py:289-290, does `_choice = Prompt(PROMPT_OPTIONS,...).get_response(); _selected = _collapsed[int(_choice) - 1]` — `int('')` raises ValueError, an uncaught exception leaking out of the cache-parse/provider-selection path under headless `--cmd` with no input. The docstring acknowledges 'other types break with whatever was read', i.e. the unvalidated return is by design but the contract is unsafe for OPTIONS.
  - *Fix:* On the EOF branch, treat PROMPT_OPTIONS like a hard abort: either raise a clean controlled error (e.g. Exit/abort) or return a sentinel the consumers check, rather than returning an out-of-range string that callers feed to int()/list-index. Alternatively, document and enforce that OPTIONS consumers validate get_response() membership before int()/indexing.

### `scripts/tui/logging_bridge.py` (1)

- **[low/dead-code]** `:24-29` (conf high) — **_tab_for_logger ignores its `name` argument (always returns 'log')**
  - After the three-tab consolidation _tab_for_logger unconditionally returns 'log' and never reads `name`. Both call sites (lines 71) still pass record.name. The companion test test_per_module_logger_names_pin_routing and its docstring still describe per-stage tab routing 'via _tab_for_logger' (e.g. 'bare athena falls back to the build tab'), which is now stale — routing-by-name no longer happens here; stage origin is carried only by _stage_prefix.
  - *Fix:* Either keep the parameter as an intentional interface stub with a comment, or drop the param and inline a 'log' constant; update the stale routing language in the pinning test's docstring so it does not imply _tab_for_logger differentiates tabs.

### `scripts/tui/render.py` (1)

- **[low/correctness]** `:283-293` (conf medium) — **Console widgets float under content instead of anchoring to bottom rows when buffer is short**
  - _render_tab computes content_rows = max(1, max_y - widget_rows) and draws content from row 0 via the incrementing `row`, then draws widget_strs starting at the current `row`. When the wrapped display has fewer rows than content_rows (short buffer / freshly cleared console), `row` ends well above max_y-widget_rows, so widgets render immediately beneath the last log line rather than on the bottom rows the docstring (line 252) and _page_rows comment in dispatcher.py promise. During a full build the buffer is saturated so widgets do land at the bottom, masking this; it only manifests with a short console buffer.
  - *Fix:* If widgets must occupy the bottom band, pad `row` up to max_y - widget_rows before drawing widget_strs (e.g. row = max(row, max_y - widget_rows)), or draw widgets at explicit rows max_y-widget_rows+i.

### `scripts/tui/state.py` (2)

- **[medium/missing-test]** `:54-97, 161-187` (conf high) — **TabState eviction/scroll bookkeeping and CmdLine history have no test coverage**
  - grep across tests/test_module.py for scroll_offset, history_prev, history_next, entry_display_rows, MAX_BUFFER returns only sys.path lines — no test imports or exercises scripts/tui/state.py's TabState.append (offset bump + MAX_BUFFER_LINES front-eviction + dropped_rows decrement) or CmdLine.history_prev/history_next/hist_draft round-trip. These are the subtle stateful paths (scroll anchor preservation across eviction, draft stash/restore) most likely to regress silently.
  - *Fix:* Add unit tests: (a) append past MAX_BUFFER_LINES with scroll_offset>0 asserting offset decremented by evicted display rows and clamped at 0; (b) history Up/Up/Down/Down sequence asserting hist_draft is stashed on first Up and restored exactly when walking past newest. wrap.* are already pure and importable with no curses.
- **[low/correctness]** `:224-231` (conf medium) — **active_tab_name() mutates state (selects a tab) without setting dirty**
  - active_tab_name() is a getter-named method (and is called from active_tab() on the hot path, e.g. dispatcher _on_print/_on_log and render). Its recovery branch sets self.tabs[first].selected = True but does not set self.dirty = True, so a recovered selection would not by itself schedule a redraw. This is a defensive path (dispatcher's TabActivate normally guarantees exactly one selected), so it is unlikely to fire, but if it ever does the tab-bar highlight could be stale until the next unrelated mutation.
  - *Fix:* Set self.dirty = True in the recovery branch before returning, or move recovery into a clearly mutating helper and keep active_tab_name() read-only.

### `scripts/tui/tui.py` (2)

- **[low/correctness]** `:298-306` (conf high) — **SystemExit with a non-int code is mapped to exit 0 (success), masking failure**
  - In _shell, `self.exit(int(_se.code) if isinstance(_se.code, int) else 0)`. Python convention is that `sys.exit("msg")` (string code) yields process exit status 1, not 0. A handler that calls sys.exit with a string or any non-int truthy code would be reported as success and propagated via build.py:2251 `Exit(getattr(tui_inst,'_exit_code',0) or 0)`. Most handlers in build.py use sys.exit(1) (int), so the live blast radius is small, but any string-coded exit silently reports success.
  - *Fix:* Mirror CPython semantics: treat non-None, non-int code as 1 (failure) rather than 0, e.g. `code = _se.code; self.exit(code if isinstance(code,int) else (0 if code is None else 1))`.
- **[low/redundancy]** `:251` (conf high) — **Redundant local `import curses` inside attr_reverse**
  - `curses` is already imported at module top (line 19) and used directly elsewhere (line 72 curses.initscr). The local `import curses` in attr_reverse() is vestigial; attr_color() (line 254-256) does not re-import and works fine.
  - *Fix:* Drop the local `import curses` on line 251 and reference the module-level import.

### `scripts/tui/widgets.py` (2)

- **[low/boundary]** `:96-103` (conf medium) — **set_max lowering below current _value overflows the fixed-width bar**
  - set_max does _max = max(1, value) with no clamp against _value. In __str__ (line 126-128) pct = _value/_max can then exceed 1, filled = floor(pct*bar_width) > bar_width, so bar = '█'*filled + '░'*(bar_width-filled) yields a string LONGER than _bar_width (the second term is '' for a negative count), breaking the fixed-width TUI layout. All current callers (utils.py:3513 set_max(bytes_written), build.py:1054, download grow-path) only raise the max, so it is not currently reached — hence low.
  - *Fix:* Clamp in set_max (self._max = max(1, value, self._value)) or clamp filled = min(filled, self._bar_width) in __str__.
- **[low/correctness]** `:178-183` (conf low) — **Spinner.__str__ mutates animation state (impure render)**
  - __str__ advances self._frame and self._last_t (lines 180-181). Unlike ProgressBar.__str__ (pure), rendering a Spinner has a side effect, so any non-render call to str() — or a renderer that calls __str__ twice per frame for measurement+draw — double-advances the glyph. It is the documented animation mechanism (next_frame_at pairs with it), so it works under the single render pass today.
  - *Fix:* Drive frame advancement from an explicit tick()/advance() called once per dispatcher wake, keeping __str__ pure.

### `scripts/utils.py` (3)

- **[low/correctness]** `:1176,1232` (conf high) — **read_build_history collapses multiple failed runs of one package via read-time ts fallback**
  - _build_history_line sets ts = record.get('finished') or _utc_now_iso() (line 1176) and _consider dedups on (package, ts) (line 1232). buildcontainer._record_phase's FAIL path (buildcontainer.py:1327-1329) writes phase='failed' WITHOUT a 'finished' field, and update_build_record only auto-sets 'status', never 'finished' — so failed records carry finished=None. When a package failed on consecutive attempts (prior run in build-history.jsonl, latest in <pkg>.build.json), both records synthesize ts=_utc_now_iso() during the same read_build_history() call; if computed in the same wall-clock second they collide on the (package, ts) key and the latest failed run is silently dropped from the history. The same fallback also clusters all finished=None records at 'now' in the final sort (line 1264), mis-ordering them. Consumer is display-only: cmd_run.py:636 (`build history`).
  - *Fix:* Use a stable per-record timestamp as the fallback instead of read-time now, e.g. ts = record.get('finished') or record.get('started') or _utc_now_iso(); and/or widen the dedup key to include record.get('started')/exit_code so distinct runs can't alias. Optionally also set finished in buildcontainer.py's phase='failed' transition.
- **[low/optimization]** `:3943,4010` (conf high) — **parse_pkg_list_groups + parse_pkg_list_group_meta re-read the same file and recompute section detection**
  - Both functions independently call readfile(path) and rebuild _section_re/_has_sections over the same pkg.list. Every live call site invokes them as a back-to-back pair on the identical path (cmd_build.py:176-177, selection_lock.py:235/238, select_packages.py:124-125), so the file is read and re-scanned twice per use. Files are small so impact is minor, but it is pure duplicated I/O/parsing.
  - *Fix:* Either parse once into a combined (groups, meta) structure shared by both call sites, or have the meta parser accept already-read lines, so a single read/scan serves both.
- **[low/redundancy]** `:1451-1457` (conf medium) — **lifecycle_touch_selected create-path reads the build record twice**
  - At line 1451 read_build_record() returns None, then the create branch calls upsert_build_record() (line 1453) which itself begins with another read_build_record() (line 1342) for the same package/dir. The first read's result is only used to choose the branch, so the disk read is repeated for every newly-selected source during `cache parse`.
  - *Fix:* For the create branch, construct + write via new_build_record/write_build_record directly (or have upsert accept an already-loaded record), avoiding the second read_build_record round-trip.

### `scripts/virtual_build.py` (4)

- **[low/dead-code]** `:855,877-886,898` (conf high) — **`was_patched`/`_at_build_delta` in validate are dead — always overridden by `override_patch_level`**
  - validate_against_build_records always computes `_rec_patch_level` (line 891, int, falls back to 0) and passes it as `override_patch_level=_rec_patch_level` (line 903). In synthesize_source_binaries line 619-620, `_patch_level = (override_patch_level if override_patch_level is not None else (1 if was_patched else 0))` — since 0 is not None, `was_patched` is NEVER consulted on this path. So `_was_patched` (line 855), the `_at_build_delta` accumulation in the loop (lines 878,885-886), and the `was_patched=_at_build_delta or _was_patched` argument (line 898) are all dead work. Note: buildcontainer._normalize_built_artifacts floors patch_level to 1 when was_patched and record counter is 0 (buildcontainer.py:1889-1890); validate does NOT replicate that flooring, so the intended 'patched build → +p1' alignment is silently inert (masked downstream only because _filename_signature strips the asg/+p layer via pristine_base).
  - *Fix:* Drop `_was_patched`, the `_at_build_delta` accumulation, and the `was_patched=` argument; if validate should match buildcontainer's flooring, pass `override_patch_level=max(_rec_patch_level, 1 if _was_patched else 0)` instead.
- **[low/dead-code]** `:482,505,523-525` (conf high) — **`asg_ledger` parameter is fully vestigial and forces a wasted disk read at the call site**
  - synthesize_source_binaries docstring (lines 504-505,523-525) states asg_ledger 'is no longer consulted for the version decision'; grep confirms no read of the param in the body (K is intrinsic via transposed_version). validate passes asg_ledger=None, but cmd_virtual.py:306 computes `_asg_ledger = _ra.published_ledger(self.config) or {}` (disk I/O) purely to feed this ignored parameter at cmd_virtual.py:335. validate_against_build_records also still carries the param (line 774).
  - *Fix:* Remove the `asg_ledger` parameter from both functions and delete the now-unnecessary published_ledger() call in cmd_virtual.py.
- **[low/optimization]** `:265-267,1296-1303` (conf medium) — **Each dep-relation field is PkgRelation-parsed/serialized up to three times in the full-repo audit**
  - synthesize_binary_record runs _transpose_relation (line 265) then _rewrite_sibling_pins (line 266) — two independent PkgRelation.parse_relations + PkgRelation.str round-trips per field. synthesize_repo_state then runs _global_pin_rewrite over the same fields post-dedup (lines 1296-1303), a third round-trip. For the full self-host audit (~11k binaries × ~tens of dep fields each, per the MEMORY closure-model notes), that is hundreds of thousands of redundant parses. The transpose+sibling pair operate on the same map and could share one parse before dedup.
  - *Fix:* Merge _transpose_relation and _rewrite_sibling_pins into a single parse→mutate→serialize pass (the global pass must stay separate since it runs after dedup).
- **[low/redundancy]** `:917-933` (conf high) — **`_strip_asg` applied on top of `pristine_base` is a no-op**
  - _filename_signature (line 933) computes `_strip_asg(utils.pristine_base(_ver))`. bump.pristine_base (bump.py:258-270) already does `strip_nmu_suffix(ASG_SUFFIX_RE.sub('', version))`, i.e. it strips the +asgRuN (and +pP/+bN tail) before its result is returned, so the subsequent _strip_asg (ASG_SUFFIX_RE.sub, lines 917-922) can never match anything. Harmless but misleading.
  - *Fix:* Drop the redundant `_strip_asg(...)` wrapper in _filename_signature; pristine_base already removes the asg layer.

### `scripts/webapi/__init__.py` (2)

- **[medium/correctness]** `:214-224` (conf medium) — **SSE stream can drop the final output line(s) due to drain-then-state ordering**
  - In `_events`, the inner `while _sent < len(_out)` drains current output, THEN `if _job.state in ('done','error')` returns. In jobs.py wait() (lines 154-155) the worker appends ALL output inside _dispatch_one and only afterward sets `_job.state='done'`. The generator and worker run concurrently: if the worker appends its final line(s) AND flips state to 'done' in the GIL window after the generator exits its inner drain loop but before it evaluates the state check, the generator sees a terminal state and returns via the `event: end` branch without ever re-entering the drain loop. The dropped line is typically the command result/error (the most important one). `_out = _job.output` is a live reference, so re-looping would have caught it, but the terminal-state branch short-circuits that.
  - *Fix:* After detecting a terminal state, perform one final drain before emitting the end event, e.g. inside the `if _job.state in ('done','error')` block re-read `_out = _job.output` and `while _sent < len(_out): yield ...; _sent += 1` before the `event: end` yield.
- **[low/missing-test]** `:206-227` (conf high) — **SSE /jobs/{job_id}/stream endpoint has no test**
  - test_api01_* covers /command, /jobs, /jobs/{id} and the ApiBackend dispatcher via TestClient (tests/test_module.py:24334-24347), but grepping the test file shows no exercise of '/stream' or the `_events` generator. The tail-loss race above lives entirely in this untested path, so a regression in the drain/terminal-state ordering would ship silently.
  - *Fix:* Add a TestClient test that submits a job whose handler emits several lines then finishes, opens the stream, and asserts every emitted line plus the trailing `event: end` are received (covers both ordering and the end sentinel).

### `scripts/webapi/auth.py` (2)

- **[medium/boundary]** `:34-38` (conf medium) — **Concurrent first-start loser raises instead of getting the winner's key**
  - The O_EXCL branch claims 'the loser re-reads the winner's key', but the winner creates the file (line 33) BEFORE writing the key (lines 39-40). If the loser hits FileExistsError in the window after create but before the winner's write+fdopen-close flush, load_api_key (line 35) reads an empty file, returns '' (strip), so `if _retry:` is False and the original FileExistsError is re-raised (line 38). The loser crashes rather than obtaining the key, contradicting the docstring and defeating the race-tolerance the O_EXCL pattern was added for.
  - *Fix:* On FileExistsError, retry load_api_key in a short bounded poll loop (e.g. a few attempts with tiny sleep) until a non-empty key appears before giving up and raising; or write to a temp file and os.rename into place so the file is never observable empty.
- **[low/correctness]** `:18` (conf high) — **Comment '32 bytes → 64 hex chars' is wrong for token_urlsafe**
  - Line 29 uses secrets.token_urlsafe(_KEY_BYTES), which emits base64url (~43 chars for 32 bytes), not 64 hex chars. The comment describes token_hex output, not the code's actual output. Misleading but non-functional.
  - *Fix:* Reword to '32 bytes → ~43 url-safe base64 chars' to match token_urlsafe.

### `scripts/webapi/jobs.py` (4)

- **[medium/dead-code]** `:159-164` (conf high) — **except PromptRequired branch is unreachable; structured prompt_required contract is not delivered**
  - wait() calls self._dispatch_one(_job.cmd) (line 154). cli.py:_dispatch_one wraps the handler call in `try: fn(*args) except Exception as exc` (cli.py:425-431). PromptRequired subclasses Exception (line 37), so when prompt() raises it (line 133) it is caught inside _dispatch_one, turned into an `ERROR("command 'X' raised PromptRequired: ...")` line, and _dispatch_one returns False. Control therefore lands in the `not _ok` branch (line 156-158) with _job.error set to the last output line, never in the `except PromptRequired` handler. The module docstring (lines 17-21) promises a 'structured prompt_required error', but the client actually receives state='error' with error='[ERROR] command X raised PromptRequired: ...' — the clean `prompt_required: <msg>` form at line 164 never materializes.
  - *Fix:* Either re-raise PromptRequired out of _dispatch_one's generic handler (special-case it in cli.py before `except Exception`), or drop the dead except branch here and instead detect the prompt-required condition from the captured ERROR line / a dedicated flag set by prompt(). Pick one so the documented contract and the code agree.
- **[low/correctness]** `:72,85-87` (conf high) — **_jobs dict grows unbounded for the life of the API session**
  - submit() inserts every Job into self._jobs under the lock (lines 85-86) and nothing ever evicts. get_job/list_jobs only read. Over a multiday build cycle driving many commands through the API, this retains every Job plus its full output buffer in memory indefinitely.
  - *Fix:* Cap retained completed jobs (e.g. ring buffer / prune done|error jobs older than N or beyond a max count) under _jobs_lock after each job finishes.
- **[low/missing-test]** `:137-169` (conf high) — **No test coverage for the job loop, drain-then-stop, or prompt handling**
  - Grep for ApiBackend/webapi/PromptRequired across tests/ returns only tests/test_module.py with no match; there is no webapi test. The drain-then-stop semantics (lines 138-149), the prompt failure path, and the _ok=False error-population (lines 155-158) are entirely untested, so the dead-branch bug above shipped unnoticed.
  - *Fix:* Add a unit test that submits a command whose handler calls self.prompt(), runs one wait() iteration with a sentinel, and asserts the resulting job.state/error — which would immediately expose the unreachable branch.
- **[low/redundancy]** `:79-81` (conf medium) — **known_command re-parses the command already split/validated at the route**
  - webapi/__init__.py:182-189 already does `_cmd.split()[0]` for control-token and unknown-command checks before calling backend.known_command, which splits again at line 80. Minor duplicated parsing of the same string.
  - *Fix:* Have the route pass the already-extracted first token, or expose known_command(first_token) taking the parsed verb, to avoid the second split.

### `scripts/webapi/readers.py` (4)

- **[medium/correctness]** `:303-330` (conf medium) — **progress() completion rate/ETA counts every record touched in the window, not just completed builds**
  - _times appends os.path.getmtime for EVERY readable record (line 303) regardless of phase. _recent (line 320) and completed_in_window/rate_per_hour/eta_hours (lines 321,329-332) are derived from that. Build records are rewritten on each phase transition (entry/container_exited/segregated/failed), so their mtimes are recent while in-flight or failed — these inflate _recent. The docstring calls this 'completion rate' and feeds eta_hours, so the rate over-counts non-completions and the ETA is over-optimistic during an active rebuild.
  - *Fix:* Restrict the rate window to terminal/success phases (only count mtimes of records whose phase indicates completion, e.g. 'done'/'built'), not all records.
- **[low/correctness]** `:170` (conf medium) — **read_artifact tail 'truncated' compares byte size to character-length sum**
  - _size is the file size in bytes (os.path.getsize / _tail_lines returns byte length), but the comparison `_size > sum(len(_l) + 1 for _l in _lines)` sums decoded-string character lengths plus one newline each. On multibyte/unicode content len(chars) < bytes, so the sum underestimates and 'truncated' reports True even when all lines were returned; a final line without a trailing newline also skews the +1 accounting.
  - *Fix:* Track whether _tail_lines actually hit file start (pos>0 when the loop exited) and report truncated from that, or compare byte counts consistently (sum of len(_l.encode())+1).
- **[low/correctness]** `:348-349, 361` (conf high) — **buildlog/vbuildlog opened without a context manager in progress()**
  - Lines 348-349 (`open(...).read()`) and 361 (`open(_vb, errors='replace').read()`) leak the file handle to the GC/refcount rather than closing deterministically, unlike read_flags/list_builds which use `with`. Over a corpus of ~985 buildlogs this relies on CPython refcount semantics for prompt closure.
  - *Fix:* Wrap both reads in `with open(...) as _fh: _txt = _fh.read()` to match the rest of the module.
- **[low/missing-test]** `:206-254, 372-389` (conf high) — **mirror_state(), repo_summary(), phase_counts() and read_flags() have no direct tests**
  - Grep of tests/test_module.py for readers.mirror_state / readers.repo_summary / readers.read_flags / readers.phase_counts returns nothing; only list_builds, get_build, read_artifact, progress, read_config_redacted, valid_package_name are exercised. The mirror_state legacy-vs-mirror.conf defect above would have been caught by a test asserting a mirror.conf-only registration is visible.
  - *Fix:* Add tests covering mirror_state against a mirror.conf-backed registry (including a mirror with no legacy .state file), plus repo_summary/phase_counts/read_flags happy + empty-dir paths.

## Fully clean files

- `scripts/_version.py`
- `scripts/commands/__init__.py`
- `scripts/commands/base.py`
- `scripts/coord/__init__.py`
- `scripts/tui/__init__.py`
- `scripts/tui/wrap.py`
