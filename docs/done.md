# Athena-Build — Archived (done / wontfix) tickets

> Tickets archived from TODO.md once their status moved to `done`
> or `wontfix`.  Section numbering mirrors TODO.md so the same ID
> stays findable here.  Rows are preserved verbatim from TODO.md;
> no editing on archive — the embedded run-history is the value.

---

## 1. Stability & correctness — P0 / P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| STA-01 | P0 | done | Verify mirror `InRelease` GPG signatures against a pinned keyring before trusting `Packages` / `Sources` indices. *(implemented via `utils.verify_inrelease()` + `[Security]` config; default uses host-provided debian-archive-keyring; tests in `test_module.py`)* |
| STA-02 | P1 | done | Remove `--force-depends` from `_configure_chroot` (`buildsystem.py:424,434`) and surface the real dep error. Pre-req: STA-03. *(both chroot and chrootless dpkg invocations now run without --force-depends; STA-03's snapshot pinning + the existing `_check_dep_drift`/`_verify_dep_resolution` chain ensure unresolved deps are caught and raised before configure.  `_ensure_initramfs` repurposed from "workaround for force-depends" to defence-in-depth.)* |
| STA-03 | P1 | done | Make snapshot pinning the **default** in `config/build.conf` so cache and live mirror cannot drift between cache build and source build. *(shipped `[Snapshot] Enabled = true`; lock-in test `test_shipped_build_conf_has_snapshot_enabled` blocks regressions; smoke confirmed snapshot.d.o → InRelease verify chain end-to-end)* |
| STA-04 | P1 | done | Validate downloaded files at the moment they finish writing in `download_source` — currently size and `sha256` are checked, but a non-200 response writes a 0-byte file and the loop continues; surface a clear error for the user. |
| STA-05 | P1 | done | Hardening of `BuildContainer.build` — wrap `client.containers.run` in a try / `finally container.remove()` so failed runs don’t leak Docker containers. Also log container ID on failure. |
| STA-06 | P1 | done | `BuildContainer.__init__` is annotated `-> bool` but returns nothing. Remove the misleading annotation. |
| STA-07 | P1 | done | `cmd_build_chroot` writes the sudo password to `subprocess.run(input=...)` and keeps it in `BuildSystem.__password` for the whole run. Audit lifetime: zero it after `verify_chroot` completes; consider switching to `sudo -A` with an askpass helper. *(scrub_password() + try/finally in cmd_build_chroot/cmd_build_iso/cmd_verify_chroot; .password property raises after scrub. sudo -A askpass left as a future task — bigger refactor with sudo TTL trade-offs.)* |
| STA-08 | P1 | done | `pre_install` `cmd_list` uses Python-formatted shell strings with `shlex.split` — paths are not quoted. Replace with argv lists (`['sudo','-S','ln','-sfv',...]`). |
| STA-09 | P1 | done | `Cache.__get_files`: when `_release_url` returns a 0-byte / non-200, current code writes the file then bails generically — preserve and surface HTTP status. *(`download_file` now returns `(size, detail)`; HTTPError captures `HTTP <status> <reason>`; `Cache.error_str` and the tunnel error log include the detail.)* |
| STA-10 | P1 | done | `BuildSystem._mount_chroot_fs` — log the mount target and check `/proc/self/mountinfo` after mounting to confirm; current code only warns on `_proc.returncode != 0`. *(uses `os.path.ismount()` rather than parsing mountinfo — stdlib, simpler, reliable for procfs/sysfs/bind targets; raises on failure so partial-mount state aborts the chroot build cleanly via build_chroot's existing finally → _umount_chroot_fs)* |
| STA-11 | P2 | done | `dependencytree.py:124` references `self._VALID_CONSTRAINTS` from inside `add_lookahead` but the class attribute is defined further down (`:157`) — works only because Python looks it up at call time. Move definition above first use. *(moved `_VALID_CONSTRAINTS = {'=', '>=', '<=', '>>', '<<', '>', '<'}` to the top of the class body, between `class DependencyTree:` and `__init__`.  All three call sites — `add_lookahead`, `parse_dependency`, `_check_dep_drift` — see it via the same `self._VALID_CONSTRAINTS` lookup; the class body now reads top-down rather than depending on Python's runtime name resolution to find the attribute three methods further down.  64/64 tests pass.)* |
| STA-12 | P2 | done | `cache.py:323-333` GCC-base selection uses string startswith / split heuristics — replace with `apt_pkg.parse_depends` on Provides graph, or an explicit allowlist in config. *(replaced with a single module-level `_GCC_BASE_RE = re.compile(r'gcc-(\d+)(?:-base)?$')`.  Note: `apt_pkg.parse_depends` was the wrong tool — it parses dependency *expressions*, not package-name patterns; gcc packages don't Provide a version-agnostic alias.  The regex is the focused fix: one fullmatch per package, named pattern with a leading comment that documents which gcc-prefixed names are intentionally excluded (gcc-mingw-w64, gcc-N-multilib, gcc-N-cross, g++-N, etc.).  Also fixed a latent bug: the old `pkg.startswith('gcc-')` filter on `self.required` would filter out cross-compilers etc. since they wouldn't appear in `latest_gcc`; the new `_GCC_BASE_RE.fullmatch(pkg)` only filters base-compiler entries, leaving unrelated gcc-prefixed packages untouched.  Removed the leftover TODO commentary describing an already-fixed sort-key bug.  3 unit tests pin the regex behaviour: matches the canonical pair, rejects other gcc-prefixed names, rejects malformed versions.  67/67 tests pass.)* |
| STA-13 | P2 | done | `_verify_chroot` reads `/etc/os-release` with a bare `open(_os_release).read()` — wrap in `with open()` or it leaks a file descriptor on the chroot path. *(wrapped in `with open(_os_release) as _osf:` in `build.py:_verify_chroot` Check 8 — fd lifetime now bounded to the read.)* |
| STA-14 | P2 | wontfix | `download_file` HEAD followed by GET means two requests per file; some mirrors throttle. Consolidate by using the `Content-Length` from the first GET response. *(decision 2026-05-09: don't.  **Git evidence**: commit `f1cf373` ("Harden error handling…", Apr 2026) **deliberately added** the HEAD probe.  Pre-`f1cf373` `download_file` was already doing exactly what STA-14 proposes — `file_size = int(response.headers.get('content-length', 0))` after `raise_for_status()`, with `max(1, file_size)` guard.  That commit replaced it with HEAD+GET *and* dropped the `max(1, ...)` guard, implying the HEAD reliably gave non-zero values while the GET path was returning 0 (which would have crashed `tui.ProgressBar` with `maxvalue=0`).  **Plausible root causes** for the GET-only path failing on Debian mirrors: (a) `Transfer-Encoding: chunked` — server omits `Content-Length` entirely, GET returns 0; (b) `Content-Encoding: gzip` auto-decompression in `requests` — `Content-Length` is wire (compressed) bytes but `iter_content` yields decompressed bytes; bar reads "120% downloaded"; (c) some mirrors omit `Content-Length` on certain conditional GETs.  **Cost/benefit**: removing HEAD saves ~1 round-trip × 30–40 files × ~100 ms = ~3–4 s per build.  Downside is regressing a deliberate hardening fix, with SHA256 verification downstream still catching wrong-bytes-on-disk but a broken progress bar (or a `maxvalue=0` crash) being a real UX regression.  **Revisit**: only when a test setup can reproduce the failing mirror behaviour and the underlying cause is addressed (e.g. a `max(1, ...)` guard + falling back to bytes-written counter when `Content-Length` is missing).)* |
| STA-15 | P2 | done | `parse_sources` regex `\+b\d+(?=_\w+\.u?deb$)` (`dependencytree.py:578`) and the inline copy in `tests/smoke_dep_drift.py:190` are *different*. Extract a single helper, use it in both, add a test. *(canonical helper now lives at `utils.strip_build_version` — single source of truth.  Three call sites consolidated: `BuildSystem.strip_build_version` (`buildsystem.py`) is a thin re-export keeping the mixin contract; `dependencytree.parse_sources` and `tests/smoke_dep_drift.py` both call `utils.strip_build_version` with a `try/ValueError → fallback` wrapper preserving each callsite's original tolerance for malformed APT entries.  Seven edge-case tests landed via TEST-04 covering trailing `+bN`, `+debNuM` preservation, mixed `+debNuM+bK`, embedded `+bN` in upstream version, `.udeb` extension, no-binNMU round-trip, and malformed-input rejection.)* |
| STA-16 | P2 | done | `Tui._activetab` silently resets state on inconsistency; should log to file too so we can post-mortem the cause. *(now routes the recovery message through both `logging.getLogger('athena').error(...)` (file logger via the FileHandler attached by `setup_file_logging`) AND the existing `self.ERROR(...)` (in-memory log tab).  Diagnostic message also enriched: previously printed only the count of selected tabs; now lists the offending tab names and the name of the tab being reset to, so a post-mortem from the on-disk log identifies *which* tabs were simultaneously selected, not just that some were.)* |
| STA-17 | P2 | done | Bare `except Exception` paths in `Cache.__build_cache` and `package.py` swallow `KeyboardInterrupt`-adjacent errors — narrow them or rethrow `BaseException`. *(narrowed all 9 sites — 6 in `cache.py` (`load_arch_table`, `Release(fh)`, `Package(record)`, `get_provides()`, `Source(record)`, `apt_pkg.check_dep`), 3 in `package.py` (`load_arch_table`, dep-field parser, `parse_src_depends`).  Per-site catch tuples chosen by what each call actually raises: `(OSError, RuntimeError, ValueError, KeyError)` for filesystem+parser paths; `(ValueError, KeyError, AttributeError, SystemError)` for record parsers (SystemError covers `apt_pkg.Error` which inherits from it); `(SystemError, ValueError)` for direct apt_pkg calls.  Note on the ticket framing: `except Exception` does NOT catch `KeyboardInterrupt` (which is `BaseException`); the real value of narrowing is letting programmer errors (`TypeError`, `NameError`, `ImportError`, `SyntaxError`) propagate instead of being silently logged-and-ignored.)* |
| STA-18 | P3 | done | **DependencyTree alias entries returned the provider's `Version:` instead of the `Provides:` clause version when checking versioned constraints against virtual names.**  Symptom: `libgcc-s1` declares `Provides: libgcc1 (= 1:12.2.0-14+deb12u1)` (epoch 1) but its own `Version:` is `12.2.0-14+deb12u1` (epoch 0).  Consumer `libwebrtc-audio-processing1 Depends: libgcc1 (>= 1:4.0)` failed because constraint check read libgcc-s1's `.version` directly, dropping the epoch — `apt_pkg.check_dep` compared epoch 0 against epoch 1 lower-bound → unsatisfied → `WARNING: unresolved dependency 'libgcc1'` + missing forward/reverse edge in selected_pkgs graph + `validate_selection` returned False → operator prompt "There are one or more dependency validation failures, Proceed?" fired.  Not install-blocking (apt re-resolved correctly at install time on /target) but operator-visible noise + dep-graph closure under-estimate (missing edges meant `pull_recommends_extras` and `get_install_sequence` under-walked through affected aliases).  **Fix landed 2026-05-22**: new `DependencyTree._version_for_constraint_target(entry_key, target_name)` helper lifts the same idiom already used by the Breaks/Conflicts arms (via `Package.explicit_provides_version()`).  Three call sites updated: `parse_dependency` alt-dep loop, `validate_selection` direct-hit alt-dep, `validate_selection` Provides-fallback alt-dep.  Returns the Provides clause version when declared, None when unversioned (per Policy §7.5).  5 unit tests (helper coverage + libgcc1-shape integration + unversioned-Provides symmetry guard); 445 tests pass; mypy + ruff clean. |
| STA-19 | P1 | done | **`_strip_nmu_from_built_artifacts` rescans the whole repo + opens every .deb with DebFile.**  Discovered 2026-05-22 during COMP-01f testing: every source build (even a tiny udeb like athena-installer-data with one output) stalled for 2-3 minutes after the container exited, with the system going unresponsive (CPU + I/O saturation).  Root cause: the strip helper walked `repo/main + doc + dbgsym + tests` (~4500 .debs), opened each with `DebFile` (fork+exec+tar-extract+parse per file), and filtered by `Source:` field — to find the 1-50 files that source actually produced.  O(repo_size) work to find O(this_source) artifacts.  **Fix**: thread `_segregate_built_artifacts`'s already-known post-move file list into `_strip_nmu_from_built_artifacts(src_pkg, built_files)`.  Skip the walk + the DebFile open + the Source-field filter — we already know which files this build emitted because segregate just moved them.  Result: post-build phase drops from ~3 min to <1 sec on small sources, ~3-5 min to ~1-2 sec on large sources (linux 50+ binaries).  2 anti-regression tests: `test_buildcontainer_calls_strip_post_build` pins the new threaded-list call signature; `test_strip_nmu_from_built_artifacts_does_not_scan_repo` asserts no `DebFile` / `os.listdir` / hardcoded subdir names in the function body.  Filed + fixed in the same session — straight to done.md. |
| STA-21 | P1 | done | **`repo_audit._read_signed_manifest` — fail closed on signing-setup failure.**  Today (`scripts/repo_audit.py:570`) catches a broad `Exception` from `import signing` / homedir lookup, logs a WARNING, and falls through to read the file UNVERIFIED.  Same shape on the write side (`_write_signed_manifest:544`) — signing failure logs WARNING but still writes an unsigned manifest, and the caller can't distinguish "manifest written + signed" from "manifest written, signature failed".  The local signed manifest is the authority for `+asg<R>u<N>` bump-N derivation (consumed by `published_ledger` → `highest_asg_update`).  Quiet trust degradation here lets a publish silently mint colliding `+asg uN` numbers when signing breaks.  **Fix:** raise (or return distinct sentinel) instead of fall-through; surface the failure to the operator at `repo publish` time.  Identified by the 2026-05-28 consolidation audit (Candidate 9). **Closed 2026-05-28**: both _read_signed_manifest and _write_signed_manifest now fail closed.  Writer: on signing-setup failure, removes the unsigned manifest before returning False (no false authority for +asg uN derivation).  Reader: on signing-setup failure with .sig present, returns '' (manifest treated as absent) rather than falling through to read unverified text.  write_published_manifest signature changed None → bool; both build.py callers (cmd_repo_publish ssh, cmd_repo_repair refresh) now bail with an operator-visible ERROR pointing at `key generate` / `key verify` on False.  5 new tests pinning the fail-closed contract + 1 source-grep test pinning the build.py wiring. |
| STA-22 | P2 | done | **Consolidate `_scan_packages_*` shell helpers.**  Two near-parallel f-string-into-`sudo bash -c "cd {cwd} && {argv} > {tmp}"` patterns: `apt_repo._scan_packages_to` (apt_repo.py:653) and `repo_audit._scan_packages_with_progress` (repo_audit.py:167).  Different tempfile/ownership/error/allow-empty contracts mean a refactor needs a unifying interface covering both.  Also `apt_repo._scan_sources_to`, `_write_subdir_release` follow the same shell-string-with-f-string-interpolation pattern.  Callers are all internal (paths from `config.deb_dir_for`) so injection risk is low, but the duplication is real and the cwd-via-`bash -c "cd"` choreography is fragile.  Identified by the 2026-05-28 consolidation audit (Candidate 7).  Defer until two-or-more change pressure exists on either side. **Closed 2026-05-28**: new apt_repo._run_dpkg_scan is the single shared subprocess helper (tempfile-then-install pattern, handles the root-owned-file truncate case that the pre-consolidation `_scan_packages_to` direct-shell-redirect couldn't).  Three consumers — apt_repo._scan_packages_to, apt_repo._scan_sources_to, repo_audit._scan_packages_with_progress — all became thin wrappers.  5 dead 'Reserved' params (count_dir, include_udeb, count_extensions, use_shell + ProgressBar shape) stripped from the back-compat wrapper.  3 anti-regression tests: assert apt_repo._run_dpkg_scan exists + is delegated to; assert no shell-string subprocess pattern remains in either scanner; assert allow_empty contract holds. |
| STA-23 | P1 | done | **Hardening gates from the 2026-06-11 e2fsprogs incident + the up-front ISO container gate.**  Four gates, each closing a hole an actual run fell through.  In plain English: a superseded broken `.deb` got silently installed while its fix sat beside it; a stale apt index got published verbatim and then *pinned* as authoritative; a routine publish deleted 17 files from a remote pool that is supposed to be append-only; and the ISO build only told the operator the build container was missing after ~10 minutes of pool staging.  **(1) Chroot pre-flight repo audit BLOCKS on stale repo artifacts** (`01e52ac`): the 16:27 disk build printed 'Repo audit OK' and then installed the superseded e2fsprogs `+asg1u1` (broken Pre-Depends) while the fixed `+asg1u2` sat beside it — the pre-flight never looked at stale files, and `find_matching_artifact` accepts *any* `+asg`-stamped variant of the predicted pristine name, so version-drift artifacts are silently consumable.  `_preflight_audit_repo` now runs `_scan_stale_files` (when `dep_check_ready`) and counts version-drift / orphan-source / malformed artifacts as gate risks alongside unresolved deps / conflicts / hash mismatches; version-drift files list first with the silent-consumption explanation and the `repo repair cleanup` remedy; the proceed-anyway prompt and no-gate bypass still apply.  **(2) `mirror publish` re-indexes when the local InRelease is STALE, not only missing** (`c05be23`): publish trusted any existing local InRelease, pushed `dists/` verbatim, and coord-head PINNED the stale index sha — a Jun-9 Packages index kept serving the broken e2fsprogs `+asg1u1` metadata while pool + claims correctly carried `+asg1u2`, and every downstream check (mirror audit's apt index chain) passed on self-consistent stale data.  Publish now walks `dists/<codename>/` comparing pool artifacts (and their parent dirs, to catch deletion-only changes) against the local InRelease mtime and re-indexes via the existing auto-index path when anything is newer; index-only dirs excluded (temp-write + `mv` bumps the dir mtime on every publish).  Remedy for an already-published stale index: re-run `mirror publish` — a zero-pending publish still pushes the dist tree and re-signs coord-head with the fresh pin.  **(3) dist-tree push protects pool `.deb`/`.udeb` from receiver-side deletion** (`b356e56`): `push_dist_tree` rsyncs `dists/<codename>/` with `--delete`, but since CONF-01 Stage D the pool lives INSIDE that tree — a local prune of superseded files propagated to the remote and deleted 17 obsolete/deprecated `.deb`s from the append-only pool.  Pool artifacts are now protected via rsync filter rules; `--delete` still reaps stale index files.  Remote pruning stays an explicit operator action (UPD-01 publish-before-prune), never a push side effect.  Same commit fixed `_mirror_audit_disk_vs_claims` false-CRITICALs: marker claims (retracted/deprecated/obsolete) now fold their `*_seq` back-refs so legitimately-pruned files aren't `missing_on_disk` and retained bytes under marker claims are prune candidates, not orphans; live-claim files missing on disk still CRITICAL.  **(4) `iso build live\|installer` gates on the build container up front** (`b78f53c`): `grub-mkrescue` — the final mastering step — runs in-container, but the only check sat at the end of the flow; the operator paid the full ~10-minute pool staging (950 MB copy, repo metadata, Release signing) before 'requires the build container' surfaced.  Both ISO commands now refuse immediately with 'Run container init first' when `self.container` is None; the late checks stay as backstops.  Tests pin all four gates.  Open follow-ups filed from the same post-mortem: STA-24 (dep-removal drift WARN), STA-25 (cleanup vs live remote claims) in TODO.md.  Filed + fixed in the same session — straight to done.md. |
| STA-38 | P2 | done | **Stale-artifact machinery missed `main-udeb` (our built udebs); `malformed` bucket structurally dead.**  `_scan_stale_files` walked `utils._REPO_SUBDIRS = (main,doc,dbgsym,tests)` — missing `main-udeb` (main/debian-installer/), where our built udebs live — so a superseded `+asgNuN` udeb there was invisible to `repo repair cleanup`, `repo audit`, AND `_preflight_audit_repo`'s chroot stale gate.  Hit live 2026-06-13: after the e2fsprogs + debian-archive-keyring rebuilds, `e2fsprogs-udeb` / `debian-archive-keyring-udeb` `+asg1u1` survived `repo repair cleanup force` and had to be removed by hand.  Separately the `_malformed` bucket was declared/returned but never appended-to since the Stage-E index-walk rewrite (truncated .debs invisible).  **Fixed 2026-06-13** (*`<this commit>`*): (1) new `utils._STALE_SCAN_SUBDIRS = (main, main-udeb, doc, dbgsym, tests)` single canon — `all_deb_dirs()` (strip) and `_scan_stale_files` (cleanup/gate) both derive from it so they can't drift apart; `all_deb_dirs()` output is byte-identical to before (main-udeb was already there).  (2) malformed recovered by diffing on-disk `.deb`/`.udeb` per scanned subdir against the index filenames.  (3) deletion now resolves the dir from the scanned `_sub` label via `deb_dir_for` (the old `deb_dest_for_filename(_f)` defaulted `component='main'` and mis-routed a non-free-firmware .deb to main/ → silent delete failure) and drops the orphaned `.verified` sidecar with each binary.  **Deliberately scoped OUT**: the non-main components (contrib / non-free / non-free-firmware) hold PRISTINE TUNNELED binaries the maintenance walks must not touch — and the stale classifier can't predict tunneled filenames anyway (it would KEEP both current and superseded tunneled copies), so adding them is both unsafe-by-design and ineffective; tunnel-aware stale detection is a separate problem if ever needed.  Tests: `test_scan_stale_files_covers_main_udeb_and_recovers_malformed` (functional — superseded udeb → drift, unscannable file → malformed), `test_cmd_package_cleanup_deletes_via_subdir_label_and_drops_sidecar` (deletion-path pin).  The two existing `all_deb_dirs`-excludes-tunneled tests still pass.  Suite 1090/1090, ruff + mypy clean. |
| STA-54 | P2 | done | **Stale scan kept superseded versions of UNSELECTED production siblings forever — single-snapshot violation + unbounded pool growth.**  `_scan_stale_files` applied "highest-version-wins within a `(name, pristine-base, arch)` group" ONLY to EXPECTED keys (selected, predicted binaries); for an unselected production sibling (a binary a selected source emits but no install closure selects — e.g. e2fsprogs's `comerr-dev` / `ss-dev` / `libext2fs-dev` / `e2fsck-static` / `fuse2fs`, all `-dev`/static variants) it fell to a blanket per-file KEEP that never deduped versions.  Surfaced 2026-06-13: after the e2fsprogs `+asg1u2` rebuild, the five `+asg1u1` siblings lingered, `repo audit` correctly (by the old design) reported "stale files clean", and the operator asked why — they're not in pkg.list/pool.list/selection, so they were classed production-sibling-KEEP.  But keeping a superseded copy violates the local single-snapshot invariant (UPD-01: local = one version per package) and accumulates without bound — and since the installer ISO stages the full repo into `/cdrom/pool`, both versions ride onto the media.  **Fixed 2026-06-13** (*`<this commit>`*): the highest-version-wins prune now runs for EVERY parseable pristine-base group BEFORE the per-selection branch — any lower version is drift, selected or not; only the highest proceeds to the expected/orphan/sibling classification (so the CURRENT sibling is still KEPT).  Unparseable filenames (`_file_key` None) skip the version compare and fall through unchanged.  Safe to delete locally: the superseded version is already on the additive remote (publish-before-prune), the same rationale already used for expected-key drift.  After this, `repo audit` flags the 5 e2fsprogs `+asg1u1` as drift and `repo repair cleanup force` removes them.  Test: `test_scan_stale_files_prunes_superseded_unselected_sibling` (lower → drift, current → KEEP, no false orphan).  Suite 1091/1091, ruff + mypy clean.  **Follow-up same session — orphaned `.verified` sidecar sweep:** the cleanup of those siblings surfaced 15 orphaned `.verified` sha-cache sidecars (binary gone, cache left behind) from removal paths that predate the STA-38 per-deletion sidecar drop — source-build output replacement (`+asg1u1`→`+asg1u2`), pre-STA-38 cleanups, and the reportbug-stack deprecation.  New read-only `_scan_orphaned_sidecars()` (filesystem consistency, needs no dep tree, so deliberately NOT part of the chroot gate — orphan sidecars are harmless and must not block a build) finds every `.verified` whose `.deb`/`.udeb` is absent over `_STALE_SCAN_SUBDIRS`; `repo repair cleanup` reports + sweeps them (live sidecars whose binary still exists are preserved), and `repo audit` shows a non-gating "orphan sidecars" row.  Tests: `test_scan_orphaned_sidecars_detects_and_cleanup_sweeps` (detect across main + udeb, preserve live, full sweep), `test_cmd_package_cleanup_sweeps_orphan_sidecars_source_pin`.  Suite 1093/1093. |
| STA-39 | P2 | done | **Surface-build commands gate on persisted flags but dereference session-only state — fresh-session crash AFTER the sudo prompt.**  `source_build_ready`/`chroot_installer_ready` persist across processes; `self.cache`/`self.dep_tree` are session-only and nothing auto-restores them.  `chroot build live` and `chroot build disk` passed their only gate, constructed BuildSystem (collects the sudo password, may prompt to wipe the chroot), then crashed `AttributeError` on `self.cache.required` — *before* the try/finally, so `scrub_password()` never ran; the repo-audit pre-flight also ran with the stale-file gate blind ("run `cache parse` to enable").  `iso build installer` was worse: its SELECT-LOCK coherence refusal was wrapped in `if self.dep_tree is not None:` — the gate silently vanished in exactly the fresh-session case it exists for, then crashed after sudo collection.  `chroot build installer` gated `dep_check_ready` correctly — the other three now match.  **Fixed 2026-06-13** (*`f82951b`*): all three commands refuse up front with "Run 'cache parse' first" — `dep_check_ready` is in-memory-only, set exclusively by a successful `cache parse` this session, which also populates `self.cache`/`self.dep_tree`, so the gate both prevents the crash and arms the stale-file + SELECT-LOCK checks.  Pinned by `test_surface_builds_gate_on_dep_check_ready`.  Filed 2026-06-12 by the full-tree review; hit live 2026-06-13 (operator's fresh-session `chroot build live` reached the proceed prompt with the stale gate blind — "shouldn't it just be gated to cache parse"). |

| STA-27 | P0 | done | **Publish-time installability gate was dead code — an indentation bug made the refusal unreachable.**  `coord/publish.py` remote_publish: the three-way branch computed `_breaks` (unresolved hard `Depends` a publish would leave on the mirror), but the `if _breaks: … return False, "mirror_closure_break…"` refusal was indented INSIDE the no-pending `else:` branch — where `_breaks` is unconditionally `[]` — so it could never fire.  The real path (`elif _pending:`, which production always reaches since `cmd_mirror_publish` wires `install_corpus`) computed the breaks via `find_publish_closure_breaks` and then silently discarded them; control fell through to `fill_sizes_from_pool` and the publish proceeded.  The docstring's "the mirror's installability invariant is not negotiable" was not implemented.  It shipped because the tests exercised `find_publish_closure_breaks` in isolation, never the gate inside `remote_publish`.  Impact: a `mirror publish` that would leave the mirror non-installable (a pkg whose hard Depends isn't satisfiable) was accepted onto the append-only remote and into the installer ISO's `/cdrom/pool` — the e2fsprogs class of failure (unsatisfiable dep reaching a shipped surface), discovered downstream at install/boot time with no pointer to the publish.  Partial safety net: `repo audit`'s own dep gate catches it IF run manually; what was gone is the automatic publish-time enforcement.  **Fixed 2026-06-13** (*`<this commit>`*): (1) dedent the `if _breaks:` refusal to after the if/elif/else chain so it acts on the computed breaks; (2) companion fix in `mirror.py:project_post_publish_state` — remote-claim satisfier stubs were keyed by `claim['package']` (the SOURCE name) into `RepoState.packages` (keyed by BINARY name), so once the gate is live a remote-only binary (firmware-atheros from firmware-nonfree) couldn't satisfy a dep → false `mirror_closure_break` in a multi-builder federation; now keyed by the binary name from `claim['filename']` (`rsplit('_', 2)[0]`, the same parse `detect_hash_conflicts` uses).  Both land together so the gate is armed AND correct.  Test: `test_remote_publish_refuses_on_closure_break` drives the real `remote_publish` end-to-end (with `write_coord_head` mocked so the bug-case would otherwise reach a clean success) — verified it FAILS with the bug re-introduced and PASSES with the fix.  Suite 1094/1094, ruff + mypy clean. |

| STA-28 | P0 | done | **`push_dist_tree` silently overwrote frozen remote pool bytes — rsync protect filters stop deletion, not transfer.**  `coord/transport.py` push_dist_tree rsyncs the whole `dists/<codename>/` subtree, which since CONF-01 Stage D contains the pool `.deb`s INSIDE it.  The `--filter=P *.deb` rules (added STA-23 / `b356e56`) protect pool files from `--delete` (receiver-side DELETION) but rsync `-aH` still TRANSFERS/overwrites any `.deb` whose size/mtime differs — so a local-ahead rebuild (same filename, new bytes) silently rewrote the FROZEN remote bytes on every publish, no reclaim claim emitted, the live claim still pinning the old sha.  This bypassed the entire RECLAIM-01 frozen-filename invariant (`push_single_deb` carefully uses `--ignore-existing`; the dist-tree pass undid it) and MASKED STA-47.  **Fixed 2026-06-13** (*`<this commit>`*): replaced the two `--filter=P` protect rules with `--exclude=*.deb` / `--exclude=*.udeb` — pool artifacts are removed from the transfer set entirely, so this pass neither OVERWRITES nor DELETES them (excluded files are also protected from `--delete`), and `--delete` still reaps stale index files + removed-component dirs.  Pool bytes are pushed solely by `push_single_deb` (additive `--ignore-existing`, or `overwrite=True` for a sanctioned reclaim); pruning stays an explicit operator action (UPD-01).  Test `test_push_dist_tree_excludes_pool_artifacts` (renamed from `_protects_..._from_delete`) pins `--exclude` present + `--filter=P` gone.  Suite 1095/1095, ruff + mypy clean. |
| STA-47 | P2 | done | **Ownership-filter takeover/re-claim paths never compared shas — could publish a live claim whose bytes the pool doesn't serve.**  `coord/publish.py` filter_pending_by_ownership KEEPs a same-filename candidate when the owner is tunneled or is us, with NO sha comparison.  Builds aren't bit-reproducible, so a KEPT candidate can carry a new sha; then Step 5b's `push_single_deb(overwrite=False)` → `--ignore-existing` → SKIPS the transfer (remote keeps old bytes) while the new live claim pins the new sha → `claim_apt_sha_mismatch` CRITICAL in `mirror audit` + delete-on-mismatch in every peer's pull.  Was MASKED by STA-28 (the dist-tree rsync illegally shipped the bytes); fixing STA-28 makes this the live path, so they landed together.  **Fixed 2026-06-13** (*`<this commit>`*): when an existing owner record is present (the remote pool already holds frozen bytes under this filename) and our candidate's `sha256` differs from the owner record's `sha256` and the candidate is not a reclaim (`reclaims_seq`), BLOCK with `frozen_bytes_blocked` — "bytes differ from the frozen pool copy (sha X≠Y) — use `mirror reclaim` for a deliberate version-less change, or bump the version".  Guard runs before the keep cases; a higher-version transfer has a NEW filename (no existing owner) so it's unaffected; matching bytes (reproducible rebuild) pass through.  The `_ownership_blocked` log line now uses each finding's `reason` (was hardcoded "not strictly higher").  The 6 existing ownership-matrix tests were using owner shas that incidentally differed from the candidate default — matched them (bytes unchanged) to isolate the matrix logic; new `test_filter_pending_by_ownership_blocks_frozen_byte_mismatch` covers owner-is-us + tunneled mismatch, reclaim exemption, and matching-bytes pass-through.  Suite 1095/1095, ruff + mypy clean. |

| STA-29 | P1 | done | **Claim-supersession fold missing at four consumer sites — any reclaim poisoned `mirror pull` and parts of `mirror audit`; centralised the fold.**  The dead-seq fold (collect `retracts_seq`/`deprecates_seq`/`obsoletes_seq`/`reclaims_seq`, then treat a claim as folded if its state is a presence-skip marker OR its seq is a back-ref target) was correctly implemented at several sites but MISSING at four consumers that filtered only by marker STATE: (1) `cmd_mirror_pull`'s claim walk — after a peer's reclaim the OLD published claim (state 'published', old sha) is superseded by the new reclaim claim under the same filename; the walk processed the old claim first, downloaded the REWRITTEN remote bytes, sha-mismatched the old claim, UNLINKED the file + returned False — every fresh pull permanently failed (and it re-imported deprecated/obsoleted files via their original published lines); (2) the cross-mirror pool-SHA section of `cmd_mirror_audit` — one mirror's jsonl contributes both the old and new claim for a reclaimed filename → permanent false `cross_mirror_pool_drift` CRITICAL with ≥2 mirrors, contradicting the per-mirror `detect_hash_conflicts` (which DOES fold) on identical data; (3) `_mirror_summary_we_own_line` — the superseded original counted as live (`we_own` showed "5 (1 obsolete)" when 4 were live); (4) `cmd_mirror_query` — superseded originals shown as current claims.  Found by two independent reviewers; root cause was the fold being hand-rolled 6+ times.  **Fixed 2026-06-13** (*`<this commit>`*): new canonical `coord.schema.superseded_seqs(claims) -> set[int]` + `coord.schema.is_superseded_claim(claim, dead_seqs) -> bool` + `SUPERSESSION_BACKREF_FIELDS`, in the leaf module everyone imports.  The four broken sites now apply it; the existing correct sites were refactored onto it too (`mirror._superseded_seqs` delegates; `_mirror_audit_disk_vs_claims`, `_mirror_recompute_base`, `reconcile.detect_hash_conflicts` use it) so "fold at every supersession site" is a single grep-able call.  (The two `coord.store` folds keep their single-pass-with-post-filter structure — intertwined with the (pkg,ver) ownership dedup, load-bearing, left correct-as-is.)  Bonus: dropped the dead `import signing` / `del signing` parity line in `_mirror_recompute_base` (HK-07).  Tests: `test_schema_superseded_seqs_and_is_superseded_claim` (unit), `test_cmd_mirror_pull_folds_superseded_old_claim` (functional — verified it fails with the fold reverted to marker-only).  Suite 1097/1097, ruff + mypy clean. |

| STA-30 | P1 | done | **Publish transaction / federation-authority hardening — four unimplemented defenses.**  All in `coord/publish.py` `remote_publish` (+ `transport.remote_flock_acquire`), each a documented-but-unenforced safeguard.  **(a) Stale-local-jsonl guard:** Step 6 assigned new seqs from `max_seq(LOCAL jsonl)` and Step 7 wholesale-replaces the remote jsonl — a wiped/restored working dir (same BUILDER_ID, empty local file) re-seqs from 1 and the push ERASES the remote's append-only history (+ its retraction/obsolescence/reclaim lineage).  The signed coord-head documents the defense but nothing enforced it.  Now (Step 3a) refuses BEFORE any push when `max_seq(local) < max(our remote claims' seq, head.last_seqs[us])`.  **(b) Verify-FAILED head treated as bootstrap:** `read_coord_head` returns None for BOTH "no head" (legit bootstrap) and "present but GPG verify failed / sig missing / homedir broken"; `_is_bootstrap = (head is None)` then skipped the federation gate AND rebuilt the head from our local view, silently dropping the remote's `revoked_builders` + every peer's `last_seqs` — the authority rollback the signed head exists to defeat.  Now refuses when `read_coord_head` is None but `coord_head_path` exists on disk.  **(c) Remote flock never confirmed:** `remote_flock_acquire` returned the Popen immediately; no caller read an acquisition token or rc, so publish raced ahead while `flock -w` was still WAITING and proceeded UNLOCKED after a timeout — the lock was decorative.  Now the flock'd shell echoes `COORD_LOCK_ACQUIRED` BEFORE its blocking `cat`, and the helper blocks (select, bounded by `flock -w` + margin) reading that token; on timeout/busy it terminates the session and returns None (caller already refuses on None).  **(d) Failed append burns a seq:** Step 6/6b/6c (and `local_publish`) did `_seq += 1` BEFORE the try, so a transient OSError consumed a seq that never landed → a permanent `sidecar_seq_gap` CRITICAL in every future `mirror audit`.  Now the counter advances ONLY on a successful append (the dropped claim's number is reused by the next), keeping seqs contiguous.  **Fixed 2026-06-13** (*`<this commit>`*).  Tests: `test_remote_flock_acquire_confirms_lock_token`, `test_remote_publish_refuses_verify_failed_head`, `test_remote_publish_refuses_stale_local_jsonl`, `test_local_publish_no_seq_gap_on_append_failure` — a/b/d verified to fail with their fix reverted (the seq-gap one shows `[1, 3]`).  Suite 1101/1101, ruff + mypy clean. |

| STA-31 | P1 | done | **Kernel/initrd selection was lexicographic `sorted(...)[-1]` at six sites; disk image paired kernel/initrd from two independent sorts.**  A plain sorted-last over kernel artifacts orders LEXICALLY, so `6.1.0-9` sorts ABOVE `6.1.0-47` (`'9' > '4'`) and `6.9` above `6.10` — picking the OLDER kernel whenever two ABIs co-reside (a stale pre-rollback `.deb`, a kernel refresh).  Sites: iso.py (live ISO boot kernel + initrd), disk_image.py (disk grub.cfg — vmlinuz AND initrd each sorted INDEPENDENTLY, so a missing/extra initrd could pair a kernel with a DIFFERENT ABI's initrd → unbootable menuentry), iso_installer.py Strategy 1 (chroot vmlinuz) + Strategy 2 (linux-image `.deb` highest-ABI fallback — whose docstring falsely claimed "numeric padding makes sort-on-name fine"; Debian ABIs are NOT zero-padded), cmd_build.py (the cache prediction feeding `expected_kernel_pkg`, and the verify display).  The standing feedback memo ("`sorted(vmlinuz_list)[-1]` picks WRONG kernel; pair initrd by suffix") had never been applied.  **Fixed 2026-06-13** (*`<this commit>`*): new `utils.kernel_abi_of()` (extract the ABI from a vmlinuz-/initrd.img-/linux-image- artifact, package, or `.deb` filename), `utils.latest_kernel_name()` (version-aware max via `apt_pkg.version_compare`), and `utils.select_latest_kernel(boot_dir)` (returns the `(vmlinuz, initrd)` pair with the initrd PAIRED to the chosen kernel by exact suffix; None — a HARD failure callers must honour — if no vmlinuz or the matching initrd is absent, never a mismatched pair).  All six sites use them.  Test `test_select_latest_kernel_version_aware_and_paired` pins 47>9, 6.10>6.9, `.deb` ABI extraction, suffix-pairing, and the missing-initrd / no-vmlinuz hard-failure.  Suite 1102/1102, ruff + mypy clean. |

| STA-33 | P1 | done | **`Source.build_depends` mutated the process-global `apt_pkg.config['APT::Build-Profiles']` from concurrent build workers.**  package.py: `build_depends` set the global then read it via `parse_src_depends`; under COMP-03 parallel builds with per-package profile overrides (ARCH-16) worker A's set could be overwritten by worker B before A's parse ran, filtering A's `<!nocheck>`-style build-deps under B's profiles → a silently wrong build-dep install set (nondeterministic, same failure class as the provider-expansion bug).  **Fixed 2026-06-13** (*`<this commit>`*): a module-level `_BUILD_PROFILES_LOCK` now serialises the global-set + every `parse_src_depends` that reads it (parse is microseconds; uncontended in single-build runs).  Test `test_sta33_build_depends_serialises_apt_pkg_profile_global`. |
| STA-34 | P1 | done | **`autorun` in build mode could never complete — final step passed the non-existent subset token `'build'`.**  cmd_run.py: `lambda: self.cmd_source_build('build')`, but `'build'` isn't in `_SOURCE_SUBSETS` (`pkg/live/installer/recommended/all/indl`), so it was classified as a package NAME → "Unknown package: build" → `source_build_ready` never set → `_run_autorun_steps` aborted at the last stage (also the route taken by bare `autorun` in build mode).  Shipped because the dispatch test stubs `cmd_auto_run_build`.  **Fixed 2026-06-13** (*`<this commit>`*): the step now references `self.cmd_source_build` bare (no-arg → the `pkg` subset, relabelled `indl` in build mode).  Test `test_sta34_autorun_build_calls_source_build_bare_not_invalid_token`. |
| STA-35 | P1 | done | **Standalone `source tunnel` skipped the asg-lineage stamp — the ledger was never loaded outside `source build`.**  cmd_tunnel.py `_do_tunnel` reads `self.container.asg_ledger`, which only `cmd_source_build` populates (`asg_ledger = published_ledger(config)`); on the standalone path (`self.container is None`) `_ledger` was None, so a delta source tunneled with strip-only normalisation and NO `+asg<R>u<N>` stamp — regressing a published `+asgRuN` binary to its pristine name (violating the lineage-continuation invariant).  **Fixed 2026-06-13** (*`<this commit>`*): when no container ledger is present, `_do_tunnel` loads `repo_audit.published_ledger(self.config)` directly (memoised on the session — `published_ledger` reads the signed manifest, so once per command).  Test `test_sta35_standalone_tunnel_loads_published_ledger`. |
| STA-36 | P1 | done | **`mirror add` confirmation could not be declined — answering "n" registered the mirror anyway.**  cmd_mirror.py: `if not _resp:` — but a YESNO `get_response()` returns `'y'/'n'/'yes'/'no'`, ALL truthy, so only a missing TUI backend (empty string) aborted; an explicit "no" fell through to state-file writes, peer registration, and `reconcile-neighbours` pushing federation membership to every peer.  **Fixed 2026-06-13** (*`<this commit>`*): `if _resp.lower() not in ('y', 'yes')` (matches every other YESNO site).  Test `test_sta36_mirror_add_confirmation_declines_on_no`.  Suite 1106/1106, ruff + mypy clean (STA-33/34/35/36). |

| STA-32 | P1 | done | **docker-py failure handling — except clauses one class too narrow, and the orphan reap ignored its own owner label.**  Three defects in buildcontainer.py.  **(a) connect paths catch only `APIError`:** `DockerClient()` / `from_env()` against an unreachable daemon raise the BASE `docker.errors.DockerException` (the server-version probe wraps the requests ConnectionError), and `APIError` is a *subclass* — so `except APIError` missed it.  The documented external-server→local fallback never ran, and the most common failure (daemon not running) escaped `__init__` as an unhandled `DockerException` past `cmd_init_container`'s `RuntimeError`-only handler → raw traceback.  Now both connect blocks catch `_DOCKER_CONNECT_ERRORS` (`DockerException` + requests transients); `cmd_init_container` also catches the base as a belt for a later image-build failure.  **(b) recovery reload escapes the keep-polling loop:** `_wait_for_exit`'s `container.reload()` was guarded only for `APIError`, but during the same daemon hiccup that timed out `wait()` it issues another HTTP GET that raises a requests Timeout/ConnectionError — which escaped `_wait_for_exit`, and `build()`'s `except APIError` didn't catch it either → a still-running multi-hour build (linux/libreoffice/webkit) recorded as failed, the exact spurious-failure class the 1800 s timeout + keep-polling was built to kill.  Now the reload guard (and the OOM-probe reload + grub-mkrescue's post-exit `logs()` reads) catch `_DOCKER_RELOAD_ERRORS` (APIError + transients).  **(c) startup orphan reap force-removed ALL `com.athena.build=1` containers,** ignoring the `com.athena.pid` owner label it declares for exactly this purpose — so a second session (or an `--api` instance alongside an interactive one) killed the first session's in-flight builds, which then recorded as failed.  Now reaps only containers whose owner PID is GONE (`_pid_alive` signal-0 probe) — never one a live concurrent Athena process owns; our-own-pid / unlabeled containers (can only be crash leftovers) stay reapable.  **Fixed 2026-06-13** (*`<this commit>`*).  Tests: `test_sta32_wait_for_exit_tolerates_transient_reload` (verified it fails with the guard reverted to APIError-only), `test_sta32_pid_alive_and_connect_error_tuples`, `test_sta32_connect_and_reap_use_widened_handling`.  Suite 1109/1109, ruff + mypy clean. |

| STA-24 | P1 | done | **Dep-drift audit: a built `.deb` that REMOVES a dependency on a selected package now WARNs.**  `dep_drift.py:_check_dep_drift` compared each selected package's deps on the upstream cache record vs the built `.deb`, logged any difference at INFO, then OVERWROTE the dep-tree with the on-disk values — so a LOST dependency (cache had it, the built binary doesn't) was recorded only at INFO and then synced away, invisible to `_verify_dep_resolution` (which runs on the post-sync deps).  When the lost dep's target is itself a selected package, the minimal SURFACES-01 closure doesn't pull it in → it silently isn't installed → boot/runtime failure far from the cause (the e2fsprogs `+asg1u1` binaries lost `libext2fs2`/`libcom-err2`/`libss2` when our patch dropped `-L shlibs.local` → root fsck exec-fail 127 → boot loop, 2026-06-11; the legacy ship-everything pool had masked it for weeks).  **Fixed 2026-06-13** (*`<this commit>`*): new `_dep_target_names()` extracts dep NAMES across all four fields (ignoring version constraints, so an NMU-strip rewriting `(= …)` → `(>= …)` is NOT a false removal); `_check_dep_drift` diffs cache-vs-disk names BEFORE the sync and, for any name removed whose target is in `selected_pkgs`, emits a prominent WARNING on both the log and console (loud, not a hard gate — a fork may intentionally drop a dep — but never silent).  Tests: `test_sta24_dep_target_names_extracts_all_fields_names_only`, `test_sta24_check_dep_drift_warns_on_lost_selected_dep` (warns on a lost selected dep; stays silent on a constraint-only change).  Companion `docs/build-quirks.md` note records that the e2fsprogs shlibs workaround stays required until the base moves to trixie/dpkg-dev 1.22.x.  Suite 1111/1111, ruff + mypy clean. |
| STA-26 | P2 | done | **Initramfs ships no fsck tools — chroot fstab is empty at `update-initramfs` time.**  The generated initramfs carried no `fsck.ext4`/`fsck.vfat`: `initramfs-tools` decides which fsck binaries to copy by reading `/etc/fstab`, but the kernel postinst runs `update-initramfs` during the chroot build when fstab holds only the virtual filesystems (`chroot.py` deliberately leaves real block devices to the image stage) — so the fsck hook copied none.  The shipped disk image then couldn't check/repair its root fs from the initramfs (it fell through to `systemd-fsck` on the already-mounted root); benign for healthy filesystems, a lost diagnostic layer when the root needs repair (noticed during the 2026-06-11 disk-image reboot-loop debugging — not the cause).  **Fixed 2026-06-14** (*`<this commit>`*): `disk_image.build_disk_image` re-runs `update-initramfs -u -k all` inside the image chroot right after step 7 writes the real fstab (ext4 root passno 1 + vfat ESP passno 2) and the `/proc /sys /dev` bind-mounts are up — the fsck hook now reads the correct ROOT fstype and pulls `fsck.ext4` in.  (Only `fsck.ext4` — the initramfs mounts just the root fs; the ESP's `fsck.vfat` stays a `systemd-fsck`-from-real-root concern at passno 2, never an initramfs one.)  **Verified on the built qcow2 2026-06-16**: nbd-mount + `lsinitramfs /boot/initrd.img-6.1.0-49-amd64 | grep fsck` → `usr/sbin/fsck.ext4` (was empty pre-fix).  Scoped to the disk surface ONLY (the live ISO boots a squashfs/live-boot root that needs no fsck, so its initramfs isn't bloated); the initrd FILENAME is unchanged so the hand-written grub.cfg still resolves it; non-fatal on failure (the image still boots via systemd-fsck on the real root).  Hoisted the shared `_chroot_env` out of the grub `try` so update-initramfs + both grub-install sites use one definition.  Test `test_disk_image_regenerates_initramfs_for_fsck_tools` (source pin — the privileged disk path can't run in CI): asserts `update-initramfs -u` runs in-chroot AFTER the fstab write and is non-fatal.  `docs/known-issues.md` § Latent entry marked RESOLVED.  Suite 1120/1120, ruff + mypy clean. |

## 2. Conformity to Debian/Ubuntu process — P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| FORK-02 | P3 | done | **Live image shipped `deb http://deb.debian.org/debian bookworm …` reference lines in `/etc/apt/sources.list`.**  The installed/live system's sources.list carried 12 commented upstream `deb` lines — `deb.debian.org` strings (and the upstream base codename `bookworm`) inside a shipped system file, violating "never default to upstream / no Debian residue in shipped artifacts".  Origin: `chroot.py:generate_system_configs` emitted the build's `[Mirror.*]` URLs as a commented "uncomment to get upstream back" convenience block; on a Debian-upstream build those render as literal deb.debian.org lines.  Found in the 2026-06-13 live-ISO residue audit (the SEC-07 leak check itself came back clean).  **Fixed + verified 2026-06-13** (*`cfff0ff`*): the sources.list template is now fully static comment-only (self-contained policy text, no mirror-URL interpolation); provenance lives in build.conf, not on the image.  `test_live_chroot_sources_list_is_self_contained` tightened to forbid any `deb <scheme>://` / `deb {…}` line, active or commented.  **Shipped-artifact verified** in the 06:50 live-ISO rebuild (squashfs `etc/apt/sources.list` = 447 B, 0 deb-URL lines).  Operator chose "strip the example lines". |
| FORK-03 | P2 | done | **Live/installed image shipped Debian's archive trust anchors (`/etc/apt/trusted.gpg.d/debian-archive-*.asc`).**  `debian-archive-keyring` dropped nine `debian-archive-{bullseye,bookworm,trixie}*.asc` into `/etc/apt/trusted.gpg.d/`, which apt consults as GLOBAL trust anchors — so the installed system would trust any archive signed by Debian's keys, a trust surface beyond the self-contained "apt pool = our repo only" policy (our mirror line already pins `[signed-by=athena-archive-keyring.gpg]`, so the Debian anchors are never needed, only a liability).  Found in the 2026-06-13 live-ISO residue audit.  **Fixed + verified 2026-06-13** (*`cfff0ff`*): new `patch/source/debian-archive-keyring/2023.3+deb12u2/9001-drop-debian-trust-anchors.patch` removes the `etc/apt/trusted.gpg.d/*` line from `debian/debian-archive-keyring.install` so dpkg never owns those paths (no ship-then-delete; the package is still installed for apt's Recommends + debootstrap's Depends).  Inert `usr/share/keyrings/*.gpg` KEPT (not auto-trusted; referenced by the `-udeb` + debootstrap by path).  Compat 10 has no `dh_missing`, so the unpackaged generated `.asc` don't fail the build.  Rebuilt as `debian-archive-keyring_2023.3+asg1u2` (asg stamp bumped u1→u2 on the new delta).  **Shipped-artifact verified** in the 06:50 live-ISO rebuild: squashfs `etc/apt/trusted.gpg.d/` carries 0 `debian-archive-*.asc`, `usr/share/keyrings/athena-archive-keyring.gpg` present, mirror line pins `signed-by=…athena-archive-keyring.gpg`, installed keyring = `+asg1u2`.  Operator chose "strip the keys now". |
| CONF-07 | P2 | done | **CycloneDX 1.5 JSON SBOM for every build.**  *(Closed 2026-06-01.)*  New `scripts/sbom.py` walks the deduped union of `dep_tree.selected_srcs` and `udeb_dep_tree.selected_srcs` and emits a CycloneDX 1.5 JSON document with one component per source.  Per-source carries upstream name + version (post-cache, pre-NMU-strip), the PURL identifier `pkg:deb/<base-id>/<name>@<version>`, the .dsc sha256 (from `Source.files`'s Checksums-Sha256 entry) as a CycloneDX `hashes` array, and four `athena:*`-namespaced provenance properties: `patch-count` (int as string), `patch-set-hash` (sha256 over the sorted *.patch files in `patch/source/<name>/<version_no_epoch>/`, reusing `utils.patch_set_hash` — same hash the build container records next to `.result` for change-detection), `patch-files` (CSV list, omitted when count == 0), so an SBOM consumer can distinguish a pristine source from a patched fork without diffing the binaries.  Top-level `metadata.component` is an `operating-system` carrying `name = build_distribution`, `version = build_version`, plus four Athena-namespaced properties — codename, arch, base-id, snapshot-timestamp (read from `BuildContainer.snapshot_ts` when available; falls back to `utils.resolve_snapshot_timestamp`).  Required CycloneDX fields all present + well-typed: `bomFormat`, `specVersion = "1.5"`, `serialNumber = urn:uuid:<v4>`, `version = 1`, `metadata.timestamp = utc iso-8601`, `metadata.component`, `components`.  Components emit in lexical name order so consecutive SBOMs diff cleanly on source changes only.  Operator surface: new `cmd_sbom [path]` in build.py (gated on `dep_check_ready`); without an argument writes to `<dir_image>/<distro>-<version>-<snapshot>-<arch>.cdx.json` so the SBOM ships next to the ISO.  Registered as the `sbom` TUI command alongside `cache` / `source` / `iso` / etc.  **Reused primitives**: `utils.patch_set_hash` (already computes per-source patch hashes for `_refresh_patches`), `utils.version_no_epoch` (Debian filename convention), `utils.resolve_snapshot_timestamp` (config snapshot resolution), `utils.snapshot_iso_tag` (basename naming).  Cross-stage helper, bare 'athena' logger → log tab.  **8 new tests** (`test_sbom_*`): CycloneDX skeleton validity + required fields + metadata properties; lexical-sort determinism; udeb-tree dedup; pristine patch-set-hash = sha256(""); patched patch-set-hash differs + patch-files property emitted; PURL format; empty-path no-op; source-level pin that `register_command('sbom')` lives in main().  699/699 pass; ruff clean.  **Out of scope (future tickets if a downstream needs them)**: SPDX 2.3 / 3.0 emit alongside CycloneDX (same source data, different schema); CONF-06's `reprotest` integration would pair naturally — a reproducible build + an SBOM with stable serialNumber-equivalent is the "this ISO is content-addressable" story; CONF-02's signed Release already covers the trust chain on the apt side. |
| CONF-10 | P2 | done | **Debian-residue audit sweep + durable handling mechanism.**  *(Closed 2026-05-31.)*  **Part 1 — sweep.**  Grep `Debian`, `debian.org`, `discover`, `installation-report`, `reportbug`, `popularity-contest`, `popcon`, `apt-listchanges`, `bug-buddy` across `fork/source/`, `installer/`, `patch/`, `config/`.  920+ raw hits collapsed to 3 actual leakage surfaces after triaging copyright/changelog/control-prose/source-comments/license-text as legitimate retention.  **Real fixes (A1-A3 + tasksel pass):** `fork/source/athena-apt-setup/debian/athena-mirror-setup.templates` had 6 lines of upstream Debian prose ("Some non-free firmware has been made to work with Debian. Though this firmware is not at all a part of Debian, standard Debian tools…") in the contrib / non-free / non-free-firmware prompts — rebranded `Debian` → `@DISTRIBUTION@` (resolves to "Asgard" at build time via the existing token-substitution pipeline).  `fork/source/athena-apt-setup/debian/athena-setup-udeb.templates:96` had a "Debian has two services that provide updates to releases" security prompt — rebranded.  `fork/source/base-files/share/staff-group-for-usr-local` shipped at `/usr/share/staff-group-for-usr-local` referencing `bugs.debian.org/299007` in stale wheezy-era prose — rewrote prose for Asgard generically.  `fork/source/athena-tasksel/Makefile:44` embedded `--center "Debian specific manpage"` in the rendered tasksel(8) page — rebranded.  `fork/source/athena-tasksel/debian/menu` had `title="Debian Task selector"` for the Debian menu system — rebranded.  `fork/source/athena-tasksel/debian/salsa-ci.yml` referenced Debian's Salsa GitLab CI infrastructure we don't use — deleted.  `fork/source/athena-tasksel/tasks/{standard,gnome-desktop}` Description prose namechecked Debian — rewrote distro-agnostic.  **Part 2 — durable mechanism (candidate (a) per the ticket text; also closes AUDIT-01).**  New `scripts/identity_scan.py` (~250 LOC) walks any tree, greps each text file for the 9 IDENTITY_TOKENS with word-boundary regex (so "discoverable" in license text doesn't match `discover`).  Skip-globs filter binaries, .po translations (echo source strings), debian/patches/ (upstream patches we don't own), .git, .pc.  Allow-list at `audit/identity-allowlist` (path-glob + token-name + reason) absorbs legitimate retention.  Three audit stages wired in:  **S1 (fork-content)** — `fork_mirror.generate_fork_mirror` calls `audit_identity(dir_fork_source)` right after `_check_and_invalidate_fork_pkg`; hard-fails the cache build on any unallowlisted hit with a per-finding ERROR log + actionable diagnostic.  **S2 (chroot apt-install audit, replaces the hardcoded strip list)** — new `identity_scan.audit_chroot_hooks` walks `usr/lib/pre-pkgsel.d/`, `usr/lib/finish-install.d/`, `lib/finish-install.d/`, extracts every `apt-install X` invocation (handling `|| true` / `|| :` soft failures, `--flags`, `$logoutput` wrappers, `$VAR` skip), and cross-references named pkgs against the build's pool.  Decision matrix: all pkgs in pool → no action; unpooled + hook in `installer/strip-hooks-allowlist` → `sudo rm -f`; unpooled + `|| true` soft, not allowlisted → WARN, build OK; unpooled + hard apt-install, not allowlisted → FAIL build.  Replaces `installer_chroot._strip_debian_residue_hooks`' rotting hardcoded `_targets = ('20install-hwpackages', '50save-logs')` list with an operator-reviewable allow-list (same two hooks ship as initial entries).  `build_installer_chroot` gains a `pool_pkg_names: Optional[set[str]]` kwarg; `build.py`'s `cmd_build_chroot_installer` computes the union of `canonical_pkgs.keys() | pool_extras_pkg_names` from the deb dep tree and passes it.  **S3 (staged-ISO audit)** — `iso_installer._audit_staged_iso` runs `audit_identity` against the staging tree after all `_stage_*` steps complete, before binary-heavy phases (apt-repo gen, signing, mkrescue) so a residue hit aborts cheaply.  Allowlist resolved by walking up from `dir_image` (typically `<working>/buildroot/image`) looking for `audit/identity-allowlist`; degrades to WARN if no allowlist found (so test fixtures without the file don't break iso build).  **Tests (16 new):** 7 for the scanner itself (token detection, word-boundary, allowlist absorption: `*` wildcard token + specific token + multi-line, skip-binary, fork_mirror gate fires on missing allowlist); 7 for S2 (strip-allowlisted, no-op when no hooks, build-flow call-site pin, hard-fail on hard unpooled, warn on soft `|| true` unpooled, skip when no pool, `_parse_apt_install_line` variants); 4 for S3 (clean tree passes, Debian leak fails, no-allowlist-degrades-to-warn, binary-pool skipped).  Test fixture `_setup_fork_test_tmpdir` writes a permissive `tmp/audit/identity-allowlist` so existing fork_mirror tests stay scoped to fork logic.  3 obsoleted `test_strip_debian_residue_hooks_*` tests replaced by audit-based variants (same intent, new function).  Commits: `b12022e` (S1 + A1-A3 rebrand), `96f6f6c` (S2 chroot audit), `<this-commit>` (S3 staged-ISO audit + ticket close).  681/681 tests pass; ruff clean.  Memory `project_filter_debian_specific_installer_hooks.md` is the principle reference; cousin discipline `project_strip_debian_identity_telemetry_from_shipped_distro` extends to freshly-authored files.  **Follow-up out of scope:** `installer/disk/info` template currently reads "Athena ${version}" — should be "Asgard ${version}" per the three-layer identity model (`project_three_layer_identity`).  Separate concern (Athena/Asgard semantics, not Debian residue); file a new ticket if it bites. |
| CONF-11 | P2 | done | **Fork-tree internal completeness audit at cache-build.**  *(Closed 2026-05-31.)*  Original 2026-05-18 incident: athena-tasksel's `Makefile install` target ran `install packages/list …` but `packages/list` was missing from the fork (never tracked in git, probably deleted in an unrecorded cleanup pass after the initial `apt-get source tasksel` seed); the original successful build's `athena-tasksel*.deb` survived in `repo/` and `check_build` skipped subsequent rebuilds, hiding the problem until step-7 prep deleted the existing artifact to force a fresh build.  Symptom: `install: cannot stat 'packages/list': No such file or directory`.  **Design path 2026-05-31:** original "diff fork against upstream tarball + per-pkg `.upstream-omit` allow-list" shape was rejected by the operator because forks legitimately trim hundreds of files (locales, task descriptions, examples) and the allow-list maintenance cost wiped out the value (athena-tasksel alone would have generated 100+ initial-triage warnings).  Reframed to **fork-internal audit** — grep `Makefile` + `debian/rules` for `install <flags> SOURCE DEST` and `cp [-flags] SOURCE DEST` patterns with concrete (non-variable, non-absolute) source paths, verify each source exists in the fork tree; glob patterns require ≥1 match.  **`debian/<binary>.install` audit was implemented then dropped** because `dh_install` runs AFTER the build target — `.install` references to built artifacts (e.g. choose-mirror's `choose-mirror` C binary, debian/port_architecture from a build hook) produce static-time false positives that can't be eliminated without an allow-list (which the operator explicitly rejected).  The original athena-tasksel incident is caught by the Makefile scan anyway; `.install` was net-negative signal.  **Shipped:** `audit_fork_tree(pkg_dir) → list[finding]` in `scripts/fork_mirror.py`, called from `generate_fork_mirror`'s per-pkg loop right after `_check_and_invalidate_fork_pkg`.  Findings logged as WARNING; operator triages.  `_scan_install_sources` / `_scan_cp_sources` helpers walk tokens past flag groups (handles `-m MODE` value-taking flags, `-d`/`-D`/`-t` no-source/target-dir forms, multi-source `install -m 755 SRC1 SRC2 DEST`).  Skips: variable-expanded paths (`$(DESTDIR)`, `$$f`), absolute paths (install destinations), `<` `>` `|` (shell I/O), comment lines (`#…`).  Glob patterns (`etc/*`) checked via `glob.glob`; ≥1 match required.  **Verification: athena-tasksel + simulated regression**: moving `fork/source/athena-tasksel/packages/list` out of the tree → audit fires with `Makefile:43 packages/list (file not found in fork tree)` — exact line, exact path.  Restoring the file → audit clean.  Other 6 forks (athena-apt-setup, athena-branding, athena-installer-data, athena-pkgsel, base-files, choose-mirror) all audit clean as-is.  **10 new tests** (`test_conf11_*`): clean-state, the original athena-tasksel regression replayed, variable-expansion + `install -d` skipped, glob existence and absence cases, comment-line skip, multi-source `install` token walking, `cp` flags + variable filtering, missing-Makefile graceful empty, sanity test against the real `fork/source/` (any future regression here is a real bug).  661/661 tests pass; ruff + mypy clean.  Memory `feedback_verify_patch_layer_via_logs` and `feedback_dh_helper_files_use_binary_name` are the cousin disciplines (don't trust a build-system success without inspecting what it actually did; binary-name dh-helper rule catches a different fork-tree shape problem).  **Follow-up same day**: extended the audit to cover **source-side upstream drift** — sources don't go through the existing collision gate (which fails the build when upstream binary/udeb >= fork), so when upstream source advances past fork's NMU layer the build silently shipped stale fork source.  `cache.py` now records dropped upstream source versions in `_upstream_source_collisions` (parallel to `_upstream_collisions` + `_upstream_udeb_collisions`) and `Cache._audit_fork_source_drift` emits a WARNING per fork-vs-upstream collision (advisory only — build continues, since source drift doesn't immediately break runtime).  Existing log line `[main] dropped source X — superseded by fork` was missing the version; now includes it for parity with binary/udeb messages.  3 new tests pin the tracking, the silent-when-fork-ahead path, and the warning-fires-when-upstream-advances path.  664/664 tests pass. |
| CONF-15 | P1 | done | **Pin Dockerfile toolchain layer to our snapshot, not the live Debian mirror.**  *(Closed 2026-05-29 after three operator-driven iterations.)*  Pre-fix: `config/Dockerfile`'s toolchain `RUN apt-get install` used the base image's stock sources.list (= live `deb.debian.org`); when the live mirror advanced past our snapshot pin, pre-installed library packages ended up newer than what our snapshot/repo shipped.  `dpkg-shlibdeps` inside the build container then read the newer `.shlibs` and embedded minimum-version constraints pointing at versions we don't carry — the 2026-05-23 wpasupplicant-udeb → `libcrypto3-udeb (>= 3.0.20)` false-bind.  Two-layer workaround had shipped (no library `-dev` packages in the Dockerfile + per-build `apt-get -y --allow-downgrades dist-upgrade`); CONF-15 closes the real fix.  **Shape evolved through three iterations:** (i) `2f129f3` initial — wipe sources → rewrite to snapshot → `apt-get install <toolchain>`.  Failed at image build with apt 100 exit + ~15 "Unable to locate" errors covering `debhelper`, `python3`, `dh-python`, `gettext`, `autoconf`, `automake`, `libtool`, `po-debconf`, `diffstat`, `zip`, `python3-{distutils,debian,gpg}`, `apt-file`, `autotools-dev`.  Trailing slash on the snapshot URL ruled out by `9c7a161`.  (ii) `09b1190` — added `-updates` and `-security` suites alongside main (matching the per-build sources.list shape).  Same packages still not found; problem is not missing pockets.  Diagnosed but not root-caused (same TS resolves those names cleanly for per-build `apt-get install <build-dep>` calls inside the already-built image, so the snapshot index DOES carry them — something about FIRST-TIME resolve at image build time misses them).  (iii) `e259e8a` final — **install-then-realign**: step (a) `apt-get install -y <toolchain>` against the LIVE mirror (works, same shape as pre-CONF-15 Dockerfile, every name surfaces).  Step (b) wipes sources, writes the three snapshot sources (main + -updates + -security at same TS, `SECURITY_ARCHIVE_KEY=debian-security` threaded as separate build-arg).  Step (c) `apt-get -y --allow-downgrades dist-upgrade` realigns the installed package set against the snapshot view (Pin-Priority 1001 on `snapshot.debian.org` origin forces the downgrade direction when snapshot < installed; pulls forward when snapshot > installed).  After (a)+(b)+(c) the image's installed set is at-snapshot — CONF-15 invariant holds (no live-mirror `.shlibs` leak), image tag carries snapshot TS so a `[Snapshot] Timestamp` change auto-invalidates the cache, per-build container starts already-aligned.  Library `-dev` packages still kept out of step (a) (their `.shlibs` would otherwise embed live versions even after the realign).  **Operator-observed realign on 20260529T210501Z**: `tzdata 2025b-0+deb12u2 → 2026b-0+deb12u1`, `libsystemd0 / libudev1 ~deb12u1 → ~deb12u2`, `libgnutls30 ~deb12u6 → ~deb12u7` — base image was BEHIND snapshot, dist-upgrade pulled forward.  Original wpasupplicant scenario was the inverse direction; same mechanism.  Tag baked: `athenalinux:build-<release>-<snapshot-ts>`.  `BuildContainer.__init__` passes 5 buildargs (`RELEASE`, `SNAPSHOT_BASEURL`, `ARCHIVE_KEY='debian'`, `SECURITY_ARCHIVE_KEY='debian-security'`, `SNAPSHOT_TS=self.snapshot_ts`); empty `SNAPSHOT_TS` fails the build loudly in step (b)'s guard.  Per-build `apt-get -y --allow-downgrades dist-upgrade` removed from `BuildContainer.build()`'s `cmd_str` (redundant since the image is at-snapshot at build start; comment in `build()` documents what it did and how to revive).  Bonus from `ef81990`: docker.errors.BuildError now caught explicitly + streamed build_log written to `log/docker_build.log` + tail printed inline (was the operator's only signal at iterations (i) and (ii) — the SDK's default APIError message truncates the streamed RUN output).  4 new tests pin the contract: Dockerfile ARG declarations + 3-source rewrite + Pin-Priority 1001 + ordering (install → rewrite → dist-upgrade) + empty-TS guard (`test_conf15_dockerfile_pins_toolchain_to_snapshot`); image tag includes snapshot TS (`test_conf15_buildcontainer_image_tag_carries_snapshot_ts`); 5-arg buildargs pass through (`test_conf15_buildcontainer_buildargs_pass_snapshot_triplet`); per-build dist-upgrade workaround is gone (`test_conf15_per_build_dist_upgrade_workaround_removed`).  640/640 tests pass; ruff + mypy clean.  Closes the last `deb.debian.org` leak in shipped artifacts — `project_self_contained_repo` invariant now holds for the toolchain layer too (the base-image install step still hits live, but its outputs are erased by the realign).  Memory: `feedback_strip_nmu_at_build` documents the broader pristine-upstream invariant CONF-15 brings into compliance; `project_dockerfile_conf15_install_then_realign` is the why-we-didn't-do-the-simpler-shape pin so a future cleanup pass doesn't re-introduce the "Unable to locate" failure mode. |
| CONF-01 | P1 | done | **`cmd_index_repo` + unified repo/ layout migration.**  *(Landed 2026-05-22 as 6 stages — see `docs/plans/conf-01-repo-layout-migration.md`.  **Stage A** lifted 7 apt-repo helpers from `iso_installer.py` to `scripts/apt_repo.py` (pure code motion).  **Stage B** added `cmd_index_repo` + multi-suite `generate_repo_indexes(repo_root, suites_spec, codename_for_suite, ...)` orchestrator.  **Stage C** added `cmd_migrate_repo_layout [dry-run|force]` one-shot migrator: tar-snapshots `repo/` first, then `os.rename` per file from `repo/{main,doc,dbgsym,tests}/` to the apt-conformant nested layout `repo/dists/<codename>{,-debug}/<comp>/binary-<arch>/` (Q1 sources under `main/source/`; Q2 dbgsym in separate `<codename>-debug` suite per Debian convention).  **Stage D** updated every consumer: `utils.BuildConfig` re-targeted `dir_repo_main` et al. to the new nested paths + added `dir_repo_main_udeb`/`dir_repo_main_source`/`deb_dir_for(label)`/`deb_dest_for_filename(filename)`/`all_deb_dirs()` helpers; `buildcontainer._segregate_built_artifacts`, `buildcontainer.verify_pkg_artifact`, `buildsystem` composer, `chroot._get_deb_files`, `dep_drift._sync_pre_install`, `iso_installer.build_installer_iso` (`_stage_pool` + `_select_pool_files` now take list-of-dirs to merge .debs and .udebs into flat /cdrom/pool), `installer_chroot.build_installer_chroot` (`dir_udebs` arg), `repo_audit.scan_repo_state`, `fork_mirror._wipe_fork_pkg_outputs`, and `build.py` cmd_* walkers all switched to `config.all_deb_dirs()` / `deb_dest_for_filename()`.  Four fix-ups landed during operator verification: `repo_audit` cache filename now includes a path hash to prevent stale-cache collisions when paths change; `dep_drift._sync_pre_install` also syncs Version (was asymmetric strip → 144 spurious mismatches); `dep_drift._verify_dep_resolution` now uses the STA-18 `_version_for_constraint_target` helper for virtual aliases; BuildConfig auto-cleans empty pre-Stage-D dirs at init.  **Stage E** swapped `_scan_for_obsoletes`'s DebFile-per-deb walk for `repo_audit.iter_packages_all_versions` (multi-version-aware iterator over the cached dpkg-scanpackages output) — `package cleanup` is order-of-magnitude faster.  **Stage F** is this row + README updates ("Consuming the built repo as an apt source" section with sources.list.d snippet, docs/branding-methodology.md path refresh).  Plus live-ISO grub.cfg got the gated `loadfont`/`insmod all_video`/`gfxmode=auto`/`background_image` block + Asgard splash (parity with installer ISO).  Verified end-to-end on operator-driven test: cache build / dep parse / source build / chroot build live / chroot build installer / iso build live + boot / iso build installer + boot + install — all green.  Tag `working-conf01-verified-2026-05-22` captures the verified state.  470 tests pass, mypy + ruff clean.  Unblocks CONF-02's standalone signing path (signing wired into `repo index` via lifted helpers).)* |
| CONF-04 | P1 | done | Adopt the Debian build-profile vocabulary properly: `BuildProfiles = nodoc, nocheck` in `[Source]` is wired to `DEB_BUILD_OPTIONS` *and* `DEB_BUILD_PROFILES`, but those are different namespaces. `nodoc`/`nocheck` are options; `noudeb`/`stage1`/`stage2`/etc. are profiles. Split into two config keys and validate. |
| CONF-05 | P1 | done | Source patches under `patch/source/<pkg>/<version>/9001-*.patch` work, but the convention has no DEP-3 metadata required by Debian — add a check that every patch has a DEP-3 header (`Description:`, `Origin:`, `Forwarded:`) before it is applied. |
| CONF-02 | P1 | done | Generate and include a project-owned signing key; sign the built `Release` file. Document key rotation.  *(**Phase 1 done 2026-05-09**: `scripts/signing.py` (~210 LOC) wraps `gpg --batch --gen-key` and `gpg --detach-sign`/`--verify` to manage the keypair under `<dir_gnupg>/signing/` (separate gnupg homedir from the verification keyring at `<dir_gnupg>/` — different trust scopes).  Three operator commands: `generate_signing_key [force]` (refuses overwrite without force; PROMPT_YESNO confirmation; RSA-4096 / never-expires), `verify_signing_key` (sign+verify roundtrip against a test payload, prints OK/FAIL plus fingerprint+uid+created+expires), and `print signing` (state snapshot, no roundtrip).  New `[Repo] SigningKeyUid` config field (default `Athena Build <athena@local>`).  Public key exported to `<dir_gnupg>/signing/athena-archive-keyring.gpg` for downstream pickup.  8 unit tests incl. a real-gpg sign+verify integration roundtrip (RSA-2048 for ~3s test speed).  **Phase 3 done 2026-05-09**: `_install_signing_keyring` in `chroot.py` (called from `generate_system_configs`) `sudo cp`s the exported pubkey into the chroot at `/usr/share/keyrings/athena-archive-keyring.gpg` (the conventional Debian distro-signing-key location — Debian itself ships at `debian-archive-keyring.gpg` next to it).  `_verify_chroot` gains a non-gating informational line reporting keyring presence/absence at the verify step.  3 mocked-subprocess tests (skip-on-absent, cp-when-present, warn-on-failure).  **Plus build_chroot now gates on the signing key up front**: new `signing_key_verified` flag in `BuildFlags`; new `_ensure_signing_key_verified()` helper called at the very top of `cmd_build_chroot` (before any sudo/mount/dpkg work); helper runs `signing.verify_key` (real sign+verify roundtrip) and on success sets the flag + prints fingerprint/uid/created/expires; on failure prompts PROMPT_YESNO to generate now and re-verifies, refusing to proceed if operator declines.  Surfaced in `print state` as a stage row.  5 mocked tests cover all four branches (key present, decline, accept-then-success, accept-then-generate-fails) plus the flag's default-False invariant.  **Phase 2 partially shipped 2026-05-13** (commit `42e03ea`): installer ISO `Release` is signed end-to-end via `iso_installer._sign_release_files` (detached `.gpg` + clearsigned `InRelease`); `_export_pubkey_to_staging` ships pubkey at `.disk/archive-key.gpg`; `base-installer` quilt patch (`9001-install-athena-archive-keyring.patch`) copies it to `/target/etc/apt/trusted.gpg.d/` before `apt-cdrom add`.  Verified end-to-end on 2026-05-13 install run (`Hit:1 cdrom://... thor InRelease`).  **CONF-01 unblocked the standalone signing path 2026-05-22**: `repo index` now signs both `dists/<codename>/{Release.gpg, InRelease}` and `dists/<codename>-debug/{Release.gpg, InRelease}` via the lifted helpers in `scripts/apt_repo.py` (see also docs/plans/conf-01-repo-layout-migration.md).  **Sources.list.d wiring still open**: an `/etc/apt/sources.list.d/athena.list` entry with `[signed-by=...]` pointing at the keyring needs a real URL the booted system can reach — defer to COMP-02 (publish_repo) since the URL is meaningless without a publishing endpoint.  Key rotation via `generate_signing_key force` works today; docs/release.md (DOC-04) blocked on CONF-03 landing — CONF-01 dependency now satisfied.)*  **Closed 2026-05-25** (revised 2026-06-02 for MIRROR-01): the final open sub-item (network apt source on the installed system) shipped config-driven.  Original implementation wrote a single `[Repo] AptSourceURL` knob → `/etc/apt/sources.list.d/athena.list`.  Superseded by MIRROR-01 Phase 5 (commit `3949db3`, key retirement in `8cc803b`): `chroot._write_athena_apt_sources` now iterates every `config/mirror.<name>.state` and writes one `/etc/apt/sources.list.d/athena-<name>.list` per configured mirror — `deb [signed-by=/usr/share/keyrings/athena-archive-keyring.gpg] <url> <codename> main`.  Empty enabled-mirror set is a no-op (target keeps relying on the /cdrom/pool source apt-cdrom-added at install time).  Mirrors are registered via `mirror add` — see [`docs/mirror-setup.md`](mirror-setup.md).  Everything else was already done at the 2026-05-25 close: key generate/verify, Release+InRelease signing in both `repo index` (apt_repo.py) and the installer ISO (iso_installer._sign_release_files), keyring install into chroot + target, InRelease verified end-to-end on install. |
| CONF-09 | P1 | wontfix | **(Supplanted 2026-05-20 by commit `1724e9d` — repo subdir layout + post-build NMU strip; the `+thor1` distro-suffix approach was dropped entirely in favour of pristine-upstream + `utils.strip_nmu_from_deb` post-`dpkg-buildpackage`.  The "subset validation + full source rebuild to canonicalise +thor1" plan is no longer the path — every binary now ships at pristine upstream version, provenance lives in the Release file's `Origin: Athena`, and our own future security-update layer goes on top of pristine.  `utils.rebump_deb_file` / `restore_deb_epoch` / `rewrite_intra_thor1_strict_equals` / the `package rebump` command are gone.)** |
| CONF-13 | P1 | done | **(Resolved 2026-05-19 by the upstream-NMU gate)** **Snapshot advance triggers full-corpus rebuild via kernel ABI counter.**  Discovered 2026-05-19 after `Timestamp = latest` re-resolved from 20260514 to 20260518 (a +deb12u14 security update window).  Symptom: 139+ sources flagged "would rebuild" by `source rescan`, including the entire linux + linux-signed-amd64 + grub-efi-amd64-signed family that no amount of rebuilding fixes.  **Root cause:** kernel's `debian/bin/gencontrol.py:_generate_abiname_part` walks the changelog and adds 1 to ABI counter per non-backport entry.  Debian's 6.1.172-1 generates ABI 47; our `_changelog_bump` PREPENDS a synthetic `+thor1` entry → ABI becomes 48-or-49.  Predictor in dep tree uses cache's expected binary names (Debian's ABI 47).  Build produces ABI 49.  Filename mismatch → check_build false → rebuild → still produces ABI 49 → loop.  **Workaround (in use):** pin `[Snapshot] Timestamp = 20260514T083402Z` in build.conf so the predictor stays on the snapshot whose artifacts are already in repo/.  Trade-off: no security updates.  **Real fix candidates:** (a) patch kernel's gencontrol.py to NOT count our `+thor1` prepended entry — detect via Author=`Athena Build <athena@local>` or version-suffix `+thor1` and skip during the changelog walk.  Minimal-touch; needs per-snapshot reapply.  (b) skip the `_changelog_bump` step for linux + linux-signed-amd64 sources specifically — accept that kernel binaries don't carry `+thor1`.  Aligns with the existing CONF-12 problem (kernel already doesn't honor the suffix anyway for module udebs).  (c) special-case the predictor to know "kernel binaries' ABI suffix = cache's ABI + N(thor bumps)" — invasive, fragile.  Lean: (b) — kernel goes through its own version-stamping pipeline; opting out of our prepend matches reality.  Pair with CONF-12's fix to put kernel binaries fully outside the +thor1 ecosystem.  Tracks the broader "version-bump cascade" pain the operator named on 2026-05-19.  **Fix landed 2026-05-19**: `utils.upstream_has_nmu()` + per-source gate in `BuildContainer._changelog_bump` and `utils.apply_distro_suffix`.  DistroSuffix is now only applied to versions that already carry an NMU layer (`+debNuN`, `~bpoN+N`, etc.).  Kernel's pristine `6.1.X-Y` is auto-skipped — no changelog prepend → no ABI cascade → predictor and builder produce identical filenames.  `[Source] NoBumpSources = linux, linux-signed-amd64` retained as belt-and-suspenders against a hypothetical future `+deb12uN` kernel security upload.  DistroSuffix token now derived from `[Build] BASE_ID` (e.g. `asgard` → `asgard1`) when not explicitly set — no more literal `thor1` hardcode.  Source/Package classes carry `nmu_upstream` flag, set during cache parse.  428 tests pass. |
| CONF-12 | P2 | obsolete | **(Subsumed by CONF-13 fix on 2026-05-19)** **Kernel binary version must honor DistroSuffix (+thor1).**  The `linux` and `linux-signed-amd64` source packages produce a subset of their binaries with the changelog-bumped source version (`+thor1`), but other binaries (the kernel image, the module udebs `*-modules-*-amd64-di`, the abi-named `linux-headers-NN-amd64`) get a version OVERRIDE that strips the suffix.  Mechanism: kernel's `debian/rules.real` calls `dh_gencontrol -- $(GENCONTROL_ARGS)` where `GENCONTROL_ARGS=-v<imagebinaryversion>`.  `imagebinaryversion` for the signed source comes from `gencontrol_signed.py:23` (`self.changelog[0].version.complete`) and for module udebs from `rules.gen`'s per-package `MAKE` invocation hardcoding `SOURCEVERSION='6.1.172-1'`.  Both compute upstream-only and miss `+thor1`.  Result: `repo/linux-image-6.1.0-NN-amd64_6.1.172-1_amd64.deb` (no suffix) while the resolver predicts `repo/linux-image-6.1.0-NN-amd64_6.1.172-1+thor1_amd64.deb` → `check_build` fails → eternal rebuild loop after every restart.  **Workaround (in use 2026-05-19):** `package rebump <name>...` retrofits the offending files post-build.  **Proposed real fix:** patch `gencontrol_signed.py:23` to use `self.changelog[0].version.complete` correctly (it already does syntactically — investigate why the result doesn't include +thor1), and patch the `rules.gen` `SOURCEVERSION=` hardcoded value to either read from changelog at make-time or include the suffix.  Latter is invasive (rules.gen is itself generated by gencontrol.py from the per-arch defines).  Confirm by inspecting the .deb's actual control during a fresh build with both sources rebuilt cleanly.  Pin: 6.1.172-1 era kernel; the issue is structural, not version-specific.  Same family as CONF-09 ("retire rebump via full +thor1 source rebuild") — the rebump mechanism survives BECAUSE of cases like this.  **Obsoleted 2026-05-19**: under the new upstream-NMU gate, kernel binaries SHIP at pristine upstream version (no `+thor1` expected), so predictor and builder agree without any version-stamping gymnastics.  The original ticket framed kernel-must-honor-suffix as the goal; the actual fix is the opposite — kernel doesn't need the suffix because cross-source strict-equal refs in `linux-latest` and `linux-signed-amd64` resolve cleanly against pristine versions. |

## 3. Completeness — P1 (toward derivative-distro state)

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| COMP-02 | P1 | done | **Repository publishing** — given CONF-01, expose a `publish_repo` command that copies `repo/` + generated metadata to a configured destination (local dir, S3-compatible bucket, rsync target). Without this the project cannot be *consumed*.  **Originally closed 2026-05-29** with `repo publish ssh|local full|minimal` and `repo summary ssh|local` (commits up to the `5e907cf` predecessor for ssh, `5e907cf` for local, `44da531` for summary): rsync `.debs` additively to the destination, run `dpkg-scanpackages` AT the destination (on the VM for ssh, in-process for local), sign locally, refresh the local manifest.  Shared refactor: `apt_repo._reindex_and_sign_via` closed over `(has_debs, scan_packages)` callables so both transports were thin wrappers.  S3 leg dropped the same day — option A (trust local pool) and option B (trust local manifest via `merge_remote_index`) sacrifice the destination-side verification that ssh+local provide; option C (S3 LIST + GET prior Packages + re-merge) preserves it at sizeable code + ongoing API/egress cost with no concrete consumer asking — defer until one does.  **Superseded 2026-06-02 by MIRROR-01** (Phase 1–5: `be015f0`, `49581d5`, `6e91ae4`, `18d6164`, `b0de8f1`, `3949db3`; deprecation sweep `8cc803b`; cross-ref retirement `d0ec621`): the `repo publish ssh|local` / `repo summary` / `repo external` / `[Repo] AptSourceURL` / `[Repo] ExternalEnabled` / `snapshot.state.{base,published,external}` surfaces were retired.  Publishing now runs through the federation umbrella — `mirror add <name> <url>` registers a per-target durable state file (`config/mirror.<name>.state`); `mirror publish [<name>]` does per-file `.deb` push under remote flock + Ed25519-signed per-claim records + tier-1 GPG-signed `coord-head` re-sign, federation-gated against the `neighbours` set on every existing peer's coord-head, with first-publish bootstrap for fresh remotes; `mirror audit` covers the destination-vs-manifest reconciliation that `repo audit external` previously did, plus claim-sig verification + hash-conflict detection + cross-mirror pool drift; `mirror pull` downloads peer-owned claim .debs missing locally (skip-own; SHA-256 verified); `mirror summary` / `query` / `status` / `list` are the read-only inspection surfaces.  COMP-02's underlying invariant (can-be-consumed) still holds; what was superseded is the command surface, not the deliverable.  See [`docs/mirror-setup.md`](mirror-setup.md) for the live operator flow. |
| COMP-01a | P1 | done | **Installer Phase 1 — file split + dep-tree exclusives**. *(landed 2026-05-10 commit `c17c238`. Split `pkg.list` into `pkg.list` + `live.list` + `installer.list`; BuildConfig grew `--live-list`/`--installer-list` flags; DependencyTree gained `live_exclusive_pkg_names`/`installer_exclusive_pkg_names`/`*_src_names` fields; `cmd_parse_dependency` runs Pass IV (live) + Pass V (installer); `derive_subset_exclusive_src_names()` mirror of `derive_extras_src_names`; new `print live`/`print installer` views; `print selected` annotates per-subset. 7 new tests; 163/163 pass. Known limitation noted inline: linear ordering of Pass IV before Pass V means a package needed by BOTH live and installer credits to live_exclusive only — latent today, revisit when udeb tree lands and the authoritative tracking moves to `udeb_selected_pkgs`.)* |
| COMP-01b | P1 | done | **Installer Phase 2 — cache parses udeb index**. *(landed pre-2026-05-12.  `cache.py` ships `udeb_hashtable` parallel to `package_hashtable`, `udeb_required` / `udeb_important` from udeb Priority tags, per-mirror `dists/<suite>/main/debian-installer/binary-amd64/Packages` fetch.  No new GPG path — same signed Release covers both arms.  Verified by recent install-end-to-end runs through this code path.)* |
| COMP-01c | P1 | done | **Installer Phase 3 — parallel udeb DependencyTree**. *(landed pre-2026-05-12.  `BuildSession.udeb_dep_tree` parallel to `dep_tree`; `cmd_parse_dependency` Pass VI seeds udeb tree from `udeb_required` + `udeb_important` + udeb-arm of installer.list; per-entry dispatch (deb match → Pass V, udeb match → Pass VI, both → both).  `parse_sources` walks both trees for union source corpus.  COMP-02 phase D follow-up extended this with Pass VII for pool.list, commit `68266a2`.)* |
| COMP-01d | P1 | done | **Installer Phase 4 — source build preserves .udeb outputs**. *(landed commit `40a7903` "COMP-01b phase 4: source build subset semantics + udeb tree downloads".  `buildcontainer.build()` keeps `.udeb` alongside `.deb` outputs; repo/ ingestion preserves both; `source build installer` filter ships.)* |
| COMP-01e | P1 | done | **Installer Phase 5 — `chroot build installer` from udeb closure**. *(landed commits `548c52d` "COMP-01b phase 5: chroot build installer from udeb closure" + `651a4ea` "COMP-01b phase 5: three fixes to get chroot build installer end-to-end working".  `installer_chroot.py` ships the udeb-only chroot build path with topo-sorted unpack, rootskel-driven PID-1 init, cdebconf+main-menu autostart, signing-keyring install, etc.)* |
| COMP-01g | P1 | done | **Installer Phase 7 — `iso build installer`**. *(landed commit `2cd13b6` "COMP-01b phase 7: iso build installer + autorun split + installer.list seeds".  `iso_installer.py` ships `build_installer_iso` orchestrator: cpio.gz initrd from buildroot/installer/, signed apt-repo via `_generate_apt_repo` + `_sign_release_files`, base_include + pool whitelist accounting, hybrid BIOS+EFI grub-mkrescue.  COMP-02 phase A-D follow-ups (`apt-cdrom-setup` chain + bin-only bootloader + pool.list) hardened the flow through 2026-05-14.)* |
| COMP-12 | P1 | done | **Installer robustness pass** (`docs/plans/comp-02-robust-build.md`) — full 6-phase ISO hardening from "boots and starts d-i" to "completes a clean install end-to-end on both BIOS and EFI", closed with a CI gate.  *(Phases A/B (stock-cdrom seed completeness, default-release helper) + **Phase C** (sign Release + ship pubkey + base-installer keyring patch, commit `42e03ea`) + **Phase D** (bin-only `grub-pc-bin` + `grub-efi-amd64-bin`, commit `875200e`) + **Phase D follow-up** (new `pool.list` mechanism for ship-but-don't-install packages — bootloader metas + `open-vm-tools` + `console-setup` + `keyboard-configuration` + `xkb-data`, commit `68266a2`) all landed pre-2026-05-14.  **Verified 2026-05-14**: BIOS + EFI VMware installs both reach `finish-install.d/20final-message` cleanly.  **Phase E Deferred** items (intel-microcode pool entry, shim-signed pool entry, dpkg-divert warning silencing, mtab-symlink warning, brltty-udeb description fallback) stay parked in plan doc § Phase E "Deferred" — cosmetic noise, no install-blocking impact.  **Phase F (CI gate) landed 2026-05-22**: harness in `tests/installer_smoke/` (note underscore — mypy package-name rule).  Shape: (a) `known_bad_patterns.py` — catalogue of `(regex, severity, meaning)` tuples scanned line-by-line against a captured serial log; fatal patterns include `succeeded but requested to be left unconfigured`, `failed with error code`, `unable to find required udeb`, `WARNING **:.*main-menu`, `40cdrom output did not verify`; warn patterns track known noise (depmod-modinfo, default-release).  `scan_log(path, extra_patterns=None)` returns findings dicts; `has_fatal(findings)` predicate.  (b) `run.py` — QEMU driver with `--quick` (default, ~3 min — boot + d-i first-dialog gate) and `--full` (~15-30 min — drives preseed.cfg through to install completion) modes; BIOS + EFI; KVM auto-detect; OVMF support for EFI; exit codes 0 (clean) / 1 (fatal pattern matched) / 2 (setup failure) / 124 (--full timed out without completion signal).  (c) `preseed.cfg` — fully-unattended single-disk atomic-recipe install (locale en_US.UTF-8, keymap us, /dev/vda, smoke/smoke creds, GRUB to /dev/vda).  (d) `README.md` — prereqs, invocation, exit codes, CI integration sketch, known v1 limitations (no HTTP preseed server, no installed-system boot-verify, no graphical splash diff — splash presence pinned separately by `test_installer_grub_cfg_wires_background_image`).  6 new unit tests (`test_installer_smoke_*`) exercise the parser: clean-log no-findings, every fatal pattern detected against synthesized samples, warn vs fatal separation, missing-file graceful empty, extra_patterns extension, run.py code-inspection asserts (--quick/--full/--iso/--mode/qemu-system-x86_64/known_bad_patterns wiring).  470→476 tests pass; mypy + ruff clean.  Operator runs the harness manually today; a CI step shape is documented for when a CI environment lands.)* |
| COMP-14 | P1 | done | **Build-host GRUB contamination — `grub-mkrescue` now runs inside the BuildContainer (fix path b).**  *(Landed 2026-05-22 after two false-start attempts.  Pre-fix, both `iso.py:build_iso` (live ISO) and `iso_installer.py:_run_grub_mkrescue` (installer ISO) shelled out to the host's `/usr/bin/grub-mkrescue`, which used host's grub-mkimage + modules from `/usr/lib/grub/`.  Build host = Debian 13/trixie → ISO bootloader self-reported GRUB 2.12-9+deb13u1.  **First attempt (fdcd670 + e82a867, reverted in daa4a41)**: extracted our debs to a temp dir + `grub-mkrescue --directory=`.  Empirically refuted: md5sum'ing modules from the produced ISO showed they matched host's byte-for-byte even with our --directory — `--directory=` only retargets the modinfo.sh probe + core.img staging dir, NOT the bulk module-copy step.  Single-`--directory=` mode is also not hybrid-capable.  **Fix path (b) chosen + shipped**: new `BuildContainer.run_grub_mkrescue(staging_dir, output_iso, password)` method runs `grub-mkrescue` inside the build container (bookworm base, apt pinned to OUR snapshot).  Container apt-installs `grub-common grub-pc-bin grub-efi-amd64-bin xorriso mtools dosfstools` from the snapshot, then runs grub-mkrescue as root (to read root-owned files in /staging) and chowns the produced ISO back to the host's uid:gid.  Volume mounts: staging→/staging rw, parent(iso)→/output rw.  Container-lifecycle pattern mirrors existing `build()`: try/finally with `force=True` remove so KeyboardInterrupt doesn't leak.  Both iso_installer.py:_run_grub_mkrescue and iso.py:build_iso now delegate via `container.run_grub_mkrescue(...)`.  build.py threads `self.container` through `cmd_build_iso_installer` (kwarg) and `cmd_build_iso_live` (kwarg to BuildSystem.build_iso).  Helper requires container — explicit error if None (caller must run `cache build` first).  Residue remaining (compiled-in PACKAGE_VERSION inside the binary): the grub binaries from bookworm still self-report `2.06-13+deb12u1` because PACKAGE_VERSION is baked at dpkg-buildpackage on Debian's buildd; our NMU-strip rewrites the .deb's dpkg metadata but can't touch compiled binaries (catalogued in branding-methodology.md § 7 separately).  Net win: the GRUB code embedded in the ISO is now bookworm's 2.06, not trixie's 2.12.  5 new tests: BuildContainer.run_grub_mkrescue docker-invocation argv shape (with mocked Docker client), failure-propagation contract, iso_installer + iso.py anti-regression code-inspection asserts that neither shells out to bare grub-mkrescue, build.py wiring asserts container=self.container is passed to both ISO builders.  460 tests pass; mypy + ruff clean.)* |
| COMP-01f | P1 | done | **Installer Phase 6 — branding via companion udeb + GRUB splash, no source patches.**  *(Landed in two pieces 2026-05-22.  **Phase 1 (strings) — athena-installer-data 1.2.0**: two override mechanisms in one S40 startup hook, keyed by template type.  **A-TEXT** (`debconf-loadtemplate`) for type=text/note templates whose rendered string IS the Description field — ships `data/athena-overrides.templates` re-declaring `debian-installer/main-menu-title` + `debian-installer/title` with our Description; loaded AFTER S20templates so our re-load wins the path-key conflict (debconf is keyed by template path).  **A-VALUE** (`debconf-set-selections`) for type=string/boolean/select templates with separate settable values — ships `data/debconf-overrides.dat` with `netcfg/get_hostname`.  Initial 1.1.0 attempt incorrectly used set-selections for type=text templates — silent no-op; the type=text vs type=string distinction was the load-bearing fix in 1.2.0 (catalogued in docs/branding-methodology.md § 3 "two override mechanisms within Pattern A").  **Phase 2 (boot splash) — Pattern B drop-in static asset**: `installer/boot/grub-background.png` (committed 800×600 PNG mirroring the Aegis SVG identity — midnight indigo radial gradient + sparse stars + gold-stroked Greek alpha) staged into `/boot/grub/` by `iso_installer.py:_stage_grub_cfg`; `installer/boot/grub.cfg` gates gfxterm setup behind `if loadfont … ; then … fi` so text-mode fallback works on firmware without 800×600 support; `installer/boot/regenerate-bg.py` is the single source of truth for the PNG (runs against PIL, no build-time dependency).  **Visual-theme cdebconf-newt palette REJECTED**: confirmed unreachable — rootskel execs startup scripts as children (export dies with the shell), AND cdebconf-newt has compiled-in palette logic ignoring newt env vars regardless.  Catalogued as irreducible residue in branding-methodology.md § 7.  **No source patches** against cdebconf / cdrom-detect / main-menu; principle enforced by anti-regression test that asserts no `patch/source/cdebconf/` etc. directory ever appears.  9 new tests (overrides + hook + palette-absence + patch-absence + GRUB-bg presence and staging).  Companion deliverables: docs/branding-methodology.md (DOC-07, the policy doc), installer/branding/README.md (RETIRED marker), fork/source/README.md current-packages table refreshed.)* |
| COMP-05 | P1 | done | **Idempotent re-runs** — `cmd_build_cache`, `cmd_parse_dependency` etc. all reset their `BuildFlags` and rerun from scratch. For long resolves this is wasteful; add an explicit `clean` command and treat repeats as no-ops when cache/dep tree are still valid. *(landed 2026-05-14.  Two parts: (a) new `clean` group dispatcher with sub-actions `cache` (alias to existing `cache purge`), `source`, `repo`, `buildroot` (sudo), `image`, `download`, `all`; each wipes the relevant working dir + resets the corresponding `BuildFlags` + drops in-memory pipeline refs.  `force` arg on any sub-action skips the YESNO prompt; `clean all` consolidates into a single up-front confirmation + one sudo unlock and orchestrates the others.  Preserved: `gnupg/` (signing key), `log/` (build history), `patch/` (patch series).  (b) Early-exit guards on `cmd_build_cache` and `cmd_parse_dependency` — when `cache_ready` / `dep_check_ready` is set and the in-memory `Cache` / `DependencyTree` is loaded, the call no-ops with a hint pointing at `clean X` (wipe + re-fetch/re-resolve) or `cache build force` / `dep parse force` (rerun over existing state).  Helper `_wipe_dir_contents(label, path, sudo, password, skip_prompt)` handles size/file-count display, optional YESNO prompt, and the actual wipe (`sudo find -mindepth 1 -maxdepth 1 -exec rm -rf {} +` for chroot dirs; `shutil.rmtree` / `os.unlink` for user-owned).  11 unit tests: dispatcher routing × 7 (cmd_clean_* matrix entries), unknown action no-op, early-exit on cmd_build_cache + force bypass, early-exit on cmd_parse_dependency + force bypass, _wipe_dir_contents missing/empty/populated, cmd_clean_source/repo/image flag-reset behaviour.  250→261 tests, all green.)* |
| COMP-08 | P2 | wontfix | **System-level firstboot** — `systemd-firstboot --root-password=root` ships an ISO with `root:root`. Replace with a firstboot wizard or, at minimum, randomize and print the password during ISO build. *(closed as duplicate 2026-05-10: scope splits cleanly along the live-vs-installed axis.  **Live ISO** part is already shipped via SEC-04: `iso.py` picks a random English first name from a 67-entry list per build, bakes it into the grub.cfg `username=` cmdline, surfaces it in the boot-menu title and a `<iso>.user` sidecar.  **Installed-system** part has no anchor point until there's an installer to write `/etc/shadow` — i.e. it depends on COMP-01 (installer ISO).  Track the installed-system password story under COMP-01's scope; no value keeping a standalone ticket that just says "do it inside COMP-01".)* |
| COMP-10 | P3 | wontfix | **Architecture for OS-release branding** — `id`, `id_like`, `vendor` are hardcoded to Athena strings in `generate_system_configs`. Move to `[Build]` config so a real derivative does not have to fork the source.  Subsumed by COMP-11 once that lands. *(closed as duplicate 2026-05-10: COMP-11 (distro abstraction) explicitly takes ownership of the `os-release` ID / ID_LIKE / VENDOR_NAME fields as part of its `[Build] Distro = debian \| ubuntu` switch.  Tracking the same change under two tickets is just bookkeeping noise; the work lands as part of COMP-11.)* |
| COMP-03 | P1 | done | **Parallel `source_build`** via worker pool with full SIGINT-driven cancellation + per-container resource caps + heavy-package serialization.  *(Landed 2026-05-29 across 4 commits: `17a3319` Phase 0+1, `94f29e5` Phase 2+3, `bf5deb1` Phase 4, `1b73b99` Phase 5+6.  **Architecture pivot vs the abandoned 2026-04-29 attempt** (commit `7917869`'s `build_all` was layered-over by subsequent apt-repo work and silently disappeared): the previous attempt's true killer was a hidden segregate race — `_segregate_built_artifacts` did `os.listdir(self.repo_path)` and attributed every `.deb` at the shared root to the just-finishing source's component.  Under parallelism, worker A would pick up worker B's unmoved `.debs` and silently misroute them.  This Phase 1 fixes that BEFORE any parallelism is enabled.  **Phase 0** (config plumbing): three new `[Build]` keys (`BuildCpus`, `BuildMemory`, `HeavyPackages`); `MaxParallelBuilds` capped at `[1, 8]` to stay under docker-py's default urllib3 pool size (10); fail-closed mutex between `AuditBuildDeps` (SEC-05) and `MaxParallelBuilds > 1` (parallel workers would collide on the audit gate's interactive prompt).  **Phase 1** (per-worker scratch repo dir): new `dir_build_stage` under `tmp/build-stage/`, auto-created + writability-checked at config load; `BuildContainer.build()` allocates `<uuid>/` per call, binds it as `/repo` (was `self.repo_path`), `rmtree`s on finally; `_segregate_built_artifacts(self, src_pkg, source_dir)` takes the scratch path explicitly, destination moves serialised under module-level `_REPO_DEST_LOCK`, all-or-nothing rollback on first OSError; startup sweep of leftover build-stage subdirs (kill-9 / OOM / crash recovery).  **Phase 2** (label-tagged registry + startup orphan reap): `self._live` (dict[short_id, container]) + `self._live_lock` (threading.Lock); every spawned container (build / SEC-05 preview / grub-mkrescue) carries `com.athena.build=1` + `com.athena.pid=<pid>` labels; helpers `_register_live` / `_deregister_live`; constructor lists containers by label filter and force-removes survivors.  **Phase 3** (shutdown_event + cancellation API): `self.shutdown_event = threading.Event()`; `request_shutdown()` sets the event AND force-removes every registered container (unblocking workers stuck in `container.wait()` / `container.logs(stream=True)`); `reap_all_live()` snapshots the registry under the lock then iterates outside it (no `dictionary changed size during iteration`).  **Phase 4** (parallel executor in `cmd_source_build`): extracted `_build_one_source` as the per-source work unit (pure src-in / result-code-out → trivially thread-safe); partitioned packages into tunneled (always serial — network-bound + dest-dir lock) and to-build; `MaxParallelBuilds == 1` short-circuits to the existing serial loop verbatim (backward-compat + debugging mode); `> 1` uses `concurrent.futures.ThreadPoolExecutor(max_workers=N)`; scoped SIGINT handler installed/restored only around the executor block (no REPL semantics change), on Ctrl+C calls `request_shutdown()` so containers vanish in ~1s and `executor.shutdown(wait=True, cancel_futures=True)` drains worker finally blocks (scratch-dir rmtree + container.remove).  **Phase 5** (per-container resource caps): `_resource_kwargs()` translates `BuildCpus → nano_cpus` (billionth-of-a-CPU) + `BuildMemory → mem_limit` (docker size strings); splatted into `containers.run()`; exit code 137 (cgroup OOM-killer's SIGKILL) detected post-`wait()` and surfaced with an operator-friendly "raise BuildMemory" hint via `tui.console.print` + `logger.error`.  **Phase 6** (heavy-package scheduler): replaced `as_completed` with `cf.wait(FIRST_COMPLETED)` driving a small state machine — when next-pending is in `config.heavy_packages`, `_can_submit_next` returns False until `_in_flight` is empty (drain-before-start), and while any heavy is in flight no new builds start (pause-new-during); empty heavy set degenerates to plain fill-to-N.  **Tests**: 38 new `test_comp03_*` tests across the six phases (config plumbing, segregate race fix, registry + label filter, shutdown API + race-free snapshot, executor wiring + SIGINT handler scoping, resource kwargs + 137 hint, scheduler invariants).  629/629 passing.  ruff + mypy clean.  README §Common failure modes gets a new entry on the resource caps + HeavyPackages knob + 137 OOMKilled hint.)* |
| COMP-09 | P2 | done | **Disk-image (qcow2) output alongside the ISO, for direct VM/cloud use.** *(Landed 2026-05-22.  New `iso build disk [size_gb]` command produces a pre-installed bootable qcow2 disk image from the live chroot — boots straight into the running OS, no installer step.  Output: `image/<distribution>-<version>-<arch>.qcow2`.  Shape: new `scripts/disk_image.py` module orchestrates a single sudo'd pipeline (1) truncate sparse raw image; (2) `losetup -Pf` attach → `/dev/loopN` with auto partition device nodes; (3) `sfdisk` GPT with 1 MB BIOS-boot + 100 MB ESP + rest ext4 root (type GUIDs pinned); (4) `mkfs.fat -F32` ESP + `mkfs.ext4` root; (5) mount root + ESP under root/boot/efi; (6) `rsync -aHAX` chroot → root (Spinner-wrapped — multi-minute on a typical chroot); (7) `/etc/fstab` generation from blkid UUIDs; (8) bind-mount /proc /sys /dev /dev/pts + chroot'd `grub-install` BIOS (i386-pc, MBR + part 1 embed) + EFI (x86_64-efi, --removable for portable boot) + `update-grub`; (9) reverse-order teardown that always runs (umounts + losetup -d) so /dev/loopN doesn't stay held on error; (10) `qemu-img convert -O qcow2` final + remove raw intermediate.  Pre-flight `_need_tools()` surfaces the first missing host tool (rsync / dosfstools / qemu-utils are the usual gaps) with an actionable `apt install` message; chroot_verified flag gate (with `force` bypass) mirrors `iso build live`.  Default size `[Build] DiskImageSizeGB` = 5 GB sparse; operator overrides per-invocation via positional arg.  6 new tests (signature stability, required-tools coverage, sfdisk script GPT layout, _need_tools missing-name surface, dispatcher routing, chroot gate + size parsing).  488 → 494 pass; mypy + ruff clean.  README §First-run gets the new command listed under the "off to one side" sidebar.  **Known v1 limitation** (deferred follow-up): grub-install runs on the host, so the produced disk image's GRUB binaries reflect the build host's GRUB version (analogous to pre-COMP-14 ISO leakage) — moving grub-install into the build container is a COMP-14-style follow-up.)* |
| COMP-13 | P2 | done | **Identity URL pass — replace surviving "debian.org" references with our project URL.**  *(Landed 2026-05-22 as part of commit `54a4c4b` "UX polish — banner rebrand, repo merge, progressbar refinements".  Quick-win surfaces shipped: `fork/source/base-files/etc/os-release` HOME/SUPPORT/BUG_REPORT URLs set to `https://github.com/summertanks/Athena-Build` (+ `/issues` for BUG_REPORT); `fork/source/base-files/origins/debian` Vendor-URL + Bugs set similarly, Vendor field updated `Athena Linux → Asgard Linux` to match the three-layer identity policy.  Will land in the installed system on the next base-files rebuild (changelog bump deferred per `feedback_fork_dev_iteration_workflow.md` until the operator verifies the ISO).  **Audit of the broader corpus**: `athena-installer-data` template files already use `@DISTRIBUTION@` token substitution (no debian leakage in branded strings); `athena-tasksel` task descriptions are clean; `/etc/apt/sources.list` URL is COMP-02's territory (needs a publish endpoint to point at); remaining `grep -RFi 'debian.org\|debbugs'` hits in scripts/ + fork/source/ are upstream maintainer attributions, changelog signatures, and copyright headers — legal/historical, intentionally untouched.  Deeper installer cdebconf template residue overlaps CONF-10 (Debian-residue audit sweep); ticket text explicitly says "could be folded in" — that work continues under CONF-10's scope.)* |
| EXTRAS-01 | P2 | done | Repo content beyond what the live ISO ships: pull depth-1 *Recommends* of selected packages into `selected_pkgs` / `selected_srcs` so they end up in `repo/`, but keep them out of the chroot install (live ISO stays minimal) and out of the default `source_build` (use `source_build recommended` to build them).  Phase 1 prep for the future signed apt-repo (CONF-01/02). *(landed 2026-05-09: new `[Build] IncludeRecommendsInRepo` knob (default true); `DependencyTree.pull_recommends_extras` populates `selected_pkgs` + tracks `extras_pkg_names`; `derive_extras_src_names()` (called after `parse_sources`) marks extras-only sources; `cmd_source_build` arg parsing reworked — `recommended` is a standalone mode (mutually exclusive with named pkgs, combinable with `force`); `_compute_install_batches` filters `extras_pkg_names`; `print extras` view added under "Packages" group; `print selected` annotates extras with `(extra)`; `print stats` and `summary` show extras counts.  8 unit tests cover the data model + filter + view.  Known gap: OR-grouped recommends `foo \| bar` are dropped by today's parser at `package.py:201` — documented in the dropped-alt-groups test.  Recommends' transitive Depends are NOT pulled — apt resolves them at install time via upstream sources.)* |
| GROUPS-01 | P1 | done | **pkg.list install-time groups** — operator-defined groups (`[base]`, `[development-tools]`, `[gnome]`, …) in `config/pkg.list`.  `[base]` is always installed (live image + target debootstrap); other groups ship in `/cdrom/pool` and the installer (tasksel) apt-installs the operator-selected subset on /target at install time.  **Phase 1 done 2026-05-14**: INI parser (`utils.parse_pkg_list_groups`) with backward-compat (flat pkg.list → implicit `[base]`); per-group resolution in Pass III with declaration-order respected (a package also reachable from an earlier group's closure is credited there, not duplicated); `DependencyTree.pkg_group_pkg_names` per-group canonical sets; `pkg_group_extras_pkg_names` (union of non-base groups) filtered from live install batches (`chroot.py:_compute_install_batches`) AND from `_base_include` (target debootstrap) but KEPT in `_pool_whitelist` (cdrom pool); `iso_installer._stage_group_manifests` writes `.disk/groups/<group>.list` per group; `print groups` view added.  7 new tests (275→282).  **Phase 2 done 2026-05-14**: generate tasksel `.desc` files at ISO-build time + installer pre-pkgsel hook + pkgsel/tasksel install-list wiring.  Shape: `iso_installer._stage_tasksel_desc()` writes `staging/.disk/athena-tasks.desc` with one RFC-822 stanza per non-`[base]` group (Section: athena, Description: from operator `## Description:` comment or fallback, Key: alpha-sorted seed names).  `utils.parse_pkg_list_group_meta()` parses `## Description: …` comments after `[group]` headers in pkg.list (sibling parser to parse_pkg_list_groups, no API break).  `installer/pkgsel/pre-pkgsel.d-athena-tasks` shell hook overlays into the installer chroot at `/usr/lib/pre-pkgsel.d/05-athena-tasks` via `_OVERLAY_MAP`; copies the .desc from /cdrom to /target/usr/share/tasksel/descs/ before pkgsel runs `in-target tasksel --new-install`.  `pkgsel` udeb added to `installer.list` (drives the Software-selection step at install time); `tasksel` deb added to pkg.list `[base]` so it's debootstrapped onto every /target.  8 new tests (282→290): meta parser (description extraction, flat-file fallback), tasksel .desc generation (RFC-822 stanzas, [base] skipped, operator description vs fallback, Key sorting), hook script exists + executable + correct cdrom→target path, OVERLAY_MAP entry, installer.list pkgsel pin, pkg.list [base] tasksel pin.  **Phase 3 done 2026-05-14**: end-to-end install validation tooling + example group.  Shape: (a) pre-pkgsel hook refactored to accept `ATHENA_TASKS_SRC` / `ATHENA_TASKS_DST_DIR` env-var overrides (defaults unchanged) so the smoke test can drive it without touching real `/cdrom` or `/target`.  (b) Two subprocess smoke tests pin the hook's install-time behaviour: copies the .desc to `<target>/usr/share/tasksel/descs/athena.desc` when source exists, exits 0 cleanly when source is absent.  (c) Pre-flight integrity check in `cmd_build_iso_installer` warns when a group resolves to zero canonical packages (operator typo'd every seed) or when non-base groups exist but `pkg_group_extras_pkg_names` is empty (declaration-order bug — all packages credited to [base]).  (d) Real example group `[development-tools]` (build-essential, git, vim, less, htop, strace) added to pkg.list with a `## Description:` — small enough for a tractable install test (~100 MB on top of [base]).  (e) `print groups` view now shows per-group repo size in MB by walking `repo/` for matching .debs.  3 new tests (290→293).  Pkgsel's pre-pkgsel.d invocation verified against the actual pkgsel 0.79 udeb (extracted + read its postinst at iso-build time): scans `/usr/lib/pre-pkgsel.d/*`, requires `-x`, runs in installer environment with /target mounted.  **Remaining manual step**: rebuild the ISO with `[development-tools]` declared, boot in QEMU, confirm tasksel surfaces `athena-development-tools` as a checkbox, install it, verify `dpkg -l` on /target shows the packages.  No code blocker for this; pure VM-time validation by the operator.  **Closed 2026-05-25**: QEMU validation confirmed by the operator — tasksel surfaces the non-`[base]` group (`athena-development-tools`) as a checkbox at the Software-selection step, selecting it apt-installs the group's packages onto /target, and `dpkg -l` on the installed system confirms them. All code (Phases 1-3: INI group parser, per-group dep resolution, tasksel .desc generation, pre-pkgsel hook, pkgsel/tasksel wiring, pre-flight zero-package check) was already shipped + tested; this closes the manual VM-validation gate that was the ticket's last item. |
| SURFACES-01 | P1 | done | **Surfaces as reachability closures — live / disk / installer-pool composed independently.**  *(Shipped 2026-06-10..11 as Chunks 1-8, `406322e`..`e9fdb7a` + fork bumps `c61b189`; live ISO + installer ISO + disk image all boot-verified 2026-06-11.)*  In plain English: instead of one chroot rsync'd into every artifact and a ship-everything pool, each shipped surface is now an explicit dependency-closure over named roots — what's on each artifact is *derived*, auditable, and minimal.  A surface = `closure(seed roots, dep graph, ± Recommends)` computed by the new `scripts/surfaces.py` helper (Chunk 1), never a credit-based group delta.  **Live** (Chunk 3): `chroot build live` installs `closure([Live] Groups seeds ∪ live.list ∪ required/important, WITH Recommends)`; shipped default `[Live] Groups = base, gnome-desktop` makes the live image the GNOME experience — 1105 pkgs on the live dataset (gnome-shell + athena-branding; openssh-server rides `[base]`).  `build_chroot`/`_compute_install_batches` gain an explicit `install_set` parameter that REPLACES the legacy internal exclusion math; `None` keeps the historic `[base]`-only behavior (pinned).  **Disk** (Chunk 4): the disk image was an rsync of the LIVE chroot — one chroot, two artifacts, no divergence possible.  Now `chroot build disk` builds its own minimal chroot at `buildroot/disk` (`BuildConfig.dir_chroot_disk`, `BuildFlags.chroot_disk_ready`): `closure([Disk] Groups ∪ required/important)` with hard deps only, no Recommends — 245 pkgs on the live dataset; `iso build disk` gates on it and masters it into the qcow2 (`4f9498c`: live-boot verify stays live-surface-only, `iso build disk` force-verifies the DISK chroot).  **Installer-ISO pool** (Chunk 5, `bc10522`): `/cdrom/pool` now ships ONLY what something on the ISO can install — `pool_whitelist = closure([base] ∪ every task group's tasksel Keys ∪ installer-defaults roots (new `config/installer-defaults.list`: d-i hook targets — grub metas, microcode, firmware, open-vm-tools, console-setup) ∪ installer.list deb-arm roots ∪ required/important, WITH Recommends)`, replacing the legacy `canonical − live_exclusive`.  Manifest delta: 1252 vs 1569 legacy — 317 packages off the ISO (the asgard-* metapackage closures: LibreOffice, evolution, rhythmbox, totem, cheese, orca/speech, plus the standard-residue block).  pool.list entries stay selected/built/published; entries reachable by nothing on the ISO are **mirror-only residue** — installable post-boot from a configured network mirror, per operator decision.  All d-i roots verified present in the staged manifest (efibootmgr caught live when the first seed composition missed installer.list).  **Tasksel menu generated from the signed lockfile at ISO time** (Chunks 6-7, `a0743d4` + `3c4dfa2`): new `scripts/tasksel_desc.py` emits makedesc-shaped stanzas (Section: user, ASCII-sanitized — the cdebconf silent-drop fragilities) with Keys = the group SEED lists; `iso build installer` derives the text from the SIGNED lockfile's seeds and stages it at `/.disk/athena-tasks.desc`; new `fork/source/athena-pkgsel/pre-pkgsel.d/05athena-tasks` hook copies it over /target's packaged fallback desc before tasksel runs (no-op when the ISO carries no generated copy).  Fork bumps: athena-pkgsel `0.79+athena2`, athena-tasksel `3.73+athena3`.  **Chunk 8** (`e9fdb7a`): the strict pkg.list↔tasks/* equality test is reworked — the fork's static `tasks/*` are a FALLBACK; every existing task file's Key set must remain a SUBSET of its pkg.list group (the 2026-05-18 silently-hidden-task failure class), while the generated path's equality is pinned by the generator tests.  **Boot verification shook out six real incidents**, all fixed same-arc and catalogued: chroot bring-up ordering + lying completion gate (`8a388ab`, `9ad420d`, `d41de39` — quirks 7.3/7.4 in `docs/build-quirks.md`), the e2fsprogs missing-deps reboot loop (`00d45f9` — quirks 3.4/9.1, gates in STA-23), nomodeset killing GNOME (`53e9225` — quirk 9.2), systemd-firstboot hostname identity leak (`ebdbc2d` — quirk 9.3), ESP fs_passno 1→2 (`840b32f`), and pkg.list task-description rebranding (`c7c5f61`).  Memory: `project_surfaces01_composition`. |
| LEDGER-01 | P1 | done | **build.json v4 — per-source lifecycle ledger + claim deprecation/obsolescence at publish.**  *(Shipped 2026-06-10 as Chunks 1-7, `2cccec3`..`68bc91d`, plus `b26484e` UX + `aab608d` no-resurrection fix.)*  In plain English: the per-source build record is no longer just "how the last build went" — it is the source's lifecycle document, answering "is this source still selected, when was it deprecated, what versions did it ship before?", and the publish step now tells the mirror when files we own are no longer wanted (ownership released) or merely superseded (ownership retained, prune candidate).  **Schema 3→4, purely additive** (Chunk 1): `lifecycle_v`, `selection` (selected\|deprecated\|retracted), `selected_version`, `snapshot`, `selected_at`/`deprecated_at`/`retracted_at`/`published_at`, and `history` — superseded episodes rolled in place when the version moves (state `obsolete`) or the selection flips.  Tracking starts at **cache parse**, not at build (Chunk 3): `upsert_build_record` synthesizes a minimal `phase='selected'` record for sources selected but never built; `classify_build_record` maps `'selected'` → `'missing'` so every phase-gate consumer (publish claims gate, check_build, audits) behaves exactly as if the record did not exist.  `preserve_lifecycle` carries the lifecycle fields through record re-creation at build-start/tunnel-entry (Chunk 2); `roll_lifecycle_history` is the single history-entry shape.  **Claim schema v3** (Chunks 4-5): new `CLAIM_STATE_OBSOLETE` cascaded across every claim consumer.  **Publish wiring** (Chunk 6, `c62e9bb`): Step 6b deprecates files we own+published but no longer select (ownership released — the SELECT-LOCK shrink path); Step 6c emits obsolescence claims — among OUR owned PUBLISHED claims, group by (binary, arch) and every claim but the newest gets a `new_obsolescence` referencing its seq (ownership retained; retained bytes are prune candidates, never auto-deleted — UPD-01 publish-before-prune).  Both steps exception-isolated.  `remote_publish` gains `on_published(set)` invoked ONLY at the success return (after pool + jsonl + coord-head pushes ALL succeeded) — `published_at` is stamped truthfully; `mirror conflict resolve` mirrors a retraction onto the record (episode rolls to history, `selection='retracted'`).  **Coherence audit** (Chunk 7 + `b26484e`) distinguishes *pending deprecation* (deselected locally, deprecation not yet published) from real incoherence, with one crisp action summary; `aab608d` pins that publish must never resurrect a deprecated source.  First real use: the reportbug stack deprecated on the mirror (`9296b8d` reflects it in pool.list's residue note).  Memory: `project_ledger01_lifecycle_architecture`. |
| RECLAIM-01 | P1 | done | **`mirror reclaim` — explicit operator escape hatch for rebuilding bytes under an already-published filename.**  *(Shipped 2026-06-12 as Chunks 1-6, `11aaa3b`..`6598c93`; live-verified with 7 real reclaims executed against the production mirror.)*  In plain English: the federation's blessed invariant is **published filename = frozen bytes** — but two legitimate cases produce different bytes under the SAME version/filename: Position-X Case-C out-of-band re-normalisation (dep-constraint-only fixes that deliberately don't bump), and disaster-recovery rebuilds (builds are not bit-reproducible).  Pre-RECLAIM the only honest options were lie (bump a version that didn't change) or stay stuck local-ahead forever.  **Rule of thumb now documented**: modified fork → bump → publish; reclaim ONLY when an already-published filename was rebuilt without a bump.  **Claim schema v4** (Chunk 1): `new_reclaim()` with `reclaims_seq` back-referencing the superseded published claim; unlike the marker shapes the reclaim record is itself the LIVE claim (pending → published via the normal pipeline).  v3-reader degrade documented: the line parses but v3 folds see two live claims for one filename → their hash-conflict scan halts publish; peers upgrade before the federation's first reclaim.  **Folded at every supersession site** (Chunk 2) including the hash-conflict scan, so a reclaimed file is never a false conflict.  **`local_ahead_candidates()`** (Chunk 3) gives the honest "your bytes differ from published" hint instead of the old misleading message.  **`remote_publish(reclaim_intents=…)`** (Chunk 4): push overwrites bytes for reclaim files ONLY — every other file keeps additive-only semantics; each intent re-validated against the just-fetched coord view inside the transaction (drift between listing and execution skips loudly), under the full machinery — stale-index guard, flock, federation gate, hash-conflict scan.  **`mirror reclaim [<source>\|<file.deb>] [<name>] [force]`** (Chunk 5): no target = per-mirror dry-run listing of local-ahead candidates with old→new shas; with a target = per-file confirmation + YESNO restating the invariant exception (force skips), then delegates to `cmd_mirror_publish`.  **Pull side** (Chunk 6): the skip-present fast path never sha-checks (correct under filename immutability), but a reclaim rewrites bytes under an unchanged filename — claims carrying `reclaims_seq` (rare by design) now sha-check the present local file and re-download on mismatch, surfaced as `refreshed=N`; normal claims keep the hash-free fast path (pinned by test).  Unblocks publishing the Position-X Case-C re-normalised artifacts (the 19-source one-shot completed locally 2026-06-08, `f5d3534`) — remaining apply tracked as CONF-16 in TODO.md.  Memory: claim schema notes in `scripts/coord/schema.py`; `project_asg_position_x_no_bump_on_dep_strip`. |

## 4. Architecture & coding practices — P1 / P2

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| ARCH-01 | P1 | done | Eliminate module-level globals in `build.py` (`build_config`, `build_cache`, `dependency_tree`, `build_container`, `console`, `_tui`). Encapsulate in a `BuildSession` object that the TUI command handlers receive as `self`. Unblocks unit tests that don’t need a TUI. *(All 11 cmd_* and 2 helpers (_do_tunnel, _verify_chroot) are now BuildSession methods. State (config, cache, dep_tree, container, tui, flags) lives on the instance. main() instantiates one BuildSession and registers each cmd_* as a bound method. New test: `test_buildsession_constructible_with_stub_tui` proves the class is unit-testable without standing up the curses TUI.)* |
| ARCH-02 | P1 | done | `tui.console` is mutable module state assigned during init in `build.py:971`. *(Dropped the redundant `console = Console()` + `tui.console = console` reassignment from `build.py.main()`; replaced with `from tui import console`. The single Console facade created in `tui.py` at module load is now the canonical one.)* |
| ARCH-03 | P1 | done | Decouple `Console`, `Spinner`, `ProgressBar`, `Prompt` from a singleton `tui_instance` — accept it explicitly. *(All four primitives now accept an optional `tui` kwarg; falls back to `tui_instance` for backward compat. `Tui.__init__` registers itself as the module singleton, so callers no longer need `tui.tui_instance = _tui`. Three new tests cover explicit-tui, singleton-fallback, and no-tui-anywhere paths.)* |
| ARCH-04 | P1 | done | Split `buildsystem.py` (1,460 LOC, 25k+ tokens) into: `chroot.py`, `iso.py`, `dep_drift.py`. The file currently crosses two-tab read budgets. *(Split via mixin classes: `_ChrootMixin` / `_IsoMixin` / `_DepDriftMixin`. `BuildSystem` is now a thin orchestrator (184 LOC) that composes the three. Side-quest: renamed all `self.__x` private attrs to `self._x` so cross-mixin access works without name-mangling. 38/38 tests pass.)* |
| ARCH-05 | P2 | done | Add `pyproject.toml` with `setuptools`/`hatch` so `pip install -e .` works and entry points are declared.  *(`pyproject.toml` shipped — `[build-system]`, `[project]` metadata, `[tool.setuptools]` flat-module layout under `scripts/`, plus stub `[tool.ruff]` / `[tool.mypy]` blocks ready for ARCH-06.  Entry-point + src-layout deferred.  46/46 tests pass.)* |
| ARCH-06 | P2 | done | Add `mypy` + `ruff` (or `flake8`+`black`) config and run them in CI; type hints exist but are inconsistent (e.g. `BuildContainer.__init__ -> bool`). *(`pyproject.toml` ships ruff + mypy config; `.github/workflows/ci.yml` runs ruff + mypy + the unit-test runner on push and PR.  Conservative initial ruleset: ruff = F + E9, mypy = `continue-on-error: true`.  Tightening rule sets is a follow-on; the gate is honest about today's discipline.  README has a "Development" section documenting the local invocation.)* |
| ARCH-07 | P2 | done | Decide on a logging story: today output is split between `print()` for stdout, `tui.console.print()` for the curses console tab, `console.error/warning/info` for the log tab, and ad-hoc files (`chroot-install.log`, `mksquashfs.log`, etc.). Wrap with a single `logging` adapter that routes by level. *(Two-step landing: (a) stdlib `logging` adapter in `tui.py` — `logging.getLogger('athena')` is bound by `Tui.__init__` to `_LogTabHandler` (INFO/WARNING/ERROR → log tab) and `_ConsoleTabHandler` (custom `DISPLAY=25` level → console tab). `tui.console.print` / `mark` / `trim_to` keep their direct API for buffer-position semantics. (b) Mass migration: 142 `console.{info,warning,error}(…)` call sites across 9 modules → `logger.{info,warning,error}(…)`. Per-command capture files (chroot-install.log, mksquashfs.log, grub-mkrescue.log) deleted — chroot/iso subprocess transcripts now route through `logger.debug` and land in a single `setup_file_logging(dir_log)` `FileHandler` whose path is `build-YYYY-MM-DDTHH-MM-SS.log`. Log-tab line wrap deferred to a future TUI-holistic pass — append-time wrap was rejected because resizes wouldn't reflow old records; render-time wrap belongs with the broader TUI rework, not this ticket. 9 new tests cover the routing paths and file handler; 55/55 pass.)* |
| ARCH-08 | P2 | done | `utils.Tree` is a generic tree but is only used by `BuildSystem.build_chroot` and `get_install_sequence`. Either move into `buildsystem.py` (private), or keep generic but add tests.  *(Dropped — both `Tree` and `Node` are gone from `utils.py`.  Their only consumer was `get_install_sequence`, dead code since ARCH-12.  Closes HK-01 too.)* |
| ARCH-09 | P2 | done | Replace `BuildContainer.is_ar_file` (re-implements `ar` parsing) with `dpkg-deb -W` or `python-debian.debfile` — fewer LOC, more correct.  *(Replaced 50-line hand-rolled `ar`-format reader with `debian.debfile.DebFile` — single audited parser shared with apt/dpkg ecosystem.  Smoke: validates real .debs in `repo/`, rejects non-debs and missing files.)* |
| ARCH-10 | P2 | done | Drop the unused `config/requirements.txt` (rich, tqdm, gnupg, docker) — `py_requirements.txt` is the source of truth used by `build-system.sh`.  *(Removed.  Single source of truth is `py_requirements.txt`, which `build-system.sh` reads.)* |
| ARCH-11 | P3 | done | Consolidate `Mirror` URL building (`url`, `dist_url`, `packages_path`, `sources_path`) with a single Pydantic-style validated model. *(landed 2026-05-14.  Refactored `utils.Mirror` from a hand-written class to `@dataclasses.dataclass(frozen=True)`.  Stdlib-only (no Pydantic runtime dep).  Field rename `mirror_id` → `id` so the dataclass field name matches the public attribute (~14 call sites in tests/ + 1 in utils.py BuildConfig; positional callers unchanged since field order is preserved).  `__post_init__` normalises (strip trailing slash on baseurl, leading+trailing on baseid, None→empty for suffix) and validates: non-empty required fields (id/baseurl/baseid/release/component/arch) with field-named ValueError; baseurl must contain `://`; suffix must be empty or start with `-`.  `with_snapshot` now uses `dataclasses.replace` so the snapshot variant is also frozen + the original instance is unchanged.  URL-building @properties (`url`, `dist_url`, `packages_path`, `sources_path`, `udeb_packages_path`, `suite`) preserved verbatim — they were already the single source of truth.  6 new validation tests pin the contract: frozen-after-construction (FrozenInstanceError on attribute assignment), slash normalisation, empty-field rejection per-field, scheme-less baseurl rejection, suffix-without-leading-dash rejection, with_snapshot returns a new instance leaving original untouched.  269→275 tests; mypy + ruff still green.)* |
| ARCH-12 | P1 | done | Replace `BuildSystem.build_chroot`'s try-fail-retry round loop with a single-pass topo-sorted install. *(implemented: `_compute_install_batches` does Kahn over `Pre-Depends ∪ Depends`; remaining SCC after libc-seed removal is emitted as a terminal "cycle batch" with `--force-depends` scoped to that batch only; `_configure_packages` is the named-list counterpart to `--configure -a`. Real cycle on bookworm: libdevmapper1.02.1 ↔ dmsetup + reachable systemd/grub/cryptsetup, ~15 pkgs. 8 unit tests cover the algorithm; live cache produces 14 batches for 212 pkgs, 13 acyclic + 1 forced.)* |
| ARCH-13 | P2 | done | Externalize hardcoded URLs/endpoints to `build.conf` so a derivative-distro setup is config-only. *(landed 2026-05-14.  Three new optional fields in `[Snapshot]`: `BaseUrl` (default `https://snapshot.debian.org/archive`), `TimestampApi` (default `https://snapshot.debian.org/mr/timestamp/`), `ArchiveKeys` (default `debian, debian-security`).  Read via `configparser` with `fallback=` so older build.conf files still parse.  `BuildConfig` exposes them as `self.snapshot_baseurl` / `snapshot_timestamp_api` / `snapshot_archive_keys`.  `Mirror.with_snapshot` gained a `baseurl=` kwarg (defaults to the Debian value for back-compat with the ~12 test sites that construct Mirror without a config); live call sites in `cache.py` + `buildcontainer.py` + `utils._validate_snapshot_timestamp` thread `config.snapshot_baseurl` through.  `_query_snapshot_latest` now takes `(api_url, archive_keys)` explicitly and computes `min(latest_per_key)` over an N-key list rather than the hardcoded `debian` / `debian-security` pair — a fork's snapshot mirror with a different archive layout works without code changes.  3 new tests: defaults read when [Snapshot] block omits the fields, overrides flow through to `cfg.snapshot_baseurl`, `Mirror.with_snapshot(baseurl=...)` actually uses the passed value.  266→269 tests; mypy + ruff still green.)*  Note: snapshot pinning is still Debian-specific — Ubuntu has no equivalent service, so under COMP-11 a non-Debian build would set `[Snapshot] Enabled = false`.  Cluster: COMP-11 + HK-05 = "distro-portability" remains. |
| ARCH-14 | P3 | done | **Holistic TUI revision — RHEL/Claude-CLI inspired layout, full keyboard surface, per-stage routing, render-time wrap, event-dispatcher rewrite.** *(7-phase initial revision + clean-slate dispatcher rewrite + cutover, 2026-05-24.  Initial revision commits `2bf01d1` (P1) → `4d689d4` (P2) → `d53d943` (P3) → `29077ab` (P4) → `29fc856` (P5) → `728c92d` (P6) → `4bb37fe` (P7).  Status-bar color fix + help layout cleanup `9d345d1` + `d7ff9ea`.  v2 dispatcher rewrite (side-by-side via `--tui-v2`): `c23fa9c` (P0-P8 — 10-file package), `4432c4e` (legacy proxy methods + widget idle cap), `aa0ce5c` (color resolution + editor-keys-while-prompting fixes).  Cutover (rename `tui_v2/` → `tui/`, delete legacy `scripts/tui.py`): `055dd89`.  **P1 — layout reshape**: `FOOTER_HEIGHT` 5 → 2; row 0 = prompt, row 1 = single-line reverse-video status bar (palette-independent via `curses.A_REVERSE`, no colored bg).  Tab content region grows to `N-2` rows.  Banner left, tab tags center-left (active = `[name]` + `A_BOLD`, inactive = ` name `), resources + key hint right.  **P2 — scrollback**: per-tab `scroll_offset` (0 = at-bottom sticky; positive = scrolled away).  `MAX_BUFFER_LINES = 10000` cap with front-drop on overflow; `scroll_offset` follows shifted indices when the cap fires.  Single `_append_lines(tab, lines)` mutator shared by `_log` and `print`.  **P3 — editable cmdline + history**: `_Commands._cursor` repurposed from horizontal-scroll offset to edit position.  New helpers: `insert_at_cursor` / `delete_before_cursor` / `move_cursor_{left,right}` / `history_{prev,next}` / `set_current` / `list_names`.  `_history_idx` + `_history_draft` track in-flight history walking (Down past newest restores the stashed draft).  Semantic flips in `_handle_key`: ← / → move edit cursor (was horizontal-scroll), ↑ / ↓ walk history (was tab scroll), PgUp / PgDn scroll the active tab (new role).  `_refreshfooter` row 0 renders the line with the cursor visible as an `A_REVERSE` cell on the char at edit position; long lines pan into view.  **P4 — Fn-key tab switching + per-stage tabs + add_tab API**: six default tabs created on init (`console`, `log`, `cache`, `build`, `chroot`, `iso`) — insertion order drives F1..F6 mapping.  `KEY_F(n)` → activate n-th tab; out-of-range no-op.  Bare `\x1b` swallowed (so Alt+Fn on terminals that split into ESC + Fn collapses to a clean tab switch on the next keystroke).  Tab key retires the old cycle binding (reserved for P5 completion).  New public `Tui.add_tab(name)` for runtime tab creation (idempotent; not mirrored on headless `Cli` — callers branch via `hasattr`).  `info` command now lists the new bindings.  **P5 — Tab-key command-name autocomplete**: `_complete_command` handles `\t`.  Restricted to the command-name position (no space yet in `current`); past that Tab is a no-op (arg completion deferred).  0 matches → silent; 1 match → `set_current('<name> ')` with trailing space hint; >1 matches → print candidates to the console tab without mutating `current` so the user keeps typing to narrow.  **P6 — per-stage logger routing + cleanup**: `_LogTabHandler.emit` now dispatches through `Tui._log_to_tab` and maps logger name → tab (`athena` → `log`; `athena.<stage>` → `<stage>` if it exists; unknown → `log` fallback).  Infrastructure only — no caller migration; the moment a future patch introduces `getLogger('athena.cache')` at the top of `cache.py`, those records start landing in the cache tab.  Cleanup: dropped the deprecated `cursor` field from `_TabEntry` and all write sites (`scroll_offset` is the sole driver); deleted dead `_cmd_input_width` (made dead by P3 cmdline render); tightened the resize clamp to `len(buffer) - content_rows`.  Observability: hoisted `LOGGER_NAME` + module `_logger`; replaced 6 silent `curses.error` swallows in resize/shutdown paths with `_logger.debug(...)` breadcrumbs (hot-path swallows in `_safe_addstr` / inner draw loop / panel ops left alone by design).  Module docstring documents the full ARCH-14 binding surface and the routing rules.  **Tests**: 20 new across the 6 phases (459→483).  Zero `cli.py` changes throughout — the 16 headless-mode gate tests at `tests/test_module.py:10957-11838` keep `Cli`'s contract enforced.  **P7 — render-time line wrap**: char-based wrap of long buffer entries at the active window width.  Resize automatically reflows on the next redraw (no buffer mutation; wrap recomputes every frame).  `scroll_offset` semantics shift from logical-line units to DISPLAY-ROW units throughout (`_append_lines`, MAX_BUFFER_LINES drop, resize clamp, PgUp clamp, `console_trim_to`).  PgUp can now scroll through wrapped chunks on narrow terminals (was previously capped at `len(buffer)-1` which under-counted).  New static helpers on Tui: `_wrap_line(text, width)`, `_entry_display_rows(text, width)`, `_total_display_rows(tab, width)`.  Backwards-compat fallback in `_LogTabHandler.emit` so test stubs without `SEVERITY_*` fall back to ERROR/WARNING/INFO direct calls.  5 new tests (483→488).  **Polish commits**: status bar switched from `A_REVERSE` to fixed bright color pair (`COLOR_FOOTER`, WHITE on BLUE, RHEL convention) because reverse-video blended badly with themed/transparent terminals; help command tips lost the leading `\t` artifact and shortened to fit ≤ 80 cols.  **Clean-slate dispatcher rewrite (the v2 redesign)**: after operator feedback that the original `_run` loop's `napms(10)` busy-poll + `_dirty`-or-`_widget` redraw gate + 3-LifoQueue/6-lock coordination dance had grown rusty, I designed a from-scratch event-dispatcher architecture: **one event queue, one consumer (the dispatcher), one state owner, zero locks.**  Producers (curses input pump, shell, logger, psutil, Console.print, Prompt, widgets) all `dispatcher.post(event)`; dispatcher pops sequentially and mutates State.  Renderer is pure: `render(state, stdscr)` reads only its arg, never mutates.  **Adaptive idle**: dispatcher blocks on `events.get(timeout=next_animation_deadline())` — no animations → 1 s wait, ProgressBar alive → 100 ms cap (legacy widgets don't post per-step), Spinner alive → its 10 fps deadline.  Idle CPU drops from ~100 Hz polling to effectively 0.  **Prompts use Futures**, not Conditions+LifoQueues+separate-value-queue: `request_prompt(msg)` returns a Future; dispatcher fulfills on Enter.  Nested prompts just stack via the Future contract; no coordination primitives needed.  **Package layout** (`scripts/tui/`, 11 files, ~1050 LOC):  `events.py` (14 frozen Event dataclasses) · `wrap.py` (pure char-wrap helpers) · `state.py` (TabState/CmdLine/State, single-owner data, no curses) · `dispatcher.py` (event loop + per-event handlers; sole state mutator) · `render.py` (pure renderer) · `input_pump.py` (blocking getkey thread) · `widgets.py` (ProgressBar/Spinner with `next_frame_at`) · `facade.py` (Console + Prompt thin shims) · `logging_bridge.py` (logging.Handler → LogEvent, dual-backend duck-typed) · `tui.py` (top-level Tui wires it together) · `__init__.py` (full public-API re-exports).  **Comparison**: locks 6→0 (Queue's internal lock only); sync primitives 3 LifoQueues + Conditions + Locks → 1 Queue + Futures; idle CPU ~100 Hz busy loop → ~1 Hz adaptive; animation CPU ~100 Hz always-when-widget-alive → per-widget opt-in via `next_frame_at`; threads 3 → 4 (one new dedicated curses input pump).  **Cutover** (commit `055dd89`): `scripts/tui.py` deleted (2131 LOC), `scripts/tui_v2/` renamed to `scripts/tui/`, `--tui-v2` / `ATHENA_TUI_V2` flags removed from `build-system.sh` + `build.py`.  Public surface preserved verbatim so all ~290 `console.print` + ~10 `Prompt` + every `COLOR_*` / `PROMPT_*` import keeps working unchanged.  `Console` facade gained legacy lowercase aliases (`info`/`warning`/`error`/`mark`/`trim_to`).  `setup_logging(backend)` is duck-typed: has `.post` → Dispatcher path (events); else → Cli-like path (direct method calls); accepts `tui=` and `dispatcher=` kwargs for legacy callers.  ProgressBar/Spinner resolve `tui_instance` (legacy pattern) so they work in both Cli (headless, prints `[start]`/`[done]` markers) and Tui (curses, posts WidgetAdd/Remove) modes with zero per-backend wiring.  **Tests**: ~840 lines of obsolete tests deleted (3 `test_console_*` legacy ctor signature, 10 `test_logger_*`/`test_setup_logging_*` legacy contract, 25 `test_tui_*` ARCH-14 P1-P7 source-inspection — file they inspect is gone; contracts they pinned are now enforced by the 9 `test_v2_*` tests against the new package).  Net repo delta: −5396 lines.  459/459 tests pass; ruff + mypy clean.)*  Deferred follow-ups: per-stage logger migration (`getLogger('athena.cache')` etc.) — infrastructure landed in P6, the actual rename pass across `cache.py` / `build.py` / `chroot.py` / `iso.py` is a separate ticket each stage can do independently; mouse support; theming; arg-level autocomplete. |
| COMP-06 | P1 | done | **Package-set selector TUI** — interactive `select` command replacing hand-editing of `config/pkg.list`. *(landed 2026-05-24, commit `c852ad2`; builds on the ARCH-14 event-dispatcher TUI.  **Dispatcher additions** (`scripts/tui/`): per-tab key interceptor — `set_key_interceptor(tab, fn)` / `clear_key_interceptor()`, consulted FIRST in `_on_key` but ONLY while the owner tab is active; `fn(key)->bool` returns True to swallow, False to fall through (so F-keys still switch tabs and the operator can always leave the interactive tab).  Two new events: `TabRemove` (drop a tab; re-activate first remaining if it was active) and `SetTabBuffer` (replace a tab's buffer wholesale for controller-rendered views).  Tui proxies: `add_tab` / `remove_tab` / `activate_tab` / `set_tab_buffer` / `set_tab_key_handler` / `clear_tab_key_handler` / `viewport_rows` / `attr_reverse` / `attr_color`.  **Controller** (`scripts/select_packages.py`, new ~430 LOC): `SelectPackages` holds an editable model (group → list of `[name, selected]`) loaded from pkg.list via the existing `parse_pkg_list_groups` + `parse_pkg_list_group_meta` parsers (selection defaults True for every existing entry).  Renders into a dedicated `select` tab: title + selected/total counts, group headers (with `## Description:`), and per-package rows showing `[*]`/`[ ]`, name, installed size, direct-dep count, transitive-closure footprint, and the one-line apt description; cursor row in `A_REVERSE`.  Transitive closure is a first-provider BFS over Depends + Pre-Depends — approximate (picks first cache candidate per name, ignores alternatives/conflicts) but cheap + prompt-free; computed in a background thread per focused row and cached, re-rendering when done (shows `…` until ready).  **Keys**: ↑↓ move, PgUp/PgDn page, Space toggle, `a` add (input prompt run on a worker thread so `request_prompt` doesn't re-enter the dispatcher and deadlock), `d` drop, `[`/`]` prev/next group, `s` save, `q` quit (prompts if unsaved).  **`write_pkg_list`**: serialises back preserving group order + `## Description:` comments + within-group ordering, dropping unselected entries and appending newly-added ones to their group; flat (legacy, base-only, no description) files round-trip flat with no spurious `[base]` header; atomic via temp + `os.replace`.  **`cmd_select`** gates on `cache_ready` (needs metadata) and on the curses backend — `set_tab_key_handler` absent on the headless `Cli` → prints a hint to edit pkg.list by hand instead.  6 new tests (459→465): load-all-selected, toggle+save round-trip, add-appends-to-group, flat-file round-trips without header, key-toggle sets-unsaved + re-renders + F-keys fall through, approx-closure BFS sums a first-provider chain.  ruff + mypy clean.)*  Deferred: full-resolver closure (vs the BFS approximation) — would need a prompt-free DependencyTree run per candidate; arg-level completion in the add prompt; multi-select / bulk-toggle. |
| ARCH-18 | P2 | done | **Audit-command refactor — directory-scope discipline (`source/` vs `repo/`), one-stop pre-ship gate, single mutator per side.** *(5-phase landing 2026-05-23; commits `a8398ac` (P1) → `b8b1237` (P2) → `e28346b` (P3) → `c2a45c0` + `4c40cc0` (P3 fixups) → `c97abd9` (P4) → `c3b0e3e` (P5).  **Motivation**: the pre-refactor surface had three commands all scanning repo/ for partly-overlapping quality dimensions (`repo audit` for dep closure + conflicts; `source verify` for content integrity + deps; `repo audit_nmu` for NMU residue), AND three source-side commands all asking variations of "is the build queue OK?" (`source audit`/`rescan`/`repair`).  Operator UX was fragmented; dep-resolution logic was duplicated between `source verify` and `repo audit` (the latter via `audit_dep_closure`); audit/repair couldn't settle in one cycle because 'repairable' didn't require .patchhash matching.  **P1 — renames (no behavior change)**: `source download` → `source sync` (+ new per-pkg `source sync <pkg> [force]`); `repo strip` → `repo repair strip`; `repo cleanup` → `repo repair cleanup` (umbrella `cmd_repo_repair` sub-dispatcher).  **P2 — source side**: `cmd_source_audit` becomes the READ-ONLY classifier (absorbs `cmd_source_rescan` — deleted).  New `_source_state(pkg, src)` helper returns one of 8 states (ok / no_pkgs / tunneled / fail / needs_sync / needs_build / stale_pass / repairable).  `cmd_source_repair` becomes the mutator — writes PASS for 'repairable', clears stale .result+.patchhash for 'stale_pass'.  Concise per-state failure list (wrapped names) in default mode; verbose adds per-name+subset detail.  **P3 — repo side**: `cmd_audit` absorbs both `cmd_source_verify` (deleted) and `cmd_audit_nmu` (deleted) as two new sections: CONTENT INTEGRITY (per-cohort DebFile control match + dep resolution against the matching RepoState) and NMU RESIDUE.  `repo audit quick` skips the slow (~30s) integrity scan.  **P3 fixup**: 'repairable' state now requires .patchhash matching current patch_set_hash as PROOF that on-disk binaries reflect current patches — without it → 'needs_build'.  Closes the audit/repair ping-pong (was: stale_pass → cleared → repairable → repair-writes-PASS-over-stale-binaries; now: stale_pass → cleared → needs_build → operator runs source build → ok).  **P4 — source fork**: new `source fork <pkg>` command absorbs `repo reload` (deleted) and adds two genuinely new features: create-from-upstream (downloads .dsc + tarballs, runs `dpkg-source -x` to extract into `fork/source/<pkg>/`) and enable/disable toggle via `.disabled` marker file (fork_mirror skips marked trees).  **P5 — chroot gate**: `cmd_build_chroot_{live,installer}` now runs `_preflight_audit_source` (new — classifies via `_source_state`, aborts with prompt on hard findings: needs_build/needs_sync/stale_pass) BEFORE the existing `_preflight_audit_repo`.  Operator can bypass both with `chroot build {live\|installer} no-gate` for emergency / iteration loops; bypass emits a visible warning.  **Final command surface**: `source {sync,build,audit,repair,fork}`, `repo {audit,repair,index,tunnel}`, `chroot build {live,installer} [no-gate]`.  Removed: `source download`, `source rescan`, `source verify`, `repo audit_nmu`, `repo strip`, `repo cleanup`, `repo reload`.  463 tests pass; mypy + ruff clean across the chain.)* |
| ARCH-15 | P2 | done | Triage the mypy findings surfaced by ARCH-06's gate (advisory today: `continue-on-error: true` in `.github/workflows/ci.yml`) and flip the gate to blocking once the count is zero. *(landed 2026-05-14: cleared 115 → 0 over 4 commits.  Round 1 hit the trivial buckets: var-annotated (13), name-defined (2), no-redef (2), override (1), call-overload (1).  Round 2 declared mixin self-attrs as class-body type stubs on `_ChrootMixin` / `_IsoMixin` / `_DepDriftMixin` (the documented mypy mixin pattern, -32), bumped pyproject `python_version` 3.10 → 3.11 so `hashlib.file_digest` resolves, added explicit `import docker.errors`, and added `type: ignore[attr-defined]` on `gnupg.GPG` (-9).  Round 3 renamed shadowing loop variables in `dependencytree.parse_dependency`'s dep-tuple iterations (`_pkg` → `_alt_dep` / `_dep`, -13), added `_footer is not None` asserts in `tui._refreshfooter` and the draw loop (-13), and typed `tui_instance` / `setup_logging`'s `tui` param as `Any` to accommodate both Tui and Cli backends without a circular import (-2).  Round 4 added missing Optional-narrowing asserts in `cache._chosen_opener`, `buildcontainer.build`, `build.py` patch refresh + tunnel paths, plus an explicit `Dict[str, Tuple[Callable[..., None], str, str]]` annotation on `print_commands.CATEGORIES` so `handler(...)` resolves.  Dropped `continue-on-error: true` from `.github/workflows/ci.yml`; mypy is now a hard gate.  Ruff still clean, tests still 266/266 throughout.)*  Breakdown by root cause: **(a) Mixin self-attrs — 29 errors** in `chroot.py` (22) and `iso.py` (7).  `_ChrootMixin` / `_IsoMixin` reach for `self._dir_chroot`, `self._password`, `self._dependencytree`, `self._dir_image`, `self._config`, `self._dir_repo`, `self.strip_build_version` — set on the `BuildSystem` composer that mixes them in, but mypy doesn't know that.  Fix: add a `class _BuildSystemProtocol(Protocol):` in each mixin file declaring the attrs the mixin reads, and type the mixin as `class _ChrootMixin(_BuildSystemProtocol):`.  **(b) Optional deref — 20 errors** in `tui.py` (13: `_pad` typed `window \| None` then accessed without a guard), `build.py` (5: `Cache \| None` / `DependencyTree \| None` / `BuildContainer \| None` accessed in `cmd_*` without prerequisite-flag asserts), `buildcontainer.py` (1: docker client), `dependencytree.py` (1).  Fix: either change the type to non-Optional once construction is past, or add `assert self.X is not None` at method entry where a `BuildFlags` prerequisite already guarantees it.  **(c) Real type bugs — 3 errors**.  `dependencytree.py:302,330` assigns a tuple to a variable typed `Package` — needs investigation, may be a real bug.  `buildsystem.py:175` assigns `list[str]` to a `str`-typed variable.  **(d) Missing list annotations — 3 errors** in `cache.py:91,92` (`pkg_list`, `src_list`) and `package.py:420` (`patch_list`).  Trivial: add `: list[str]` (or correct element type).  **(e) Re-assignment — 2 errors**.  `cache.py:113` `_config_valid` defined again after `:44`; `package.py:478` `files` defined again after `:404`.  Either drop the redundant assignment or factor.  **(f) Misc — 7 errors**.  `package.py:114,410` use `utils.X` without `import utils` (the module sees `utils` only via the `from utils import …` shape elsewhere); `utils.py:886` calls `hashlib.file_digest` which is 3.11+ — bumping mypy `python_version` to `"3.11"` would silence this *if* we also bump the runtime floor; otherwise wrap with a fallback; `cache.py:237` calls a `None`-typed callable.  Suggested order to land: (d) + (e) first (cleanup), then (a) (one-shot Protocol stub per mixin), then (b) (case-by-case), then (c) (potential bugs — review carefully), then (f).  Once the count hits zero, drop `continue-on-error: true` from the mypy job in `.github/workflows/ci.yml` so future regressions block PRs. |
| ARCH-16 | P2 | done | **Per-package `DEB_BUILD_OPTIONS` / `DEB_BUILD_PROFILES` overrides** in `config/build.conf`.  Today `[Source] BuildOptions` / `BuildProfiles` are global (`nodoc, nocheck`) and applied to every source build via `buildcontainer.py:253-256`.  Some packages need per-package tuning that doesn't fit the global axis — currently the workaround is a per-source `debian/rules` patch (cf. firefox-esr 9002 appending `parallel=1` for amd64).  Add a `[Source.<pkg>] BuildOptions = …` / `BuildProfiles = …` block (mirroring how `[Snapshot]` reads optional sub-keys); `BuildContainer.build()` checks for a per-source override and falls back to the global value when absent.  Surfaced 2026-05-15 when firefox-esr needed `parallel=1` to keep cc1plus under the 16 GB OOM threshold; the patch-based workaround is fine for one package but a wider mem-budget tuning surface (or any per-package profile tweak) would benefit from operator-facing config.  Scope: ~50 LOC across `utils.BuildConfig`, `buildcontainer.py`, plus 3-4 unit tests for the override-lookup and fallback paths. **Closed 2026-05-28**: new `[Source.<pkg>]` sections parsed in `utils.BuildConfig` into `build_options_per_pkg` / `build_profiles_per_pkg` dicts; new accessors `config.build_options_for(pkg)` / `config.build_profiles_for(pkg)` return the per-pkg override or fall back to the global value.  `buildcontainer.build()` updated to consult the accessors between the explicit `[bracket]` override and the global default (3-layer precedence).  Empty `[Source.]` section name rejected at config load with a clear error.  `config/build.conf` carries a documented commented-out firefox-esr example.  3 new tests: per-pkg override + asymmetric fallback (options only / profiles only); buildcontainer source-grep pinning the accessor calls; empty-section-name rejection.  569/569 tests pass. |
| ARCH-17 | P3 | done | **Type-annotation coverage pass for support modules.**  Consolidation audit 2026-05-21 found uneven mypy coverage: mypy is clean (0 errors after commit `a1e193c`) but several modules have many functions with no arg/return annotations, so the bodies skip `--check-untyped-defs` and bugs land late.  Top offenders by unannotated function count: `iso_installer.py` (13/27 — incl. `build_installer_iso`, `_sudo`, `_prepare_staging`, `_find_kernel`, `_stage_kernel`, `_build_initrd`, `_stage_*`, `_parse_deb_filename`, `_select_pool_files`, `_stage_pool`); `utils.py` (6/29 — incl. `version_no_epoch`, `_extract_version`, `_rewrite_sibling_idiom_in_text`, `_collapse_sibling_pair`); `fork_mirror.py` (6/20); `installer_chroot.py` (5/12); `repo_audit.py` (3/9).  Scope: pure annotation pass, no behaviour change, no new tests required (mypy + ruff already gate).  Doing it makes future-mypy stricter mode (`--check-untyped-defs` on by default) tractable.  Pair with: enabling `[mypy] check_untyped_defs = True` once coverage is >90%. **Closed 2026-05-28**: 34 unannotated functions across the 5 top-offender modules now carry return + argument annotations (AST-counted: 0 remaining).  TYPE_CHECKING blocks added to forward-reference utils.BuildConfig / dependencytree.DependencyTree / buildcontainer.BuildContainer across the cross-module surfaces (avoiding runtime import cycles).  1 new policy-enforcer test (`test_arch17_top_offender_modules_fully_annotated`) walks the 5 modules' ASTs and asserts every function has both return-type and arg-type annotations — guards against coverage regression.  3 pre-existing mypy errors (apt_repo type-narrowing across boolean guard + utils.py:682 same pattern) are LEFT IN PLACE — they were not caused by this pass and fixing them is out of ARCH-17 scope (the ticket explicitly says "pure annotation pass, no behaviour change").  ruff + tests stay green. |

## 5. Tests & CI — P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| CI-01 | P1 | done | **Installer-smoke harness wired into CI + the publish-side release index that feeds it.**  The QEMU smoke harness (`tests/installer_smoke/run.py`, quick = "does d-i boot cleanly?") existed but nothing ran it automatically — a boot regression sailed past CI (all green on lint+unit, none of which boot the artifact).  The blocker was "where does CI get the ISO": ISOs were built locally and never published anywhere fetchable, and CI can't build them (multi-hour pipeline, Docker-in-Docker, tens of GB).  Designed + built in two stages 2026-06-13.  **Stage 1 (`965d294`):** `mirror publish` now ships a coherent release — apt repo + install media + a static `index.html` / `releases.json` at the pool root (`scripts/release_index.py`, a pure generator; the smoke job reads `releases.json` for the installer ISO url + sha256).  Gate-with-escape: publish REFUSES unless the current version+snapshot live+installer ISOs exist in image/, `mirror publish --no-iso` bypasses for incremental publishes (reclaim/hotfix/stale-index).  ISOs push to `<pool>/iso/` (immutable by name), index pair overwrites.  **Stage 2 (*`<this commit>`*):** `.github/workflows/installer-smoke.yml` — nightly cron + `workflow_dispatch`; resolves the ISO from `${vars.ASGARD_MIRROR_URL}/releases.json` (or a dispatch `iso_url` input), downloads + `sha256sum -c` verifies, runs `run.py --quick` for BIOS and EFI, gates on the exit code; uploads the serial logs as artifacts.  Runs under TCG (no /dev/kvm on GitHub runners — fine for quick; --full deferred).  No-ops green until `ASGARD_MIRROR_URL` is set, so the nightly lights up the moment a release is published.  **Stage 3 (*`<this commit>`*):** new `prep-mirror.sh <ssh-url> <ssh-key> [--check]` codifies the mirror-host bootstrap so `mirror add` registers cleanly — it SSHes in and brings the host to a serving state idempotently with a four-way state machine: PREPARED (marker present → no-op), ADOPT (existing repo, no marker → verify + ensure dirs + write marker, e.g. the already-hand-set-up 140.245 host), FRESH (empty → install nginx + create the `dists`/`iso`/`<root>-coord` tree + configure serving), UNEXPECTED (foreign content / bad marker → dump details, change NOTHING, exit loud — never clobbers what it didn't create).  Writes an HTTP-served `mirror-info.json` marker; `--check` is a read-only dry-run; clean boxed banner UI.  `cmd_mirror_add` gains Step 5b (`_mirror_is_prepared`): probes `<public_url>/mirror-info.json` and REFUSES an unprepared ssh mirror with a pointer to prep-mirror.sh (skippable `--no-probe`).  Tests: `test_release_index_manifest_and_html`, `test_release_iso_descriptors_finds_and_reports_missing`, `test_mirror_publish_release_gate_and_push_wired`, `test_installer_smoke_workflow_contract`, `test_cmd_mirror_add_refuses_unprepared_mirror`, `test_prep_mirror_script_contract`.  Deferred (as designed): `--full`-mode integration once quick has been stable a week.  Suite 1117/1117, ruff + mypy clean. |
| AUDIT-01 | P1 | done | **Identity-leakage scanner — generalize CONF-10 candidate (a) into a CI-gated audit.**  *(Closed 2026-05-31, simultaneous with CONF-10.)*  Shipped as `scripts/identity_scan.py` + `audit/identity-allowlist` + the S1/S2/S3 integration points described in CONF-10's closing note (see section 2).  Originally filed as a parallel ticket pending CONF-10's candidate selection; once CONF-10 picked candidate (a), AUDIT-01's deliverable folded into the same code path.  Tests run in the existing test_module.py CI loop; no separate `test_no_unallowlisted_identity_leakage` wrapper needed because each stage's gate (`test_identity_scan_fork_mirror_gate_fails_build`, `test_audit_chroot_hooks_fail_on_hard_unpooled`, `test_audit_staged_iso_fails_on_debian_leak`) pins its own contract.  See CONF-10 row in section 2 for full diff + token regex list + the three-stage integration. |
| TEST-01 | P1 | wontfix | Adopt `pytest` and split `tests/test_module.py` (8 tests) into `tests/test_<module>.py` per module — currently every test is in one file with a hand-rolled runner. *(decision 2026-05-09: keep tests in a single `tests/test_module.py` with the hand-rolled runner.  Rationale: the runner is ~80 lines and zero dependencies; pytest would add a build-time dep, change the import shape, and require either keeping the hand-rolled runner alongside it or rewriting CI invocation.  64 tests in one file is well below the threshold where a single file becomes hard to navigate, and `grep -n "^def test_"` is a perfectly good index.  **Revisit threshold: when the test count exceeds 150.** Below that, single-file stays.)* |
| TEST-03 | P1 | wontfix | Add unit tests for `BuildSystem.get_install_sequence` and the unpack-forest logic — circular pre-deps, missing-from-selected, gcc-base bootstrap, edge cases. *(closed as irrelevant 2026-05-10: ARCH-12 replaced `get_install_sequence` with `_compute_install_batches` (Kahn's algorithm over Pre-Depends ∪ Depends, with libc-seed cycle break and a terminal "force-depends" cycle batch); HK-01 then deleted `get_install_sequence` entirely along with `utils.Tree`/`utils.Node`.  The replacement already has 10 unit tests in `tests/test_module.py` (`test_compute_install_batches_*`) covering linear chain, independent packages, fan-out, Pre-Depends ∪ Depends union, libc-seed cycle break, self-deps, cycle-emitted-as-forced-batch, cycle-with-pre-depends-chain-splits, acyclic-then-cycle, external-deps-filtered.  This ticket's stated coverage is therefore already in-tree — under the new function name.)* |
| TEST-04 | P1 | done | Add tests for `strip_build_version` covering `+bN`, `+deb12uN`, mixed-suffix, multi-`_` filenames. The smoke driver has its own (different) regex that must match production. *(landed jointly with STA-15 — 7 tests in `tests/test_module.py`: trailing `+bN`, `+debNuM` preservation, mixed `+debNuM+bK`, upstream-embedded `+bN`, `.udeb` extension, no-binNMU round-trip, malformed-input rejection.  Smoke-driver-vs-production divergence eliminated because both call sites now route through the same `utils.strip_build_version`.)* |
| TEST-06 | P1 | done | GitHub Actions (or equivalent) CI: lint, unit tests, smoke driver against the in-tree fixture. No CI exists today. *(lint + unit tests landed via ARCH-06 — `.github/workflows/ci.yml` runs ruff + mypy + the unit-test runner on every push/PR; tests use system Python with apt-installed Debian-only deps for `apt_pkg`/keyring coverage.  The "smoke driver against the in-tree fixture" sub-bullet is blocked on TEST-05 (the in-tree fixture itself doesn't exist yet) — wire the smoke driver into CI when TEST-05 lands.)* |
| TEST-05 | P1 | done | Add a fixture `Cache` built from a tiny in-tree `Packages` / `Sources` blob so cache/dep-tree tests run offline. *(Landed 2026-05-23 as `_make_offline_cache(tmpdir, packages={}, sources={}, udeb_packages={})` + `_make_offline_mirror()` helpers in `tests/test_module.py`.  Approach: write Packages/Sources blobs to disk under a tempdir, populate `Cache.mirror_cache_files` / `mirror_udeb_cache_files` directly, then invoke the real `Cache.__build_cache(arch)` via the name-mangled accessor.  Bypasses `Cache.__init__`'s mirror downloads + GPG verify but exercises the same parsing + hashtable-population path so future refactors are covered.  Smoke test `test_offline_fixture_builds_a_valid_cache` pins the base case (one mirror, one binary, one source, no fork).  Used by TEST-02 (parse_dependency) and TEST-10 (collision-gate integration) tests landing in the same change.)* |
| TEST-02 | P1 | done | Add unit tests for `DependencyTree.parse_dependency` — auto-pick path, alt-deps, virtual-package resolution, version constraint propagation. *(Landed 2026-05-23 as 10 tests under the TEST-02 banner in `tests/test_module.py`.  Coverage added on top of the existing `test_parse_dependency_reuses_lookahead_for_multi_version_same_name` + `_auto_pick_candidate_*` tests: empty-name guard (returns None, no crash on cache lookup), no-candidates Case II (returns None), single-candidate Case III, already-selected short-circuit, Provides-virtual-alias registration (`gawk` provides `awk` → both names in selected_pkgs), recursive dep propagation (foo→bar walks bar's deps + populates depends_on/depended_by), cycle protection (A→B→A doesn't infinite-loop), Recommends inclusion gated on `select_recommended` flag, alt-deps "already-selected wins" path, alt-deps "default to first" fallback.  Helper `_make_parse_dep_tree(cache_pkgs, *, select_recommended=False, auto_pick_highest=False)` + `_ParseDepPkg` minimal Package surface keep each test compact.  Removed a tautological test that just set the auto-pick flag to False and asserted it was False (`test_dependency_tree_default_does_not_auto_pick_across_names`); the real signature check is in the surviving `test_dependency_tree_constructor_accepts_auto_pick_flag`.)* |
| TEST-07 | P2 | wontfix | Integration test: `cmd_auto_run` against a fixture mirror inside Docker, asserting the chroot 8-check verifier returns all green. Tag as `slow` so it only runs nightly / on demand. *(Closed wontfix 2026-05-23 — superseded by COMP-12 Phase F's `tests/installer_smoke/` harness, which is a stronger end-to-end gate than this ticket proposed.  TEST-07 wanted: run `cmd_auto_run` against a fake mirror inside Docker, assert the 8-check chroot verifier passes.  The installer_smoke harness does: drive an actual install through QEMU (BIOS + EFI) against a real-shape ISO + preseed.cfg, scan the serial log for fatal patterns, fail on any.  Same coverage of "the build pipeline produces a working artifact end-to-end" but exercised against the real install path (including grub-installer, partman, base-installer, finish-install) rather than a synthetic chroot-verify.  Building a parallel Docker-fixture path would duplicate effort for less coverage.  If a true unit-style integration gate is wanted later — e.g. "every chroot bit flips True without sudo" — file a new ticket; the original TEST-07 framing is obsolete.)* |
| TEST-08 | P2 | done | Property test for `Mirror.with_snapshot` — no-op on `None`, idempotent, preserves `suite`. *(Landed 2026-05-23 — 3 tests on top of the existing `_returns_new_instance_untouched_original` / `_uses_passed_baseurl` / `_skips_file_scheme` coverage: `_none_returns_self` (ts=None returns same instance — call sites can invoke unconditionally), `_is_idempotent` (single-step result is equal across invocations; pinned the single-step property explicitly because the chain m→once→twice DOES double-baseid today and that's by design — re-applying a snapshot to an already-snapshotted mirror is not a documented call pattern), `_preserves_suite_and_component` (snapshot wrapper rewrites baseurl + baseid only — suite, component, arch, suffix, release stay).  Tests are property-style — assertions are over invariants the consumer relies on rather than specific URL strings, so a future baseurl convention change doesn't ripple here.)* |
| TEST-09 | P2 | done | **De-brittle version-pinned test assertions.** *(Closed 2026-05-23 with audit revision + one strengthened test.  **Audit revision**: the original ticket's concrete line refs (1885/1914 for `linux-image-amd64_6.1.170-3`, 2749 for kernel ABIs, 299 for `openssh_9.2p1-2+deb12u9.dsc`, 1633/1661/1683 for `Athena 0.1 amd64 INSTALLER`) turn out to be **test-owned fixtures, not pins to actual repo state**.  Each test writes the literal into a temp file or constructs a fake stanza, then asserts the helper-under-test parses/filters it correctly — the test owns both sides of the equality.  None will rot on snapshot advance or version bump.  The ticket author misread the audit pass.  **Concrete change**: replaced the genuinely-trivial `test_mirror_repr_does_not_crash` (called `repr(m)` with no assertion) with `test_mirror_repr_includes_identifying_fields` — same path, but asserts id/url/suite/component are in the output so a Mirror dataclass field rename or repr-format refactor is caught.  **No other tests touched** — the audit was wrong about them.  Left in place: future genuine version-pin rot in tests will be flagged by snapshot-advance runs and addressed with regex assertions per the ticket's "fix shape".)* |
| TEST-10 | P2 | done | **Coverage gaps in load-bearing helpers.** *(Landed 2026-05-23.  5 new tests across signing + cache: (a) `signing.get_key_info` `shutil.which('gpg') is None` branch (separate from the existing missing-homedir branch — both paths now pinned, regression in either surfaces a single failure rather than a confusing skip).  (b) `signing.generate_key` no-gpg short-circuit returns False without subprocess.run (callers see actionable failure, no FileNotFoundError).  (c) `signing.generate_key` export-step failure after successful gen — mocked subprocess.run such that `--gen-key` succeeds + `--export` fails; pins both the False return AND the call ordering (gen-first-then-export, so a refactor reversing the order leaves a half-created key on disk).  (d) `cache.__build_cache` integration test: a fork mirror + an upstream mirror in the same Cache, upstream version `> fork`, drives the real ingest path that populates `_upstream_collisions` from on-disk Packages records — pins the gate firing AND the collision-tracking side-channel.  Complements the existing `_make_collision_cache` unit tests of the gate function in isolation.  (e) Fork-supersede happy path: fork dominates → gate passes, upstream record is dropped from `package_hashtable`, supersede is logged in `_upstream_collisions`.  All 5 use the `_make_offline_cache` fixture landed under TEST-05.)* |

## 6. Documentation — P1 / P2

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| DOC-01 | P1 | done | `README.md` ends mid-section (`### RHEL/Debian/Ubuntu` / `### Stiched together` are empty headings; “Building Image / Intro” is just `...`). Finish the operator guide: prereqs, first run, common failure modes, where logs live. *(filled the three empty sections in-voice with the existing background prose: RHEL/Debian/Ubuntu philosophical comparison + practical port note for COMP-11; "Stiched together" as the bridge from concepts to practice; Building Image / Intro lays out the eight-stage pipeline.  Added four operator-facing sub-sections — Prerequisites, First run, Where logs live, Common failure modes (9 entries with cross-refs to STA-02/03/07/10, ARCH-12/14, SEC-04, COMP-03).  Closing footer points future-me at DOC-06 to keep this in sync.)* |
| DOC-07 | P2 | done | **`docs/branding-methodology.md` — policy reference for branding decisions.**  *(Focused approach landed 2026-05-22: five principles up front (P1 never patch for branding, P2 own the data, P3 accept residue, P4 same-name fork is heaviest, P5 companion+P/C/R is lighter), decision tree for new surfaces, four d-i exemplars (Kali / Devuan / Parrot / Pop!_OS — the contrarian), eight-derivative runtime-system matrix, Athena's current pattern catalogue with one row per branded surface, five anti-patterns we reject, irreducible-residue table with rationale.  Pairs with fork/source/README.md (mechanism layer) — this doc is the policy layer.  Anchors the COMP-01f decision to do installer branding via debconf-overrides + companion theme udeb instead of cdebconf source patches.)* |
| DOC-02 | P1 | done | A `docs/architecture.md` describing the pipeline stages and `BuildFlags` contract. Today this knowledge is only in commit messages and inline docstrings.  **Closed 2026-05-29**: high-altitude tour written.  Sections: three-layer identity (Athena / Asgard / thor), 8-stage pipeline table (cache build → cache parse → source sync → container init → source build → chroot build → chroot verify → iso build, plus repo index/publish as 9–10), BuildFlags contract with every flag mapped to set-by + required-by stages, module overview grouped by role (foundation / data layer / orchestrator / build execution / ISO+repo / operator surface), process invariants (self-contained pool, snapshot pinning, pristine versions, UPD-01 update layer, publish-before-prune, identity strip).  Cross-references `docs/pseudocode.md` (per-function detail), `docs/diagrams/build-fsm.png` (state-machine view), and the relevant project memories at every load-bearing point.  Deliberate non-goal: doesn't duplicate per-function detail — pseudocode.md owns that layer. |
| DOC-03 | P1 | done | A `docs/patching.md` formalising the `patch/source/<pkg>/<ver>/9001-*.patch` and `patch/{pre,post}-install/` conventions. The README has fragments.  **Closed 2026-05-29**: complete formalisation written.  Sections: decision shortcut (when to use source vs pre-install vs post-install vs preseed-instead), full source-patch convention (layout under `patch/source/<pkg>/<exact-version>/`, 9001-prefix numbering rationale, container-side `dpkg-source -x` + `/patch` bind mount + `patch -p1` loop with rationale for read-fresh-at-build-time), DEP-3 header rules (40-line window per `utils.check_dep3_header`, required Description + Origin/Author, soft warning vs gate, Author-first layout discipline per memory `feedback_dep3_header_40_line_limit`), snapshot-advance migration workflow (re-evaluate / copy / rebase, commit-message convention), what NOT to use source patches for (forks for identity, preseed for d-i answers, branding via companion udeb per `docs/branding-methodology.md`), pre-install layout + `_ChrootMixin.pre_install` walk, post-install layout + identity-strip discipline, the `patch/empty/` stub, commit conventions for adding/migrating/retiring patches. |
| DOC-04 | P2 | done | `docs/release.md` describing how to cut a derivative distro release once CONF-01..03 land (signing key, snapshot timestamp, pkg.list freeze).  **Closed 2026-05-29** (revised 2026-06-02 for MIRROR-01, commit `d0ec621`): operator runbook for release cuts written (CONF-01 and CONF-02 done; CONF-03 still open and called out as affecting patch *form* not the release flow).  Sections: preconditions checklist, 10-step sequence (pick snapshot → generate/rotate signing key → freeze pkg.list family → walk pipeline → verify chroot+repo+smoke → index → `mirror publish` → `mirror audit` + `mirror summary` + `mirror query` → tag + record + version-bump cycle), update-vs-release table (per-binary `+asg uN` derivation, per-stage `source sync → source build all → mirror publish` walk, no-new-ISO contract), common pitfalls (cache invalidation after snapshot change, stale pubkey on consumer side, rebuild-loop signatures, federation-gate BLOCK, COMP-01h real-hardware delta).  Cross-refs architecture.md, mirror-setup.md, branding-methodology.md, memory `project_upd01_update_architecture`. |
| DOC-05 | P2 | done | Move the inline "Installing Docker" block out of `README.md` into `docs/install-docker.md`.  **Closed 2026-05-29**: standalone doc carries the apt incantation (Debian + Ubuntu variants), the `usermod -aG docker $USER` step + group-cycle reminder, smoke-test command, notes on why distro packages are too old + why Engine not Desktop + which `docker-buildx-plugin` / `docker-compose-plugin` packages are habit-not-requirement.  README's Prerequisites bullet now links to the new doc; the standalone "Misc / Installing Docker" section replaced with a "Further reading" links list covering all the post-DOC-01..07 doc tree.  Resolves the "block at the bottom of the README that doesn't belong there" smell + frees the README to focus on getting-started narrative. |

## 7. Security & supply-chain — P0 / P1

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| SEC-01 | P0 | done | (Same as STA-01) Verify `InRelease` GPG signatures with a pinned keyring before trusting any mirror data. |
| SEC-02 | P1 | done | The Docker image grants `athena ALL=(ALL) NOPASSWD:ALL`. Acceptable for a build sandbox, but document the threat model and ensure the image is *never* exposed to a network the host doesn’t control. *(`docs/security.md` documents the trust model — build container is a privileged sandbox not a security boundary; `BuildContainer._guard_docker_server` refuses non-loopback / non-TLS daemon URLs with a pointer to the docs; Dockerfile comment explains the NOPASSWD line and points to the doc.)* |
| SEC-03 | P1 | done | Source archives from snapshot.debian.org are downloaded over plain `http://` (`Mirror.baseurl = http://snapshot.debian.org/archive`). Switch to `https://` everywhere — snapshot supports it. |
| SEC-04 | P1 | done | `systemd-firstboot --root-password=root` (`buildsystem.py:1254`). Replace with a generated random password printed to console and written to the ISO label, or force a reset on first boot. *(Live ISO scope: `iso.py` picks a random English first name from a 67-entry list at every ISO build, bakes it into the grub.cfg `username=` cmdline, surfaces it in the boot-menu title and a `<iso>.user` sidecar file. live-config's default password "live" is intentionally left — live ISO is a test surface, not a production system.  The installed-system root-password story belongs with COMP-01 (installer ISO) when that lands.)* |
| SEC-06 | P2 | done | The smoke driver and tests do not exercise GPG paths; once SEC-01 lands, add fixtures with a known-bad signature to confirm the verifier rejects it. *(landed alongside STA-01 — see `test_verify_inrelease_*` in `tests/test_module.py`)* |
| SEC-05 | P2 | done | The build container runs `apt-get install -y` on whatever the resolved build-deps are — without dep-graph review. Acceptable today, but add an opt-in “show me what is about to be installed” gate for hostile-mirror scenarios. **Closed 2026-05-28**: new `[Security] AuditBuildDeps = true|false` knob (default false → no behaviour change).  When true, `BuildContainer.build()` calls a new `_audit_build_deps_gate(src_pkg, plain_deps, or_groups)` BEFORE rendering the real install cmd; the gate spins up a transient container that writes the snapshot-pinned sources.list, runs `apt-get update -qq`, then `apt-get install -y --simulate <build-deps>`, captures stdout, prints it to the operator, and prompts y/n.  On 'n' or preview-infrastructure failure → returns False → build skipped.  On 'y' → continues with the real container build unchanged.  Source-of-truth `_render_install_cmd(plain_deps, or_groups, simulate=bool)` static helper now produces both the real install string AND the simulate preview from one rendering path (the previous f-string-into-shell pattern is gone, eliminating drift between the two).  Empty-deps short-circuit returns True without prompting (nothing to audit).  Documented commented `AuditBuildDeps = false` entry added to `config/build.conf` [Security] with a practicality note (800-source full builds would prompt 800 times; intended for targeted audits — switching mirrors, single suspect source, etc).  7 new tests: knob default-false + parse-true; render helper injects `--simulate` only when requested + empty-deps OK; build() source-grep pinning the gate call ordering (after dep enum, before any container.run); short-circuit when no deps; operator-decline → False; preview-infrastructure-failure → False.  576/576 tests pass. |
| STA-25 | P2 | done | **`repo repair cleanup` deleted files that still had live remote claims with no warning (publish-before-prune guard).**  The local prune of orphan-source / version-drift `.deb`s walked local repo state only — it could delete bytes a published claim on a mirror still names as live, violating UPD-01's publish-before-prune (the receiver side was protected by `b356e56`'s rsync exclude rules, but the local deletion was silent; own-claim bytes deleted before the deprecation/obsolescence is published are gone, and the mirror keeps serving a sha nothing reproduces).  **Fixed 2026-06-16** (*`<this commit>`*): new `_live_published_claim_filenames()` reads our local coord ledger (`config/coord/claims/*.jsonl`, no sig-verify — our own record) and, via the standard `iter_live_claims_by_filename` supersession fold, returns filenames covered by a genuinely-`published` claim (deprecate/obsolete/retract markers are prune signals → excluded).  `cmd_package_cleanup` intersects its obsolete targets with that set into `_claimed` (split `_claimed_drift` / `_claimed_orphan`): flagged in the dry-run report, and in `force` mode a dedicated YESNO fires BEFORE the generic delete prompt.  **Message is a single clear directive, not a confusing dual-possibility** (refined after review): `mirror publish` RELEASES every flagged file automatically — superseded versions obsoleted (remote_publish Step 6c `emit_obsolescence_claims`), dropped sources deprecated (Step 6b `emit_deprecation_claims`) — so the line is just "run `mirror publish` first; it's about ORDER not safety (the append-only mirror keeps the bytes)".  Verified there's **no deadlock**: `mirror publish` gates on signing-key / federation / hash-conflict / closure-installability / release-media — NOT `own_claim_disk_missing` — so cleanup-then-publish isn't blocked, and publish then clears the audit.  And cleanup only ever targets orphan/drift (never closure files), so it can't make the closure incomplete — the only effect of deleting first is a transient `mirror audit own_claim_disk_missing` until the next publish.  Non-federated operators (no coord ledger) are unaffected — the guard returns the empty set.  The helper is read-only (safe under the readonly-command pin).  Test `test_sta25_cleanup_guards_live_published_claims`: a published claim is flagged, a later deprecation folds it out, missing ledger → empty, + source pin on the gate.  `docs/known-issues.md` § Latent marked RESOLVED.  Suite 1134/1134, ruff + mypy clean. |
| STA-37 | P2 | done | **Chroot completion gate warned but never failed; the disk surface had no blocking verify at all.**  `build_chroot` computed `_broken` (half-configured) + `_missing` (planned-but-never-installed) from the in-chroot `dpkg-query` Status diff — the "authoritative pass/fail gate" its docstring promises — then printed a warning and `return True` REGARDLESS.  So an incomplete chroot (the 2026-06-11 python3-never-unpacked / gnome-menus-half-configured class) flowed straight into the squashfs / qcow2: on the live path `_verify_chroot`'s 8/8 boot checks only partially compensate, and the disk path explicitly set `chroot_disk_ready = True` with an "Informational verify — does NOT gate" comment, so a disk chroot with never-installed packages shipped unguarded.  **Fixed 2026-06-16** (*`<this commit>`*): `build_chroot` gains `gate_complete=True`; a non-empty broken/missing set now prints a loud ERROR and returns False (skipping the post-install / config-write steps on a known-broken chroot), so both `cmd_build_chroot_live` and `cmd_build_chroot_disk` — which already `if not _result: return` — bail before setting `chroot_*_ready`.  Escape hatch: both commands thread `gate_complete=not _no_gate`, so the existing `no-gate` arg (same one bypassing the pre-flight audits) lets the operator override (warns + proceeds).  proc/sys/dev unaffected.  Test `test_sta37_build_chroot_gates_on_incomplete_set`: complete→True, missing→False, broken→False, no-gate→True, + source pin that both commands pass the escape hatch.  Suite 1133/1133, ruff + mypy clean. |
| STA-42 | P2 | done | **Stale-mount safety: single-pass umount + recursive chroot wipe with no mount guard could delete host `/dev` nodes as root.**  `_mount_chroot_fs` does `mount --bind /dev <chroot>/dev` — `<chroot>/dev` is the HOST's real `/dev` (same inodes, not a copy).  `_umount_chroot_fs` ran exactly one `umount -lf` per target, so a SIGKILL'd / OOM-killed prior run (mksquashfs + dpkg are memory-hungry) whose `finally` didn't run could leave `/dev` bind-mounted multiple stacked layers; a single umount peels only the top one.  Then `BuildSystem.__init__`'s wipe ran `sudo find <chroot> -mindepth 1 -delete` (depth-first, crosses filesystems) with NO unmount-first and NO `-xdev` — descending into the still-bound `<chroot>/dev` and unlinking the host's `/dev/{null,sda,console,…}` as root, crippling the running host until reboot (or permanently on static-`/dev` systems).  Conditional trigger (needs an unclean prior run + a confirmed wipe), but root-level blast radius.  (The sibling `build.py` wipe uses `-maxdepth 1`, so it can't descend — safe; the dangerous one was `buildsystem.py`'s unbounded recursive wipe.)  **Fixed 2026-06-16** (*`<this commit>`*): three independent guards — (1) `_umount_chroot_fs` now loops `while os.path.ismount(dst)` (bounded to 64) to peel EVERY stacked layer; (2) the wipe calls `_umount_chroot_fs()` first (idempotent); (3) the `find -delete` gains `-xdev` so it never crosses a surviving mount boundary.  Any one closes the hole; together they're belt-and-braces.  proc/sys are `-t proc`/`-t sysfs` (fresh pseudo-fs, not host binds) so never at risk.  Test `test_sta42_chroot_wipe_cannot_delete_host_dev_nodes` (source pin): the peel-loop + `-xdev` + unmount-before-find ordering.  Suite 1132/1132, ruff + mypy clean. |
| STA-40 | P2 | done | **Banned `sudo bash -c f"…"` interpolation — three live sites + dead ssh strings.**  Three live sites built a shell command by f-string-interpolating paths, breaking on a working dir with spaces/metachars (the house rule + "grep `bash -c f'` before ship" feedback exist for exactly these): `disk_image` sfdisk (`sfdisk --wipe always {loop} < {script}`), `iso_installer._build_initrd` (the `cd {dir} && find | cpio | gzip > {initrd}` pipeline), and `apt_repo._run_dpkg_scan` (`cd {cwd} && {argv} 2>/dev/null > {tmp}` — the `2>/dev/null` also discarded the very stderr its failure handler claims to report).  **Fixed 2026-06-16** (*`<this commit>`*): all three are argv-form now.  New `utils.sudo_askpass_env` context manager supplies the password via a short-lived 0700 SUDO_ASKPASS helper (`sudo -A`), freeing the command's stdin for its own data and working regardless of the operator's sudo `timestamp_timeout` (no credential-cache dependency).  sfdisk → `sudo -A sfdisk …` with `stdin=<script file>`; the cpio pipeline → askpass + chained `Popen` (`find`/`cpio` under `sudo -A` with `cwd=`, gzip as the user writing the user-owned `initrd.gz` via the output handle — no more chown-from-root needed); `_run_dpkg_scan` → `sudo -S` argv with `cwd=`/`stdout=<tmp handle>`/`stderr=PIPE` (RESTORES the discarded scanner stderr — the centerpiece).  The unquoted ssh `remote_root`/`rel` sites (`_remote_has_debs`/`_remote_scan_packages`) live inside HK-07's documented dead-code block (`apt_repo.py` pre-MIRROR-01 `merge_remote_index`/`remote_reindex_and_sign`, no production callers) — left for HK-07's deletion rather than de-shelling code slated for removal (no runtime exposure).  Tests: `test_sta40_no_shell_interpolation_in_sudo_sites` (askpass helper round-trips a metachar password + cleans up; all three sites de-shelled + stderr captured); six existing dpkg-scan / initrd mocks updated from the old `bash -c '> tmp'` shape to the new `stdout=<handle>` argv shape.  Suite 1131/1131, ruff + mypy clean.  *(Live sfdisk + cpio paths verified by reasoning + structure tests; a disk + installer ISO build is the runtime confirmation.)* |
| STA-44 | P2 | done | **Downloaded `Packages`/`Sources` indexes were never hash-verified against the GPG-verified InRelease.**  The InRelease SHA256 (itself GPG-verified before its entries are trusted) gated only the re-download SKIP — the cached-file fast path.  On the actual download path the compressed index was fetched, decompressed, and INGESTED with no check, so a truncated / corrupted / tampered transfer was parsed silently this run (and silently re-downloaded every subsequent run, since the corrupt bytes never match the skip-sha — looking like a perpetual cache miss).  The "per-mirror SHA256 gating" docstring was half-true.  Both the required-index loop (`__get_files`) and the optional-index helper (`_fetch_optional_index`) had the gap.  **Fixed 2026-06-16** (*`<this commit>`*): new shared `Cache._verify_index_against_release` hashes the freshly written file (`get_sha256(use_cache=False)`) and compares to the InRelease entry — prefers the UNCOMPRESSED sha (the value the skip-check keys on), falls back to the compressed artifact's sha when the mirror lists only the compressed variant.  On mismatch both paths delete the corrupt artifact and fail LOUD (required → `error_str` + return -1, aborting the cache build; optional → ERROR log + return None).  Test `test_sta44_index_verified_against_release_sha`: uncompressed match/mismatch, compressed-only match/mismatch, + a source pin that both download paths invoke the verifier.  Suite 1130/1130, ruff + mypy clean. |
| STA-48 | P2 | done | **`cve <path>` silently overwrote the input SBOM when the filename didn't end `.cdx.json`.**  `cmd_cve` derived the report path as `_sbom_path.replace('.cdx.json', '.cve.json')` — a no-op for any name not ending `.cdx.json` (e.g. an operator's `/tmp/sbom.json`), so `_report_path == _sbom_path` and the later `open(_report_path, 'w')` clobbered the operator's SBOM with the grype CVE report, silently (grype accepts any input filename).  **Fixed 2026-06-16** (*`<this commit>`*): new `SupplyChainCommandsMixin._cve_report_path` (staticmethod) derives via `os.path.splitext` + a `.cdx` double-suffix strip (`foo.cdx.json`→`foo.cve.json`, `sbom.json`→`sbom.cve.json`), which never collides with the input; plus a defence-in-depth guard that refuses (before the multi-second grype scan) when `os.path.abspath(report) == os.path.abspath(sbom)`.  Test `test_sta48_cve_report_path_never_overwrites_input_sbom`: canonical strip + the regression cases + the never-equals-input invariant across 5 path shapes.  Suite 1128/1128, ruff + mypy clean. |
| STA-46 | P2 | done | **`_was_patched` derived from patch-dir *existence*, not patch files — an empty dir minted a spurious `+asg` stamp.**  `BuildContainer.build` set `_was_patched = (src_patch_path != self.patch_empty)` — True whenever `patch/source/<pkg>/<ver>/` exists, regardless of whether it holds any `.patch` files.  A dir with zero patches (operator removed the last patch but left the dir, or a stray README) → `_was_patched=True` → `_normalize_built_artifacts` sets `_was_delta=True` → asg-stamps byte-pristine artifacts, violating Position-X (a no-delta build must stay pristine) and diverging from the three sites that key off `bool(patch_list)` (`bump.compute_post_build_versions`, `cmd_source validate`, `virtual_build`), so `virtual validate` would flag the real build as drift.  **Fixed 2026-06-16** (*`<this commit>`*): `_was_patched = bool(_live_patch_list)` — the already-computed sorted list of actual `*.patch` files applied in the container (same value driving `patch_cmd` + `patch_set_hash`), in scope in the same `build()` method.  Now matches the three mirrors.  Test `test_sta46_was_patched_keys_on_patch_files_not_dir_existence` (source pin).  Suite 1127/1127, ruff + mypy clean. |
| STA-52 | P2 | done | **Live-ISO staging tree never wiped between runs — stale files ship in every subsequent ISO; squashfs exclude fallback ineffective.**  `build_iso` only `os.makedirs(exist_ok=True)`'d over `image/staging/`, while the installer path has `_prepare_staging` (wipe + recreate) for exactly this reason — grub-mkrescue images the WHOLE tree, so a since-removed asset (a dropped background/test file) shipped forever.  Sibling: the mksquashfs `-e dir/*` PermissionError fallback (root-owned dev nodes) added the glob WITHOUT mksquashfs `-wildcards`, so mksquashfs treated `dir/*` literally, matched nothing, and silently shipped the unlistable dir's contents.  **Fixed 2026-06-16** (*`<this commit>`*): step 2 now `sudo rm -rf`'s the staging tree before recreating (sudo — a prior run's `live/filesystem.squashfs` is root-owned; mirrors `iso_installer._prepare_staging`; fails the build loudly if the wipe fails); the mksquashfs command tracks `_used_glob` and prepends `-wildcards` only when the fallback fired (governs all following `-e` patterns, kept tight).  Test `test_sta52_live_iso_wipes_staging_and_wildcards_exclude_fallback` (source pin — full build needs sudo): staging `rm -rf` before reuse + `-wildcards` on the glob fallback.  Suite 1126/1126, ruff + mypy clean. |
| STA-41 | P2 | done | **`_write_chroot_file` shipped root-only 0600 files into the live image.**  The helper wrote content to a `NamedTemporaryFile` (0600) then `sudo cp`'d it into the chroot — and `cp` propagates the source mode when the destination doesn't pre-exist, so every file it wrote landed 0600 (root-only).  Affected shipped files: `/etc/machine-id` (spec is world-readable — userspace `sd_id128_get_machine` fails, masked by the dbus fallback), `/etc/hostname`, `/etc/hosts`, `/etc/issue`, `/etc/os-release`, `/etc/debconf.conf`, `/etc/fstab` (live surface — inconsistent with the disk surface, already healed by disk_image's own `install -m 644`), and `/etc/apt/sources.list` + `sources.list.d/athena-*.list` (non-root `apt list` couldn't read the sources).  **Fixed 2026-06-16** (*`<this commit>`*): swap `sudo cp` → `sudo install -m 644` — explicit world-readable mode rather than relying on cp's propagation, matching disk_image + the SEC-07 writer.  Every file written via this path is world-readable config, so 0644 is always correct (audited all 10 call sites).  Test `test_sta41_write_chroot_file_ships_world_readable_mode` (source pin — the privileged chroot path can't run in CI): asserts `install -m 644`, not bare `cp`.  Suite 1125/1125, ruff + mypy clean. |
| STA-49 | P2 | done | **`clean all` missed the disk chroot (wiped live+installer only) and `cache purge` left a stale udeb tree.**  `cmd_clean_all` wiped `dir_chroot` + `dir_chroot_installer` then reset every flag — so a root-owned `buildroot/disk` (the disk surface's own minimal SURFACES-01 chroot) survived while the flag reset cleared `chroot_disk_ready`, claiming it was gone (and the disk the operator asked to reclaim wasn't); `cmd_clean_buildroot` already wiped all three, the cascade into `clean all` was missed.  Sibling: `cmd_cache_purge` nulled `cache`/`dep_tree` but not `self.udeb_dep_tree` (the installer dep-tree built off the same cache), so a standalone `cache purge` stranded a tree pointing at deleted files (`clean all` nulls it explicitly, masking the bug there).  **Fixed 2026-06-16** (*`<this commit>`*): `cmd_clean_all` adds the third `_wipe_dir_contents('buildroot/disk', dir_chroot_disk, …)` + the prompt text now reads `buildroot/{live,installer,disk}`; `cmd_cache_purge` adds `self.udeb_dep_tree = None` alongside the cache/dep_tree drops.  Test `test_sta49_clean_all_wipes_disk_chroot_and_purge_nulls_udeb_tree` (source pin): cache_purge nulls all three trees; clean_all wipes all three buildroot chroots.  Suite 1124/1124, ruff + mypy clean. |
| STA-43 | P2 | done | **Durable state written non-atomically: snapshot pin, `build.conf` rewrite, SBOM.**  `write_snapshot_state` did plain `open('w')` + `json.dump` onto the live `config/snapshot.state` — a crash mid-write truncates it, `read_snapshot_state` swallows the resulting `ValueError` and returns `{}`, and the next build silently falls back to `[Snapshot] Timestamp` (possibly `latest`) — the exact drift the "durable, survives clean cache" docstring exists to prevent, while its sibling `append_snapshot_history` already used `_atomic_write_bytes`.  `reconcile_snapshot_pin` rewrote the operator's master `build.conf` the same unsafe way; `sbom.generate_cdx` likewise (low stakes).  **Fixed 2026-06-16** (*`<this commit>`*): all three route through `_atomic_write_bytes` (temp + fsync + `os.replace`, POSIX rename atomicity — a crash leaves the prior file intact).  Added a `mode` param to `_atomic_write_bytes` (default 0o644, so the two existing callers are unchanged) + `_existing_mode(path)` helper; the snapshot.state and build.conf rewrites pass `_existing_mode(...)` so an operator-set group-writable 0o664 survives the rewrite instead of being narrowed to 0o644 (both files are 0o664 on this host).  Test `test_sta43_durable_state_writes_atomic_and_preserve_mode`: `_existing_mode` real-vs-default; `_atomic_write_bytes(mode=…)` honours mode + leaves no `.tmp`; write_snapshot_state + reconcile_snapshot_pin write correct content AND preserve 0o664; source pins all three on the atomic helper.  Suite 1123/1123, ruff + mypy clean. |
| STA-50 | P2 | done | **`_ensure_repo_indexed_for_chroot()` return value discarded at both call sites — chroot build proceeded after a failed auto-index.**  `cmd_build_chroot_live` and `cmd_build_chroot_disk` called the helper as a bare statement.  The helper auto-indexes the local repo (MIRROR-01 Phase 8 — `repo index` is no longer operator-visible) and returns False after printing "Auto-index failed (see log)…" when `cmd_index_repo()` fails — but with the return discarded, both callers fell through into a multi-minute chroot bring-up that's guaranteed to die later on the missing/unsigned InRelease (apt under `--no-check-valid-until` can't install the built packages), the failure surfacing far from its cause.  **Fixed 2026-06-16** (*`<this commit>`*): both sites now `if not self._ensure_repo_indexed_for_chroot(): console.print("Aborted by repo auto-index"); return` — matching the existing `_preflight_audit_source`/`_preflight_audit_repo` abort style (the helper already printed the cause).  Test `test_sta50_chroot_build_gates_on_repo_auto_index` (source pin, mirrors `test_chroot_build_wires_both_audit_gates_with_no_gate_bypass`): both entry points contain `if not self._ensure_repo_indexed_for_chroot():`, and the helper still advertises its False-on-failure contract.  Suite 1122/1122, ruff + mypy clean. |
| STA-51 | P2 | done | **apt Pin-Priority 1001 hardcoded `origin snapshot.debian.org` while sources honour `[Snapshot] BaseUrl`.**  `buildcontainer._write_snapshot_sources_cmd` wrote the snapshot sources from `_m.url` (derived from `[Snapshot] BaseUrl`) but hardcoded `Pin: origin snapshot.debian.org` in `/etc/apt/preferences.d/athena-snapshot`.  apt's `origin` pin matches the repo URL's HOST, so on any custom snapshot mirror the pin no longer matched the sources, its Priority-1001 (force-downgrade) silently fell back to 500, and the exact downgrade-refusal loop the pin exists to break returned — the 2026-05-23 wpa→libssl3 case (bookworm-slim's installed lib newer than the snapshot's max, `apt-get upgrade --allow-downgrades` refusing because 500 doesn't outrank "installed is newer").  Default-mirror hosts were unaffected, so it lurked.  **Fixed 2026-06-16** (*`<this commit>`*): derive the pin host from `urlparse(self.config.snapshot_baseurl).hostname` (the same host `_m.url = {baseurl}/{baseid}` uses, so pin ↔ sources always agree), with an `or 'snapshot.debian.org'` fallback for an empty/odd baseurl.  Consumer trace: `run_grub_mkrescue` calls `_write_snapshot_sources_cmd`, so its two `__new__`-built test stubs gained a `config.snapshot_baseurl` injection (the method now reads `self.config`, not just `self.mirrors`).  Test `test_sta51_snapshot_pin_origin_tracks_baseurl_host`: custom mirror → `Pin: origin <custom-host>` (and NOT snapshot.debian.org); default → unchanged; empty → safe fallback.  Suite 1121/1121, ruff + mypy clean. |
| SEC-07 | P0 | done | **Sudo password leaked into published per-component Release files — rotate + scrub + fix the writer.**  The build host's sudo password sat in line 1 of six per-component `Release` files in the published repo tree.  Mechanism (`apt_repo.py` `_write_subdir_release`): the file was written via `['sudo','-S','bash','-c', f'cat > {path}']` with `input=password + '\n' + content`; `sudo -S` consumes the password line ONLY when it needs to authenticate, but `cmd_index_repo` runs `sudo -S -v` first which caches the credential — so every subsequent call passed the password line straight through to `cat` and it became the Release file's first line (before `Origin:`).  Blast radius: 6 files under `repo/dists/thor/` (main/binary-amd64, main/source, main/debian-installer, doc, tests, non-free-firmware), 3 more in `image/staging-installer/dists/` (so the built installer ISO carried it on media), and `push_dist_tree` rsync'd `dists/` wholesale so the remote mirror served it.  The unit test hard-coded the "sudo always eats the first line" assumption, which is why CI never caught it.  **Closed 2026-06-16** (remediation across `fc489a0` + this session): (1) operator rotated the sudo password 2026-06-12; (2) writer fixed — content now written to a user-owned tempfile then `sudo install -m 644 tmp path` (the `_run_dpkg_scan` pattern), so sudo's stdin is the password ONLY, never mixed with content; regression test `test_write_subdir_release_never_mixes_password_with_content` pins it; (3) `mirror publish` re-indexed (index files are not byte-frozen) + pushed `dists/`, re-signing InRelease, healing local + remote; (4) installer ISO rebuilt + republished this session; (5) **verified clean 2026-06-16** — local `repo/dists/` scan: no leak; installer-ISO staging `dists/` first lines `Origin: Athena` / `Architectures: amd64`; all 6 remote per-component Release files (curl) start `Origin: Athena`; live ISO never had `dists/` (squashfs+boot only).  No residual leak vector.  Filed 2026-06-12 from the full-tree review. |

## 8. Operator UX — P2 / P3

| ID    | Sev | Status | Title |
|-------|-----|--------|-------|
| UX-04 | P2 | done | Persist `BuildFlags` to disk between runs so a re-launched TUI can resume mid-pipeline.  **Closed 2026-05-30 after out-of-band measurement on the operator's bookworm workload showed clear ship signal.**  First attempt 2026-05-10 (`ceb3d1a` + `a53eea9`, reverted by `0398dd0` + `d919b2e`) was put on hold without recorded benchmarks; the operator's gut-feel "roughly the same as fresh build" turned out to be wrong.  **Measurement experiment** (`experiments/ux04_pickle_perf.py`, committed for reproducibility) on the real workload — 102,732 packages / 35,249 sources / 856 udebs — produced: T_fresh = 42.5s, T_pickle_save = 8.5s, T_resume = 18.4s, blob 34.9MB, ratio T_resume/T_fresh = **0.433** (clear ship, well under the 0.50 threshold).  **Shipped:** (1) `__getstate__` / `__setstate__` on `Cache` (drops `_arch_table` + flattens lambda-factory defaultdicts), `DependencyTree` (drops `__cache` backref + flattens `__lookahead`), `Package` (field-replay through `Packages.__init__` to bypass `_Deb822Dict__keys` OrderedSet weakrefs), `Source` (raw_text-based re-init through `Sources.__init__` because multi-valued fields like `Files`/`Checksums-Sha256` carry weakrefs inside their python-debian wrapper objects).  The Package vs Source split was the discovery beyond the reverted commits' scope — the post-mortem agent's summary had been imprecise about where the weakref problem lived.  (2) `scripts/persistence.py` (~340 LOC) — `save_session`, `restore_session`, `compute_fingerprint`, `fingerprint_diff`, SHA256 sidecar primitives, format-version (`_FORMAT_VERSION=1`).  On-disk layout under `<dir_cache>/`: `session.pkl.gz` (pickle protocol 5 + gzip), `session.pkl.gz.sha256` (corruption gate), `session.fingerprint.json` (restore-gate input).  Atomic writes via temp-file + `os.replace` so a kill mid-save leaves the previous good blob intact.  (3) `BuildFlags` class refactor: class-level type annotations, `_FIELDS` tuple, `_IN_MEMORY_ONLY` frozenset (`cache_ready`, `dep_check_ready`, `build_container_ready`, `signing_key_verified`), `__setattr__` interceptor that autosaves to `<dir_cache>/buildflags.json` on every flag transition (~1ms write), `load(path)` classmethod that reads the JSON and resets `_IN_MEMORY_ONLY` flags to False (their backing state isn't loaded yet — only `cmd_resume` flips them True after the rebuild + restore succeeds), `restored_summary()` for the startup banner.  Both `BuildSession.__init__` and `cmd_clean_buildroot` updated to use `BuildFlags.load`.  (4) `cmd_resume` orchestrator: defensive entry reset of `cache_ready`/`dep_check_ready` (UX-05g compliance) → `persistence.restore_session` with closure-captured console callback (fingerprint check → pickle load + format-version check → rewire DT's `_DependencyTree__cache` backref) → best-effort `cmd_init_container` (failure leaves `build_container_ready=False`, doesn't void the restore).  Operator gets a clear "what changed" message on fingerprint mismatch (e.g. `"resume: refused — fingerprint mismatch (config_hash changed; mirror_inreleases.security changed)"`); the fix is `cache parse`.  (5) `save_session` autosave hooks at the tails of `cmd_parse_dependency` (mandatory — first save with a real DT) and `cmd_source_build` (refreshes `last_source_build_counts`).  Best-effort: save failure logs WARNING but never aborts the build.  (6) `--resume` flag plumbed through `build-system.sh` (RESUME var, getopt long option, help-text line, PY_EXTRA forwarding) → `scripts/build.py:main()` strips it from argv before BuildConfig sees it (mirrors `--headless` / `--yes` shape) → auto-fires `session.cmd_resume()` after TUI registration when set.  Plus `resume` command registered in the TUI for opt-in invocation without the CLI flag.  Restored-flags banner prints on every startup ("Restored from prior session: download, source_build, ...") regardless of `--resume`, so the operator always sees prior state.  **Fingerprint composition** (reuses existing `utils.compute_tree_hash` / `utils.patch_set_hash` / `utils.get_sha256` / fork_mirror's `.tree-hash` sidecars — no new hashing code): config_hash over `config/`, mirror_inreleases (SHA256 of every mirror's `InRelease` or `Release` for `file://`), fork_tree_hashes (per-fork-pkg tree-hash), patch_set_hashes (per-source patch_set_hash), snapshot_ts, arch, build_profiles, include_recommends.  Mismatching fingerprint → restore refused; operator runs `cache parse` to rebuild.  **11 new tests** (`test_ux04_*`): SHA256 sidecar round-trip + mismatch detection, silent return on missing blob, refusal on missing fingerprint, refusal on corrupt sidecar, fingerprint_diff named-field + dotted-key reporting, BuildFlags autosave round-trip, BuildFlags `_IN_MEMORY_ONLY` reset on load, `restored_summary` excludes in-memory flags, `resume` command registered, `--resume` plumbing in `build-system.sh`, `save_session` called at `cmd_parse_dependency` tail.  651/651 tests pass; ruff + mypy clean.  Experiment script `experiments/ux04_pickle_perf.py` retained in-tree for future re-measurement after any cache-shape change (operator runs `python3 -u experiments/ux04_pickle_perf.py` for a fresh ratio). |
| UX-01  | P2 | done | `print` command takes only `config|required|important|selected`. Add `print sources`, `print mirrors`, `print snapshot`. *(subsumed by the broader `print` overhaul in `scripts/print_commands.py`.  All three asked-for views shipped, plus 8 more — full set: configuration (config / mirrors / snapshot / paths), build state (state / stats), packages (required / important / selected / tunneled / `pkg <name>` / `deps <name>`), sources (sources / `src <name>`), relations (provides), meta (help).  Help screen groups categories into named sections; parametrized views route `*extras` through the dispatcher.  10 unit tests — including a safety test that every registered handler runs cleanly against an uninitialised BuildSession (catches any future view that forgets a `_require_cache()` / `_require_dep_check()` guard).)* |
| UX-02  | P2 | done | `cmd_auto_run` does not respect prerequisites if any earlier step fails — it still tries the next step and produces a confusing error. Make it bail at the first failed flag. *(checks each step's progress flag after the call; aborts on first unset flag with a clear `autorun: '<step>' did not complete — aborting` message)* |
| UX-03  | P2 | done | Emit a final summary at end of `auto_run` (counts of cached/built/tunneled/failed, ISO path, total wall time). *(`cmd_auto_run` now wraps the stage loop in `time.monotonic()` brackets and always emits a summary on exit — success path AND aborted-at-stage path; the early `return` was replaced with a `break` so the summary is unconditional.  `_print_autorun_summary()` renders header (SUCCESS / ABORTED at <stage>) + start/finish wall-clock + elapsed in `Hh MMm SSs` form via the new `_format_duration` static method, then per-stage rows: cache name count, dep tree canonical/source counts, source-build breakdown (built / tunneled / failed / skipped), chroot status (built+verified / build-only / not-built), and the predicted ISO path with a hint that `build_iso` is a separate step.  Side-cleanup: split `cmd_source_build`'s `_success` counter into `_built` vs `_tunneled` (was conflated) and persist the result on `self.last_source_build_counts` for the summary to read.  5 unit tests: 3 for `_format_duration` boundary cases (s-only / m+s / h+m+s) and 2 end-to-end summary renders covering success and aborted variants.  83/83 tests pass.)* |
| UX-07  | P2 | done | Modularise the flat command surface into noun-verb groups (`cache build`, `dep parse`, `chroot build`, etc.) so the operator menu reads as categories rather than a flat list of 13 names. *(landed `caf4fdc` 2026-05-10: replaced the 13 flat registrations with 9 group dispatchers — `cache`, `dep`, `patch`, `source`, `package`, `container`, `chroot`, `iso`, `key` — each implemented as one `cmd_<noun>(action, *args)` method that switches on the verb and forwards to the existing `cmd_<old_name>` handlers (kept as the underlying implementation, now private to the dispatcher).  Bare noun prints the action menu via a tiny `_group_help` helper.  `autorun` and `print` stay flat (orchestrator + already-a-dispatcher).  Mapping: `build_cache → cache build`, `parse_dependency → dep parse`, `patch_refresh → patch refresh`, `source_download → source download`, `source_build → source build`, `tunnel_package → package tunnel`, `build_container → container init`, `build_chroot → chroot build`, `verify_chroot → chroot verify`, `build_iso → iso build`, `generate_signing_key → key generate`, `verify_signing_key → key verify`.  Clean break — old names dropped, no aliases.  Knock-on updates: 7 prerequisite-error strings (`Run 'cache build' first` etc.), autorun stage labels in `cmd_auto_run`, README operator guide (all 11 names), `build-system.sh` tool-check messages.  New unit test `test_group_dispatchers_forward_to_underlying_cmd_methods` pins all 12 verb→target forwards plus the unknown-verb fallthrough; existing BuildSession bound-methods test extended to cover the 9 new dispatchers.  137/137 pass.)* |
| OBS-01 | P2 | done | **Per-source structured metrics — JSON sidecar.**  Original framing (2026-05-29 filing): `log/build/<pkg>` carried raw container stdout and `<pkg>.result` was a single-line `PASS`/`FAIL`; wall-clock per source not captured.  Proposed `time.monotonic()` brackets around `BuildContainer.build()` + a `log/build/<pkg>.metrics.json` per build with `{started, finished, elapsed_seconds, status, exit_code, oom_killed, patch_set_hash, source_version, output_count}`.  **Closed 2026-06-02** (commits `34434f2`, `60c3753`, `cbdc521`): shipped the canonical signed `log/build/<pkg>.build.json` record — replaces (not augments) `.result` and `.patchhash` sidecars.  Beyond the original proposal: (a) phase state machine (entry → container_exited → segregated → normalized → done | failed | tunneled) so a crash mid-build leaves the record at the last completed phase; next session classifies as `interrupted` and forces rebuild; (b) `intended_version` vs `built_version` fields to detect "we asked for X, what shipped is Y" silently (relevant under tunneled + NMU-strip paths); (c) HMAC-SHA256 signature over canonical JSON, key auto-generated 0600 at `log/build/.metrics.hmac.key` — local tamper detection; (d) atomic write per phase (tempfile + fsync + os.replace) so a crash mid-write leaves the prior valid file intact; verify-failure on read treats the record as missing and triggers rebuild via the existing classifier.  `new_build_record` / `read_build_record` lifted into `utils.py`; consumed by `BuildContainer` (per-phase writes), `cmd_source_build` (audit classifier), and `cmd_mirror_publish` (claim-generation walks `output_hashes` from the signed record).  `print build-times` view + slowest-5 section in `print summary` surface `elapsed_seconds`.  Foundation for OBS-02 / OBS-03 (both still open). |
| OBS-04 | P2 | done | **Exhaustive per-package build/tunnel log.**  *(Shipped 2026-06-06, commit `b31a98f`.)*  New `log/build/<pkg>.buildlog` — a verbose, human-readable narrative SEPARATE from the raw container stdout (`log/build/<pkg>`) and the signed structured record (`<pkg>.build.json`).  Per build it captures: resolved build-depends, container name, files **expected** (declared Package-List), **emitted** (scratch scan pre-segregate, with sizes), **relocated** (segregate src→dst), **purged** (with reason), **NMU strips** (old→new), **+asg stamps** (old→new+tag), per-file **hash+size**, timing, and an **expected-vs-emitted delta** (the per-package virtual-vs-practical signal).  The tunnel path (`_do_tunnel`) writes a parallel `kind=tunnel` log: expected upstream binaries, downloaded, purged-stale, strip, stamp, final + republished marker.  **Load-bearing safety invariant**: strictly additive + fully exception-isolated — every logging path is guarded (`BuildLog.write` atomic temp+rename; `_write_buildlog` and the tunnel block wrapped) so a log failure can NEVER break or skip a build (a full repo rebuild is 24-36h; a logging bug that aborts a build would waste it).  `_segregate_built_artifacts` / `_normalize_built_artifacts` take an optional `events` accumulator and only append AFTER the real move/strip/stamp — control flow byte-for-byte unchanged.  Module: `scripts/buildlog.py` (`BuildLog` + `human_size`/`safe_size`).  Tests: BuildLog render + never-raises safety + segregate event capture (937/937 pass; ruff+mypy clean).  **Note on ID**: originally committed under the label "OBS-03", renumbered to OBS-04 to avoid colliding with the earlier-filed (still-open) OBS-03 resource-telemetry ticket — telemetry keeps OBS-03 in TODO.  **Companion housekeeping same session** (commit `deac710`): removed the unused `download/` dir (`dir_download` was created/checked/wiped/shown but never written to — `download_source` always wrote to `dir_source`; its param renamed `dir_download`→`dst_dir`), the now-dead `clean download` command, the orphan `packages/` dir (no config key / attribute / creation site anywhere), and the obsolete `repo repair rescan-outputs` command (its segregate kept-existing bug is fixed, so fresh builds record `output_hashes` correctly). |
| UX-05  | P3 | done | **Make the TUI / CLI more robust and flexible.**  *(Re-scoped 2026-05-14: previously "replace curses with Textual"; **explicitly NOT shifting to Textual** — the curses code is solid and we get more value from polishing it in-place than re-architecting.  ARCH-14 owns the structural rework (render-time line wrap, scroll-by-row vs scroll-by-record, theming, broader keystroke routing); UX-05 now owns the operator-facing flexibility gaps that have piled up.)*  Concrete sub-items worth filing as separate tickets when picked up: **(a) `--yes` auto-answer flag** for unattended runs (CI / scripted installs); **(b) `ATHENA_SUDO_PASSWORD` env-var pickup** so passwords never hit prompt buffers in scripted contexts; **(c) ProgressBar output throttling** in CLI mode so a fast inner loop doesn't flood stdout / log files; **(d) ANSI colour in CLI/TTY mode** mirroring the curses colour scheme so log-grep stays readable; **(e) `-c <cmd>` one-shot execution** (`./build-system.sh -c "cache build" -c "dep parse"`) for ad-hoc scripting without a REPL; **(f) prompt UX** — distinguish hard-required prompts (sudo password, conflict-resolution choices) from informational confirmations that could default-yes under `--yes`; **(g) graceful Ctrl+C** mid-resolve without corrupting `BuildFlags` state.  *(**Path B done 2026-05-09**: `--headless` flag in `build-system.sh` + `scripts/cli.py` (~280 LOC) implementing a Cli class that mirrors the Tui surface (print, INFO/WARNING/ERROR, add_widget/del_widget, console_mark/console_trim_to, prompt, register_command, run, wait, exit, sig_shutdown).  Cli registers as `tui.tui_instance` on construction so all 291 console.print + 10 Prompt + 12 register_command + Spinner/ProgressBar callsites work unchanged.  Stdout for operator output, stderr for diagnostic logger noise (separable via shell redirection).  ProgressBar gets `[start]`/`[done: N/M]` markers; Spinner keeps its own `… done` line.  17 unit tests pin the contract incl. a Console-facade end-to-end exercise that catches name-drift between facade and backend.  Path B is the foundation the (a)–(g) items above all build on top of.  Full research + Path B implementation outline in `~/.claude/plans/piped-tinkering-milner.md`.)* **Closed 2026-05-28**: all seven sub-items shipped.  **(a) --yes**: parsed by `build.py:main` from sys.argv, stored on the backend; consulted only by `Prompt(..., informational=True)`.  **(b) ATHENA_SUDO_PASSWORD**: BuildSystem.__init__ + BuildSystem.for_iso both `os.environ.pop('ATHENA_SUDO_PASSWORD')` before reaching for Prompt(PROMPT_PASSWORD); the pop removes the value from the process environment so it doesn't leak to child subprocesses or appear in /proc/<pid>/environ.  **(c) ProgressBar throttling**: audit confirmed `ProgressBar.step()` already doesn't print in either backend (Cli prints `[start]`/`[done: N/M]` markers via add_widget/del_widget only) — anti-regression test `test_ux05c_progressbar_step_does_not_print_in_cli` pins the property by inspecting `ProgressBar.step`'s source.  **(d) ANSI colour**: Cli.print wraps message in ANSI escape (red/yellow/cyan/green/inverse/dim per COLOR_* key) when stdout is a TTY and NO_COLOR isn't set; redirected output gets plain text.  **(e) --cmd <cmd>**: build.py:main parses one or more `--cmd "<cmd>"` from argv (note: `-c` was already taken by build-system.sh for --config-file, hence --cmd here); each gets queued on Cli.one_shot_cmds; Cli.wait detects the non-empty queue and dispatches each in order then exits.  --cmd implies --headless.  build-system.sh forwards `--cmd` + `--yes` + `--headless` to the Python entry-point.  **(f) hard vs soft prompts**: `Prompt(..., informational=True)` is the explicit opt-in for --yes-skippable YESNOs.  6 existing prompts marked informational (post-failure 'Proceed?' and one-time signing-key 'Generate?'); destructive prompts (wipe chroot, clean cache, clean all, container purge) and security gates (SEC-05 audit, snapshot select) deliberately left as hard prompts.  **(g) Ctrl+C BuildFlags integrity**: AST audit found 4 sites where `self.flags.X = True` was set without a matching `False`-reset in the same function — added explicit `False`-resets on entry in cmd_build_chroot_live (`chroot_ready` + `chroot_verified`) and cmd_source_build (`source_build_ready`); the 2 remaining sites (`chroot_verified` in cmd_build_iso_live/disk) are side-effect refreshes after successful in-line verify, honest by construction (gated on verify rc=0), allowlisted in the anti-regression test.  Test `test_ux05g_cmd_methods_reset_flags_on_entry` walks build.py AST and asserts the invariant.  8 new tests cover the contract; 585/585 pass.  ruff clean. |

## 9. House-cleaning — P3

| ID     | Sev | Status | Title |
|--------|-----|--------|-------|
| HK-06  | P3 | done | **Fresh disk images carried a benign ext4 orphan list — added a post-umount `e2fsck -f -p` in `disk_image.py`.**  First boot of a freshly-built qcow2 logged the kernel/e2fsck cleaning a small ext4 orphan-inode list on the root fs — files deleted inside the chroot while the image's loop mount was live leave orphan-list entries when `disk_image.py` unmounts, and nothing replayed them on the artifact before it shipped.  Harmless (cleanup succeeds, never recurs; root fs + ESP otherwise verified clean), but a cosmetic first-boot log line.  **Fixed 2026-06-16** (*`<this commit>`*): `build_disk_image` runs `e2fsck -f -p <root-part>` on the now-unmounted root, after the root umount and before the loop detach (the fs must be unmounted to fsck, the loop device must still exist) → the shipped qcow2 is pristine.  rc 1/2 ("errors corrected" — the expected orphan cleanup) logs at INFO; rc ≥ 4 (serious uncorrected) warns loud but doesn't fail the build (the image still boots; deeper corruption is a separate bug).  No new dependency — e2fsck ships with e2fsprogs, already required for the `mkfs.ext4` root.  Test `test_hk06_disk_image_e2fsck_clears_orphan_list` (source pin — privileged disk path): `e2fsck -f -p` on `_root_part`, ordered after umount + before loop detach.  `docs/known-issues.md` § Cosmetic marked RESOLVED.  Suite 1129/1129, ruff + mypy clean. |
| HK-01  | P3 | done | Remove the dead `get_install_sequence` if it is truly unreached after `build_chroot` migrated to the unpack-forest model — or wire it back in for the post-final-configure pass and add a test.  *(Removed — `get_install_sequence` had no callers post-ARCH-12.  Same change as ARCH-08.)* |
| HK-02  | P3 | done | Drop `cmd_print`’s commented-out `# TODO` items in `build.py`; turn them into entries in this file (UX-01) and remove the inline TODOs. *(no-op on inspection — `cmd_print`'s in-body TODOs were already removed in earlier cleanup; UX-01 captures the `print sources/mirrors/snapshot` follow-ups.  The remaining TODOs in `scripts/` (`build.py:57`, `chroot.py:950/990`, `buildcontainer.py:217`, `cache.py:371`) are unrelated to `cmd_print` and stay as in-code reminders.)* |
| HK-03  | P3 | done | Empty `config/testpkg.list` — either populate (intent: a tiny smoke pkg list) or remove. *(removed.  File contained a single line `libunwind-dev` with no readers anywhere in `scripts/` or `build-system.sh` — unused single-package list, deleted via `git rm`.  If a curated tiny smoke list is wanted later, add a fixture under `tests/fixtures/` instead.)* |
| HK-04  | P3 | done | `scripts/__pycache__/` is committed-adjacent (in tree) — confirm `.gitignore` covers it (it does), but inspect whether any stray `.pyc` was ever committed (`git log -- '*.pyc'`). *(audit complete: 10 `.pyc` files were added in older commits and all removed by 2026-04-29; current working tree has none tracked; `.gitignore` line `__pycache__/` covers the directory.  Old history pollution left as-is — rewriting history with `git filter-branch`/`bfg` would be invasive for harmless dead bytes.)* |
| HK-05  | P3 | done | `config/build.conf:100` has a dead `MIRROR_URL="deb.debian.org"` line — no reader anywhere in `scripts/`.  Drop it.  Also `PACKAGE_LISTS_SUFFIX="default"` on the next line — same story.  Cluster: ARCH-13 + COMP-11 + HK-05 = "distro-portability". *(both lines + their comment headers removed from `config/build.conf`.  Confirmed zero readers via `grep -rn "MIRROR_URL\|PACKAGE_LISTS_SUFFIX" scripts/ build-system.sh`.  Bonus signal-to-noise: both used bash-style `KEY="value"` syntax which would not parse correctly in this INI file anyway — they were dead in two ways.)* |
| HK-01  | P3 | done | **Codebase consistency cleanup — bundle of small fixups surfaced by the 2026-05-21 consolidation audit.** *(Closed 2026-05-23.  NB: ID re-used — an earlier HK-01 row in this file covers `get_install_sequence` removal; this is the 2026-05-21-audit cleanup bundle.  Five sub-items, all landed with no production-behaviour change.  (a) **faulthandler fd leak** — `scripts/build.py:22` lifted the inline `faulthandler.enable(open(...))` into a module-level `_FAULT_LOG` handle paired with `atexit.register(_FAULT_LOG.close)` so reload-during-dev cleans up and normal-exit doesn't leak the fd.  (b) **`_VALID_CONSTRAINTS` dedupe** — promoted to `utils.VALID_CONSTRAINTS` (frozenset, public), both `Cache._VALID_CONSTRAINTS` and `DependencyTree._VALID_CONSTRAINTS` now alias the canonical set so call sites (`self._VALID_CONSTRAINTS` everywhere) stay unchanged.  Single source of truth for the seven-operator comparison set.  (c) **`get_packages` dedupe** — extracted module-level `cache._lookup_packages(hashtable, name, version, constraint)` consumed by both `Cache.get_packages` and `UdebCacheView.get_packages` (~58 LOC → ~4 LOC at each call site).  Post HK-01b the two methods were literally identical save for the hashtable they read; the helper takes the table as an arg.  (d) **Naming convention outlier** — `scripts/diag_installer_status.py`'s `NEW_SEEDS` + `FIELD_HEADER_RE` renamed to leading-underscore (`_NEW_SEEDS`, `_FIELD_HEADER_RE`) to match the rest of the codebase's module-private constant convention (`_NMU_SUFFIX_RE`, `_OVERLAY_MAP`, etc.).  No external consumers — both were only referenced inside `diag_installer_status.py` itself.  (e) **Hardcoded path constants** — promoted `'bookworm'` (CONTAINER_RELEASE fallback) and `/usr/share/keyrings/debian-archive-keyring.gpg` (Security.Keyring fallback) to module-level `_DEFAULT_CONTAINER_RELEASE` and `_DEFAULT_SECURITY_KEYRING` at the top of `utils.py`.  The chroot.py PATH env + `/usr/*` glob patterns left in place per the ticket's own assessment ("Most of these are stable Debian conventions and fine to leave").  ruff + mypy clean, 455/455 tests pass.)* |

---

