[Build]
# target architecture - i386, amd64 or all
ARCH = amd64

# distribution name
NAME = "Athena Linux"

# distribution codename
CODENAME = "thor"

# distribution version
VERSION = "0.1"

# distribution channel
CHANNEL = "stable"

# number of source packages to build in parallel
MaxParallelBuilds = 1

# base release for the build container image (independent of BASECODENAME)
CONTAINER_RELEASE = bookworm

# external Docker daemon URL (leave blank to use local socket)
# DOCKER_SERVER = tcp://192.168.1.100:2375

[Base]
# Repo Base
baseurl = deb.debian.org

# base codename
BASECODENAME = bookworm

# base distribution
BASEID = debian

# base version
BASEVERSION = 12.0


[Directories]
Log = log
Download = download
Cache = cache
Temp = tmp
Source = source
Build = build
Repo = repo
Config = config
Patch = patch
Image = image
Chroot = buildroot

# mirror to fetch packages from
MIRROR_URL="deb.debian.org"

# folder suffix for the package lists to use
PACKAGE_LISTS_SUFFIX="default"

[Source]
SkipTest =
# SkipTest = systemd, libsoup2.4, libpsl, libical3, lilv, procps, keyutils, vim
BuildProfiles = nodoc, nocheck
# Packages in this list are downloaded directly from the base Debian repo
# instead of being built from source (comma-separated source package names)
Tunneled =
# Tunneled = gcc-12, linux
